; bystander.bl — the CLEAN half of the `bl test --diagnose` demonstration: it
; minds its own business, so the ledger has nothing to say about it. Pair it with
; leaker.bl to see a warning that names one file and not the other.
(ns fixtures.ward.bystander)

(deftest a-bystander-passes
  (is (= 1 1)))
