; run.bl — launch the canonical research harness.
; Loads bench (latency), census (node counts), oracle (test file assertions)
; in order. Each script runs its output at top-level load time.
(ns run (:require [bench] [census] [oracle]))
