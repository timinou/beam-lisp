#+TITLE: Harness: index-parallel serial≠parallel under cached analysis; unnamed '?' INCOHERENT file; ExUnit tail crash after ward summary
#+STATE: DONE
#+SESSION_ID: 158190337cc00a7b
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-16T07-08-17-780Z_158190337cc00a7b.jsonl]]
#+CUSTOM_ID: BUG-055-harness-index-parallel-serialparallel-un
#+COMPLETION_HASH: bea59668ceac2e61

* Initial Message

see how the feat/bb-compat branch works and how to merge it back to main

* Symptom

Post-merge full-suite run, 2026-09-16: three unattributed harness failures.

1. `code.index-parallel.test / four-workers-index-the-same-thing-as-one`:
   `(= serial parallel)` fails. The test passes `:memo false` and a fresh
   BLANALYSIS_DIR, yet every result row shows `:analysis :cached` — the cache is
   coming from somewhere the test did not disable (suspect: the persistent corpus memo
   landed in 32bd910 "code.semantic: read a source the loader's way, so the corpus
   memo persists"). Whether serial≠parallel is a real worker-pool bug or stale-cache
   skew is UNRESOLVED — the run was on a stale toolchain (BUG-052).
   Test: test/bl/code/index_parallel_test.bl:60-72.

2. `⊘ ? INCOHERENT — test file declares no (ns …) form`: ward's summary lists an
   UNNAMED file. Every non-fixture .bl/.bl.md under test/ carries an (ns …) form
   (verified 2026-09-16, 210 files), and a partial local rerun (172 files) showed no
   INCOHERENT — so the "?" file is transient (created mid-run by another test and swept
   by discovery?) or ordering-dependent. Two sub-defects: (a) a phantom file enters the
   run at all; (b) ward reports it as "?" instead of its path, making it undebuggable.

3. ExUnit tail crash: after ward's summary ("✗ not green"), the runner dies with
   "function ExUnit.start/1 is undefined (module ExUnit is not available)" from
   priv/std/bl/test.bl.md:99 run-exunit via bl/cli.bl:932, preceded by a
   Kernel.ParallelCompiler return_diagnostics warning from
   Bl.Test."compile-test-support!". Mid-suite ExUnit legs DID work ("Ran 6 tests …
   0 failures" after build.test etc.) — so ExUnit is available in some contexts and
   not in the final one. Possibly: the exunit leg runs in a different VM context
   (daemon vs drop) post-merge, or compile-test-support! now fails under the merged
   tree. Blocks: the suite's exit status is a crash, not a clean red/green.

* Approach

Step 0 for all three: rebuild + reseed + restart the daemon (BUG-052 gate), re-run the
full suite ONCE, then attribute what remains. Do not root-cause 1 or 3 further against
the stale image.

NOTE [2026-09-16]: 2026-09-16 post-cutover reruns (fresh toolchain): (1) code.index-parallel serial≠parallel REPRODUCES on the fresh toolchain, twice — a real bug, not skew. `:memo false` still yields `:analysis :cached` on every row: find where the corpus memo consults a cache that ignores the :memo knob (suspect code.semantic's persistent corpus memo from 32bd910) and why serial's result differs from parallel's (the runner prints expected as the bare form `serial`, hiding its value — fix the printer or probe manually). (2) NEW: datom.datahike-differential.test is ABSENT FROM DISCOVERY in both post-cutover runs (no Testing line, no INCOHERENT, sits between datom.cost and datom.dlit alphabetically but never appears) — the file exists at test/bl/datom/datahike_differential_test.bl. Something in ward's discovery now silently drops it; check whether discovery is index-driven and the daemon's index was mid-build. (3) '?' INCOHERENT and the ExUnit tail crash not observed in the post-cutover partial runs (suite was cancelled before the summary both times) — status unknown.

NOTE [2026-09-16]: 2026-09-16 RESOLVED (HarnessCleanup agent 6-HarnessCleanup + orchestrator), verified green with bin/bl (NOT bare bl — that is djinn-bl's launcher, see below):

1. index-parallel serial≠parallel: ROOT CAUSE was provenance-only skew, not a worker bug — serial ran first against the cold analysis store (:indexed/:filled), parallel then read the store serial had just written (:cached). `:memo false` was never ignored: it disables the corpus MANIFEST memo; the per-source analysis store is a different cache. Worse, the test was VACUOUS about parallelism (the 4-worker branch never analyzed anything). Fix: one fresh BLANALYSIS_DIR per topology + `every? :analysis :indexed` assertions so a cache read can't masquerade as a worker comparison. Verified: 18 passed.
2. Discovery vanish: NOT reproducible from code — discovery is filesystem-driven (bl.test/files → expand-targets → File/ls!, no index/cache), and datahike_differential IS discovered on the current tree. The post-cutover absence was an artifact of bare `bl` being the djinn-bl checkout's launcher (its daemon/priv view), not this tree's. Hardening delivered: missing targets are named (missing-targets, resolved absolute paths, exit 1 — orchestrator's precedence fix over the 'no test files found' usage error), and ward entries carry {:path :src} so an unreadable/ns-less file is INCOHERENT BY PATH, never '?'. Verified: test_verb 18 passed.
3. Printer: is-eq-report records the evaluated left operand as :expected; ward's expected-line prints the VALUE plus '← <form>' when they read differently. Verified by new ward_test property (48 passed).
4. BONUS real bug found+fixed: ward's ensure-test-lib! read (priv_dir)/test.bl (nonexistent since the tier reorg) and silently fell back to a STALE test library embedded in BeamLisp.TestRT — edits to priv/std/test.bl never reached a run. Now reads priv/std/test.bl and re-interns on change.

Environment lesson (filed as playbook PB-beam-lisp--verify-with-bin-bl-from-the-c): bare `bl` on PATH is a symlink to the djinn-bl checkout — a different substrate. Verify this tree with bin/bl only.