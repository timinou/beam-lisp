#+TITLE: Repair live update invalidation and enforce dependency safety
#+STATE: DONE
#+SESSION_ID: 158a0b0357af8b72
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-22T21-12-43-102Z_158a0b0357af8b72.jsonl]]
#+CUSTOM_ID: BUG-102-repair-live-update-invalidation-and-enfo

* Initial Message

this change, done by a previous agent, fucked up the live updates in many cases. use your own tests and your own analysis of the code to find out what now causes button clicks to not be shown back on the frontend in some cases, and find the structural improvement that makes this class of issues impossible

Investigate regressions after 435ac2d0 (beam-lisp) and b5b697d (blueprint): button clicks sometimes do not reach frontend. Reproduce with independently designed tests, trace event/commit/subscription/projection data flow, fix root cause with structural guard making missed invalidations impossible, and verify relevant suites. Preserve unrelated work in both repos.

* Diagnosis and verification (2026-09-23)
- Reproduced in beam-lisp with a deterministic queue test: with :listen-attrs #{:n/v}, a relevant commit deferred to a queued :n/w commit; the latter failed the attribute gate. Tap stayed at 1 (mount only), whereas 2 is required. Original test run: 103 pass, 1 fail. This proves the new commit coalescing + attribute filter can permanently drop a visible update.
- Static :listen {:attrs …} is unsound for arbitrary view functions, multi-world/session reads, basis changes and future schema changes. Filtering at datom/listen! drops the evidence before the socket can recover; filtering again in on-commit duplicates policy. A fixture-schema membership test cannot prove the view's read dependencies.
- Separate structural gap: app-socket relied on the manually populated :observe list although intents write :shared. A missing shared conn in :observe left even the author's own write invisible. The shared conn now subscribes unconditionally, with :observe adding secondary worlds only.
- Removed attr-scoped filtering from live socket and app; live :listen opts reject :attrs instead of silently permitting unsafe liveness. Every observed commit is now eligible to render, and the final commit in a coalesced burst always renders. Removed the duplicate obsolete app-socket server.
- Removed Backdesk SCREEN-ATTRS and its fixture guard. Backdesk's installed ./bl drop predates the attr filter and ignored this declaration, so these logs cannot be attributed to the new filter on that particular drop without evidence of a different runtime. Header timestamp ETS warnings and keyless diff warnings do not establish the cause of missed updates.
- Verified beam-lisp live.socket.test 98, live.app.test 3, live.app-context.test 13 pass; blueprint backdesk.ui-test 25 and work-test 750 pass. Blueprint initially failed the new source assertion because its _build/bl AOT copy still carried the old declaration; explicitly rebuilding src/backdesk/app.bl then made it pass. A running server must reload/restart to pick up new code. Unrelated worktrees preserved.
- Perf tradeoff: the txInstant-only bookkeeping commit again costs a projection, rather than risking stale UI. Future optimization should eliminate the redundant transaction at the command protocol, not guess view dependencies from a hand-maintained attribute list.

NOTE [2026-09-23]: Final proof: regression test now writes a second :n/v entity while a :n/w commit is queued and asserts both the new [:div "2"] tree and an actual [:patch ops] frame; passes (socket_test 101 assertions). Entire beam-lisp live directory passed 11 files before this final assertion strengthening; focused socket + app-context passed after it. Blueprint ui/work 25/750, sunny/scenario 160/78 passed after rebuilding its stale AOT module. Browser test unavailable: localhost:4072 connection refused. Installed ./bl runtime still predates library source change; the live service must run a refreshed runtime to receive library fix.