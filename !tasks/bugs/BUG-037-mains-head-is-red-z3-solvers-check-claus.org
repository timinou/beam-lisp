#+TITLE: main's HEAD is red: Z3.Solver's check clause takes 4 elements, its caller sends 3
#+STATE: DONE
#+SESSION_ID: 157b5513e8d5bdf2
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp--semantic/2026-09-11T10-59-05-763Z_157b5513e8d5bdf2.jsonl]]
#+CUSTOM_ID: BUG-037-mains-head-is-red-z3-solvers-check-claus
#+COMPLETION_HASH: ed1e6cd3c99d08a2

* Initial Message

merge the latest main onto here

* Symptom

On a clean build of main's HEAD (`194f119`), 30 of the 219 examples fail with

```
(FunctionClauseError) no function clause matching in BeamLisp.Z3.Solver.handle_call/3
  {:check, "<smt>", false}, {#PID<…>, [:alias | …]}, %{owner: nil, port: #Port<0.6>, waiting: {[], []}, mon: nil}
```

Small, reproducible: `mix bl run --path priv --path examples
examples/system/10_multivar_invariant.bl` → EXIT 1, in a worktree freshly merged
with main. `mix compile --force` does not change it.

* Cause — the call and the handler disagree by one element

`lib/beam_lisp/z3_solver.ex`:

```
:132  def check(handle, smt, model? \\ false) do
:133    call(handle, {:check, smt, model?})            # THREE elements
…
:241  def handle_call({:check, _handle, smt, model?}, _from, state) do
                                                    # FOUR — never matches
```

So every `Z3.check/2` — which `priv/lib/z3.bl:144` and every `system/*` and
`veritas/*` example reach through `verify`/`preserves?` — raises instead of
answering. One of the two shapes has to move; the 4-tuple form suggests the
lease/conversation handle is meant to be the caller's, in which case `check/3`
is what needs its arguments threaded, and its `handle` argument is already in
scope.

* Why main's own worktree looks green

It is running uncommitted work. `git status` in `/home/user/code/undefine/beam-lisp`:

```
 D lib/beam_lisp/z3_pool.ex
 D lib/beam_lisp/z3_solver.ex
 M lib/beam_lisp/z3_port.ex
```

— the PLAN-106 session deleted the module that is failing and is mid-refactor, so
`mix bl run examples/system/10_multivar_invariant.bl` there returns EXIT 0 with
the same command. The committed trunk does not.

* Guidance

Do not "fix" this from another worktree without checking with the PLAN-106 work:
the file is deleted in the tree where the fix is being written, so a second
edit here is the collision this note exists to prevent. The check that matters
is the examples gate on a CLEAN build (not `mix bl run` in that worktree):

```
mix test test/beam_lisp/examples_test.exs      # 188/219 at 194f119
```

* Triage of the 219

31 failures at `194f119`. 29 mention `BeamLisp.Z3.Solver`; one is a 30 s timeout
(`examples/veritas/05-the-property-is-behaviour.bl`, likely the same layer);
one is the known flaky `examples/mcp-demo.bl` (FUP-054). Everything this branch
owns passes on the same tree: bl_ask 7, bl_cache 41, bl_search 12,
system/ask 39, system/cli 36, code/semantic 58, datom/blanalysis-cache 42, and
`code-semantic/01`+`02` run clean.

NOTE [2026-09-12]: DONE — fixed on main by `30e633e z3pool: the oracle as a bundle — the pool belongs to the language, not Elixir` (merged here as `7d361f2`). The examples gate on a clean build of the merged tree went from **188/219** (29 of 31 failures mentioning `BeamLisp.Z3.Solver`) to **216/219** with ZERO z3-clause failures; `BeamLisp.Z3.Solver` no longer appears in any failure block.

The lesson kept: the trunk was red for as long as the worktree that owned the fix kept the file deleted, and the worktree's own run looked green.