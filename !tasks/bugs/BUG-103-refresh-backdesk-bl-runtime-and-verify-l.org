#+TITLE: Refresh Backdesk bl runtime and verify live updates
#+STATE: DONE
#+SESSION_ID: 158a0b0357af8b72
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-22T21-12-43-102Z_158a0b0357af8b72.jsonl]]
#+CUSTOM_ID: BUG-103-refresh-backdesk-bl-runtime-and-verify-l

* Initial Message

this change, done by a previous agent, fucked up the live updates in many cases. use your own tests and your own analysis of the code to find out what now causes button clicks to not be shown back on the frontend in some cases, and find the structural improvement that makes this class of issues impossible

Build beam-lisp executable/drop from the current fixed beam-lisp source, replace blueprint's ./bl safely without discarding unrelated work, verify embedded live/app and live/socket implement unfiltered subscriptions and shared-world observation, then run Backdesk UI/work/scenario tests and an end-to-end socket update proof on the refreshed runtime. Preserve prior executable backup and user changes.

* Result (2026-09-23)
- Blueprint ./bl was a v2026.4 drop (sha256 cb620198…), predating live invalidation repair. The legacy tracked .local/bl-build used removed mix.exs and copied the stale source checkout ./bl; replaced it with checkout `bin/bl self-build --bin <stock launcher> --out <target>`, followed by a cold AOT build. `bash -n` and diff --check pass.
- Two attempted self-builds hung: `priv/lib/harness/serve.bl` had a top-level `(hm/run)` inside recursively compiled priv/lib; AOT evaluates top-level forms and cold harness parks forever. Relocated task to priv/tasks/harness-serve.bl and updated env.bl task path. `./bin/bl tasks` resolves new path; new drop includes it. Rebuilt successfully: 258 tier sources, 142420370 payload bytes, self-build exit 0.
- Installed new binary in blueprint/bl, sha256 b7c61e38cd95d9c048fb63c230c3b66a49924581e9902c6c3d547020197dade9; retained old at blueprint/bl.pre-livefix-20260923, sha256 cb620198aca2502be08f8988fed7e2777551f4a0b8a0a038e8c7f5a20dd7b0. New drop extracted at ~/.local/share/drop/e755a46c; payload live.socket contains listen-options rejection and unfiltered on-commit; live.app unconditionally observes shared conn. `BL_DAEMON=off ./bl version` and doctor pass all required/optional probes.
- Cold tests using blueprint ./bl with bundled library: upstream live.socket.test 101 and app-context.test 13 pass (including queued commit actual :patch assertion and shared-write observation); Backdesk ui 25, work 750, sunny 160, scenario 78 pass.
- Gracefully stopped old warm daemon PID 2025978 (compiler key 9b7c…) and restarted using native launcher `./bl daemon start` (not BL_DAEMON=off, which runs foreground and was timed out once). New PID 2441240 compiler key 3ded534c…; warm `./bl test` passes Backdesk ui 25 and upstream live socket 101 + app-context 13. Warm daemon's optional code index reports `failed — code.index/ensure-tree ... argument error`, not yet diagnosed; functional test path and doctor pass. No Backdesk server on port 4072 was present, so no browser gesture was verified.