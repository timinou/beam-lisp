#+TITLE: `bl doctor` reports the oracle tier as absent while the solver answers :sat
#+STATE: ITEM
#+SESSION_ID: 157f0c246a28a2b5
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-14T08-14-21-608Z_157f0c246a28a2b5.jsonl]]
#+CUSTOM_ID: BUG-050-bl-doctor-reports-the-oracle-tier-as-abs

* Initial Message

ok can you please look at the pending worktrees and pending features and pending work, and tell me what still needs to be done to releave the next version (v2026.2) and what it would have in its features?

* Observed

`bl doctor` reports the oracle tier as absent, in a checkout AND in a packed drop:

```
$ mix bl doctor
  --   z3                undefined var: z3pool/z3-keeper

$ /tmp/bl-preflight doctor      # a real drop, built by `mix bl.build`, v2026.2 tree
  --   z3                undefined var: z3pool/z3-keeper
```

`--` is "absent", and the tier is marked optional, so nothing crashes — the report
is simply wrong.

* The tier WORKS

Called directly, on the same tree, in the same kind of VM:

```
$ mix bl run /tmp/probe-z3b.bl
check: {:status :sat, :core [], :tier :oracle, :why nil, :model {}, :raw-model nil,
        :us 158566, :raw-core nil}
```

where the script is `(z3/check :z3-leases "(assert true)")` — the exact call the
doctor's probe makes. And `(z3pool/up?)` → `false`, `(z3pool/ensure!)` → `4`, both
without error.

So this is a REPORTING bug in the probe path, not a missing capability. That
matters for how it is prioritised: the oracle answers; `bl doctor` says it does
not.

* Where to look

  - `priv/std/bl/doctor.bl.md:129` — the probe:
    `(probe "z3" false (fn [] (let [p (z3/open) r (z3/check p "(assert true)")] …)))`
  - `priv/lib/z3.bl:39` — `open` calls `(z3pool/ensure!)` and returns `:z3-leases`
  - `priv/std/z3pool.bl` — `z3-keeper` is a `defserver` at line 206, and line 127
    is `(call :z3-keeper [:pid] call-timeout-ms)`, i.e. the name is referenced 80
    lines before it is defined. The error names the VAR, so the leading
    hypothesis is a load/definition-order path reached by the probe and not by a
    direct call: something evaluates a body naming `z3-keeper` while the
    namespace is still being loaded.

Not yet established, and stated as such: whether the trigger is load order, or a
second entry point into the pool (`:z3-leases` lease registry) that the probe
takes and a direct `check` does not.

* Acceptance

  - `mix bl doctor` → the `z3` row reads `ok`, with a detail that names the status
    the solver answered;
  - the same row in a drop built from this tree;
  - and `bl doctor` on a machine with NO solver present still reports absence
    (a report, not a crash) — the row must distinguish "no solver" from "the
    probe itself is broken".
