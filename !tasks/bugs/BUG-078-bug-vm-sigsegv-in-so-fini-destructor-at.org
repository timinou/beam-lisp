#+TITLE: BUG — VM SIGSEGV in .so fini destructor at exit under reader/lazy-seq churn (LazyMemo NIF suspect)
#+STATE: ITEM
#+SESSION_ID: 15842c57d3c72c53
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-18T07-47-27-183Z_15842c57d3c72c53.jsonl]]
#+CUSTOM_ID: BUG-078-bug-vm-sigsegv-in-so-fini-destructor-at

* Initial Message

"""
- *The loader's cross-process cycle deadlock is a class.* BUG-064's instance is
  fixed, but the mechanism — one process holding a `:global` load lock while
  another runs the init, plus a retry-forever `set_lock` — will produce the same
  silent hang for the next cycle anyone introduces between a substrate and an
  application namespace. Either bound the lock acquisition (fail with a named
  cycle) or move the lock to the process that runs the init.
"""

can you explain this from zero context in simple language? go find the root cause first and explain what gets in the way of a clear system

* Symptom
`beam.smp` dies with SIGSEGV (sometimes SIGBUS) at VM shutdown, or mid-run, under workloads heavy in lazy-seq/reader allocation. Exit code 139/135 after an otherwise ALL-GREEN test run, or mid-suite at varying points — flaky, ~50% under the repro below.

* Evidence
Core dumps (systemd-coredump, 2026-09-18):
  PID 3768638 SIGSEGV  /usr/lib/erlang/erts-17.0.1/bin/beam.smp  (39M)
  PID 3756025 SIGSEGV  (25.9M)  ·  PID 3793174 (30.7M)  ·  PID 3662171 (34M)

Backtrace of 3768638 (`coredumpctl debug`):
  #0 0x0  ← jump to NULL
  #1-2 ld-linux (_dl_call_fini path)
  #3-4 libc exit()
  #5-8 beam.smp
i.e. a SHARED LIBRARY's fini destructor calls a null/garbage function pointer during library finalization at VM exit — a NIF .so teardown bug, not BEAM bytecode. Candidates by what the workload exercises: `priv/native/lazy_memo.so` (backs every LazySeq and every Atom — reader churn creates these by the thousand).

* Attribution data points (same suite, same tree)
  - `test/bl/reload/` at HEAD reload.bl: exit 0 (n=1).
  - with PLAN-125's reload.bl requiring source-graph and parsing namespace sources per commit (unmemoized): SIGBUS/SIGSEGV mid-suite, 2/2.
  - with source parsing stubbed out: exit 0.
  - with parse-once memoized: suite all-green, but shutdown SIGSEGV ~1/2 runs.
  So reader/lazy-seq churn correlates with triggering a latent finalizer bug. Host is memory-pressured (29/46 GB, heavy zram) which may lower the threshold.

* Reproduction
A workload that parses many full files through the self-hosted reader in one VM (e.g. reload's image-requires parsing every loaded namespace's source), then lets the VM exit. The dump's fini path fires at `halt`/exit.

* Not done
No disassembly of which .so owns the crashing destructor (needs symbols; `info sharedlibrary` in gdb at the crash). Fixing the NIF is out of PLAN-125's scope; PLAN-125's reload gate memoizes to minimize churn and documents this bug as the reason.

NOTE [2026-09-18]: 2026-09-18: warm multi-suite runs (bl test A B …) flake three ways on identical code: (1) green, (2) rc=139 SIGSEGV after tests print, (3) bl.json "fast path refused a value the walk encodes" on values that encode fine standalone (empty map/vector values, keyword-keyed maps). Repro pair: bl test test/bl/system/lsp_test.bl test/bl/system/lsp_rpc_test.bl — same pair ran green, then 139, on consecutive invocations. Value-level corruption accompanying the crash class suggests memory unsafe NIF (lazy_memo?) rather than bl.json logic. The new BEAM_LISP_NIF_TRACE flight recorder (lazy_memo.ex) is the tool for the next sighting.