#+TITLE: bl test runs against a stale seed/drop and the results look like real regressions — make the generation mismatch a hard gate
#+STATE: ITEM
#+SESSION_ID: 158190337cc00a7b
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-16T07-08-17-780Z_158190337cc00a7b.jsonl]]
#+CUSTOM_ID: BUG-052-bl-test-runs-against-a-stale-seed-drop-a

* Initial Message

see how the feat/bb-compat branch works and how to merge it back to main

* Symptom

Post-merge (`5549526`, feat/bb-compat → main) full `bl test` run, 2026-09-16 ~08:15:
7 failed + 2 incoherent of 207 files. Root-cause work (three explorer agents,
session 158190337cc00a7b) shows the run executed a STALE pre-merge toolchain:

- `bl doctor` in-run: "seed 21 beams, built by ANOTHER toolchain (codegen
  c2e30b58745e… vs fc89914e5b03…) — reseed with `bl seed`"
- `datom.datahike-differential.test` INCOHERENT "namespace not found: datahike.api":
  searched path = [tree, tree/examples, tree/priv/lib, DROP/priv/boot, DROP/priv/std,
  DROP/priv/lib, DROP/priv/compat, DROP/priv/build] — the tree's own priv/boot, priv/std
  and priv/compat are NOT on it; the drop (~/.local/share/drop/9dcd3cad) has
  compiler.bl 188.3K (tree: 209.0K), ir.bl 11.5K (tree: 13.2K), no priv/compat/datahike/.
- `test.seed` logged "beam/beam_load.c: Error loading module 'Elixir.BeamLisp.Ns.Anf':
  corrupt file header" — stale beam vs current ABI.
- The run's error trace pointed at lib/beam_lisp/env.ex:1317 for fetch!/2; post-merge
  fetch!/2 is at line 1354 (pre-bb-compat main had it at 1251). The executing env.ex
  was neither — a third, older generation.
- The warm daemon (drop 9dcd3cad) predates the merge; ~/.local/bin/bl symlinks to a
  DIFFERENT checkout (/home/user/code/ora/djinn-bl/bl).

* Why it matters

Every suite result against a stale seed/drop is meaningless noise, and the failures it
produces look like real regressions (this cost a full root-cause pass to attribute).
`bl doctor` KNOWS the seed is stale ("reseed with bl seed") but nothing stops `bl test`
from running anyway. A beta cut from such a run would ship unknown code.

* Fix direction

1. `bl test` (and ideally every `bl` verb that loads sources) must HARD-REFUSE when the
   warm daemon/seed generation ≠ the tree's compiler_key, printing the exact remedy
   (`bl seed` / rebuild the drop / restart daemon). Today the mismatch is a doctor
   footnote; make it a gate. The comparison already exists (doctor computes both keys).
2. Document/verify the post-merge ritual: rebuild drop from THIS tree → `bl seed` →
   restart daemon → `bl test`. NB the installed `bl` symlink points at another checkout;
   rebuilding from the wrong tree is a real hazard — the gate should also catch THAT
   (tree path vs daemon's build tree).
3. Re-run the full suite on the fresh toolchain BEFORE attributing any of the
   2026-09-16 failures to source. Candidates that may evaporate: datahike-differential
   INCOHERENT, code.index-parallel serial≠parallel, parts of the diagnostics-leak counts.

* Evidence

Agent reports: agent://0-SessionConn, agent://1-LinearSelfApply, agent://2-McpMountEdge
(session 158190337cc00a7b). Suite log: user's 2026-09-16 paste; partial reproduction
at /tmp/bltest_full.log (timed out at 600s while daemon was wedged by overlapping runs).

NOTE [2026-09-16]: 2026-09-16 post-cutover: largely ADDRESSED by the mix-cutover commits. bin/bl now recompiles the substrate when stale, not only when absent (e7e37a2, find -newer gate incl. the .substrate-partial two-half rule); the toolchain key is derived from codegen SOURCE digests via priv/codegen.sources (ed5762b, aot_cache.ex +202) and 'stale artifacts compiled by a different toolchain are never linked' (aot_cache.ex:170); BUG-048 shipped in v2026.3 with the artifact verified. Fresh-toolchain evidence: post-cutover suite runs show NO corrupt-file-header, NO env.ex line skew, and doctor's stale-seed warning is gone (the seed probe no longer exists — key coherence is structural now). REMAINING before close: (a) verify there is a hard gate for `bl test` when the DAEMON's booted image drifts from the tree mid-session (aot_cache.ex:186-199 mentions 'must force a restart, not a stale reuse' — confirm the daemon actually restarts or refuses); (b) the run-to-run variance observed post-cutover (see BUG-041 note) means daemon-state churn, not toolchain skew, is now the dominant noise source.

NOTE [2026-09-16]: 2026-09-16: the warm-reload design question ('shouldn't reload check which updates are safe and do them warmly?') is filed as FUP-084. Short answer: the safety gate already exists (reload.reconcile HOLD reasons) and the warm-apply path exists for source edits (Executor.run_reload FIFO); what's missing is routing DRIFT detection through reconcile — tree drift can apply warmly, toolchain drift needs a warm-handover restart, never a silent stale run.

NOTE [2026-09-16]: Coordination (session 1581b4ed0954e773, 2026-09-16): PLAN-118 W2/W3 touch the daemon REQUEST and the TRANSPORT DECISION, which is one step upstream of your stale-seed gate and shares FUP-084's territory. Boundaries I will hold:
- I add the caller's ENVIRONMENT to the request (`tooling/drop/src/daemon.rs` client, `lib/beam_lisp/daemon/{protocol,executor}.ex`, the program side) so a warm run and a cold run of the same command are the same program. Evidence it is missing: `FOO=bar bl eval '(System/get_env "FOO")'` answers nil warm and bar cold.
- I make the tree's own declaration reach the transport (`env.bl :env` / a `:daemon` key) so `BL_DAEMON=off` in a tree is honoured as it is from the shell. Measured: it is inert today (env.bl declares it, `bl eval` confirms it, the run still goes through `BeamLisp.Daemon.Executor` because the launcher chooses before the project env is applied).
- I do NOT implement drift APPLICATION (warm-apply / warm-handover); I only make `bl daemon status`/`bl doctor` NAME the drift class, boot id and served fingerprint so your gate has a public signal to read.
If your gate needs a field in `hello`/`status` that I am adding, say so here and I will put it in rather than have you derive it.