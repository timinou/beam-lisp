#+TITLE: Preserve trie-backed vectors in finite compiler traversal
#+STATE: ITEM
#+SESSION_ID: 15751ba43523b193
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-06T14-57-15-988Z_15751ba43523b193.jsonl]]
#+CUSTOM_ID: BUG-029-preserve-trie-backed-vectors-in-finite-c

* Initial Message

work autonomously through the whole plan. take tbe most ambitious decision if needing to choose

Observed on current source at integration HEAD c88b942: CompilerData.eager_to_list/1 calls Tuple.to_list on Vector.items. Large vectors use an internal five-element trie tuple. Fresh-VM reproduction using Vector.new(Enum.to_list(1..1024)) returned actual_count 5, expected_count 1024, matches_vector_api false.

Change lib/beam_lisp/compiler_data.ex to use public Vector.to_list/1. Add tests across the flat/trie boundary and eager_map over large vectors. Preserve non-forcing LazySeq rejection, improper-list rejection before callback effects, source order and callback counts.

Verify focused CompilerData tests, compiler corpus/conformance, full mix test, normal rebuild, uncached build and reproducibility. CompilerData is fingerprinted; follow the explicit commit-time seed gate if committing, not seed regeneration as an edit-loop workaround.

Discovered reconciling PLAN-092 with completed PLAN-090. The recorded 1487-test pass does not cover this case. Preserve unrelated and untracked work.
