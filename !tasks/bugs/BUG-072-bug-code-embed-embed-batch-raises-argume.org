#+TITLE: BUG — code.embed/embed-batch raises ArgumentError (native embed path broken in checkout)
#+STATE: ITEM
#+SESSION_ID: 15842c57d3c72c53
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-18T07-47-27-183Z_15842c57d3c72c53.jsonl]]
#+CUSTOM_ID: BUG-072-bug-code-embed-embed-batch-raises-argume

* Initial Message

"""
- *The loader's cross-process cycle deadlock is a class.* BUG-064's instance is
  fixed, but the mechanism — one process holding a `:global` load lock while
  another runs the init, plus a retry-forever `set_lock` — will produce the same
  silent hang for the next cycle anyone introduces between a substrate and an
  application namespace. Either bound the lock acquisition (fail with a named
  cycle) or move the lock to the process that runs the init.
"""

can you explain this from zero context in simple language? go find the root cause first and explain what gets in the way of a clear system

* Symptom
`code.embed/embed-batch` raises `%ArgumentError{message: "argument error"}` on any input, model present (`priv/embed/potion-code-16M-v2`), natives present (`priv/native/code_embed.so`).

* Consequence
9 model-touching tests in `test/bl/code/semantic_test.bl` error (search-ranks-by-meaning, a-warm-column-needs-no-model, re-indexing-an-unchanged-file-changes-nothing, similar-excludes-the-function-itself, a-second-band-duplicates-rather-than-overwrites, index-dir-takes-each-namespace-from-its-file, results-descend-by-score, search-in-restricts-to-a-namespace, search-returning-joins-the-tag-by-name). The 33 non-model tests pass.

* Context
Found during PLAN-125 verification (2026-09-18); reproduces on a direct `embed-batch` call with zero involvement of the plan's changed files, so it is pre-existing, not caused by PLAN-125. Recent HEAD commits (a7e49277 "install the cdylib at the name the loader asks for", 2b90e8ae "ci: build the native crate the boot needs") suggest the native install path was just reworked — likely a stale or misnamed cdylib in this checkout. Unverified: whether a fresh `bl build --native` (or the CI's native build step) fixes it locally.

* Reproduction
    ./bin/bl eval '(do (BeamLisp.Loader/ensure_loaded "code.embed") (BeamLisp.RT/invoke (BeamLisp.Env/fetch! "code.embed" "embed-batch") [["hello world"]]))'
    → %ArgumentError{message: "argument error"}
