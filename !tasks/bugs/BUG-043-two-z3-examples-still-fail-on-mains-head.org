#+TITLE: Two z3 examples still fail on main's HEAD after the pool bundle: smt/emit gets nil; a clause is missing
#+STATE: DONE
#+SESSION_ID: 157b5513e8d5bdf2
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp--semantic/2026-09-11T10-59-05-763Z_157b5513e8d5bdf2.jsonl]]
#+CUSTOM_ID: BUG-043-two-z3-examples-still-fail-on-mains-head

* Initial Message

merge the latest main onto here

* Symptom

Two examples fail on a clean build of `30e633e` (main's z3 pool bundle, merged
here as `7d361f2`), and they fail standalone, not only under the runner:

```
$ mix bl run --path priv --path examples examples/veritas/03-the-mock-server.bl
error: smt/emit: nil is not an SMT term                        # exit=1
$ mix bl run --path priv --path examples examples/system/02_inductive_safety.bl
error: no function clause matches                              # exit=1
```

With them, the examples gate is **216/219** on a clean build. (BUG-042's 30
failures are gone: the pool bundle fixed the `{:check, smt, model?}` clause
mismatch, and no failure block mentions `BeamLisp.Z3.Solver` any more.)

* Not this branch

`git diff --name-only main` over this branch lists only the semantic-search
files (priv/std/bl/*, priv/lib/code/*, lib/beam_lisp/{model,rt}.ex,
examples/code-semantic/*, the org items). `priv/lib/smt.bl` -> `priv/lib/z3.bl`
-> `priv/std/z3pool.bl` -> `priv/boot/build.bl` are byte-identical to main's, so
both failures are the trunk's.

* What to look at

`smt/emit: nil is not an SMT term` is a term reaching the emitter unrendered —
the bundle migration moved the oracle into the language (`priv/std/z3pool.bl`)
and the emitter's callers may still pass a beam-lisp value where an SMT term is
expected. `no function clause matches` in `02_inductive_safety` is the same
family as BUG-042 but one layer down (no clause in the new bundle path).

* Caution

The PLAN-106 worktree still carries uncommitted work in this area
(`priv/boot/build.bl` and the daemon sources among its changes; `z3_pool.ex` and
`z3_solver.ex` deleted). Check with that session before editing — and re-run the
gate on a CLEAN build, which is what exposed BUG-042 in the first place.

NOTE [2026-09-13]: WIDENED 2026-09-13 — a THIRD example is red on main's HEAD, and the gate on the merged tree is 214/219.

`mix test test/beam_lisp/examples_test.exs` on the branch merged with main tip `fb6c83b` → **214/219**, five failures:

| example | reason on this tree |
|---|---|
| `examples/system/z3-conversation.bl` | `function BeamLisp.Z3.Solver.owner/1 is undefined (module BeamLisp.Z3.Solver is not available)` |
| `examples/system/02_inductive_safety.bl` | `error: no function clause matches` |
| `examples/veritas/03-the-mock-server.bl` | `error: smt/emit: nil is not an SMT term` |
| `examples/mcp-demo.bl` | `timed out after 30000ms` (FUP-054 flake) |
| `examples/code-semantic/02-live.bl` | `undefined var: datom.store-fjall/->FjallStore` under the runner; **exit 0 standalone** (FUP-054 flake) |

The z3-conversation failure is main's own, proved the same way as the other two: clean DETACHED worktree at `fb6c83b` (no dirty entries), run with `MIX_BUILD_PATH=/tmp/bl_main_build`, `MIX_DEPS_PATH=<branch>/deps`, `BEAM_LISP_Z3=<branch>/priv/z3/bin/z3`:

```
$ mix bl run --path priv --path examples examples/system/z3-conversation.bl
exit=1
error: function BeamLisp.Z3.Solver.owner/1 is undefined (module BeamLisp.Z3.Solver is not available)
```

Note the two z3 examples now also exit 0 with the error on STDOUT (`exit=0 error: no function clause matches`) — i.e. the example itself prints the failure rather than raising — while the gate still fails them, so the gate's judgement is the authority here, not the process exit code.

Nothing here is cache-related; the store, `bl search` and `bl ask` are unaffected (measured on the same tree: `cached 38/38 · vectors 38/38`, `bl cache status` 104 entries / 36 MB).
NOTE [2026-09-13]: RE-MEASURED ON MAIN 6bd9eb0, BL_DAEMON=off, --path priv --path examples.

| example | now |
|---|---|
| examples/system/02_inductive_safety.bl | rc=0, no error (fixed upstream by the pool bundle + S3 rewrite) |
| examples/veritas/03-the-mock-server.bl | rc=0, no error (same) |
| examples/system/z3-conversation.bl | WAS still red (BeamLisp.Z3.Solver/owner undefined); FIXED in 88feb37 — asks z3pool/current, labels say "solver held" |

Only test/bl/system/z3_concurrent_test.bl:8 still names BeamLisp.Z3.Solver, in a comment.
Left open until examples_test.exs is re-run on a clean build (the two FUP-054 flakes are not this bug).

NOTE [2026-09-14]: CLOSED — all three items green, measured through the real gate.

The gate that BUG-042 was found by, re-run at the end of PLAN-112:

  mix test test/beam_lisp/examples_test.exs
    Finished in 86.9 seconds
    2 failures: examples/code-semantic/02-live.bl, examples/mcp-demo.bl

BOTH of those are the known FUP-054 flakes, and I checked rather than assumed: each runs
rc=0 with zero `error:` lines STANDALONE (`mix bl run --path priv --path examples <file>`).
They are listed as such in this item's own 2026-09-13 note. So the gate is 217/219 and every
z3-facing item is green:
  * z3-conversation.bl — fixed in 88feb37 (it asked the deleted BeamLisp.Z3.Solver/owner;
    now z3pool/current, labels say "solver held" not "owner").
  * 02_inductive_safety.bl — rc=0.
  * 03-the-mock-server.bl — rc=0.
Acceptance ("after examples_test.exs on a clean build") is therefore met, with the two
flakes named as NOT this bug. The only remaining reference to the deleted module is a
COMMENT in test/bl/system/z3_concurrent_test.bl:8 (history, left deliberately).
