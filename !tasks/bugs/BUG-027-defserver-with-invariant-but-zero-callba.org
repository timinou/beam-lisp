#+TITLE: defserver with invariant but zero callback clauses crashes compile (tuple_to_list nil)
#+STATE: ITEM
#+SESSION_ID: 15729b4fce888940
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-04T16-19-37-402Z_15729b4fce888940.jsonl]]
#+CUSTOM_ID: BUG-027-defserver-with-invariant-but-zero-callba

* Initial Message

Check that PLAN-074 is done and if it is, understand all the things written about MLIR/LLVM IR and beam lisp in this repo. Rebuild your understanding from scratch to make it your own and then come back to me with your own assessment. You should also read docs/memory-policy and cross it with the other work on compiling beam lisp to MLIR/LLVM IR

* Repro

```clojure
(ns repro)
(defserver account
  (init [b] (ok b))
  (invariant [b] (>= b 0)))
```

`mix beam_lisp.run` → **(ArgumentError) tuple_to_list(nil)** in
`BeamLisp.Ns.Body.Compiler."compile-defserver"/3` (a lazy_map over the
callback partition).

* What works

- init + handle-call (no invariant): ok
- init + invariant + handle-calls (examples/guards.bl): ok at HEAD
- init alone: ok
- invariant clause verified from source (examples/system/28_invariant_clause.bl): ok

So the crash needs: invariant clause present AND zero handle-*/callback clauses.
Likely a downstream consumer of the callback partition (defaults/ordering or the
client-API generator) assumes ≥1 real callback. Edge case, low severity (a server
with no handlers is near-useless), but the error message names nothing.

* Context

Found while de-risking PLAN-086 (S-D.emit). The invariant compile path is owned
by the PLAN-085 line (80c82f1 "invariants verify from source"). When S-D.emit
E4 rewrites compile-defserver to direct bl-ANF emit, this case must be in the
test corpus.
