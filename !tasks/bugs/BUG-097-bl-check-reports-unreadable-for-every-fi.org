#+TITLE: bl check reports unreadable for every file, including the tree's own
#+STATE: ITEM
#+SESSION_ID: 158823020c6ed79f
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-21T09-40-35-505Z_158823020c6ed79f.jsonl]]
#+CUSTOM_ID: BUG-097-bl-check-reports-unreadable-for-every-fi

* Initial Message

file+fix the defects that prevent you from having a clean showcase.

you ahve unlimited budget to make it work.

you can make a .bl app that opens the browser for the OAuth when the time comes, and I will connect myself

* Found 2026-09-21.

** Symptom
```
$ bl check priv/std/vm/ports.bl
priv/std/vm/ports.bl  diags=0 smells=0 fns=0 pure=0 eligible=0
x unreadable (priv/std/vm/ports.bl)
x 1 regression
```

** Not file-specific — measured on four unrelated files, ALL "unreadable"
  priv/std/vm/ports.bl            -> unreadable
  priv/lib/gen/llm.bl             -> unreadable
  examples/live/11-pulse-app.bl   -> unreadable
  priv/lib/credential/oauth2.bl   -> unreadable

`diags=0 smells=0` says the analyzers ran and found nothing. `fns=0 pure=0 eligible=0` says function EXTRACTION produced nothing. So the stage that populates the function list returns empty, and "unreadable" is the fallback message for that — not a statement about the file's bytes. The "1 regression" line implies a baseline comparison that never happened.

** Cost
It misreports working files as broken. During the credential work it made two correct files (credential/flow.bl, examples/connectors/01-authorize.bl) look unreadable, while the real cause of the app not running was an unrelated unclosed paren. `bl lsp check FILE` is the working alternative.

** Not investigated further
Filed rather than chased: it blocks nothing, and the file-reading path likely shares a root with BUG-096 (the same drop-vs-checkout split).
