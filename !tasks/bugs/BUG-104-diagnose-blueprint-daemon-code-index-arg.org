#+TITLE: Diagnose Blueprint daemon code index argument error
#+STATE: DONE
#+SESSION_ID: 158a0b0357af8b72
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-22T21-12-43-102Z_158a0b0357af8b72.jsonl]]
#+CUSTOM_ID: BUG-104-diagnose-blueprint-daemon-code-index-arg

* Initial Message

diagnose it and find what's happening

* Symptom
After replacing Blueprint's ./bl with the repaired beam-lisp drop and restarting the daemon, `BL_DAEMON=off ./bl daemon status` reports `index failed — build failed — code.index/ensure-tree at /home/user/code/ora/blueprint: argument error`. Independent `BL_DAEMON=off ./bl eval` invoking `code.index/ensure-tree` reproduces `%ArgumentError{message: "argument error"}` with no ex-data. UI/live unit tests still pass. The daemon is PID 2441240; compiler key 3ded534c…. No service on port 4072 was verified.
* Objective
Locate the exact failing operation and cause; repair the underlying issue if in scope, add regression coverage, verify fresh daemon index ready and preserve unrelated work. Do not discard uncommitted changes or mistake the index failure for proof of missed UI updates.
* Plan
1. Isolate code.index/ensure-tree stages in the Blueprint context and inspect underlying exception stack.
2. Fix root cause minimally with focused regression test where appropriate.
3. Run affected tests, rebuild/install drop only if fix changes its embedded runtime, restart daemon and confirm index status.
* Constraints
Preserve unrelated Blueprint research media, beam-lisp FEAT-045, and ongoing live repair modifications; path-limit any commit. No git stash/reset/checkout/clean.

* Diagnosis and result (2026-09-23)
- Independent reproduction: `BL_DAEMON=off ./bl eval` invoking `code.index/ensure-tree` raised `%ArgumentError{message: "argument error"}`. Project discovery found 73 sources; index-pairs had 75 (2 engine bands). `readable-corpus` accepted all 75. Embedded index with prefixes up to 60 succeeded; the full corpus had a persisted semantic manifest of 75 sources and failed even when replaying its first source alone.
- Inspecting that manifest showed its first `:facts` value is a serialized `%BeamLisp.LazySeq{metadata: nil, resource: …}`; `count` on it raises `ArgumentError` after deserialization. In `code.semantic/cached-facts`, `:facts` was constructed by `concat` (lazy), then `memo-of` stored it in a binary term. The lazy resource cannot be replayed from that term, so a full manifest hit crashes before a useful file progress frame; vm.index reports only `argument error`. Prefixes worked because each had a different corpus key and avoided the poisoned 75-source manifest. This failure is independent of live socket filtering and frontend updates.
- Fixed `priv/lib/code/semantic.bl`: materialize facts with `vec` before any memo persists them; increment `MANIFEST-VERSION` 1→2 so already-poisoned manifests are ignored and rebuilt from per-file stores. Added `corpus-memo-survives-a-fresh-connection` regression in `test/bl/code/semantic_test.bl` asserting a second index uses `:manifest` for both sources and retains 4 embeddings. Targeted upstream tests: semantic 61, index-walk 17, vm.index 21 pass. The old backed-up drop errored in the new regression (its older test suite also has unrelated failures).
- Rebuilt and installed Blueprint `./bl` using `bash .local/bl-build`; embedded `code.semantic/MANIFEST-VERSION` evaluates to 2. In an independent cold process, `code.index/ensure-tree` returned 75 files / 1368 functions / no skips. Stopped previous daemon and started PID 1444912; `BL_DAEMON=off ./bl daemon status` now reports `index ready 75 files, 1368 functions (0 analyzed, 75 from cache, 20.8s)`. An additional independent process replayed the persisted v2 semantic manifest: 75 results, 75 `:manifest` hits, 0 skips. Installed drop tests passed semantic 61, index-walk 17, live.socket 101, live.app-context 13. Browser gestures were not exercised; index failure is resolved, not evidence of frontend correctness.