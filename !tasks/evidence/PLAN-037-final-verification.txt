PLAN-037 final verification — 2026-08-24

## beam_lisp.test  (clean clone at 5a8d7e1, /tmp/blclone)
Ran 655 tests containing 1731 assertions.
0 failures, 0 errors.

  Baseline before this work was 648/1717. The +7 are the checker tests
  added here: unfired-events (3), nested-each scoping (2), literal fire
  argument (1), report carries an unreachable event (1).

  WHY A CLONE: another session holds an uncommitted AOT-prelude change in
  the working tree (priv/core.bl gained `(ns core)`, plus lib/beam_lisp.ex
  and lib/beam_lisp/aot.ex). It makes `mix beam_lisp.test` die with
  BadFunctionError in BeamLisp.Ns.Core before any test runs, and a full
  `rm -rf _build/dev/lib/beam_lisp` does NOT clear it — so it is their
  source change, not a stale artefact. The clone is this repo at MY HEAD
  without their uncommitted files, which is the only way to verify my
  commits against the suite that covers them.

## reel mix test
Result: 61 passed          (baseline 48)

## reel scripts/check.exs
8/8 checks passed
1 contract, 1 view, 0 errors, 4 warning(s) — every declared event is reachable
22379 B of EDN accepted by spacetime

## Browser (headless Chrome, http://127.0.0.1:4040)
interactive controls: 57      (before this work: 2)
  9 move · 36 judge · 9 record · 1 role select · composer input + add

  created "End to end proof"      → notice: created task "End to end proof"
  clicked move--1 as tech         → todo → doing, notice "moved todo → doing"
  reloaded the page               → still doing (persisted to redb)
  judged payment-capture ready    → missing → ready, notice "component
                                    payment-capture is now ready"
  orphan mark                     → "no component behind it" on every task
  lead selector                   → tech offers [doing, dropped];
                                    product offers [dropped];
                                    sales offers [dropped]

## Pre-existing failures, NOT caused by this work
`mix test` (full Elixir suite) in the clone: 1203/1238, 35 failures.
Each checked against 7a88e4c, the commit BEFORE this session's work:

  specter_compat_test.exs   10/13 at 5a8d7e1 — and 10/13 at 7a88e4c
  host_regen_test.exs        0/2  at 5a8d7e1 — and 0/2  at 7a88e4c
  loop_test.exs / examples   unchanged

∴ identical before and after. Not regressions from this plan.
