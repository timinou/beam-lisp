#+TITLE: Pin compiler generation and characterize language behavior before cutover
#+STATE: DONE
#+SESSION_ID: 15729b4fce888940
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-04T16-19-37-402Z_15729b4fce888940.jsonl]]
#+CUSTOM_ID: FEAT-036-pin-compiler-generation-and-characterize
#+ORDER: 1
#+PARENT: PLAN-090-converge-direct-compilation-through-one
#+COMPLETION_HASH: 98f1b808926a0735

* Initial Message

You are taking over from a competitor's agent.

Please assess the reasons for why the plan hasn't converged. Consider all angles, all the way down to the programming language design.

Come back to me with an in-chat explanation and your own strategy, from zero context, in simple language

* Purpose
Beam Lisp is self-hosted: committed executable seed modules start a compiler which compiles newer source. Source edits, cached modules and in-memory hotpatches can therefore refer to different compiler generations. Before changing the definition pipeline, make test evidence identify the compiler that actually ran, and lock down existing language behavior. This is an implementation task, not a request to rerun all previous investigations.

* Scope and starting points
Inspect lib/beam_lisp/{bootstrap,aot_cache,dev_hotpatch}.ex, priv/bootstrap/gen_manifest.exs, priv/boot/compiler2.bl and existing bootstrap/ANF tests. Do not edit all of these in a single worker: assign at most 3–5 explicit files per implementation worker after identifying the owning functions. Coordinate with PLAN-089 and current uncommitted aot_cache.ex/prelude_interface.ex work; do not overwrite or duplicate it. Preserve committed E2 (440fb3b) and uncommitted E3.

* Required behavior
1. Produce a machine-readable generation receipt containing source/codegen key, seed manifest identity, actually loaded compiler/lower/ANF module hashes, hotpatched namespaces, build mode and relevant cache mode. Label source identity and loaded-code identity separately. A source hash alone is not proof of the running compiler.
2. Attach that receipt to focused test, bootstrap and release-gate results. Reuse existing provenance/stamp machinery rather than introducing a second cache or version scheme.
3. Fail loudly if a bootstrap artifact claims a generation different from its bytes/provenance. Add a regression against seed installation overwriting a valid freshly built generation. Do not treat a successful exit from a no-op rebuild as proof of regeneration.
4. Characterize existing observable behavior with small tests: local name versus linked global in call position (callable, non-callable, wrong arity, missing global); fixed/variadic calls; legal and illegal guards; typed and untyped catch, reraising and finally side effects; macro expansion timing; declare after def preserves value; a closure retained through three redefinitions. Test runtime results and errors, not only internal syntax shapes.
5. Preserve the currently implemented local/global dispatch for this migration. Do not silently replace it with conventional lexical shadowing or broaden it to satisfy an example. Record the surprising rule in test descriptions and language documentation. An intentional language redesign is a separately approved change.

* Verification and failure handling
Use existing targeted test files where possible. Add a dedicated generation receipt test only if no owner exists. Corrupt/mismatched generation input must fail before falsely stamping output. Hotpatch tests must say hotpatch; only a fresh VM from the regenerated seed establishes bootstrap correctness. Capture known baseline failures with exact invocation, loaded generation and isolated reproduction. Do not delete or skip functionality tests to make the migration green. Environment-only skips list their unmet dependency and have a separate environment-enabled gate.

* Resource and collaboration rules
Serialize mix commands. Memory-risk processes run asynchronously in a systemd user scope with MemoryMax and MemorySwapMax, and captured output; check scope journal for OOM and leftover scopes. Preserve other sessions' modifications. No git stash/reset/revert/checkout/clean to discard work.

* Completion
Generation receipts distinguish source, seed and loaded compiler; targeted semantic tests pass or expose explicitly recorded baseline defects; no language semantics changed; bootstrap mismatch regression passes. The downstream module-contract and vertical-slice tasks consume these tests and receipts as gates.

NOTE [2026-09-06]: Implemented live module MD5 vs code-path SHA256 receipt, current/memoized source keys, seed identity and per-namespace successful hotpatch tracking. Corrected get_object_code misconception: it reads disk, not necessarily live bytes; added stale-disk/new-live regression. Added generation semantics and isolated Bootstrap.install!/2 seed-dir tests for fresh-generation preservation, inconsistent companion handling and corrupt seed refusal. Capped same-VM seed-staged gate /tmp/bl-plan090-generation-gate.log: 16 passed (host modules loaded from current source). Closure fixture tests returned closure retention, not a namespace shim capture that deliberately follows redefinition. Fresh final seed/bootstrap proof remains PLAN-091 release gate.