#+TITLE: Make finite compiler transformations eager and bounded by compilation lifetime
#+STATE: DONE
#+SESSION_ID: 15729b4fce888940
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-04T16-19-37-402Z_15729b4fce888940.jsonl]]
#+CUSTOM_ID: FEAT-039-make-finite-compiler-transformations-eag
#+DEPENDS: FEAT-037-define-and-validate-one-complete-anf-mod
#+ORDER: parallel-compiler
#+PARENT: PLAN-090-converge-direct-compilation-through-one
#+COMPLETION_HASH: b08066201c5db427

* Initial Message

You are taking over from a competitor's agent.

Please assess the reasons for why the plan hasn't converged. Consider all angles, all the way down to the programming language design.

Come back to me with an in-chat explanation and your own strategy, from zero context, in simple language

* Purpose
Compiler inputs are finite trees. Producing lazy intermediate trees registers realized values in a VM-wide memo table whose lifetime exceeds compilation. Deleting quoted roundtrips reduces work, but does not by itself prove retention is bounded. Make compiler-owned transformations use eager data with the lifetime of a compilation unit. Preserve language-level lazy sequence semantics; this task is distinct from BUG-028's memo correctness repair.

* Scope
Start at priv/boot/compiler2.bl, priv/boot/anf.bl and priv/boot/lower.bl, following only transformations that construct/traverse compilation data. Inspect current core collection primitives before choosing replacements. Reuse existing eager host operations or add one narrowly named eager internal traversal primitive if none exists. Do not change public map/filter/lazy-seq semantics or globally make the language eager. Coordinate lower.bl ownership with module-contract and vertical-slice tasks.

* Implementation
Measure first: compile a fixed representative small and large fixture under generation receipts, collecting elapsed time, process/VM memory, lazy memo size/memory and loaded-module counts before, during and after each unit. Separate permanent executable module retention (closures can reference it) from temporary syntax/IR retention. Repeat multiple units in one VM. Use existing profiling facilities where available.
Replace lazy map/mapcat/filter/concat chains that build finite compiler data with eager list/vector construction, preserving source evaluation order, clause order and error timing. Pay special attention to compile-time effects: eagerly traversing a user lazy value is not allowed merely because it passes through an evaluator. Only compiler-owned finite structures qualify. Canonical module descriptors must not contain deferred compiler thunks. Drop references after emission; do not retain complete trees in a global diagnostics/cache structure without an explicit lifetime.
Do not purge loaded eval/body modules to improve the graph: returned closures may depend on them. Do not use whole-table memo clears or force GC repeatedly as the optimization. One post-unit GC for measuring retained memory is acceptable if reported.

* Verification
Record baseline and changed measurements using the same inputs, generation and settings. Add a deterministic structural/regression check that canonical compiler artifacts are eager and do not register lazy memo entries in the covered pipeline; if bootstrap initialization contributes fixed entries, warm it first and report that fixed cost. Assert behavior, guard ordering, exception timing and reproducible output remain unchanged. Run repeated compile units and show temporary-data retention does not grow proportionally to all prior units; separately report expected loaded-module growth.
All risky processes run asynchronously in memory-capped systemd scopes. Record peak memory and OOM status, not only successful elapsed time. A run killed by the cap is failed evidence, not a benchmark result.

* Completion
Finite compiler transformations on the migrated canonical route use eager structures; targeted behavior and determinism tests pass; repeat-unit evidence shows removal of compiler lazy-memo accumulation. State remaining memory sources explicitly. Do not claim that arbitrary user lazy-value reclamation or loaded-module lifetime is solved.

* Binding lifetime and determinism decisions
Do not introduce scoped compiler memo tables in this task. The canonical compilation pipeline instead contains no compiler-owned LazySeq nodes or delayed compiler functions at all. Validate descriptor trees and instrument the lazy registration/force boundary during a warmed compilation unit so covered transformations cannot silently fall back to memoized laziness. User literal data remains distinct from deferred compiler traversal; if a user runtime value cannot be embedded as a Core literal, refuse through the existing literal error path rather than realizing arbitrary user effects. No close-scope API or escape proof is necessary because the compiler introduces no such scope.
Byte reproducibility uses FEAT-037's pinned cross-VM AOT domain, excluding deliberately fresh runtime module names. Memory acceptance is a retention trend plus zero compiler memo-registration proof, not a hard total-VM byte ceiling. Warm once, run at least three identical compilation batches with the same unit count, then report per-batch retained memory after one measurement GC and loaded-module counts. Temporary compiler data must not remain reachable through memo entries; expected module/code memory growth is reported separately and is not hidden by purging.

NOTE [2026-09-07]: Verified eager traversal milestone: /tmp/bl-plan090-eager-final.log reports 89/89 ExUnit, no skips/exclusions; 29 language tests, 110 assertions, zero failures/errors. Current canonical record ABI also supports Elixir literal/pattern compilation and redefinition (__info__(:struct), exports fingerprint); reserved __struct__ fields reject before replacement. Shared workspace validation replaces nonexistent File.realpath in Spell. Eager helpers refuse deferred/improper inputs and preserve callback order and exceptions. Controlled same-VM comparison against immutable commit 3f0db2d, small and 64-binding large fixtures, 3 batches x10: baseline registrations 1630 small /13910 large per batch, eager 0 for every batch; emitted byte hashes agree across generations and repeats; loaded module delta 0. Baseline elapsed_us small167276/180335/160634, large1187750/1232721/1170209; eager small22234/21035/21270, large148718/145330/145492. Whole comparison command later failed only because a concurrently added compiler2 corpus fixture was not yet converted; the performance/hash sub-gates completed. Later 89-test gate passes with fixture scope isolation. Durable evidence: !tasks/artifacts/eager-gate-result.log and compiler-retention-comparison.log. Candidate seed /tmp/bl-plan090-candidate-15729b4f/seed contains33 modules under key4950131e7dadc1d1..., with source-owned boot definitions pruned before re-emission; fresh-VM smoke passed without the legacy host. Committed seed remains unchanged. Pending final closure: docs, full regression, final legacy deletion/canonical namespace rename and final seed publication. Do not treat focused gates as full convergence.

NOTE [2026-09-07]: Implemented and committed fba3291/bc0e656. Controlled same-VM three-batch small/large comparisons preserve emitted hashes and report zero compiler LazySeq registrations (baseline1630/13910 per10units), with zero loaded-module deltas. No scoped memo table or user-laziness change. Proper/improper/deferred inputs, order and callback exceptions have behavioral coverage. Executable finite-data guide passes; native/code lifetime limits are explicit. Final full suite1487passed and explicit uncached141-source build succeeds. Evidence: !tasks/artifacts/PLAN-090-final-verification.md.