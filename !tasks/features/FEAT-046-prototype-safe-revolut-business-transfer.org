#+TITLE: Prototype safe Revolut Business transfer preparation
#+STATE: DONE
#+SESSION_ID: 158aea01f2254f71
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-23T13-26-59-784Z_158aea01f2254f71.jsonl]]
#+CUSTOM_ID: FEAT-046-prototype-safe-revolut-business-transfer

* Initial Message

Use the beam-lisp-harness MCP to read my Gmail through the existing beam-lisp connector and stored grant. Register :~msg/messages, provision
 ▏ my Gmail connection, and query it with datom/q. Summarize what needs my attention and recommend next actions, citing message subjects and
 ▏ senders. This connector currently reads at most 20 messages per query, so don’t claim you reviewed my whole inbox. Never print tokens or
 ▏ secrets; if access fails, report the error rather than saying there are no messages.


my gmail connection is already there, can you go there and find messages from GrowIn? Show me which quries worked

Goal: In a new git worktree branched from main, create a standalone Beam Lisp prototype for Revolut Business API transfer preparation. No live money movement, beneficiary creation, credential ingestion, or integration into the existing connector registry yet. Research current official API requirements and model amounts as exact minor units, account currency, beneficiary identity, idempotency, and explicit approval boundary. Implement focused tests with mocked transport and documentation for setup/limitations. Acceptance: worktree branch exists off current main HEAD; tests pass; example prepares/validates but cannot submit a transfer. Existing dirty main workspace remains untouched.

## Implemented boundary
Worktree `/home/user/code/undefine/beam-lisp--revolut-business` is branch `prototype/revolut-business` from `main` commit `1cf72d26`. Main's pre-existing uncommitted files are untouched. The standalone planner at `examples/revolut-business/prototype.bl` validates explicit payer/counterparty/beneficiary IDs and positive EUR amounts in exact cents or strict decimal strings, renders JSON numeric amounts, and returns a review preview without opening a socket. The separate `examples/revolut-business/sandbox.bl` uses a fixed sandbox host for GET `/accounts`, GET `/counterparties` (first page), and POST `/payment-drafts` only. It accepts an access token per call, stores none, never sends a draft for processing, never targets production, never retries POST, and returns status-bearing errors without echoing provider error bodies. No real Revolut credentials or account details have been used; no sandbox or production Revolut requests have been made. The API's POST creates a draft, not a payment; the user must verify account/payee/amount/reference independently and send a draft in the Revolut Business app.

Verification: `bl test test/bl/revolut_business_prototype_test.bl` → 126 assertions passed, 0 failed; `git diff --check` clean. A loopback-only transport probe showed POST `/payment-drafts` carries JSON content type/body and returned status 201. `bin/bl test` in the fresh worktree cannot boot because its local `.bl/.../lazy_memo.so` is missing; the installed `bl` launcher runs focused tests successfully. Global `bl check` is not claimed as passing. Official contract: https://developer.revolut.com/docs/api/business/operations/create-payment-draft.md .