#+TITLE: Broadcast a board on write, so a second tab is not stale
#+STATE: DONE
#+SESSION_ID: 15636107deb514e2
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-08-23T20-27-46-426Z_15636107deb514e2.jsonl]]
#+CUSTOM_ID: FEAT-016-broadcast-a-board-on-write-so-a-second-t

* Outcome — DONE 2026-08-25

beam-lisp 7b2b8cc (topic accessor), 34cba7b (connected mount subscribes),
b3756ea (announce/2 + ex-info-head quoting); reel 976b57d (transact-as!
announces, @role becomes a declared assign, on-info re-reads).

The design survived contact: the payload is a BASIS, never a projection.
Verified live with two real browser tabs — one row, "written by the other
tab", offering [doing, dropped] to tab B and [dropped] to tab A (product)
AT THE SAME INSTANT. Tab A went 4→5 tasks untouched and kept its own role.
Evidence: !tasks/evidence/FEAT-016-two-tabs.txt

Two bugs found on the way, both upstream, both now covered:

- `ex-info-head` emitted `{:spell/changed, _}` — a syntax error, since an
  Elixir atom containing `/` must be quoted. The first contract to use a
  namespaced info tag generated a module that would not compile,
  surfacing as "cannot compile module … (errors have been logged)" with
  no line and no clue. Nothing covered `ex-info-head` at all.
- `@role` had to move from page-local to DECLARED assign: `on-info`
  re-reads on the server and cannot see a browser signal, so a woken page
  would have silently reset to the default lead.

beam_lisp 659 tests / 1736 assertions, 0 failures; reel 65 passed;
check.exs 8/8.

Still open, deliberately: the adjacent cleanup at the foot of this item
(@components and @leads computed on every refresh, rendered by nothing).

* The defect

`Phoenix.PubSub` is started in reel's supervision tree
(`lib/reel/application.ex:58`, `{Phoenix.PubSub, name: Reel.PubSub}`) and
NOTHING EVER PUBLISHES TO IT. Two tabs on one board diverge permanently.

Reproduced without a browser (2026-08-25):

    tab A at mount:        0 task(s)
    (tab B creates a task)
    tab A still holds:     0 task(s)   <- stale
    tab A if it re-asked:  1 task(s)

The last line is the important one. **Re-asking is already sufficient.**
Nothing is missing from the rendering path; what is missing is a REASON
to run it again.

* First principles

** What is true at the instant of a write

Exactly one thing: the database advanced from basis N to basis N+1.

That is the entire content of the event. Which rows changed, whose board
now differs, what any given tab should render — all of it is DERIVED by
the reader from its own question. The writer cannot know it.

** ∴ the payload is a basis, not a board

The tempting design is to broadcast the fresh board. It is wrong, and
wrong in a way this very session already paid for.

`board-assigns` is ROLE-SCOPED — `task-rows db role`. Tab A is product,
tab B is tech. If A's write broadcasts A's board, B renders affordances
computed for the wrong lead and is offered `doing` as a product lead:
exactly the defect commit 2107d67 removed, reintroduced through the back
door, and HARDER to see because it needs two tabs as two leads.

A broadcast cannot carry a projection. A projection is a function of
(database, who is asking) and the sender knows only its own half.

** ∴ broadcast adds no rendering path

Each subscriber re-runs `:refresh`, which it already has, with its own
role. A second way for a page to acquire state — parallel to `read!` and
able to disagree with it — is the thing to avoid. One need, one
implementation.

* Why upstream, not in reel

The real `Reel.Web.BoardLive` is GENERATED by
`spell.contract/machine-module`; `lib/reel/web/board_live.ex` is a
placeholder replaced in the code server at boot. A `handle_info` written
there does not exist on the module that serves the page.

And the receiving half ALREADY EXISTS: `on-info` (contract.bl:110)
parses a server-internal message and `ex-info-head` (contract.bl:543)
generates a `handle_info/2` head per pattern. Nothing ever subscribes,
so no message reaches the head being generated.

That is a one-directional gap in precisely the shape `unfired-events`
was written to catch — a receiving half with no sending half. The
asymmetry recurred in a second place, which is worth noticing.

* Verified before designing

1. **PubSub server name must be configuration.** beam_lisp names
   `SpellWeb.PubSub` (application.ex:31), reel names `Reel.PubSub`
   (application.ex:58). Hardcoding either upstream is wrong.
2. **No new walker capability is needed.** `register_performer/2`
   performers run in the LiveView's OWN process (server.ex, comment at
   perform_intents) and return assigns to merge. A broadcast is
   something the performer does; `do!` already reaches it. Do NOT widen
   the walker's whitelist for this.
3. **`mount/3` runs TWICE** — disconnected static render, then
   connected. `server.ex` has NO `connected?/1` call anywhere; the
   double-mount is currently handled by refusing writes in a mount
   event, not by branching. A subscribe on the disconnected pass joins a
   topic from a process that exits immediately.
4. **`datom/basis-t` exists** and `scripts/check.exs` already uses it to
   prove a mount does not write. The primitive is there.

* Design

** Contract declares its topic

    (topic :board)

alongside `assign` / `on` / `on-info`. Absent ⇒ no subscription emitted,
so every existing contract is unaffected.

** Generated mount subscribes, on the CONNECTED pass only

`spell.server/mount` gains a subscribe when the term declares a topic
and `Phoenix.LiveView.connected?(socket)` is true. Server name from
application config, not a literal.

** The domain announces

`reel.intent/perform`, after a SUCCESSFUL write only:

    (do! :announce {:topic :board :basis (datom/basis-t db')})

A refusal announces nothing — there is no new basis. This keeps the
whole feature inside the existing intent seam.

** The page re-asks itself

    (on-info [:spell/changed basis] <the body :refresh already has>)

Own role, own question, own answer.

* Acceptance

- TWO mounted sockets in ONE test: A writes, B sees it. A single-socket
  test cannot fail for the reason this feature exists.
- A test that B is mounted as a DIFFERENT ROLE than A and still gets
  ITS OWN affordances — this is the regression that the
  broadcast-the-board design would introduce, so it must be pinned.
- A refused write broadcasts nothing.
- A contract with no `(topic …)` emits no subscribe (every existing
  contract keeps working).
- `mix beam_lisp.test` green; reel `mix test` green; `check.exs` 8/8.
- Browser: two tabs, create in one, watch the other.

* Adjacent, same file, cheaper

`@board`, `@components` and `@leads` are computed on every refresh and
rendered by nothing (`check.exs` reports all three). `@board` is
load-bearing — the refresh handler destructures it — but `@components`
and `@leads` are pure waste on every board read. Either render them or
stop computing them.

* Independent re-derivation (2026-08-25), and what it added

A later session reached this item's design from first principles WITHOUT
having read it: payload is a basis not a board; invalidation coarse now,
precise later; and the same (db, role) argument for why a rendered
projection must not travel. That is weak evidence the design is FORCED
rather than chosen — the role-affordance failure mode is discoverable
from `board-assigns`' arity alone, before anyone opens two tabs.

Two things the re-derivation added that this item does not already carry.
Recorded here rather than reopening a DONE item; both are follow-on work.

** `read!` could RECORD as well as evaluate

`walk` already accumulates `:intents` from `do!`, `:pushes` from `push!`,
`:ask` from `ask!`. `read!` alone evaluates without recording — correctly,
because a body must compute with the answer, but the two are not exclusive:

    (= hd "read!")
    (let [op (walk (second form) env st)
          payload (walk (nth form 2 nil) env st)]
      (swap! st (fn [s] (assoc s :reads (conj (get s :reads []) {:op (name op) :payload payload}))))
      (perform-read (name op) payload))

~5 lines, and the walker gains ZERO authority — recording is exactly as
pure as `:intents` already is. `handle` would then answer with the
dependency set the body actually touched.

What that buys: dependence becomes OBSERVED rather than DECLARED, so
`:topic :board` need not be written by hand at all — the read name IS the
topic, and it is already registered via `register-reader!`. A contract
that reads something is subscribed to it by construction, which is the
same "one need, one implementation" argument `:topic`'s own docstring
makes against a second spelling of `:mount-event`.

It also SUBSUMES the adjacent cleanup above. `@components` and `@leads`
computed-and-rendered-by-nothing is an assign nothing depends on — with
reads recorded, that is a DERIVED fact rather than a `check.exs`
heuristic, and the same machinery answers "what invalidates this page?"
and "what does this page waste?".

** A watermark would make live-push and reconnect ONE path

As shipped, `on-info` re-reads unconditionally and the basis in the
message is bound but unused. That is correct and cheap, and it leaves one
gap: PubSub is fire-and-forget, so a page that missed a broadcast while
backgrounded — or across a socket blip — stays stale until something else
wakes it. Same class of gap this item fixed, at a different cause.

A socket holding `t_seen` closes it:

    t_seen <  basis  → re-read (or `reel.read/changed-since` for the delta)
    t_seen >= basis  → ignore

The ignore-arm is also free idempotence: the writer's own socket, which
already got its board from `perform`, drops its own echo without
`broadcast_from`/`self()` bookkeeping — and unlike the pid-based answer,
a value-based one still works when there are two nodes.

`reel.read/changed-since` already exists and already computes this.

** Monotonicity, checked rather than assumed

Both of the above rest on the basis being totally ordered. Verified:
`next-tx-id` is a store counter from `FIRST-TX`, allocated inside the
single writer, and RECOVERED from the store on reopen — so the order holds
across a restart, not merely within one VM. `transact-in-writer` advances
the basis with `max` rather than `reset!` for the same reason (BUG-022).
If either were process state, both designs above would collapse.

