#+TITLE: FUP: provec gate could not self-verify (no session git baseline)
#+STATE: ITEM
#+SESSION_ID: 156ed468261ed466
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-01T17-55-09-080Z_156ed468261ed466.jsonl]]
#+CUSTOM_ID: FEAT-034-fup-provec-gate-could-not-self-verify-no

* Initial Message

make wry first-class and exceedingly simple to set up in beam-lisp. show me the syntax and how you'll do the first prototype.

Follow-up bookkeeping only — the underlying WORK IS COMPLETE and committed.

## What happened
The roster node "Build + prove height round-trip + screenshot + commit" (FEAT-033)
carries a verify.commit gate. The gate repeatedly reports:
  "gateCommit: No git baseline was available to verify the commit against."
This session resumed mid-work across several aborted turns, so the gate never
recorded a pre-work git baseline; it therefore cannot diff HEAD against a
baseline to confirm the commit — regardless of how many real commits land.

## Evidence the work is actually done (git log, main)
- e52e19f  wry: first-class native WebView + loom.desktop, glass statusbar with
           dropdowns  (14 files: native/wry_webview, priv/wry.bl,
           priv/loom/desktop.bl, examples/desktop/*)
- 77dc81c  docs(FEAT-033): mark statusbar layer-shell + dropdowns DONE
- 07c85ab  chore(FEAT-033): gate evidence — height round-trip proven

## Proof of the deliverable (observed, not inferred)
- Surface grows 40->200px on dropdown open (layer-surface remap); JS state
  survives the remap; shrinks back to 40px on close (headless: /tmp/h_cycle.bl
  → "grow h=200 n=42" / "shrink h=40 n=42").
- Auto-height path stable (open/close via reducer, driver alive throughout).
- Screenshots on niri: docked glass bar, dropdown overlaying below without
  moving tiled windows.
- FEAT-033 marked DONE with full root-cause note; playbook PB-docking-a-wry-bar
  saved.

## Action
None required. This FUP exists solely so the roster node can be closed with a
reference. If the gate baseline can be reset in a future session, re-run
`mix compile` + the h_cycle smoke and mark verified; otherwise no work remains.
