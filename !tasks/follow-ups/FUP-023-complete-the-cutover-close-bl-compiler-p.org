#+TITLE: Complete the cutover: close .bl compiler parity gaps, then delete .ex frontend (CK3)
#+STATE: ITEM
#+SESSION_ID: 156c0f8c84d848d3
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp--self-hosted/2026-08-30T14-18-06-227Z_156c0f8c84d848d3.jsonl]]
#+CUSTOM_ID: FUP-023-complete-the-cutover-close-bl-compiler-p

* Initial Message

ok so make sure no mention of redb stays and then complete the cutover

** State (measured this session)

THE CUTOVER is live: `Compiler.compile/2` delegates to the self-hosted
`BeamLisp.Ns.Compiler` (`.bl`) under the default `:auto` backend. The fixpoint
holds — `priv/compiler.bl` self-compiles 143/0 to byte-identical AST, reader/
reader-node/oracle 0 FAIL. The frozen seed EXISTS as the AOT beams
(`Elixir.BeamLisp.Ns.Compiler.beam` + `Ns.Body.Compiler.beam`, `Ns.Reader`).

The DoD (PLAN-057 P13, item 5) gates deleting `lib/beam_lisp/{reader,compiler}.ex`
on **full `mix test` green**. That gate is NOT met yet. Differential-oracle
measurement (the discriminating method):

  - genesis backend (`:compiler_backend = :genesis`): 62 failures  <- pre-existing baseline
  - .bl backend (`:auto`, the cutover):                116-129     <- variance is the FUP-014 flake

∴ ~39 failures are CUTOVER-INTRODUCED (fail on `.bl`, pass on genesis) — real
`.bl`-compiler parity gaps. The other 62 are pre-existing (FUP-014 Env-lifecycle
flake + example-script infra: bundled z9 missing, `namespace not found:
semantic.*/relations.*`, Bandit absent). ExamplesTest passes 0-failures IN
ISOLATION but fails in-suite — a shared-state ordering flake, not a compiler bug.

** Closed this session (metadata-on-name cluster, ~9 tests)

`(def ^:private x 42)`, `(defn ^{:doc "d"} ^:private f […] …)`,
`(def ^{:doc …} …)`, defmacro name meta — the `.bl` compiler dropped author
metadata on the NAME symbol (only a docstring STRING was read). Fixed via
`name-author-meta` + `var-meta-map` merge + `lower-meta-value` (Macro.escape for
rich values). Commit c3ac945. Wave25Reader 22->27/29; fixpoint still 143/0.

** Remaining cutover-introduced gaps (the differential set, by category)

*** Compile-time privacy enforcement (Wave25SpecterGaps, 4)
`.bl` does not reject, AT COMPILE TIME, access to a `defn-` private var by
qualified name / alias / `:refer` from another namespace, nor an empty defn-
body. Genesis has `private-fetch-quoted` + `reject-private-link` +
`private-var?`. Port these checks into the `.bl` call/symbol compile path.
Tests: "defn- … not reachable by qualified name / through an alias / cannot be
:refer'd / rejects an empty body".

*** Guard validation (GuardsTest, 4)
`.bl` does not: refuse a side-effecting call in a guard NAMING the vocabulary,
refuse a dangling `:when`, refuse excessive nesting by name, or reject a guard
reading a destructured parameter. Genesis's `compile-guard`/`compile-guard-form`
raise `CompileError` with the guard vocabulary; the `.bl` guard compiler is
thinner. Port the guard dialect checks.

*** Caps gating at compile time (CapsTest, 5)
`.bl` does not deny, at compile time, static Elixir/Erlang interop or `defnative`
in a capped env, nor enforce fork caps = parent ∩ spec. Genesis has
`gate-remote!` + `caps-allowed?` in `slash-target`. Port the compile-time
capability gate into the `.bl` remote-call path. (Security-relevant — do this
CAREFULLY, it is a trust boundary.)

*** defprotocol / defmulti docstring (Wave25SpecterGaps 3, Wave11Multi 1)
`.bl` `compile-defprotocol`/`compile-defmulti` drop a leading docstring between
the name and the first method. Store it as var metadata like defn.

*** Records / deftype (Wave24Records, 4)
pr-str round-trip of a record literal (read back to an equal record), deftype
inline protocol impl, `.-field` access. `.bl` defrecord/deftype parity.

*** Destructuring (Wave18 1, Wave19 2)
`:as` in a rest pattern (binds whole rest / nil when exhausted); `:as` in loop
bindings rebinding each iteration. `.bl` `destructure-vector` `:as` in rest slot.
(`:strs` map destructure also missing — seen in Wave8.)

*** Macro / form metadata (Wave15Macro 3, Wave14Meta)
`&env` compile-time locals map; a with-meta'd form round-trips; form metadata
never leaks into value equality/printing. `.bl` macro-env / FormMeta parity.

*** Misc (JankCompat 2, Wave20Locations 1, Wave26AtomGuard 1)
jank slices (bit-not, distinct?), a runtime error naming the .bl file+line,
novel-keyword atom-guard path. Investigate individually.

*** Trailing attr-map after docstring (Wave25Reader 1)
`(defn f "doc" {:attr v} [x] …)` — Clojure attr-map after the docstring. `.bl`
`parse-fn-clauses` treats it as a body form. Parse + merge into var meta.

** Method

For each category: read genesis's implementation (`lib/beam_lisp/compiler.ex`),
port the equivalent into `priv/compiler.bl`, rebuild
(`rm the compiler beam; mix compile.beam_lisp --source-dir priv`), run the
category's test file, and CONFIRM THE FIXPOINT STILL HOLDS
(`mix beam_lisp.run research/sh13_cutover/fixpoint.bl` → compiler.bl 0 FAIL)
after every compiler.bl change. The differential oracle (bl-fail ∧ genesis-pass)
is the acceptance signal per test.

** Then CK3 (the actual deletion)

Once full `mix test` is green (modulo the FUP-014 flake, run per-file if needed):
move the genesis lowering (`compile_elixir/2` + `do_compile` + the reader body)
to `bootstrap/genesis/{compiler,reader}.ex` labeled "not the language — the
seed's Elixir origin, kept to regrow it", leaving only the thin delegators
(`compile/2`, `macroexpand_1/2`, `new_env/1`, `read_all/read_one` delegating to
`Ns.Reader`). Then CK4 (fresh-clone cold boot from the frozen seed, ties off
FUP-013).

** Provenance
PLAN-057 P13 / FUP-016. redb removal + wow diagnostics + try/finally fix + name
metadata this session (commits 20da0be, f3148e7, c3ac945). Fixpoint harness:
research/sh13_cutover/fixpoint.bl.
