#+TITLE: PLAN-112 leftovers: the z3_stats MCP tool, oracle/explain in synthesis, and the corpus' advisory re-read noise
#+STATE: ITEM
#+SESSION_ID: 157bcd97046112e7
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-11T19-45-37-298Z_157bcd97046112e7.jsonl]]
#+CUSTOM_ID: FUP-071-plan-112-leftovers-the-z3-stats-mcp-tool

* Initial Message

ok please. what is the final, drema state, and what is left? please do a review of the codebase and tell me what's left

* What PLAN-112 deliberately left, with the evidence needed to finish each

All three are small; none blocks anything. Listed here so they are not re-derived.

**1. `bl z3 stats` as an MCP tool.**
The report exists as a function (`bl.z3/stats-report` in `priv/std/bl/z3.bl.md`, `--json`
available on the CLI), so this is a thin wrapper in `priv/lib/mcp/tools.bl`. Not done because
that file was another session's WIP during PLAN-112. The tool should return the same map the
CLI renders. Acceptance: `z3_stats` appears in `tools/list`, answers on the repo, and its
coverage field is the same assertion the CLI makes (an empty corpus must not answer "ok").

**2. `oracle/explain` in the SYNTHESIS path.**
`repair-process` now carries `:blame` from `oracle/explain` (PLAN-112 P3), which is its first
engine caller. Synthesis (`synthesize-capacity`, the `:len-bound-inductive?` lane) still gets
a bare refusal. The dual-lane rule is already written down in core.bl: a REFUTATION is `sat`
so its hint is a MODEL (`oracle/witness`); a BLAME is a question expecting `unsat` so its
hint is a CORE (`oracle/explain`). Acceptance: for a capacity query that fails, the result
names which of the named parts is at fault, with a test that pins the keyword.

**3. The corpus' 5 advisory re-read failures.**
`examples/z3/10-corpus.bl` and `bl z3 stats` report `re-read failures: 5-6`, all docstring
false positives: prose that contains the word `defserver` (core.bl, z3corpus.bl, mcp/tools.bl,
priv/std/reload/upgrade.bl, examples/system/14_discover_invariant.bl). No FORM in any `.bl`
file is missed, and the count never affects the coverage assertion. Finish by making the
re-read attempt conditional on the text look like a form (a leading `(defserver`), or by
classifying these as `:advisory`. Acceptance: the failures count is 0 on the tree, or the
report separates advisory from real, with a test for each.

**4. Not a task, a caveat.** `BUG-041` (analyzer counts leak between analyses — test ORDER
changes the result) is pre-existing and orthogonal: run the z3 suites in their own
`mix bl test` invocation until it is fixed, or a red `check-test`/`lsp-test`/`mcp-project-mount-test`
will look like it came from this lane. It does not.
