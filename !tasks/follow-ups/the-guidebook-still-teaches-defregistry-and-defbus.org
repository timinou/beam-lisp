#+TITLE: the docs still teach defregistry and defbus, which were deleted; the guidebook's examples should run as written
#+STATE: ITEM
#+SESSION_ID: 15865cad45844222
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-18T11-27-51-014Z_15845ec9b98441d6/9-Tier093.jsonl]]
#+CUSTOM_ID: FUP-111-the-docs-still-teach-defregistry-and-def

* What

Clause094 deleted `defregistry` and `defbus` (they are `(registry …)` and
`(bus …)` clauses on `proc.server/defserver` now). The CODE moved; the DOCS did
not, so the guidebook still teaches two forms that no longer exist.

Remaining references, measured 2026-09-20:

| file | what |
|---|---|
| `docs/guidebook/05-the-registry.bl.md:26` | `(reg/defregistry sessions (keys :user-id))` and the prose item 1 that explains `defregistry` |
| `docs/guidebook/06-the-bus.bl.md:33` | `(bus/defbus payments (demand 16))` |
| `docs/the-five-bundles.md:145,165,182,198,281-283,310-311` | the `defregistry` / `defbus` sketches, the "Implementation" notes, and the build ledger's steps 5 and 6 |
| `docs/the-process-pattern-language.md:392,705` | the API bullets that name `defregistry` as the Registry surface |
| `docs/guidebook/08-the-capstone.bl.md` | fixed 2026-09-20 (the snippet now reads `(proc.server/defserver … (bus …))` / `(registry …)`), listed here only for the sweep's completeness |

* Why it matters

These documents INSTRUCT. A reader who copies `(reg/defregistry workers (keys :id))`
gets `unknown clause`-free… no: gets nothing, because the form is gone — the
compiler reports an unknown head with the accepted vocabulary, which is a fine
error but a poor tutorial. `docs/bl/03-…` is not the issue; the guidebook is the
front door.

* Acceptance

- No `.bl.md` / `.md` under `docs/` shows `defregistry`, `defbus` or `defbeat`
  outside a historical note that says so.
- Every example in the guidebook runs as written (`./bin/bl run <the extracted
  cell>`), including `05-the-registry` and `06-the-bus`.
- The five-bundles ledger's steps 5 and 6 name the clause spelling.

* Evidence

- `docs/guidebook/08-the-capstone.bl.md` — the same sweep done for the capstone.
- `examples/bundles/00-all-five.bl` — Clause094's own migration of the same forms,
  the spelling to copy: `(proc.server/defserver workers (registry (keys :id)))`.
- `docs/scheduling/the-clock-and-the-wheel.md` §4.6 and its comparison table —
  the authoritative before/after for this migration.
- `docs/guidebook/05-the-registry.bl.md`, `docs/guidebook/06-the-bus.bl.md`,
  `docs/the-five-bundles.md`, `docs/the-process-pattern-language.md`.
