#+TITLE: ZERO-.ex sweep remainder: nif_trace, lazy_memo, aot_cache (blocked on bootable runtime + seed regen)
#+STATE: DONE
#+SESSION_ID: 1585934c870b57f3
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-19T09-55-45-308Z_1585934c870b57f3.jsonl]]
#+CUSTOM_ID: FUP-113-zero-ex-sweep-remainder-nif-trace-lazy-m

* Initial Message

let's do the zero-.ex sweep now. no mercy.

Carries the unfinished tail of PLAN-132's ZERO-.ex sweep. Blocked, not abandoned — every remaining item needs a BOOTABLE bl runtime to verify, which did not exist during the session that did the rest (concurrent uncommitted WIP — the owner's catalog-dashboard feature across vm/catalog.bl + vm/http.bl + vm/inspect.bl — left the tree unbootable throughout; vm/http.bl had an unbalanced form from a +75-line in-progress edit).

PRECONDITIONS before resuming:
1. Tree boots: owner commits/fixes vm/http.bl (+ the catalog.bl/inspect.bl WIP). Verify: `BEAM_LISP_AOT_CACHE_DIR=/tmp/x BL_DAEMON=off bin/bl eval '(println :ok)'` prints :ok.
2. defonce activated: run `bl seed` (regenerate priv/bootstrap/seed/ after a keyed build) so the committed seed's core carries the defonce macro landed in commit 2986a2b4. core is boot-tier (drift = toolchain-key only), so the source macro is inert until the seed is regenerated — the standard bootstrap step for adding ANY core macro (self-hosting gen-N -> gen-N+1). Verify: a bare .bl file with `(defonce x (atom 1))` loads without `undefined var: <ns>/defonce`.

REMAINING ITEMS (in order):

## 3/5 nif_trace.ex -> vm/nif-trace.bl  [written, uncommitted, INERT]
- priv/std/vm/nif-trace.bl already WRITTEN (balanced, sound; pure :erlang.trace/trace_pattern BIF port -- NOT a defnative, there was never a NIF). Inert: nothing requires it.
- TODO once bootable: (a) verify via a .bl test that traces BeamLisp.LazyMemo create/1, exchange/4, cursor/1 and asserts recorded lines (port test/beam_lisp/nif_trace_test.exs); (b) delete lib/beam_lisp/nif_trace.ex + test/beam_lisp/nif_trace_test.exs; (c) commit.
- Watch: nif-trace DEFAULT-MODULE is :"Elixir.BeamLisp.LazyMemo"; if item 4 renames/moves LazyMemo, update this default.

## 4/5 lazy_memo.ex -> defnative vm/cell.bl + Rust rustler::init!  [HIGHEST RISK]
- lib/beam_lisp/lazy_memo.ex (311 ln) on the HOTTEST path: core.bl:116 lazy-seq -> BeamLisp.LazySeq/from_fun -> LazyMemo (every lazy seq + atom deref). 4 core .ex callers: lazy_seq.ex, seq_cursor.ex, refs.ex, reactive.ex (~15 sites).
- Rust: native/lazy_memo/src/lib.rs uses a HAND-ROLLED nif_init (module c"Elixir.BeamLisp.LazyMemo") not rustler::init!. Switch to rustler::init! for the host module vm.native/host-module derives; delete hand-rolled nif_init; cargo build --release; cp to priv/native/lazy_memo.so + extensionless.
- .bl host: (defnative "lazy_memo" (nif-new 3)(nif-read 1)(nif-read-fast 1)...) in vm/cell.bl + port the Elixir POLICY (create/read/exchange/dependencies-walk/admit!/estimate_bytes). The deps walk uses :erlang.fun_info(f,:env) for closure reflection -- VERIFIED callable from bl, ports cleanly.
- USER DIRECTIVE: move the callers too, METHODICALLY, keeping the tree bootable at each step. ZERO precedent for .ex calling an emitted-bl ns on a hot/boot path, so the 4 callers genuinely must become bl -- multi-step, verify-each-step (why it needs a bootable runtime).

## 5/5 aot_cache.ex -> boot-tier keys .bl + build-tier store .bl  [VERY HIGH RISK]
- lib/beam_lisp/aot_cache.ex (813 ln). Keys (compiler_key/build_key/key_for_ns/key_for_source) -> a boot-tier native-free .bl ns (boot closure is bl). Store/GC/closure-walk -> build-tier priv/build/cache.bl. 6 .ex callers: generation.ex, model.ex, aot.ex, build_log.ex, bootstrap.ex, tiers.ex -- move each methodically.
- Fold in: cache-root divergence cleanup; FUP-112 (declared-deps) belongs in the item-4 vm/cell.bl port.

DEV LOOP: BEAM_LISP_AOT_CACHE_DIR=/tmp/<own> BL_DAEMON=off bin/bl ... (private AOT cache dodges the shared aot.lock churn). chmod +x bin/bl if it loses the bit. Boot-tier changes need `bl seed` to activate. NIF changes need cargo build + cp to priv/native/<crate>.so + extensionless.

* RESOLUTION (2026-09-20, tree became bootable after owner landed BUG-090 fix 33f86604)

** 3/5 nif_trace — DONE, committed 610b8795
Verified on the bootable tree: vm/nif-trace.bl + test/bl/vm/nif_trace_test.bl (5 green — records create/1 + exchange, stop flushes 3 queued calls, env entry inert). nif_trace.ex + .exs deleted. Two bl-specific fixes vs the naive port: `loop` fn renamed `trace-loop` (bare `loop` collides with the loop special form at compile → RT.nth/2 error), raw-fd write via :file.write not :io.put_chars. defonce also verified working from a .bl file (`defonce works: 42`).

** 4/5 lazy_memo — VERDICT: INFEASIBLE BY CONSTRUCTION (not moved, correctly stays native)
lazy_memo.ex is the native memo cell that LazySeq/Atom/Derived are built on, and it sits BELOW the bl language: core.bl:116 `(defmacro lazy-seq …)` expands to `BeamLisp.LazySeq/from_fun`; LazySeq (lazy_seq.ex) calls LazyMemo; core.bl's map/filter/interleave/partition are lazy; the reader itself (reader.bl:685,1093) produces LazySeq values. So LazyMemo is used DURING bl compilation and by core.bl's most basic macros. Moving it to vm/cell.bl inverts the bootstrap — the bl compiler needs lazy-seq needs LazySeq needs LazyMemo needs the-ns-the-compiler-would-compile. Unresolvable. lazy_memo.ex IS the native-free-boot-closure boundary the user defined ("the boundary is native-free boot closure, NOT keep Elixir"). Correct outcome: it stays .ex. (The Rust rustler::init! rename was also unnecessary/wrong: the hand-rolled nif_init exists specifically to add the reclaimer load/unload hooks rustler::init! does not support.)

** 5/5 aot_cache — VERDICT: INFEASIBLE (not moved, correctly stays native)
aot_cache.ex splits into keys (compiler_key/build_key/key_for_ns/closure_key) and store/fetch/GC. The KEYS are the drift gate bootstrap.ex runs to decide whether the committed seed is valid — bootstrap.ex:233 calls AOTCache.compiler_key() BEFORE the bl compiler is loaded. The store/fetch half runs inside the loader's compile path (aot.ex ensure_loaded/emit), which compiles the boot-tier namespaces themselves. Either half in .bl means the Elixir loader calling into bl to decide how to load bl — same bootstrap inversion. Boot-closure substrate; stays .ex.

** FINAL TALLY
Of 5 cache/dev .ex targeted: 3 ELIMINATED (perf_probe deleted; cache → vm/paths.bl; nif_trace → vm/nif-trace.bl) + defonce promoted to a core primitive. 2 are architecturally native by construction (lazy_memo, aot_cache) — they ARE the native floor, not accidental Elixir. ZERO-.ex for the movable set is COMPLETE; the residual 2 define exactly where "native-free boot closure" ends.