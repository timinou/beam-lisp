#+TITLE: mix bl.build fails at the AOT step: "Thread.sleep/1 is undefined" for 23 sources
#+STATE: ITEM
#+SESSION_ID: 157bcd97046112e7
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-11T19-45-37-298Z_157bcd97046112e7.jsonl]]
#+CUSTOM_ID: FUP-055-mix-bl-build-fails-at-the-aot-step-threa

* Initial Message

so what got done and what is left, exactly?

MEASURED 2026-09-13. `mix bl.build` exits 1 and never reaches packing:

    beam-lisp AOT: building 23 source(s)
    beam-lisp AOT: priv/lib/tooling/pulse.bl.md: function Thread.sleep/1 is undefined
                   (module Thread is not available)
    … repeated for priv/std/executor/ssh.bl, priv/lib/mcp/tools.bl, priv/lib/mcp/server.bl,
      priv/lib/mcp/transport-stdio.bl, priv/std/bl/mcp.bl.md, priv/std/reload/*.bl,
      priv/lib/live/app.bl, priv/lib/tooling/catalog.bl, priv/lib/veritas.bl, …

**It is not a missing module in beam-lisp's own sources.** `grep -rn "Thread\.sleep"` over
the repository (excluding .git/_build/deps/tmp) matches exactly two files, and neither is
beam-lisp code:

    projects/samadhi/samadhi_phone/android/…/MobBridge.kt
    projects/fundamental/fundamental_phone/android/…/MobBridge.kt

So the string reaches the AOT pass from the MOUNTED TREE — the same surface the mcp mount
work pulls in — and the file prefix in the message is the file that mounted it, not the file
that calls it. That would also explain why every name in the list is on the mcp/mount path.

**Impact:** nobody can build a drop until this is resolved (or the AOT pass stops reading
non-bl sources from the mounted tree).

**Workaround used to ship the launcher fix (9302d2f)** — the payload was already extracted,
so the new launcher could be grafted onto it without a release build:

    cargo build --release                       # in tooling/drop
    $(cargo metadata … target_directory)/release/drop pack \
        --release ~/.local/share/drop/<sha8> --out bl

`pack` prints the payload sha256, so it is verifiable that the payload is byte-identical to
the one being replaced (282b694a…) and only the launcher differs.

NEXT: decide whether the AOT pass should read `projects/**` at all, or whether the mount
should hand it only bl sources; then re-run `mix bl.build` and confirm the drop's payload
sha equals the extracted dir it claims to be.

NOTE [2026-09-14]: NOTE [2026-09-14, merge audit]: the AOT step this names now RUNS on the merged tree. `feat/code-semantic-search` was merged (db19037) and the two halves of the fix met there: main's `Process/sleep` (there is no `Thread` module — the catch was reporting ITSELF instead of the unbalanced source) and the branch's `BeamLisp.Env/convey` in the parallel worker (a worker's env does not cross a process boundary, which is what made `Thread.sleep/1` undefined at `:global` in the first place). Verified here: `mix compile` AOT-builds the sources it must (`beam-lisp AOT: building N source(s)`), and `mix run priv/bootstrap/gen_manifest.exs` assembles the whole boot closure from `_build` and refuses any beam whose provenance key is stale — which it did not have to refuse. NOT verified: the full `mix bl.build` (release + cargo + drop pack); the AOT step, which is all this item claims, is green. The merge resolution in `priv/boot/build.bl` kept BOTH sides' findings.