#+TITLE: Self-hosting: the live runtime cutover + substrate migration (dedicated session)
#+STATE: ITEM
#+SESSION_ID: 156b39b3bdb8f77a
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp--self-hosted/2026-08-29T22-43-47-574Z_156b39b3bdb8f77a.jsonl]]
#+CUSTOM_ID: FUP-016-self-hosting-the-live-runtime-cutover-su

* Initial Message

how hard would it be to bootstrap beam lisp fully?

** Context

PLAN-057 self-hosting is complete through CHECKPOINT 2 preflight (17/22 waves):
the .bl reader+compiler compile the WHOLE prelude (932/932 forms) byte-identical
to the Elixir compiler, reproduce their own source (the fixpoint), run real code
end-to-end (preflight: sum-sq=30 via the .bl backend), handle author metadata,
and are analyzed by the language's own tools. The mechanism is fully proven.
This FUP covers the remaining work, which is a coherent DEDICATED session, not
tail-of-marathon work.

** Why these were deferred (honest reasons)

The live cutover flips the LOAD-BEARING `Compiler.compile/2` and deletes ~2400
lines. Its validation gate is the full `mix test` (863 tests), which:
- has a PRE-EXISTING Env-lifecycle flake (FUP-014) that makes pass/fail hard to
  read cleanly;
- takes ~15 min per run;
- a latent .bl bug on any runtime edge-case the AST-corpus (932/932) did not
  exercise would break the ability to compile ANYTHING, including the rebuild.
Rushing this at the end of a very long session is the one place where haste is
genuinely dangerous. The mechanism is de-risked; the careful wiring + full-suite
validation is what remains.

** Remaining items (from PLAN-057 board)

*** CUTOVER (the live delegation flip)
- Add to `Compiler.compile/2` (compiler.ex:119) a delegation guard: delegate to
  `apply(BeamLisp.Ns.Compiler, :compile, [form, env])` WHEN
  `Code.ensure_loaded?(BeamLisp.Ns.Compiler)` and it exports compile/2; else run
  the existing Elixir body (the stage-0 seed that compiles compiler.bl on a cold
  build). Same for `Reader.read_all/read_one` -> `Ns.Reader`. This is the
  frozen-seed ladder; no infinite regress (compiler.bl calls macroexpand_1/
  new_env/Env/Link, never Compiler.compile).
- GATE at each step: full `mix test` green (modulo the FUP-014 flake, run
  per-file if needed), `mix beam_lisp.test`, the 932/932 corpus, the fixpoint,
  jank slices, benches within budget.
- Then DELETE the ~2400 lines of Elixir lowering below `compile/2` and the
  Reader body, keeping ONLY the thin retained substrate: `compile/2` (delegating),
  `macroexpand_1/2`, `new_env/1`, `read_all_data/1`, `read_all/read_one`
  (delegating). Move the deleted source to `bootstrap/genesis/{compiler,reader}.ex`
  labeled "not the language \u2014 the seed's Elixir origin, kept to regrow it".

*** CHECKPOINT 3 (post-cutover) — full acceptance
- `.ex` frontend gone from the runtime path (grep-clean:
  `grep -rn 'BeamLisp.Compiler.compile\|do_compile' lib/` returns only the thin
  delegator).
- fixpoint green over .beam; mix test (863) + beam_lisp.test + jank + benches +
  examples/self/compiler-as-codebase.bl all pass; tier-1-off still correct.

*** CHECKPOINT 4 (fresh-clone / cold boot)
- wipe _build, prove a clone boots from the frozen priv/bootstrap seed without
  the 30-60min AOT hang (ties off FUP-013).

*** P10 — runtime prims to .bl (SUBSTRATE — benchmark-exempt by the floor rule)
- The 335 rt prims (first/rest/count/map/reduce/...) are the HOTTEST path: the
  compiler itself calls `first` 140x. They are already leaf ops over :erlang
  BIFs; porting to .bl and AOT-linking yields the SAME BEAM code but adds a
  bootstrap-ordering hazard (they must AOT before the compiler that uses them).
  By PLAN-057's own floor principle ("substrate migrates only if .bl-expressible
  WITHOUT a benchmarked perf loss"), they stay UNTIL a benchmark earns the move.
  The cutover does NOT touch them. If pursued: port a pure non-hot slice first
  (inc/dec/second/nth-derived), bench each op, keep any that regress as thin
  :erlang wrappers "earned by a number".

*** P11 — data structures to .bl (SUBSTRATE — same rationale)
- Vector 32-way trie / Set / LazySeq over :erlang bitwise. Same floor-principle
  deferral; 100k-conj bench (12ms bar) must not regress. Enumerable/Inspect
  defimpl is the deliberately-last Elixir-shaped seam (only for Elixir-side Enum
  interop; .bl's own map/filter never need it).

** Acceptance

- Runtime path routes .bl loading through .bl reader+compiler; .ex frontend
  deleted (only thin delegators + retained substrate remain).
- All CHECKPOINT 3 gates green.
- Fresh-clone cold boot fast (CHECKPOINT 4).
- P10/P11: either done with per-op perf budgets met, or explicitly kept as
  benchmark-exempt substrate with the decision recorded.

** State of the tree (all committed on feat/self-hosted-compiler)

priv/reader.bl, priv/reader-node.bl, priv/compiler.bl (self-documented, 100%
docstring coverage), priv/self/oracle.bl. Proofs + gates in research/sh*/ .
docs/self-hosting.md written. ~40 commits, each gated. The .ex frontend is
UNCHANGED and still the live path \u2014 the repo is in a safe, fully-working state.

NOTE [2026-09-01]: READER CUTOVER SEAM landed (session 156c5bab, 2026-08-30), config-gated opt-in — NOT auto-flipped.

MECHANISM (mirrors Compiler.compile/2 ↔ compile_elixir/2 exactly):
- `BeamLisp.Reader.read_string/2` is now the backend switch: `use_bl_reader?()` → `:reader_backend` app-env (`:auto`|`:bl`|`:genesis`). `:auto` delegates to `BeamLisp.Ns.Reader` once the `reader` ns is interned (Env.loaded_ns?), else genesis. All 5 live call sites (compiler.ex:60/2809/2819, aot.ex:115, compile.beam_lisp.ex:432) + read_all/read_one/read_data/read_all_data funnel through read_string, so ONE seam flips everything.
- Genesis body renamed `read_string_elixir/2` (public) — the bootstrap seed + oracle yardstick, never deleted. Added `read_all_elixir/1` as the oracle answer key (parity/position tests + priv/self/oracle.bl repointed to it, else they self-compare once reader ns interns).
- `read_string_bl/2` translates reader.bl's internal exceptions (BeamLisp.ExInfo / AtomGuard.LimitError) back to the PUBLIC contract (Reader.SyntaxError / Reader.AtomLimitError) so a delegated read is byte-identical at every observable point incl. the rescueable type.
- `enable_bl_reader/0` (mirrors enable_bl_backend/0): interns reader ns, forcing :genesis during the load so reader.bl's own tail isn't read by the half-built bl reader. NOT called from init/0 (deliberate — see perf).

PARITY EARNED (was the real gate — the 45-case suite was too weak):
- Corpus differential over ALL 408 real .bl files, both readers, position-bearing: 408/408 byte-identical, 0 structural diffs, 0 error-class mismatch. Found + fixed 3 divergence classes the snippet suite missed, all reader.ex QUIRKS reader.bl now reproduces bug-for-bug (else diagnostics shift):
  (1) fn-literal `#(...)` returned-position 2-col undercount → sibling after `#()` was 2 off;
  (2) data-reader `#tag"..."` / record `#Name{}` returned-position undercount (tag/name length) → sibling off;
  (3) BIG: `^` author metadata was DROPPED by reader.bl (written to match read_all); read_string must ATTACH it as `{:meta, form, author-map}` — the compiler reads it via name-author-meta (:private/:doc/:args/:ret; "^ meta flows to the checker"). Rewrote read-meta to attach (metadata-spec + attach-meta + metadata-key), read_all still strips.
- Tests: reader_parity 15/45, reader_position 16/38 (added fn-literal-sibling, tag-sibling, caret-metadata-attached, read-all-still-strips), reader_atom_guard 4/7, NEW reader_cutover 8/14 (forces :bl, proves seam transparent + exception contract + read_all/read_one inherit + :genesis override). All green. 120 Elixir reader units green.

PERF FINDING (why config-gated, per this FUP's floor principle): bl reader is LINEAR but ~27× slower than genesis (core.bl 68KB: 3.7ms ex vs 57ms bl; scales cleanly 27× at n=100..800 — constant factor, not O(n²)). reader.bl IS AOT-compiled (Elixir.BeamLisp.Ns.Reader.beam) — the tax is inherent self-hosting overhead, same KIND as compiler.bl's 5× (which ships default-on). By P10/P11's rule ("migrates only if .bl-expressible WITHOUT a benchmarked perf loss; keep regressers as thin wrappers earned by a number"), the reader flip stays OPT-IN until the 27× is closed. Correctness is DONE and proven; only perf gates default-on.

REMAINING before reader.ex deletion: (a) close/accept the 27× perf gap → decide default-on; (b) add `reader`+`reader-node` to priv/bootstrap/seed/ + manifest IF the compiler seed ever needs the reader pre-built (currently reader.ex genesis reads reader.bl from source, so seed not strictly required — unlike compiler which deleted its genesis fallback). reader.ex then stays as genesis + differential oracle, exactly like compiler.ex.