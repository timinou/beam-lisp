#+TITLE: the daemon half of lib/ is bl: the sunset plan for elixir in this tree
#+STATE: ITEM
#+SESSION_ID: 1581e38fd270ecd3
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-16T13-12-30-281Z_1581e38fd270ecd3.jsonl]]
#+CUSTOM_ID: FUP-082-the-daemon-half-of-lib-is-bl-the-sunset

* Initial Message

/home/user/code/products/knowledger
∙ bl gateway
bl gateway: not running
  bl gateway start       start it now (detached)
  bl install gateway     keep it running across logins
∙ bl gateway start
bl gateway: starting… (a cold start takes a few seconds)
bl gateway: up on 7777
∙ bl install gateway
bl install gateway

  ok   unit  /home/user/.config/systemd/user/bl-gateway.service
  ok   service  enabled + started (systemctl --user)
  --   port 80  run: bl install redirect (loopback only, removable), or: sudo sysctl -w net.ipv4.ip_unprivileged_port_start=80
  ok   next  open what it routes: bl ports, then a name



look at localias and how it works. i want automatic port management for all bl daemons in my home smply, and without making 80 overwritable (like localias). Look at how Caddy does it and does auto-cert and tell me what the BEAM-native (and the beam-lisp native) way of doing it looks likw

* The ask that added this item

why would elixir own primitives? the full cutover of the daemon to beam lisp can be an FUP, but we
need to have a plan for sunsetting that

* The question, and what PLAN-116 got wrong

PLAN-116's D6 said *"what stays Elixir forever — the daemon (tree identity, tokens, protocol),
launcher, native glue, and read-model shims. Anything that decides moves."* That list conflates
three different things, and only one of them is irreducible:

| # | category | can it run before bl exists in the VM? | verdict |
|---|----------|----------------------------------------|---------|
| T1 | the language's own bootstrap: reader, compiler, AOT cache, loader, `RT`, `Env`, the runtime types the compiler LOWERS ONTO | yes — it is what makes bl exist | irreducible until self-hosting (a separate, much larger project) |
| T2 | the program `bl` runs: `lib/beam_lisp/daemon/` — tree identity, paths, the socket protocol, the registry, names, the gateway, the CA, the HTTP reader, the executor, the workers | **no** — every one of these needs only sockets, processes, timers, filesystem and a place to be started from, all of which bl now has | **policy in the wrong language, with a sunset** |
| T3 | native artifacts: `native.ex`/`native_task.ex`/`z3_port.ex`, `cargo.ex` (NIF/port plumbing) | n/a — C/Rust, not Elixir policy | the artifact stays; its Elixir-facing shim is a name bl can call |

The discriminating question is not "is it a primitive?" (every daemon module can be called one) but
**"does it have to exist before the first line of bl runs?"** Nothing in T2 does. `Ports` is a file
format and a claim protocol. `Names` is string arithmetic. The daemon's `Server` is `gen_tcp.listen`
plus a framed protocol. The gateway is socket → socket. The CA is `x509` over `:public_key`.

The counter-argument is not "it is impossible", it is **"it is not free"**: the daemon half is the
crash-restart surface, and a bl namespace has no OTP-grade supervisor today. PLAN-116's prototype
answered that with `super/defsupervisor` + `reg/defregistry` (it really does build a supervisor tree,
with the child spec being `(start-link <defserver> <args>)`) — mitigated, not eliminated. So this is
a *scheduled* move with exit criteria, not an aspiration.

* The inventory (measured 2026-09-16, `wc -l` over `lib/**/*.ex`)

| tier | modules | lines |
|------|---------|-------|
| T1 the language (compiler / AOT / loader / `RT` / `Env` / runtime types) | 61 | 16,180 |
| T2 the daemon (`lib/beam_lisp/daemon/` + `daemon.ex`) | **17** | **5,364** |
| T3 native glue | 4 | 1,017 |
| dev-only (`lib/dev/dev_server.ex`) | 1 | 37 |
| total | 83 | 22,598 |

(The 17th daemon module is `lib/beam_lisp/daemon.ex` itself — the session's identity — which sits
beside the directory, not in it. A `find lib/beam_lisp/daemon -name '*.ex'` misses it, and `rg -l
'BeamLisp\.Daemon' lib/` — the acceptance below — does not.)

This session measured them: `find lib -name '*.ex' -exec cat {} + | wc -l` = 22,598 over 83 modules;
`lib/beam_lisp/daemon/` = 16 modules / 5,184 lines, plus `lib/beam_lisp/daemon.ex` = 180.

T2, named — the list this FUP burns down:

| module | lines | what it is | moves in |
|--------|-------|------------|----------|
| `paths.ex` | 227 | tree id, runtime dir, endpoint paths | S1 |
| `names.ex` | 204 | name derivation (`env.bl` + branch → hosts) | S1 |
| `ports.ex` | 356 | the claim registry + the allocator | S2 |
| `protocol.ex` | 130 | the client↔daemon framing | S3 |
| `listener.ex` | 277 | the accept loop + socket peer checks | S3 |
| `server.ex` | 612 | the tree's warm session (dispatch, sandbox handoff) | S3 |
| `daemon.ex` | 180 | start/stop/status/hello — the session's identity | S3 |
| `workers.ex` | 108 | the daemon's process table | S4 |
| `watch_registry.ex` | 250 | who is watching what (the bus substrate) | S4 |
| `index_worker.ex` | 213 | the background index build | S4 |
| `io.ex` + `std_err.ex` | 420 | stream forwarding across the daemon boundary | S4 |
| `executor.ex` | 203 | running a form in the sandbox | S4 |
| `inspect.ex` | 305 | reading live state (the read model) | S4 |
| `http.ex` | 599 | the HTTP/MCP surface of a session | S4 |
| `gateway.ex` | 1,046 | the name gateway | S5 (PLAN-116/117) |
| `ca.ex` | 234 | the local CA | S5 (PLAN-116/117) |

Evidence that T2 is *reachable* from bl — all measured 2026-09-15/16:

- bl owns a raw listener, an accept loop, and a process per connection (PLAN-116 prototype, plus
  PLAN-117 E4's gateway, which is a real one);
- bl owns a unix socket, and Bandit serves HTTP over one (PLAN-117 E1);
- bl terminates TLS by BOTH entry points: `ssl.listen`/`transport_accept` (E2) and the upgrade path
  `ssl.handshake` on a socket accepted by `gen_tcp` (E4) — which additionally requires
  `gen_tcp.controlling_process` to the handling process, also proven;
- bl matches vector-tagged messages in `receive` (`(receive [:go sock] …)`) — E4; that is the
  hand-off primitive a bl accept loop needs;
- bl builds an OTP supervisor tree (PLAN-116 prototype);
- the bl side already speaks the daemon's protocol: `priv/std/bl/daemon.bl`, `serve.bl.md`,
  `gateway.bl.md`, `watch.bl.md`, `ports.bl.md`, `install.bl.md` are its clients, and
  `priv/lib/code/index.bl` already asks the daemon for work.

* What stays Elixir, and why that is an answer rather than a dodge

A language cannot be built by itself before it exists. `RT` (2,928), `Env` (1,374), `Aot` (973),
`AotCache` (821), `Loader` (742), `Reader`/`Compiler`/`Emit`, `CompilerData`, `AtomGuard`,
`FormMeta`, and the runtime types the compiler LOWERS ONTO — `Vector`, `Sorted`, `Record`, `Refs`,
`Set`, `Num`, `LazySeq`, `Multi`, `Decimal` — are the machine, not the program. The measured
contract (grep of `priv/boot/*.bl` + `priv/lib/system/*.bl`): the compiler emits or calls
`BeamLisp.Env` (37×), `RT` (24×), `CompilerData` (22×), `Reader` (10×), `Vector` (9×),
`AtomGuard` (9×), `ExInfo` (7×), `Record` (6×), `FormMeta` (6×), `Refs` (5×), `Num` (5×),
`Loader` (5×), `Set` (4×), `Link` (4×), `LazySeq` (4×), `Multi` (3×).

That tier is not a privilege, it is a *contract* — and this FUP's job includes keeping it from
growing: **a module in T1 is a module bl cannot express yet, and every new name in it must be
justified as such.**

* The sunset, in stages — each with an exit criterion

- **S0 (in force now)** — no new policy in Elixir. A new capability lands in `priv/std/bl/*.bl.md`
  first; if it cannot, the plan that added it says why. (PLAN-116's rule, kept and sharpened.)
- **S1 — identity**: `paths.ex` + `names.ex` become `priv/std/bl/{paths,names}.bl.md`. *Exit:* a bl
  program computes its own tree id, runtime dir, socket/token paths and host names, and the daemon
  takes those paths from the same computation (no second implementation).
- **S2 — the registry**: `ports.ex` becomes bl, including PLAN-117's broker (a listener handed over
  instead of a number reserved). *Exit:* `bl ports` and `bl gateway` read a registry whose writer is
  bl; the Elixir module is deleted, not wrapped.
- **S3 — the session itself**: `daemon.ex` + `server.ex` + `listener.ex` + `protocol.ex` — the warm
  session is a bl program. *Precondition (the pass-the-parcel):* a bl program can BE the daemon, i.e.
  `bin/bl` → bootstrap → `priv/std/bl/daemon.bl.md` (PLAN-116's S4 "detached boot of a std
  namespace", still unproven). *Exit:* `bl daemon start/stop/status` runs a namespace; the handshake
  and token path are PORTED (not rewritten); crash-restart is demonstrated by killing the namespace
  and showing the session comes back with a sentence naming what died.
- **S4 — the services**: `workers`, `watch_registry`, `index_worker`, `io`/`std_err`, `executor`,
  `inspect`, `http`. *Exit:* a session with no Elixir process in its supervision tree; the MCP/HTTP
  surface and the Watch bus served from bl.
- **S5 — the gateway and the CA** (PLAN-116/117). *Exit:* `gateway.ex` and `ca.ex` deleted, the
  read-model shims point at what the bl side publishes, the two PLAN-105 behaviour fixes kept.
- **S6 — the end state**: `lib/beam_lisp/daemon/` does not exist. What remains is T1 (+ T3's shims),
  and this FUP closes with a *gate*, not a note: a test that fails when a `.ex` appears under `lib/`
  outside an explicit allow-list (the allow-list being exactly the bootstrap modules), so the
  boundary cannot erode silently.

* What "done" means (machine-checkable)

1. `lib/beam_lisp/daemon/` is gone; `rg -l 'BeamLisp\.Daemon' lib/` returns nothing.
2. `bin/bl` starts the daemon as a bl program; `bl daemon status` works with no Elixir daemon module
   on the code path.
3. The corpus passes (`bl test`), the CLI verbs are unchanged, `bl doctor` reports no missing module.
4. The allow-list gate is in place and green.
5. The ported-not-rewritten list is honoured and tested: the token/handshake path, the sandbox
   handoff, stdio forwarding, and PLAN-105's two fixes (absolute-form normalization,
   one-request-per-proxied-route).

* Risks

- **Crash loops with no OTP supervisor above them.** Mitigation is PLAN-116's: the namespace is owned
  by the drop/daemon supervisor, the accept loop's death is visible in the log the verb reads, and
  the failure sentence names the namespace that died. Demonstrated at S3, or S3 does not close.
- **Hot reload / dev ergonomics.** The daemon's reload semantics (which module version a live session
  serves) must be preserved or explicitly re-decided. A bl daemon that cannot be hot-patched is a
  regression the user feels immediately.
- **The security surface must be ported, not rewritten**: hello/token, the peer check (same uid,
  socket perms), the sandbox boundary. A rewrite under time pressure there is how a local privilege
  hole opens.
- **Two implementations during the move.** Every stage is a cutover (one need, one implementation):
  the bl version replaces the Elixir one in the same change, never beside it.
- **Stopping after S5**, leaving identity/protocol in Elixir because "it works". S6's gate exists to
  make that visible.

* Non-goals

- self-hosting the compiler/reader (T1) — a different project with a different cost curve. Criterion
  for revisiting: bl gains a compiler written in bl that can build the tree's own image;
- rewriting the native NIFs (T3) in anything;
- any general "bl as a systems language" claim. This claim is narrow: **the program is bl.**

* Relationship

| artifact | what it does to this |
|----------|----------------------|
| PLAN-116 D6 ("what stays Elixir forever") | **corrected by this FUP**: the daemon is not forever. T1 stays; T2 moves; T3's shim becomes callable from bl. |
| PLAN-116 S4 (detached boot of a std namespace) | becomes a precondition of S3 here, not a curiosity. |
| PLAN-117 | its registry/broker change is S2 here; its gateway+CA cutover is S5 here. |
| `bl test` / the corpus | is the regression net for every stage. A stage the corpus cannot verify is not done. |

* 2026-09-17 — the vm.* cutover lands the daemon-as-bl-program precondition (PLAN-121)

PLAN-121 does the SUPERSEDE+PORT half named across S3/S4: it replaces the Elixir single-serial Executor + per-tree Server with the pure-beam-lisp global VM manager (priv/std/vm/{owner,scope,core,spec,manager}.bl), wired behind the EXISTING transport. On its completion:

- S3's precondition ("a bl program can BE the daemon") is DISCHARGED — bl daemon start boots vm.manager, a defserver, as the session; the request path is bl.daemon/handle under a per-VM env.
- S1 identity is partially delivered: vm.spec/read computes the collision-proof :id (name@instance, path-hash fallback) from env.bl + .env.local.bl — strictly stronger than names.ex (covers two same-branch worktrees). What remains for S1: paths.ex (tree_id/runtime_dir/endpoint paths) and the DNS-label formatting half of names.ex.
- The reload-ordering half of watch_registry (D3a in PLAN-121) becomes a per-VM sequencer in bl — S4's watch service, delivered early.

∴ after PLAN-121, this FUP's remaining scope is precisely the TRANSPORT + edge: listener/protocol/io/std_err/paths (S3/S4 transport) and gateway/ca (S5). Those are the PRESERVE items PLAN-121 explicitly does NOT touch.

* Reusable prior art for the remaining PRESERVE items (measured, sibling worktree beam-lisp--names @ ed57f531)

The PLAN-117 prototype is a RUNNABLE pure-beam-lisp transport wedge. Maturity ledger for the remaining PRESERVE burn-down:

| PRESERVE item | prototype? | where | maturity |
|---|---|---|---|
| gateway (reverse proxy) | YES, working | !tasks/evidence/PLAN-117-prototype/gateway.bl | routes Host/absolute-form/CONNECT, unix+TCP splice, TLS per-name, /bl.pac, refusal. Gaps: allocator (constant port), route table (reads registry per req), supervision, bodies/chunked, load. NOT product code — I4 rebuilds against real registry/CA/verbs. |
| transport (AF_UNIX) | primitives proven | app.bl + gateway.bl accept-loop | bl owns gen_tcp listen + accept + controlling_process handover (t3.bl), Bandit over a unix socket (E1), ssl:handshake on an accepted socket (E2/E4), receive of vector-tagged messages. Listener itself still Elixir. |
| ports (registry) | read+render proven; bl WRITE proven | portsview.bl, app.bl/claim.bl | writer/allocator still Elixir. F6: drop [:safe] decode (silently empties registry for VMs lacking the :dest/:unix atoms). |
| ca (TLS/CA) | TLS consumption proven; CA itself Elixir | k1/k1b/k2.bl | sni_fun from bl works; per-name leaf gated on a live claim ("registry is the cert's permission module"). ca.bl.md greenfield. |
| protocol (ETF) | NO prototype | — | term codec portable; client↔daemon framing greenfield. |
| io (group-leader) | NO prototype | — | bl sets/reads group_leader; frame-forwarding proxy greenfield. |
| std_err | NO prototype | — | greenfield. |
| paths | NO prototype | — | greenfield (S1). |

Key proofs from the K-spikes (de-risk the transport port):
- K10: a running bl service hot-reloaded under itself — live behaviour change, no restart; a broken edit HELD with a named reason; namespace state does NOT survive a reload (hits 2→0). ∴ listener/CA/route-table MUST live OUTSIDE the reloaded namespace (separate process/ETS/registry), never as plain namespace state.
- K1/K1b: the {dtls_upgrade,notsup} trap is an argument-SHAPE error (is_list false from a bl vector/concat), never the client — to-list the ssl opts.
- K2: TLS 1.3 sends no cross-SNI ticket; the cross-name resumption leak is closed.

NB the README bars promoting gateway.bl into priv/ as-is; the spike GRADUATES its skeleton, it does not move it.

* The end-state gate (unchanged, sharpened by PLAN-121)

S6's machine-checkable done list stands. PLAN-121 adds one gate ahead of it: after the cutover, a test fails if BeamLisp.Daemon.Executor / Workers is referenced anywhere (the serial worker is deleted, not wrapped) — the same allow-list mechanism, one entry.