#+TITLE: PLAN-029 remainder: run_test migration + exs verification after concurrent session settles
#+STATE: ITEM
#+SESSION_ID: 155ca7d3a4ea23a8
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-08-18T15-07-43-123Z_155ca7d3a4ea23a8.jsonl]]
#+CUSTOM_ID: FUP-004-plan-029-remainder-run-test-migration-ex

* Initial Message

the way that st templates are written in e.g. test/bl/spell/run_test.bl is super annoying. is there a reader macro or sth that automatically lets me write html without putting quotes around that makes is look like strings?

* Context

PLAN-029 (markup as hiccup trees) shipped its core as commit 53942e2:
tree-walk emitter in contract.bl (hole-markers, binding-ref-text, the
template-html brace scan and its angle-bracket attr heuristic all deleted),
machine.bl hole-roots → tree-holes, seed.bl migrated with byte-identical
golden (14/14). run.bl's tree-only refusal shipped as 6e43c2f.

The remainder exists because a concurrent session (PLAN-028 W4 code rung +
PLAN-031 live-state) was committing in the same repo during execution; two
uncommitted migrations were wiped by its git operations before being
committed.

* Remaining

1. *run_test.bl*: re-migrate fixtures to tree syntax (the completed migration
   was wiped; the file is at string-era HEAD) AND reconcile assertions with
   W4's intentional semantics — `(def pwned 99)` is now ACCEPTED (code rung
   via fence), so `a-form-that-is-not-a-definition-is-refused` and
   `the-source-is-NEVER-evaluated` need redesigning against defmacro/defserver
   refusal instead of def. Coordinate with the W4 session before editing —
   it owns the semantics.
2. *host_regen / loop / persist exs*: fixture migrations are committed
   (6e43c2f) but unverified — the concurrent session's in-flight
   spell.live-state edit broke app.bl load, taking every SpellCase suite
   down. Verify once the tree boots again.
3. *Full suite*: `mix test` + all `mix beam_lisp.test` suites green.
4. PLAN-029's org item was deleted mid-session (likely by the concurrent
   session's workers); this FUP carries the remaining truth.

* Acceptance

- run_test.bl green with tree fixtures AND W4-consistent assertions.
- exs trio green.
- `grep -n "class='" test/bl/spell/*.bl` → no string-template markup left.
- Full `mix test` green on a settled tree.

NOTE [2026-08-18]: Item 1 DONE by the concurrent session (6d4c031): run_test.bl migrated to tree fixtures with W4-consistent semantics (def/defn are code heads; refusal vocabulary moved to defmacro/junk heads), and undeclared-template-holes re-armed for tree bodies (the string-era guard had made it dormant). Independently verified this session: run_test 42/42, machine 18/18, view 34/34, contract_edn 18/18 — all green. Items 2-3 (exs trio + full suite) remain BLOCKED on PLAN-031's uncommitted live-state work: goldens not yet regenerated for the two-definition seed, persist tests still expect one view, host_regen reports .state* ghosts, loop rung-1 def-refusal predates W4. Those are the live-state session's in-flight scope, not hiccup-migration defects.
* W4-session update (2026-08-18, session 155b996b2e53d8b8)

Item 1 DONE (commits 6d4c031, 99ee66d): run_test.bl on tree fixtures,
pwned tests redesigned for W4 semantics in both run_test.bl and
loop_test.exs. run_test 42/42 green.

Bonus fix found while migrating: undeclared-template-holes was DORMANT
for tree bodies ((string? html) guard) — the blank-render guard silently
dead since 53942e2. Re-armed with a deref-node tree walk in machine.bl.

Item 2 partial: exs trio state on this tree —
- loop_test: 11/13; the 2 failures are ghost-selector refusals naming
  .state__* — live-state.bl's styles landing in the machine while its
  markup isn't in the candidate page. PLAN-031 in-flight, NOT W4.
- persist_test 2 failures: seed-assertion era ("clean seed machine"
  expects ["chat-view"]; the live-state default shell changes the seed).
  PLAN-031's call.
- emit_golden 4 failures: golden bytes predate hiccup trees — regenerate
  or re-record, PLAN-029's call.
- served_loop 4 failures + host_regen 2 (one timeout): unexamined,
  likely same era shift.
