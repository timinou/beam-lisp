#+TITLE: The routing edge: delete lib/beam_lisp/daemon, repoint ports/gateway/index to vm.*
#+STATE: DONE
#+SESSION_ID: 158301c372c2175d
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-17T10-02-56-331Z_158301c372c2175d.jsonl]]
#+CUSTOM_ID: FUP-085-the-routing-edge-delete-lib-beam-lisp-da
#+COMPLETION_HASH: effb819c581c5b89

* Initial Message

look at the beam-lisp projects in ~/code, and tell me the workarounds they have used and for what purpose. Realise that `bl` is supposed to be the one command they should run to let them do the stuff that they need to do. what is missing there? it can be:
- a bug
- a missing feature
- a bad design, especially in env.bl

the goal is that the typical DX is:
1. a global daemon
2. per-env 'VMs' that are project-specific, and have a nice routing to local hosts with clean hostnames for the web apps AND for  the dev tooling AND for side-apps wherever necessary (i.e. e.g. to arm a scenario test runner whenever I want)
3. easily see them all
4. warm daemons that cache indices (both codebase and semantic and AOT, content-addressed super lean)

i have had to fix a few bugs where this wasn't the case. please find where leaks happen, where hangs or contention happens. trust the BEAM to help you manage the concurrency story, and trust beam lisp to let you write those orchestrators properly

* Intent

PLAN-123 D1-D3 moved the daemon TRANSPORT + SESSION to pure beam-lisp
(vm.paths/names/wire/net/io/session) and repointed `bl daemon start` to boot it.
What remains to make `rg -l 'BeamLisp\.Daemon' lib/` empty is the ROUTING EDGE —
a distinct, larger surface with its own live consumers. This finishes FUP-082's
T2 by porting/repointing that edge and deleting lib/beam_lisp/daemon/.

* The remaining Elixir daemon modules and their consumers (measured 2026-09-17)

| module | lines | live consumer | action |
|--------|-------|---------------|--------|
| paths.ex | 227 | cache.ex | repoint cache.ex → vm.paths (ported, byte-identical) |
| names.ex | 204 | serve.bl | repoint serve.bl → vm.names (ported) |
| ports.ex | 356 | serve.bl, gateway.bl | PORT → vm.ports (--names worktree: read+render+WRITE proven) |
| gateway.ex | 1046 | gateway.bl | PORT → bl.gateway (FUP-082 S5; PLAN-117 prototype graduates) |
| ca.ex | 234 | gateway TLS | PORT → bl.ca (FUP-082 S5) |
| index_worker.ex | 213 | code/index.bl | PORT → per-VM warm base (vm.spec :base seam) |
| server.ex | ~560 | daemon_integration + vm_cutover tests, launcher autostart | delete after tests drive vm.session |
| listener/io/protocol.ex | ~590 | server.ex | delete with server.ex (vm.net/io/wire replace) |
| inspect/http.ex | ~900 | server.ex status/dashboard | PORT read-model → bl.inspect/bl.http over vm.manager/list-vms |
| executor/workers/watch_registry/std_err.ex | ~600 | server.ex | port watch reload-ordering to a per-VM sequencer; std_err → vm.io stderr |

* Sequence (each ends green on the corpus)

1. REPOINT THE EASY TWO: cache.ex → vm.paths, serve.bl → vm.names (both golden-tested).
   Delete paths.ex + names.ex. Gate: cache + serve green.
2. PORT vm.ports (claim registry + broker; F6: drop the [:safe] decode that silently
   empties the registry for VMs lacking :dest/:unix atoms). Repoint serve.bl + gateway
   route reads. Delete ports.ex.
3. MOVE THE TESTS: daemon_integration + daemon_vm_cutover drive Server.start_link →
   repoint to vm.session/start. Then delete server/listener/io/protocol/executor/workers
   (reload sequencer moves to a per-VM sequencer first) + std_err.
4. PORT THE READ MODEL: bl.inspect + bl.http over vm.manager/list-vms + owner counts.
   Delete inspect/http/index_worker (index → per-VM warm base).
5. THE GATEWAY (FUP-082 S5): graduate the --names PLAN-117 gateway.bl prototype against
   the real vm.ports registry + a bl.ca. Delete gateway.ex + ca.ex.
6. THE GATE: extend sunset_gate_test.exs @sunset_modules with every deleted module;
   rg -l 'BeamLisp\.Daemon' lib/ → empty.

* Verification
- Each port golden-tested against the Elixir it replaces BEFORE deletion (the pattern
  vm.paths/names/wire established: capture live output, assert byte-identical).
- The launcher (tooling/drop) autostart still works against vm.session.
- The sunset gate makes the boundary permanent.

* Relationship
| artifact | relation |
|----------|----------|
| PLAN-123 | this finishes its T2 (D1-D3 done; this is the routing-edge remainder) |
| FUP-082 | S5 (gateway/CA) is step 5 here; the rest completes the T2 sunset |
| vm.paths/names/wire/net/io/session | the substrate this repoints consumers onto |
| beam-lisp--names @ ed57f531 | the gateway/ports prototype ledger for steps 2 + 5 |

NOTE [2026-09-17]: COMPLETE. rg 'BeamLisp.Daemon' lib/ → EMPTY. All 16 daemon .ex (5113 lines) + facade deleted; 8 new vm.* modules (ca/ports/index/gateway/inspect/exec/http/client) on main (4e70ed52→c6a57344). vm.session composes them: rich status + :ui dashboard + sequencer. Proven live (start→status→eval 42→stop) + 13 vm suites + all migrated consumer tests green. Sunset gate seals it (17 modules banned, .bl scanned).