#+TITLE: mcp: a verdict outran its evidence — unknown read as violated; boundary fixed, engine still collapses
#+STATE: ITEM
#+SESSION_ID: 157bcd97046112e7
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-11T19-45-37-298Z_157bcd97046112e7.jsonl]]
#+CUSTOM_ID: FUP-058-mcp-a-verdict-outran-its-evidence-unknow

* Initial Message

ok can you explain to me how z3 is now integrated with beam lisp and how it is bein used to more of its capacities, and how we could get even more out of it? this is for design/brainstorm

* PARTIALLY FIXED — the MCP boundary is honest (4a0d3a0); the engine still collapses.

**The defect.** `preserves-joint?` returns `{:preserved (= "unsat" (z3/check …))}`
(`system/core.bl:701`), so a three-valued solver answer becomes two: `unknown` lands
on the NOT-PROVEN side, `verify-process` sets `:holds (and init-est (empty? failures))`,
and `mcp.tools:547` renders it `verdict "violated"`. "The solver gave up" and "your
invariant is broken" were the same answer.

**Why it became reachable now.** Before FUP-057 an obligation over ~5 s killed its
caller, so the undecided path was unreachable by accident. The deadline chain
deliberately makes a hard question return `"unknown"` — so the collapse converts what
was a crash into a FALSE CLAIM, which is worse. This is the fix that had to land
before the ceiling could be trusted.

**Reproduced by execution.** The balance machine (`^{:invariant (>= balance 0)}`,
which z3 proves in milliseconds), with the pool's ceiling brought down to 1 ms:

```
verdict -> {:verdict "violated", :tier :z3, :complete true,
            :warnings ({:message "invariant not preserved by withdraw"}), …}
```

**The fix at the boundary (done, 4a0d3a0).** The ledger already records every
decision, so it now records the STATUS too (`BeamLisp.Z3.Ledger.record/3`), and:

* `system.decide/undecided` and `undecided?` expose the trail's `unknown` decisions;
* `code-verify-handler` asks before letting a verdict out, and answers
  `verdict "undecided"` with a count and `:complete false`. `:tier` stays `:z3` — the
  oracle did answer, `unknown`.

Chosen over editing the engine's twenty call sites because it is one place, it covers
the whole path (including `repair`), and the mechanism it needs already existed for
attribution.

**Getting the test to reach the path DETERMINISTICALLY took three measurements:**

1. A 1 ms ceiling is NOT enough: z3 decides a one-variable linear query in
   microseconds, so the ceiling is a race — the same machine answered `holds` on one
   run and `unknown` on the next.
2. A hard conjunct is NOT enough either: with a handler that PRESERVES the invariant,
   z3 prunes the nonlinear term as irrelevant and answers in microseconds (measured —
   the test said `holds`).
3. What works: a handler that drives the state to a point that VIOLATES the invariant,
   which makes `¬inv'` trivially true and leaves `p>1 ∧ q>1 ∧ p·q = 43925…3463` as the
   entire obligation — the factoring instance, unsettled by z3 in 30 s. 3 of 3 runs
   green.

**NEGATIVE CONTROL.** With HEAD's `tools.bl` swapped in (0 `gave-up` references), the
same test reports `actual: violated` and 2 assertions fail; restored byte-identical
after. So the test catches the bug it claims to catch.

**Regression.** `test/bl/system` + `test/bl/veritas`: 66 files, 1329 assertions, 0 red.
Through the rebuilt drop (payload `67567ee5`): undecided 7 passed, deadline 12 passed,
and the deadline probe still `status: unknown` at 10021 ms.

---

**REMAINDER — the engine still collapses, and this is NOT closed:**

`verify-process` still returns `:holds false` and still emits
`"invariant not preserved by <label>"` for an UNDECIDED transition, so every in-tree
consumer — `bl check`, `verify-file`, `verify`, `reload/upgrade`, the tree-level
report — still reads "the solver gave up" as "violated". The MCP surface is fixed
because it asks the ledger; the engine does not.

The precise fix, and it is cheap because the instrument now exists: in
`verify-process`'s `step-rs`, bracket each `preserves-joint?` with
`(decide/clear-here!)` / `(decide/undecided)`, and then

* `failures` = only the transitions that are NOT preserved AND NOT undecided, so the
  warning names a real counterexample;
* a new `:undecided [labels]`, and `:holds` false only when neither failures nor
  undecided is empty (a proof must not be claimed on the strength of silence);
* propagate `:undecided` through `verify-process-report` (1467), `verify-file` (1490)
  and `verify` (1533), which aggregate per child;
* `verify-capacity` (1178), `verify-content` (1389) and `verify-liveness` (1442) each
  aggregate their own `= "unsat"` sites and need the same treatment — `verify-content`
  is the plausible one in practice, since its quantified array reasoning is the
  dialect documented as able to return `unknown`.

**Not observed today:** no obligation in the corpus is slow enough to hit the ceiling,
so the in-tree lie is theoretical for the safety engine and plausible only for the
quantified content dialect. That is a statement about measurement coverage, not a
proof — nothing times individual obligations yet.
