#+TITLE: A daemon-served `bl test` fails in full-suite runs: is-eq-report/7 undefined in a fork
#+STATE: DONE
#+SESSION_ID: 157b9728a48199f4
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-11T15-47-48-498Z_157b9728a48199f4.jsonl]]
#+CUSTOM_ID: FUP-044-a-daemon-served-bl-test-fails-in-full-su
#+COMPLETION_HASH: 6d87a2b635de51fd

* Initial Message

find and run the pulse example

* What this is, from zero context

`bl test` runs a tree's test files, each in its own isolated fork off one warm
image (the runner `ward` used to be; see PLAN-103 W4). A daemon can serve it:
the whole suite runs inside one daemon request, forking per file.

`test/beam_lisp/daemon_executor_test.exs` has a case for exactly that — "a bl
test suite runs non-halting and maps pass to 0" — which writes a scratch test
file and asks the daemon to run `bl test ok_test.bl`. It expects exit 0.

* The symptom (observed 2026-09-12, twice, in full-suite runs)

    ERROR in (ok-test)
      passes
    expected: passes
      actual: function BeamLisp.Ns.Core."is-eq-report"/7 is undefined or private

Exit code 1 instead of 0, so the suite reports a failing test that does not fail
standalone. The same case PASSED in an earlier full-suite run the same day, and
passes when its file is run alone (3 consecutive runs, 7 passed each).

* What is known

- `is-eq-report/7` is defined in `priv/std/test.bl` and referenced by the `is`
  macro; the test library is interned into `core` (`BeamLisp.Ns.Core`).
- The error is a MODULE call to a core function whose arity the loaded module
  does not export: the macro's expansion and the loaded module disagree.
- It needs the full-suite context to reproduce — some earlier test leaves the VM
  (or the AOT image) in a state where that disagreement exists.

* What is NOT known

Whether a specific earlier test causes it, and whether the arity mismatch is a
stale AOT beam, a second load of the test library into a different env, or the
daemon's fork base. Establishing that is the first task here.

* Why it was not chased now

The tree was being edited concurrently by another session during the runs that
failed (`mix.exs`, `priv/lib/datom*.bl`, `priv/lib/lsp/*.bl.md`, a new
`priv/env.bl`, a new `lib/beam_lisp/daemon/workers.ex`) — a stale/mid-flight AOT
is a live possibility, and attributing the failure while the build inputs move
is guesswork. The instructions to the next agent: reproduce with the tree
frozen, `mix test --seed 0`, and bisect the preceding test files.

* Acceptance

- The case passes in a full-suite run, and the reason it failed is understood
  well enough to state in one sentence (which of the three candidates above it
  was, and why).

* Context

PLAN-103 (the warm project session), W4 — the wave that made `bl test` mean the
isolated runner.

NOTE [2026-09-12]: Sharper signature, 2026-09-12 15:00. In a full-suite run the same class appears under TWO names, which localises it to the TEST LIBRARY interned into core: `BeamLisp.Ns.Core."is-report"/4 is undefined or private` (TestVerbTest's JSON case, with the suite's own output stored in the failure message) alongside the earlier `"is-eq-report"/7`. So the `is` macro's expansion and the loaded `BeamLisp.Ns.Core` module disagree on arity — i.e. the macro is current and the loaded module is stale, or the reverse. It reproduces ONLY inside the full-suite VM: the same case passes when its file is run alone (4/4 runs at 14:32, 14:44, 14:58). NOTE that the tree was being edited concurrently during every failing run (`priv/lib/lsp/{rpc,server}.bl.md` were mid-edit to the point that `mix bl version` failed with `unexpected )` at 14:57), so a mid-flight AOT/compile-closure state is a live candidate and must be excluded first: freeze the tree, `mix test --seed 0`, then bisect. First experiment: in the failing VM, print `BeamLisp.Ns.Core.__info__(:functions)` filtered to is-report/is-eq-report and compare with a fresh VM (`mix run`).

NOTE [2026-09-12]: RESOLVED 2026-09-12 15:45 — root cause found by forcing a deterministic reproduction instead of hunting suite order.

ROOT CAUSE: ward's `deftest-available?` asked only whether `deftest` resolves in core's ENV. But a compiled test file does not CALL an env var — `is` expands into a call to `BeamLisp.Ns.Core."is-report"/4` (and `deftest` into `test-record/6`), i.e. into the compiled core MODULE. Those two can disagree: a later reload of `core` puts the module back from its beam (`_build/test/lib/beam_lisp/ebin/Elixir.BeamLisp.Ns.Core.beam`), which carries core.bl alone — the test library is not in it — while core's env keeps the macro's home. The guard then said "available", `ensure-test-lib!` skipped the load, and every file in the run died with `is-report/4 is undefined or private` (or the `/7` twin). Confirmed by printing the state at the failure site in a real suite run: `is-report/4=false guard=true beam=.../ebin/Elixir.BeamLisp.Ns.Core.beam`.

REPRODUCTION (deterministic, in a fresh VM, no suite order needed): purge + delete `BeamLisp.Ns.Core`, load it back from its beam, then run `bl test` in-VM — same error, same text. Now a regression test: `test/beam_lisp/test_verb_test.exs`, "a test library that vanished from core's MODULE is re-loaded".

FIX: `priv/std/reload/ward.bl` — `deftest-available?` now requires BOTH the env var AND `assertion-runtime-loaded?` (`:erlang.function_exported/3` for `is-report/4` and `test-record/6` on the compiled core module). A miss reloads the library into the module, which is what the run needs.

EVIDENCE: the reproduction failed before the fix and passes after; `mix test test/beam_lisp/` went from 1535-1541/1541 with this class present to 1542/1542 passed, 0 failures, and got faster (234s vs 421s). The bl corpus lost one failure of the same class (8 → 7 files, the rest being the corpus's own business).