#+TITLE: Unified cache substrate — generalize the LazyMemo/fjall NIF-boundary pattern (probes first)
#+STATE: ITEM
#+SESSION_ID: 1585934c870b57f3
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-19T09-55-45-308Z_1585934c870b57f3.jsonl]]
#+CUSTOM_ID: PLAN-132-unified-cache-substrate-generalize-the-l

* Initial Message

can you look at the different caches across the codebase and tell me if there's something redundant?

* Status
* Status

ITEM. Phase 0 (probes) + Phase 1 (read self-guard, F1) shipped. Phase 5 FLAGSHIP shipped
2026-09-19: the derived embedding-column cache (datom.query.corpus), the first tenant of the
unified substrate.

** Shipped

- Phase 0: op counters in the lazy_memo NIF + bench (commit 8bd405b4).
- Phase 1 (F1): reads self-guard, nif_lane deleted — crossings/logical read 2.25 -> 1.13,
  op_lane 213k -> 0 (commit 73beeaf3).
- Phase 2 (F7) + FUP-022 + Phase 2b: landed by the datom-read session (d8a92fa4, 849ce66c,
  4d3576f9) — chunked scan (point-get-under-scan 3421ms -> 1.39ms), native columnar reads,
  the :columns resolve mode.
- Phase 5 FLAGSHIP: datom.query.corpus — memoises the assembled [[entity dvec]] corpus of an
  embedding column, keyed by (store, attr), discriminated by (basis-t, filter-mode, since-t).
  Routes the three re-assembly sites (analytic similar-to/cluster/project-2d, relations,
  semantic-via-analytic) through one cache. Pure bl (ZERO .ex): ETS table, policy in bl,
  vector decoding stays native.
  MEASURED (bench/corpus_cache_bench.bl, 8000 x 768 synthetic column, this host):
    cold (assemble + search) 1213 ms  ->  warm (cached + search) 293 ms  =  4.1x, ~920 ms/eval
    of assembly removed; bit-identical result order preserved. Assembly cost scales with N, so
    on the live 62 396-row corpus this is the ~3.85 s -> ~0 the handoff (§10) predicted.
  Invalidation is AUTOMATIC and sound: a transaction advances basis-t -> discriminator
  mismatch -> rebuild+overwrite. Never serves a basis the caller is not reading (tested).
  Tests: test/bl/datom/corpus_cache_test.bl — 10 assertions green (hit preserves rows+order,
  cached==fresh-assembly, transaction invalidates, old basis still sees its own rows, mistyped
  value skipped).

** Next

- Phase 3 (F5): native nif_dependencies walk; delete Elixir walk/4; append-only edge delta so
  chunk-boundary CAS stops rerouting (the Phase-0 n/32 finding).
- Phase 4 (F3): L2 eviction + native generation counter.
- Phase 5 continued (ZERO .ex): quantised-resident column (needs a NIF search-prequantised
  entry) as a follow-on; then AOTCache keys -> boot-tier .bl, store/GC -> build-tier .bl,
  cache.ex -> vm/paths.bl, lazy_memo/nif_trace -> defnative, delete perf_probe.ex.
- Phase 6 (F9): daemon owns L3 — align with PLAN-121/122/123.
- FUP: resident hnsw-adjacency (relations.bl) needs background warm-up + persisted index
  (cold build >60s, Agent get/update hit the 5s GenServer timeout) before a request path calls it.

* 1. Context — the two caching systems and the pattern between them

Two NIF-backed caches exist today, built on the same boundary discipline:

**LazyMemo** (lib/beam_lisp/lazy_memo.ex, native/lazy_memo/src/lib.rs) — in-memory cells:
- resource-as-cell: value in OwnedEnv/SavedTerm off-heap, identity = native u64, GC-owned
- two scheduler lanes (fast regular / DirtyCpu), SELF-GUARDING: the fast NIF answers
  :reroute for unbounded work (big term, new dependency edges) and the caller retries dirty
- decisions made once and threaded (cursor dirty_chunks at creation; byte estimate before crossing)
- native ownership graph: dependency edges + cycle detection in Rust; CAS publishes state AND
  sends notifications in one crossing
- budget admission: RETAINED_BYTES accounting, admit!/0 raises over budget, deferred reclaim thread
- truth probes in the NIF (nif_loaded? — "host-module reload cannot lie")

**datom_fjall** (priv/lib/datom/store-fjall.bl, native/datom_fjall/src/) — durable substrate:
- six-method Store protocol; ETS/map/fjall interchangeable above L1
- engine matched to workload by measurement (redb B-tree bulk-load ~13s vs fjall LSM memtable)
- atomic cross-index commit: index tag in first key byte -> one keyspace -> one Batch
- coarse boundary: whole tx per commit, whole tx's keys in ONE crossing (keycodec_batch,
  522ms -> 25ms measured); classification native to kill per-component BEAM allocation
- oracle + fallback: Rust codec byte-identical to codec.bl (differential-tested); out-of-lane
  -> nil -> bl fallback. Correctness never depends on Rust coverage
- explicit durability: WAL per put, fsync only at named fjall_sync (per-group fsync measured
  at 26s vs few hundred ms)

THE SHARED IDEA (the thing to generalize): state lives native behind a resource handle; the
boundary is crossed once per unit of work, at coarse granularity; the fast path refuses what
it cannot bound and the caller falls back; identity, accounting, and durability boundaries are
explicit and probed from the native side.

* 2. Flaws found (evidence: file:line, observed vs inferred)

LazyMemo:
- F1 read/1 pays TWO crossings always (nif_lane then nif_read*, lazy_memo.ex:219) and races:
  an exchange between them can grow the cell past the ceiling so nif_read_fast copies a
  megabyte term on a regular scheduler. CAS self-guards with :reroute; read does not.
  OBSERVED.
- F2 estimate_bytes = :erts_debug.flat_size on the CALLING (regular) scheduler — O(term)
  before any lane is chosen (lazy_memo.ex:187). The lane-selection step can violate the 1ms
  bound the lanes protect. OBSERVED design; magnitude inferred.
- F3 No eviction — only refusal. Over budget -> admit!/0 raises ForceError; GC is the only
  way space returns. Fatal if generalized as-is into a unified cache. OBSERVED.
- F4 One global GRAPH mutex serializes every allocate/CAS; would_cycle walks while holding it.
  OBSERVED.
- F5 dependencies/1 walks terms in Elixir with 2 NIF calls per embedded handle
  (nif_dependency_resource + nif_id, lazy_memo.ex:108-171) — the per-item crossing tax
  keycodec_batch eliminated in the fjall crate. OBSERVED.
- F6 Accounting is estimate-only (flat_size overcounts shared subterms), never reconciled
  with real RSS; the 512MB budget is heuristic. OBSERVED design, impact inferred.

fjall:
- F7 fjall_range materializes the WHOLE window into BEAM terms in one call (lib.rs:122-156).
  No cursor/chunking — LazyMemo's 32-head cursor pattern was not reused. FUP-022 adjacent.
  OBSERVED.
- F8 Every op DirtyIo including memtable-hit point gets. On THIS host ELIXIR_ERL_OPTIONS pins
  ONE dirty-IO scheduler (+SDio 1:1) — every fjall op queues on a single thread; a long range
  scan blocks point gets behind it. OBSERVED (config + host env).
- F9 Exclusive file lock + eager partition creation: opening for READ still takes the lock
  and can mutate the directory. conn.bl's registry/release! machinery exists because of this;
  two bl processes cannot share an analysis store. Durable tier is single-process by
  construction. OBSERVED.
- F10 keycodec.rs mirrors codec.bl byte-for-byte; bl is the oracle so the Rust copy can never
  become canonical — permanent parallel-implementation tax, drift guarded only by
  differential tests. OBSERVED, deliberate.
- F11 Cross-range consistency undocumented: each -range is its own snapshot iterator; a query
  doing several ranges under a concurrent writer can tear across them. Basis-t filtering above
  presumably absorbs it. INFERRED risk.
- F12 The single Mutex<()> also covers fjall_sync — one writer's fsync stalls all writers.
  OBSERVED, minor.

Also: TWO NIF-load disciplines (LazyMemo ensure_loaded! + :global.trans vs fjall defnative +
vm.native/available?) — two answers to "is my NIF loaded". And build/datom_redb.so is a fossil
of the retired engine (no crate remains).

* 3. Goal / non-goals

GOAL: one cache substrate — one crate, one cell API, two tiers (L2 volatile cells a la
LazyMemo, L3 durable keyspaces a la fjall), with L1 per-process views (pdict/pterm) validated
by a native generation counter. Unified: identity, accounting, invalidation (dependency
EDGES, not per-cache epoch/key/mtime inventions), stats surface, load discipline.

NON-GOALS: rewriting the on-disk formats (content addressing stays); forcing hot tiny reads
through a NIF crossing (Env.fetch's per-deref path stays L1); live NIF upgrade support;
touching the query engine.

* 4. Phases

** Phase 0 — probes (THIS SESSION)
- P0a op counters in lazy_memo NIF: per-entry crossing counts, lane split, reroute/retry/cycle
  counts, exposed via nif_stats. [started 2026-09-19]
- P0b bench/cache_boundary_bench.bl: read-crossing cost, reroute rate under exchange load,
  dirty-queue depth during bulk work, fjall range materialization curve.
- P0c record baselines in this item.
Gate: bench runs on the rebuilt NIF; numbers appended to §7.

** Phase 1 — LazyMemo read self-guard (fix F1)
nif_read_fast returns :reroute over the ceiling; read/1 becomes one crossing hot path.
Deletes nif_lane from the API. Gate: lazy-seq tests + bench p50 read latency.

** Phase 2 — fjall range cursor (fix F7)  ⟶ DONE (2026-09-19, commit d8a92fa4)
Chunked scan: `fjall_range_chunk` returns the next N pairs, resuming from the last key
received (inclusive `start` for the first chunk, exclusive `after` for the rest — re-seeking
costs µs against a scan measured in seconds). `datom.store-fjall/-range` loops it at
CHUNK-ROWS = 512; the port's contract is unchanged.
Gate: conformance suite green (73, incl. a new test that crosses TWO chunk edges with 1101
keys — the mutation that makes the resume bound inclusive HANGS it, so the pin is real), plus
24 durable + 60 file + 44 store. Memory: the native transient is bounded by the chunk.
MEASURED on a COPY of the live corpus (filali `sources-legales`: 969 016 rows, 1.1 GB):

| probe | before (whole window) | after (chunked 512) |
|---|---|---|
| full scan, alone | 15.58 s | 13.08 s |
| point-get under a scan — median | 0.051 ms | 0.102 ms |
| point-get under a scan — **max** | **3421 ms** | **1.39 ms** |

⟶ the F8 tail (a get behind a full scan) drops ~2460×, and the scan itself is not slower.
WHAT THIS DOES NOT FIX, measured the same way: a SINGLE scan's own cost — the NIF's 969k
pairs take 2.48 s, and the same scan through the port takes 8.57 s, i.e. **71% of one wide
read is the bl-side `decode-value`, one call per row**. A live projection that reads the whole
index therefore stays ~8–17 s per render; chunking stops it blocking everyone else, it does
not make the read cheap. The next lever is FUP-022 (native/columnar decode, or a datom read
that does not decode values the query discards), not more chunking.

** Phase 2b — the datom read path, natively (FUP-022)  ⟶ DONE (2026-09-19, commit 849ce66c)

Delivered in three parts, each measured:

1. THE VALUE SLOT (`datom.value-codec`): a datom value rides a datom lane — fixed-width e/tx
   at known offsets, op as one byte, the attribute with its own length, v last. Two composites
   still fall back to the any-term lane (bignum entity; attribute name past 255 BYTES, which is
   reachable because an Erlang atom allows 255 CHARACTERS), and the reader treats that lane as
   a legal row. Rows written earlier start with the ETF version byte, which no lane tag is
   (1..6, 255) ⇒ **a store upgrades lazily**: no migration gates anything.

2. THE READER (`fjall_resolve_chunk`, native/datom_fjall/src/lib.rs): one crossing, three
   shapes — `:datoms` (datoms, decoded in Rust), `:pairs` (what `-range` needs), `:columns`
   (dense union: e/tx/op columns, DICTIONARY-ENCODED attribute, lane tag + per-lane index per
   row, dense fixed-width arrays, one shared offsets+bytes area for variable lanes).

3. THE CAPABILITY: fjall now implements `datom.store/Scan`, the hook that was declared for
   exactly this substrate and had no implementation since redb was deleted.

MEASURED, 50 000 identical rows (three read paths):

| path | legacy (ETF) values | packed (datom lane) |
|---|---|---|
| generic `-range` | 522 ms | 448 ms |
| `Scan` (native decode) | 114 ms | 87 ms |

On the live corpus copy (all-legacy, so the packed lane is still ahead of it):
EAVT 412 649 rows — `Scan` 11.19 s vs generic 13.96 s, identical digests.

THREE MEASUREMENTS THAT CHANGED THE DESIGN (each cost a wrong assumption):

- **A packed lane decoded IN THE BEAM is a loss**: 1964 ms against the ETF's 479 ms over the
  same 50k rows (4×). The win is a compiled reader, not a byte layout. Hence `-range` decodes
  natively too, and a failed native read THROWS rather than reading as "no rows" (a NIF's
  error arrives in bl as a `{:error, reason}` VALUE, which reads exactly like an empty range).
- **A bl vector is not an Erlang tuple** (it is the runtime's struct: a map with `items`), so a
  NIF that builds tuples returns datoms that print correctly and answer every accessor with
  nil. The shape is named once, in Rust.
- **rustler's `binary_to_term` is the SAFE variant** — it refuses any atom not already in the
  atom table, i.e. every attribute name on a fresh VM, so every legacy row "failed to decode"
  for a reason unrelated to the row. The trusted variant matches the BEAM's own
  `binary_to_term/1` that the bl-side reader already used: parity, not a new exposure.

CONSEQUENCE FOR B1 (late materialization), which is NOT done and is the next step:
per-row access to a column FROM BL is not a win — a bl-side `binary/decode_unsigned` costs
~1.7 µs (measured, 200k calls), against a vector `get` on a materialized datom at a fraction of
that. So a columnar filter cannot be a bl loop over columns; it needs a NATIVE selection
primitive: mechanical predicates over a column (a byte pattern, a prefix, a numeric bound)
returning a SELECTION VECTOR of row indices, with materialization of only those rows. The
policy (visibility, supersession) stays in `db.bl`; only the mechanical matching moves.

The same shape is what makes the text search over the filali corpus cheap: one
`binary/matches` over the `v-var` blob tests every value in one BIF, where the per-row
equivalent is one interpreter step per value — the projection layer's own per-row predicate is
therefore the remaining cost, and it is where B1 must land.

** Phase 3 — native dependency walk (fix F5)
nif_dependencies(term): one crossing, native term walk. Deletes the 60-line Elixir walk.
Gate: LazySeq/reactive tests; create/exchange cost at K handles.

** Phase 4 — eviction for L2 (fix F3)
Priority/LRU eviction under budget pressure instead of only ForceError. Requires native
generations (a cell invalidated native-side must make L1 views stale-checkable).
Gate: budget stress test — cache degrades by eviction, not by raising.

** Phase 5 — tier merge + invalidation by edges
- compiler_key/build_key/Tiers namespace lists become cells with edges on a priv-manifest
  cell: one invalidation mechanism replaces reset_keys/reset_compiler_key/the unresettable
  Tiers memo (fixes the asymmetry found 2026-09-19).
- Cache.root consolidation: AOTCache.dir, embed-asset.bl cache-root, store.bl root all call
  BeamLisp.Cache.root() (store.bl's hand-rolled XDG/HOME is a latent divergence bug).
- fjall fast-lane gets for memtable-resident keys, self-guarded (fix F8; biggest single win
  on this host per the +SDio 1:1 constraint).
Gate: stale-serve rate = 0 under a reload_watcher edit loop; dirty-queue depth ~0 during
bulk transact; no end-to-end gate regresses >5%.

* 5. Improvement estimates (to be replaced by Phase-0 measurements)

- read/1 2->1 crossings: crossing ≈ sub-µs–2µs for small terms; fast-path read p50 −40–50%.
  Multiplier: every LazySeq force, every reactive deref.
- deps walk 2K->1 crossings at create/exchange: typical K small (0–3) → modest; large for
  graph-heavy reactive values.
- fjall fast-lane gets: removes dirty-queue SCHEDULING from point reads; with one dirty-IO
  scheduler on this host, p99 under concurrent scan load goes from ms-scale queueing to µs.
  Potentially the largest single win; P0b measures it.
- range cursor: memory from O(window) to O(32 rows); latency neutral (more crossings, each
  bounded); enables scans that today spike or die.
- Env.fetch unification (Phase 5+): perf-neutral by design (L1 stays pdict), wins are in
  deleted mechanisms (epoch dance, per-cache resets).

* 6. What gets simpler — before/after syntax

*** 6.1 LazyMemo.read (Phase 1)
#+begin_src elixir
# BEFORE — two crossings, race window, lane API surface
def read(resource) do
  ensure_loaded!()
  case nif_lane(resource) do
    :fast  -> nif_read_fast(resource)
    :dirty -> nif_read(resource)
  end
end

# AFTER — self-guarding; one crossing on the hot path; nif_lane deleted
def read(resource) do
  ensure_loaded!()
  case nif_read_fast(resource) do
    :reroute -> nif_read(resource)
    value    -> value
  end
end
#+end_src

*** 6.2 Dependency collection (Phase 3)
#+begin_src elixir
# BEFORE — 60-line Elixir walk, 2 crossings per embedded handle
def dependencies(term) do
  ensure_loaded!()
  walk([term], MapSet.new(), MapSet.new(), [])
end
# ... walk/4 x8, include_resource/4, prepend_tuple/3 ...

# AFTER
def dependencies(term) do
  ensure_loaded!()
  nif_dependencies(term)
end
#+end_src

*** 6.3 Cache-root derivation (Phase 5) — four implementations become one
#+begin_src elixir
# aot_cache.ex BEFORE
nil -> :filename.basedir(:user_cache, ~c"beam_lisp") |> Path.join("aot")
# AFTER
nil -> Path.join(BeamLisp.Cache.root(), "aot")
#+end_src
#+begin_src clojure
;; store.bl BEFORE — hand-rolled XDG/HOME (divergent: skips :filename.basedir)
(let [xdg (System/get_env "XDG_CACHE_HOME")
      base (if (and (some? xdg) (not= "" xdg)) xdg
             (Path/join (System/get_env "HOME") ".cache"))]
  (Path/join (Path/join base "beam_lisp") "lib"))
;; AFTER
(Path/join (BeamLisp.Cache/root) "lib")
;; embed-asset.bl cache-root likewise: (BeamLisp.Cache/root) — same VM, no reason to re-derive
#+end_src

*** 6.4 Invalidation (Phase 5) — N mechanisms become edges
#+begin_src elixir
# BEFORE — manual resets, asymmetric (Tiers memo has NO reset):
AOTCache.reset_keys()          # erases @toolchain_key_pt + @build_key_pt
AOTCache.reset_cleanup_throttle()
# Tiers tier_namespaces memo: nothing erases it — add a file to priv/build mid-VM
# and build_key recomputes but build_namespaces() serves the stale list.

# AFTER — one dependency edge; editing priv/** CASes the manifest cell and every
# key cell hanging off it is stale natively:
Cache.cell(:compiler_key, deps: [priv_manifest_cell])
Cache.cell(:tier_namespaces, :build, deps: [priv_manifest_cell])
#+end_src

*** 6.5 Store GC (Phase 5) — two policies, one sweep
#+begin_src clojure
;; BEFORE: bl.cache/evict (mtime+byte-cap, bl) AND AOTCache.cleanup_obsolete_generations
;; (keep-N + max-age, Elixir) — two GCs for two content-addressed stores.
;; AFTER: one sweep, policy as data:
(Cache/sweep aot-dir      {:keep-generations 8 :max-age-days 30})
(Cache/sweep analysis-dir {:cap-bytes cap :keep current-run})
#+end_src

* 7. Baselines — MEASURED 2026-09-19 (bench/cache_boundary_bench.bl, bin/bl, this host)

Host: Ryzen AI 7 350, ELIXIR_ERL_OPTIONS pins +S 4:4 +SDio 1:1 (ONE dirty-IO scheduler).

| probe | baseline | after |
|-------|----------|-------|
| crossings per small-cell read | **2.25** (20k reads: 22502 read + 22502 lane crossings; 0.89 µs/read) | 1 (Phase 1) |
| crossings per logical read, whole CLI session | **2.0 exactly** — op_lane 213132 ≈ op_read+op_read_fast 213132: every read pays the lane crossing | 1.0 |
| dependency walk | **2.0 crossings per embedded handle** (K=64 → 64 dep_resource + 64 id) | 1/term (Phase 3) |
| reroute rate under CAS load | 2.9% — and an UNPLANNED FINDING: reroutes tick at exactly n/32. Lazy-seq chunk-boundary realizations (32-elt chunks) each carry NEW dependency edges → the fast CAS always reroutes them to dirty. Every lazy-seq chunk realization pays the dirty migration. | fast lane (Phase 3) |
| fjall_range, 50k rows full window | **41 ms in ONE call, +17 MB heap spike** (F7 confirmed) | bounded (Phase 2) |
| point-get under one concurrent range | p50 10 µs idle vs loaded — unchanged; p99 33→53 µs; **max 24 µs → 29 284 µs** — a get waits a FULL SCAN behind the one dirty-IO scheduler (F8 confirmed, tail latency = scan duration) | µs-scale (Phase 5) |

Phase-0 gates: PASS (bench runs on instrumented NIF; numbers above).

Artifacts: native/lazy_memo/src/lib.rs (op counters in nif_stats), bench/cache_boundary_bench.bl.
Old NIF backed up at /tmp/lazy_memo.so.bak (restore + restart any VM to revert).
Note: `bin/bl` (checkout runner) is the dev vehicle — `./bl` is the installed drop and loads
NIFs from ~/.local/share/drop/*/priv, NOT from ./priv/native.

* 8. Risks

- NIF rebuild on this host: live upgrades unsupported — every bl invocation is a fresh VM, so
  dev iteration is safe; a running daemon holds the old .so (build.bl already guards claimed
  target dirs).
- Fast-lane reads for fjall (Phase 5) must reroute on any disk touch — fjall get cannot know
  before trying; the self-guard must be inside fjall's own error path. Needs a spike.
- Generation-validated L1 views: a stale L1 read is a correctness bug, not a perf bug —
  stale-serve probe is the gate, and it must run under edit load, not just at rest.

* 9. The maximalist frame (CORRECTED 2026-09-19): ZERO cache .ex

** The correction: there is NO Elixir floor. I was wrong.

Earlier drafts of this section claimed a "seed floor must stay Elixir" and that AOTCache keys
must remain ~150 lines of .ex. That is FALSE, verified against the tree 2026-09-19:

- The compiler is already bl. `priv/boot/compiler.bl` is 4116 lines; `lib/beam_lisp/compiler.ex`
  is a 126-line FACADE whose moduledoc states the hand-written lowering was DELETED — bl lowers
  .bl -> ANF -> Core Erlang -> .beam directly (anf.bl, lower.bl). No Elixir in the emit path.
- The floor is bl-EMITTED BYTES. `priv/bootstrap/seed/` holds the Core-Erlang beams of the boot
  closure (compiler/anf/lower/reader/core/sugar), produced by bl compiling bl. The
  `Elixir.BeamLisp.Ns.*` names are just the emitter's module-naming scheme — "Elixir" in a
  module name is NOT Elixir the language.
- `defnative` already dissolves NIF host stubs (PLAN-122, landed). `vm/native.bl` synthesizes
  the host BEAM module in pure bl (anf+lower), "no BeamLisp.Emit, no Elixir policy." The
  irreducible substrate is THREE BIFs: erlang/load_nif, code/ensure_loaded, anf->core->beam.
  datom_fjall, code_embed, bl_json, wry, datom_vector all host their NIF from a .bl ns with
  ZERO .ex. lazy_memo.ex and nif_trace.ex are simply the last two NOT-YET-migrated — the Rust
  even uses a hand-rolled nif_init instead of rustler::init! like the others.

** The one real constraint (which is NOT Elixir): the boot closure is native-free

`grep defnative priv/boot/*.bl` -> nothing (the single hit is compiler.bl teaching itself the
FORM, not using it). The drift gate that decides "are these seed beams loadable" must not
depend on a NIF, or rebuilding a stale NIF loops. That is a constraint about not pulling a Rust
crate onto the cold-boot path — it says nothing about keeping Elixir. AOTCache's key logic must
live in the boot closure, and the boot closure is BL, so the keys become a BOOT-TIER .bl ns
(native-free), not Elixir.

Load-order note: core.bl's `lazy-seq` (L115) expands to `BeamLisp.LazySeq/from_fun`, so the
lazy-memo NIF is on the runtime path for any lazy seq. `defnative` synthesizes a lazily-loaded
@on_load host, which is fine — the only rule is the boot closure must not FORCE a lazy seq
before the NIF can load (a load-order property, already true: anf.bl L55 notes compiler-owned
collections never introduce LazySeq handles).

** Corrected ledger — target is ZERO cache .ex

| module | earlier (wrong) | ACTUAL TARGET | mechanism |
|---|---|---|---|
| lazy_memo.ex (311 ln) | ~60-line stub | 0 | `defnative "lazy_memo"` in vm/cell.bl; Rust -> rustler::init! like every other crate; delete hand-rolled nif_init |
| nif_trace.ex | (unlisted) | 0 | same defnative migration |
| aot_cache.ex (813 ln) | ~150 ln keys stay | 0 | keys -> boot-tier .bl (native-free ns); store/GC/closure-walk -> build-tier priv/build/cache.bl |
| cache.ex (105 ln) | 0-10 ln | 0 | path layout -> vm/paths.bl neighbor (already proves fs-identity in bl) |
| perf_probe.ex (56 ln) | delete | 0 | DONE — native op counters (Phase 0) |
| sandbox.ex memo | cell | 0 | daemon-held cell |
| env.ex fetch memo | "stays" | stays ONLY while env.ex is .ex | separate migration; not a cache concern |

** Phases (retargeted)

- Phase 1 (F1): read/1 self-guard; delete nif_lane. ~15 ln. FIRST.
- Phase 2 (F7): DONE (commit d8a92fa4 — point-get-under-scan 3421ms -> 1.39ms). FUP-022 read
  half DONE (849ce66c).
- Phase 3 (F5): nif_dependencies native walk; delete Elixir walk/4; append-only edge delta so
  chunk-boundary CAS stops rerouting (the Phase-0 n/32 finding).
- Phase 4 (F3): L2 eviction under budget + native generation counter (enables stale-checkable
  L1 views).
- Phase 5 (ZERO .ex): migrate lazy_memo + nif_trace to defnative (vm/cell.bl, vm/nif-trace.bl);
  move AOTCache keys -> boot-tier .bl, store/GC -> build-tier .bl; cache.ex -> vm/paths.bl;
  delete perf_probe.ex. Cache-root x4 -> one bl fn. Net: NO cache-related .ex remains.
- Phase 6 (F9): daemon owns L3 — align with PLAN-121/122/123 (pure-bl VM manager); fjall
  handles move into the daemon, CLI runs are clients, cache stats join the vm/inspect model.

The drift gate stays native-free — but AS BL, in the boot tier. That is the whole correction:
the boundary is "native-free", never "Elixir".

* 10. Re-baseline + flagship redirect (handoff from the datom-read session, 2026-09-19)

Verified against the tree before recording (git + native/datom_fjall + analytic.bl).

** More landed than §9 knew — the substrate Phase 5 sits ON

- 4d3576f9 "PLAN-132 Phase 2b — the native datom read path, with its numbers".
- 849ce66c added a `:columns` resolve mode: DENSE-UNION layout, fixed-width e/tx arrays, ONE
  shared offsets+bytes area for variable lanes, attribute column DICTIONARY-encoded. O(1)
  random access, built for filter-then-materialize (native/datom_fjall/src/lib.rs:229-537,
  fjall_resolve_chunk :608). datom.store/Scan is implemented for fjall ⇒ scan-datoms is the
  fast path. Reads chunked at 512 rows, one dirty-IO lane per chunk.
  ⇒ Phase 5's derived-column cache SITS ON `:columns`, does not re-derive it.

** Gotchas that constrain the cache design (all verified)

1. A NIF error reads as an EMPTY RANGE: `-range` throws on native error, and a {:error,…}
   value otherwise looks identical to 0 rows (cost the prior session a silent "0 rows" bug).
   ⇒ the stale-serve / invalidation probes MUST assert on an explicit error channel, never on
   row count — a broken cache must not read as a cold miss.
2. NEVER key on value bytes. The value slot now has THREE legal encodings: packed datom lane
   (first byte 6), legacy ETF (131), unpackable ESC (255). ⇒ cache DECODED terms; key on
   (store, key) — confirms Phase 5's (store, attribute, basis) key shape.
3. NO bl-side lane decoding. Decoding the packed lane in bl is a 4× LOSS vs the ETF it
   replaced (1964 vs 479 ms / 50k rows) — the win is compiled-reader-only. ⇒ "move policy to
   bl" MUST NOT drag decoding to bl: decode stays NATIVE, policy is bl.
4. Datom-mode scans need index-range bounds (an unbounded walk hits the counter rows and
   errors by design); use :pairs for arbitrary keyspaces.
5. Process: `sh bin/bl` if bin/bl loses its exec bit; BL_DAEMON=off for tests; a native change
   needs `bl build <file> --out …` + `bl daemon stop` if a warm daemon holds old NIFs; the
   artifact cache is content-keyed, so revert-then-build can silently reuse an older .so — use
   --force. (This session's manual cp into priv/native is the dev-loop equivalent; a daemon
   would need stop.)

** The measurement that RESHAPES Phase 5 — new flagship target

The real hot path is NOT lazy-cell reads. It is knowledger.search → datom.query.analytic/
similar-to (analytic.bl:68) → embedding-corpus (:106), which RE-MATERIALIZES the whole
:chunk/embedding column every evaluation:

  62 396 chunks × 768 dims = 191.7 MB, ~3 853 ms engine + 1 071–1 865 ms brute-force search.
  warm == cold (6 844 / 6 436 / 6 209 ms through datom/q).
  vs the STORE READ of that 62 396-row attribute: 203–400 ms. ⇒ the engine adds ~10–20×.

∴ the flagship Phase 5 deliverable is a DERIVED-COLUMN CACHE keyed by (store, attribute,
basis), invalidated on that attribute's edges — exactly Phase 5's shape — worth ~4 s per
evaluation on the live corpus. A quantised resident copy is ~24 MB (fits L2 comfortably) and
MUST keep the exact, bit-identical result order the app's tests and films rely on. This leads
Phase 5, AHEAD of the generic AOTCache move (which becomes the follow-on, not the headline).

Related (not this plan, but blocks the request path): the resident hnsw-adjacency state
machine (relations.bl:203/300) is the right index, but its cold build for this corpus exceeds
60 s and its Agent update/get hit the 5 s GenServer timeout ⇒ needs background warm-up + a
persisted index before a request path can call it. Track as a FUP.

** Phase 5 restated

Flagship: derived-column cache = a (store, attribute, basis) → quantised resident column,
built ONCE from the native `:columns` resolve, invalidated by that attribute's edges, decoded
NATIVE, policy in bl, bit-identical order preserved, error channel explicit (not row-count).
Then: AOTCache keys → boot-tier .bl, store/GC → build-tier .bl, cache.ex → vm/paths.bl, the
lazy_memo/nif_trace defnative migration (ZERO .ex). Same end state; the embedding column is
just the first and most valuable tenant of the unified substrate.

NOTE [2026-09-20]: Phase 5b shipped (commit e537ca59): packed-corpus cache. vector/search re-concatenated candidates into the strided corpus binary every call (~36% of the search path, 60ms/168ms @ 8k). Made it a value — datom.vector/PackedCorpus {ids corpus dim n} via pack-corpus/search-packed; datom.query.corpus/packed memoises it beside the pair rows under the same (store,attr,basis) key. Refc binary → cache/share by pointer. analytic/similar-to free-entity hot path uses packed; prefiltered path keeps pairs. search is now a pack-corpus+search-packed wrapper (one assembly path). Measured 8000x768: warm pairs 404.5ms → warm packed 156ms = 248.5ms/eval concat removed, cold→warm 16.4x, bit-identical. Tests 12 green (+packed==pairs, +packed-count), vector suite 29 green. The lone datom-suite 'incoherent' is an untracked sibling recur_test.bl (no ns form), not this change. NEXT unchanged: Phase 3 (F5 native dep walk), Phase 4 (eviction), ZERO-.ex sweep, Phase 6 daemon L3, hnsw FUP.

NOTE [2026-09-20]: Phase 5c shipped: resident (decoded+quantised) index. vec_search decoded the whole corpus to f32 AND re-quantised every row on EVERY call — identical work per query at a fixed basis (the crate's own comment flagged a resident index as the scale step). Added datom_vector NIFs vec_build_resident/vec_search_resident/vec_resident_count over an immutable ResourceArc<Resident>{flat,codes,dim,n} (no Mutex — read-only after build), reusing decode_corpus/quantize/search_flat/search_quantized so results are bit-identical to vec_search. bl: datom.vector/Resident + build-resident/search-resident; datom.query.corpus/resident memoises it in the slot; analytic/similar-to free-entity hot path uses it. NOTE: full native dep-walk (original Phase 3/F5) is NOT safely feasible — the Elixir walk/4 introspects closure envs via :erlang.fun_info(:env) to find cells captured in lazy-seq thunks, and rustler exposes no fun-env introspection; a native walk would miss captured cells → premature free. So the resident index was the right next move instead. Measured (8000x768, this host): warm pairs 228.55ms → packed 64.45ms → resident 0.9ms; decode+quantise ~63ms/eval removed; 254x over pairs on warm search; bit-identical to packed at rerank 0 and 200. Tests 15 green (+resident exact/quantised equivalence +count), vector suite 29 green, full datom 52 files pass (lone incoherent = untracked recur_test.bl, not this change). NIF backup /tmp/datom_vector.so.bak. NEXT: quantised-resident is DONE; remaining — Phase 4 eviction (esp. now resident indexes hold real memory: needs a budget/LRU over the corpus ETS), ZERO-.ex sweep, Phase 6 daemon L3, hnsw FUP.

NOTE [2026-09-20]: CORRECTION (2026-09-19): the earlier note claiming "F5 native dep-walk is NOT safely feasible — rustler exposes no fun-env introspection" is WRONG, and conflated two boundaries. F5's target was never a Rust NIF; it is to move walk/4 to .bl for ZERO-.ex. erlang/fun_info(f, :env) is a normal BIF, callable from beam-lisp like any other — VERIFIED: (erlang/fun_info (make-thunk 7) :env) => {:env, [7]}, and the nested case returns the inner closure. So the walk (closure reflection included) ports to bl cleanly; F5 IS feasible as a mechanical port. The rustler limitation is real but only bars hosting the walk INSIDE the Rust crate — never relevant, since bl is the destination. Successor idea (retire the walk entirely by DECLARING deps at cell creation) captured as FUP-112. Phase 5c (resident index) was still the right thing to ship next on value, but it was NOT forced by an F5 blocker — F5 remains open and feasible for the ZERO-.ex sweep.

NOTE [2026-09-20]: Phase 4 shipped: byte-budgeted LRU eviction for the derived-column cache (datom.query.corpus). Phase 5c made this load-bearing — a fully-built 62k×768 column is ~580MB (rows ~196 + packed ~190 + resident ~190), one slot per (store,attr), previously unbounded across stores. Now: each entry is a wide ETS tuple {key atime bytes disc rows packed resident n dim}; ets/update_element stamps atime on every hit without recopying payload; a running byte total (ets/update_counter on a meta row) + a configurable budget (DEFAULT 512MiB, set-budget!); on any insert/grow past budget, evict lowest-atime columns until under. Soft floor: the just-built entry is never its own victim, so one oversized column can exceed budget but nothing grows unbounded. Meta rows keyed by atoms, entries by tuples (is_tuple distinguishes; that was the one bug — first draft used is_list and evicted nothing). stats now returns {:columns :rows :bytes :budget :evictions}. Tests: corpus_cache_test.bl 23 green (8 correctness + budget-bounds + lru-under-pressure + hit-does-not-grow). VERIFIED my ns loads+runs in isolation. BLOCKER (external, not mine): full `bl test test/bl/datom/` fails because priv/lib/web.bl has an unbalanced paren (depth -1) from ANOTHER session's uncommitted WIP — the AOT build-plan key-for reads the whole require closure and web.bl's broken paren throws unexpected-). My 4 edited files all balance; corpus passed 23-green before web.bl broke. Do NOT touch web.bl. NEXT: ZERO-.ex sweep, Phase 6 daemon L3, hnsw FUP; eviction could later move to the daemon (Phase 6) as a background duty.

NOTE [2026-09-20]: ZERO-.ex sweep started (2026-09-19). Committed dac0a974 = sweep 1/5 + 2/5:
- perf_probe.ex DELETED (0 callers; Phase-0 native op counters replaced it).
- cache.ex (105 ln) DISSOLVED into priv/std/vm/paths.bl: ported the two-tier cache-path identity as cache-root/cache-project-root/cache-tree-id/cache-host-dir/cache-project-dir, REUSING paths.bl's existing tree-id (sha256 realpath, 16 hex) + real-path so cache and daemon name a tree identically (verified cache-tree-id => 57461829a20cc2af, bit-identical to paths.bl's own golden docstring). Callers repointed: cache.bl + codebase.bl (.bl) -> vm.paths/cache-* (added [vm.paths :as paths] require, no cycle); model.ex (.ex, a MODEL file not a cache file) -> inline :filename.basedir BIF (the exact call cache.ex delegated to; no new .ex->.bl hot coupling); embed-asset.bl already had its own cache-root (docstring only). VERIFIED both .bl callers compile + return correct paths BEFORE the tree broke.

sweep 3/5 (nif_trace): WRITTEN priv/std/vm/nif-trace.bl (balanced, structurally sound, INERT — nothing requires it yet, so its presence breaks nothing; nif_trace.ex + its .exs test still live). It is a pure BIF tracer (:erlang.trace/trace_pattern) — never had a NIF despite the name, so it's a plain bl port, not a defnative. NOT YET verified, NOT committed, nif_trace.ex NOT yet deleted.

HARD BLOCKER (external, not mine): the entire bl runtime is currently NON-FUNCTIONAL. Another session's UNTRACKED priv/std/vm/catalog.bl (created 13:39 today, :require'd by tracked vm.inspect:30) uses `defonce` which fails to resolve at load (undefined var: vm.catalog/defonce). Because the AOT build-plan walks the whole source closure, this poisons EVERY build: `bl eval '(+ 1 2)'` itself fails. Confirmed by re-running paths_test.bl which PASSED 20 min earlier and now fails identically. CANNOT verify ANY bl code (mine or theirs) until catalog.bl's owner fixes the load-order (defonce lives in priv/compat/clojure/core_more.bl — catalog needs compat loaded first, or the def guarded).

z4 (lazy_memo -> defnative vm/cell.bl + Rust rustler::init!) and z5 (aot_cache 813ln -> boot-tier + build-tier .bl) are the two HIGHEST-RISK changes in the tree: boot-critical, hottest path (core.bl:116 lazy-seq -> LazySeq -> LazyMemo on every lazy seq), 4 core .ex callers + 6 build-system .ex callers respectively, needing NIF rebuild + full elixirc recompile + BOOT verification. Established by investigation: there is ZERO precedent for .ex calling an emitted-bl ns on a hot/boot path (the one precedent, apply(BeamLisp.Ns.Compiler,...), is a cold build-time call) — so z4/z5 require moving the CALLERS to bl too, not just the callee. Doing either blind (no boot verification possible) would risk bricking the compiler. HELD pending: (1) catalog.bl unblock so verification works, (2) a decision on whether the .ex callers move too. Owner is actively committing to boot files (5+ commits mid-session) — coordinate.

NET so far: 2 cache .ex gone (perf_probe, cache), 1 more (nif_trace) ready-to-land the moment the runtime is verifiable again. lazy_memo + aot_cache remain.

NOTE [2026-09-20]: RUNTIME UNBLOCK (defonce) + ZERO-.ex sweep status (2026-09-19, session 2):

Commit 2986a2b4 — defonce promoted to a real core primitive (priv/boot/core.bl, beside `declare`), removed from priv/compat/clojure/core_more.bl (defmacro + install! intern list; `locking` stays in compat). Macro BODY byte-identical to the proven compat version — only home+docstring changed. Resolves via BeamLisp.Env/peek's core fallback like when/declare/->.
  ACTIVATION GAP (important): core is BOOT-TIER; the running compiler interns it from the committed seed beam (priv/bootstrap/seed/), drift-checked by toolchain-key only. So defonce becomes visible to other namespaces ONLY after `bl seed` (seed regen following a keyed build) — the standard bootstrap step for any new core macro (self-hosting gen-N→gen-N+1). I COULD NOT complete the seed regen: it is circular-blocked — the pre-command Bootstrap.install! rebuild + `bl eval`'s own closure both boot the full tree, which is UNBOOTABLE due to concurrent uncommitted WIP. Sequence observed: vm/catalog.bl (untracked, uses defonce → the unbootstrapped macro) blocked it; after temporarily neutralizing catalog's defonce (byte-restored after), the blocker moved to vm/http.bl (depth -1 unbalanced paren, owner's active WIP, mtime 13:40). Cannot reliably diagnose http.bl (hand paren-counter lies on its HTML/CSS string literals) and MUST NOT edit another session's in-flight file. So the runtime stays unbootable until the owner commits/fixes http.bl; then `bl seed` activates defonce and unblocks catalog.

ZERO-.ex sweep tally (of 5 cache .ex):
  DONE+committed: perf_probe.ex (deleted, 0 callers), cache.ex (→ vm/paths.bl cache-* fns) — commit dac0a974.
  READY but unverifiable: nif_trace.ex → priv/std/vm/nif-trace.bl (WRITTEN, balanced, INERT — nothing requires it, so harmless; pure :erlang.trace BIF port, not a defnative). NOT committed, .ex+.exs NOT deleted, because I cannot boot to run its test.
  BLOCKED on bootable runtime: lazy_memo.ex (→ defnative vm/cell.bl + Rust rustler::init! + 4 core .ex callers moved to bl) and aot_cache.ex (813 ln → boot-tier keys + build-tier store, 6 build-system .ex callers moved to bl). Both are the highest-risk changes in the tree (hottest path / boot+build tiers) and REQUIRE moving their .ex callers too (user directive: "move their callers, methodically, don't leave broken") — impossible to do safely without a bootable runtime to verify each step.

BLOCKER SUMMARY: the entire remaining sweep is gated on a bootable runtime, which is gated on (a) the owner's http.bl fix, then (b) a `bl seed` regen to activate defonce. Neither is in my control. All my committed work (178329b1, dac0a974, 2986a2b4) is correct source; the uncommitted nif-trace.bl is inert and ready.

NOTE [2026-09-20]: UNBLOCK ATTEMPT (2026-09-20, session 2 cont.): tried to unblock the checkout boot so defonce could be seeded and the ZERO-.ex sweep resumed. Could NOT — the blocker is BUG-090, an owner-owned bug in the in-flight diagnostics refactor, which the owner is actively debugging.

WHAT I DID (per user: "do what needs to be done to unblock, commit others' work if needed"):
- defonce: confirmed my core.bl macro is CORRECT and innocent. Isolation test — removed the defonce block entirely, boot STILL crashes identically → defonce is not the cause. Restored byte-exact (commit 2986a2b4 unchanged).
- Diagnosed the boot crash to the end: `bin/bl eval` dies with `:erlang.tuple_to_list("n")` at reader-node.bl:67 <- compiler.bl:2208 parse-fn-clauses <- compile-defn* — i.e. a defn's params slot receives a bare string instead of a node while loading priv/build/ns-interface.bl.
- The DROP (./bl) works (its bundled floor is healthy); it compiles individual checkout files (diag/errors/typed/reader/bl.cli) fine. The CHECKOUT's own boot-tier rebuild crashes.
- Tried the prescribed fix (bl build && bl seed) via the drop: `./bl build --force --out build` produced boot beams but stamped with the CHECKOUT key 3d7b0984 (self-host build), so `./bl seed` (drop key fc89914e) refused — "refusing to seed a stale floor" (correct guard). Booting bin/bl from those build/ beams still hit the same `"n"` crash → not a stale-artifact issue, a real source/compile bug.

OWNER'S AUTHORITATIVE DIAGNOSIS (committed babc7e11, aac1fc34 while I worked): BUG-090 — reader.bl and compiler.bl are EXONERATED BY MEASUREMENT. The crashing artifact is `{:meta, ["def","value"], %{offset:5801, "end-offset":5821}}` — an EVALUATED VALUE (["def","value"]) plus a DIAGNOSTIC span-map (offset/"end-offset" — keys the bl reader never produces; string "end-offset" ⇒ a serialization round-trip) handed back AS A FORM. The producer is the diag/repair rendering path (priv/boot/diag.bl + priv/std/repair.bl, the new committed refactor), NOT reader/compiler. Owner filed BUG-091 too (the boot is also ~125s from a cold AOT cache). BUG-090 STATE is still ITEM (open); owner holds those files and is actively fixing (commits 6 min before I checked).

DECISION: STOP — do not blind-fix BUG-090. It is a form/value confusion in the owner's actively-developed diag/repair refactor; I cannot trace it fully without their probes (/tmp/probe-read.exs), and editing files they committed minutes ago and are debugging would collide with and could corrupt their in-flight fix. The owner explicitly scoped it as theirs ("its owner holds that file", BUG-091). My work is innocent, intact, and committed; every file I touched during diagnosis is restored byte-exact (catalog.bl the owner has since rewritten further).

GATING CHAIN unchanged: BUG-090 fix (owner) → tree boots → `bl seed` activates defonce → FUP-113 (nif_trace verify+delete, lazy_memo defnative + caller moves, aot_cache boot/build-tier + caller moves) executes. Nothing of mine is blocked by my own code.