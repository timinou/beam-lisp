#+TITLE: PLAN — one require graph: sg/cycles API + three consumers (build-plan, codebase, reload)
#+STATE: DOING
#+SESSION_ID: 15842c57d3c72c53
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-18T07-47-27-183Z_15842c57d3c72c53.jsonl]]
#+CUSTOM_ID: PLAN-125-plan-one-require-graph-sg-cycles-api-thr

* Initial Message

"""
- *The loader's cross-process cycle deadlock is a class.* BUG-064's instance is
  fixed, but the mechanism — one process holding a `:global` load lock while
  another runs the init, plus a retry-forever `set_lock` — will produce the same
  silent hang for the next cycle anyone introduces between a substrate and an
  application namespace. Either bound the lock acquisition (fail with a named
  cycle) or move the lock to the process that runs the init.
"""

can you explain this from zero context in simple language? go find the root cause first and explain what gets in the way of a clear system

* Goal
Kill BUG-064's class: the require graph is extracted ONCE (`priv/build/source-graph.bl`), cycle detection is one fn (`sg/cycles`), and all three consumers — build-plan (build), codebase (std), reload (std) — use it. Tier law amended (option B): `build/` splits into the requireable graph KERNEL (`source-graph`) vs the DRIVER (`build`, `build-plan`, `ns-interface`, …) which std still may not reach.

* Commits (in order)
1. `source-graph/cycles` — new pure host-only fn in priv/build/source-graph.bl returning named cycle paths; `BeamLisp.SourceGraph.cycles/2` facade in lib/beam_lisp/source_graph.ex; tests in test/beam_lisp/source_graph_test.exs.
2. Tier law amendment — lib/beam_lisp/tiers.ex moduledoc: kernel vs driver; barrier test updated/extended to pin that std may require source-graph but not the driver.
3. build-plan — priv/build/build-plan.bl calls sg/cycles over plan roots and fails with named cycles instead of silently tolerating back-edges; test in test/bl/build_plan_test.bl.
4. codebase — priv/std/codebase.bl: `:ns/name`/`:ns/requires` facts from sg/header*-forms on the same forms read; `require-cycles` query delegating to sg/cycles; test.
5. reload — priv/std/reload.bl commit-coherent gains :require-cycle predicate (staged headers ∪ live-image requires → sg/cycles on staged roots; hold with named cycle + by-name-idiom hint); direct requires stamped into beam provenance at lib/beam_lisp/aot.ex emit; test in test/bl/reload/.

* Out of scope (enabled, not done)
The bounded `:global` lock in lib/beam_lisp/aot.ex:238 (BUG-064 follow-up proper): the facade from commit 1 is what it will call to name the cycle. Changing bootstrap lock semantics needs its own proof.

* Constraints discovered
- tiers.ex:36 law: "no program under std/, lib/ or compat/ may reach into build/" — THIS is what option B amends.
- Enforcement: test/beam_lisp/boot_build_barrier_test.exs (reads ns forms, asserts direction).
- Working tree has pre-existing unrelated modifications (priv/lib/*, priv/std/bl/*) — commit ONLY files this plan touches.
- source-graph beams ship in drops (the drift gate runs on them, aot.ex:162), so std→source-graph requires resolve in a drop.
- reload live-image requires: Env records none today; re-derive from sources via sg/header + stamp direct requires into __bl_provenance__ at emit.

NOTE [2026-09-18]: 2026-09-18 — Commits 1-4 landed (b5b13626 sg/cycles; 88180349 tier law kernel-vs-driver + enforcement test; 877322ff build-plan named-fatal cycles; f5649344 codebase :ns/requires + require-cycles). Commit 5 (reload gate) in tree, tests green (16/16 require_cycle + full reload dir 10 files green), uncommitted pending full-suite outcome. Discoveries filed: BUG-072 (native embed-batch ArgumentError, pre-existing), BUG-075 (reload apply: static require of bundle sibling fails in ward fork — pre-existing, test reshaped around it), BUG-078 (VM SIGSEGV in .so fini destructor under reader/lazy-seq churn — the reason the reload gate memoizes image requires instead of re-parsing per commit). Also fixed pre-existing stale build_namespaces expectation in the barrier test. Design deviation from the original note: NO aot.ex provenance stamp — reload is disabled in drops by policy, so source-derived + reload-recorded edges suffice; the stamp would have touched the drift gate's equality for marginal value.