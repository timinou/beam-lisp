#+TITLE: Complete generated forms and retire every legacy compiler path
#+STATE: ITEM
#+SESSION_ID: 15729b4fce888940
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-04T16-19-37-402Z_15729b4fce888940.jsonl]]
#+CUSTOM_ID: PLAN-091-complete-generated-forms-and-retire-ever
#+DEPENDS: FEAT-038-route-live-and-disk-compilation-through
#+ORDER: 4
#+PARENT: PLAN-090-converge-direct-compilation-through-one

* Initial Message

You are taking over from a competitor's agent.

Please assess the reasons for why the plan hasn't converged. Consider all angles, all the way down to the programming language design.

Come back to me with an in-chat explanation and your own strategy, from zero context, in simple language

* Purpose
After the complete vertical route works, move the remaining module-generating forms onto it and remove the obsolete compiler implementation. Finish PLAN-086 rather than creating a permanent second compiler. One IR and one backend apply to every generated Beam Lisp module, including initialization and forwarding plumbing; host Elixir implementation code and legitimate literal tuple data are not deletion targets.

* Remaining form families
Inventory compile-defserver, defprotocol, defrecord, defmulti, defmethod, extend-type, extend-protocol, reify, defnative and compile-ns in compiler2.bl and the existing delegation surface. For each, identify its generated modules, attributes, runtime registry effects and disk-load behavior. Reuse canonical expressions/clauses and the shared emitter; no form-specific backend. Work in small form-family batches with disjoint source scopes where possible.

* Required feature proofs
Server: callback arities, behavior attributes, invariant clauses, start/client functions, supervision and redefinition; existing guards example must pass. Protocol/record/reify: dispatch, fields, implementation lookup and errors. Multi-method: registration, dispatch and reload. Native: preserve host/native companion module packaging, loader behavior and malformed signature errors; this is not new JIT/SIMD work. Namespace: requires/aliases, exported definitions and initialization replay survive fresh-VM AOT load. Each batch must test runtime plus disk/fresh-load behavior, not only interactive examples.

* Deletion sequence
For each migrated family remove its quoted producer/consumer immediately when no remaining caller needs it. At the final wave remove ast-* constructors, quote-node readback, normalise and quoted accessors, quoted lowerers in priv/self/core.bl, mixed-shape *-any builders, bootstrap import adapter after new-seed proof, Link.defvar_elixir and :elixir backend configuration/fallbacks, and Emit quoted module constructors. Cut compiler2 over to the canonical compiler namespace/API using existing delegation conventions, then remove transitional duplicate implementation/delegation. Do not retain compatibility aliases for deleted backend options: reject obsolete configuration clearly, with migration guidance if exposed publicly.

* Census and evidence
Maintain a finite table: producer, stored representation, module consumer, test covering it, legacy path removed. Completion is zero live legacy routes, not a growing percentage of ANF functions. Search code and tests for normalise, quote-node, compile_quoted, Module.create, defvar_elixir, backend options and old compiler entry points; classify every remaining hit. Literal data, host build code and historical documentation are not false failures; live Beam Lisp emission references are failures. Replace obsolete backend comparison tests with behavior tests tied to live functionality, rather than deleting coverage.

* Final gates
Focused form tests; full suite with exact executed/passed/failed/skipped totals and explicit environment-enabled examples gate; interpreter-versus-Core behavior oracle; clean bootstrap from committed seed in a fresh VM without prior local cache; reproducible AOT byte comparison using the existing full fixture set (record actual current count rather than hard-coding historic 303); retained closure through three redefinitions; generation stamps match emitted artifacts. Never relabel a skipped example as an executed pass.

* Documentation and operations
Update PLAN-086 to point to the final architecture and mark individual historical waves honestly. Update current pipeline diagrams and runnable language guides under docs/AGENTS.md. Regenerate seed only at a verified milestone, not every edit. Serialize mix invocations and use asynchronous memory-capped systemd scopes for risky tests/builds. Capture journal OOM status, exit status, command, generation receipt and result artifact. A full-suite failure remains open and is repaired or explicitly blocks completion; user requested pre-existing failures found along the way be fixed, not hidden.

* Non-goals
Do not change shadowing semantics, implement MLIR/LLVM/native backends, redesign module lifetime, or silently change lazy-sequence guarantees. Those have separate ownership. Keep useful topology and runtime helpers, not parallel emitters.

NOTE [2026-09-06]: Resumed canonical cutover verified on 2026-09-06. Fixed upgrade proof to stage the committed seed in a fresh temporary ebin: Bootstrap.install! correctly preserves coherent newer _build generations, so pairing it with the explicit 440fb3b host was invalid. Final command: systemd-run --user --scope -p MemoryMax=8G -p MemorySwapMax=1G --collect --unit bl-plan090-source-gate-2 timeout 600 env MIX_ENV=test elixir -pa '_build/test/lib/*/ebin' -e 'Mix.start(); Code.require_file("mix.exs"); Code.require_file("!tasks/artifacts/canonical-upgrade-gate.exs")'. The plain elixir driver avoids the concurrent Mix build lock and writes seed staging/hotpatch outputs to temporary directories. Result: 58/58 ExUnit, zero skips/exclusions; canonical module suite 6 tests /16 assertions, zero failures/errors. Includes rich def/defn/defmacro metadata, server dispatch/redefinition, record/deftype ABI, real Fjall NIF calls, missing-NIF refusal, and native declaration replay after loading emitted bytes. Native AOT test now selects the namespace shim explicitly instead of assuming it is the first artifact (body-first publication). Nested metadata quotes stay literal data; only the outer quote is stripped. Removed unused compiler2 quoted constructor definitions. Journal: 4m59.362s wall, 380M memory peak, 31.5M swap peak, no OOM; no remaining bl-plan090 scopes. Durable stdout + generation receipt: !tasks/artifacts/canonical-gate-result.log. One trailing blank line in lower.bl was removed after behavioral verification; no executable change. This is a focused migration milestone, not full convergence: previous-generation bootstrap import helpers remain until seed/deletion proof; full current-tree regression, fresh-seed/reproducibility, eager compiler retention, native memo completion and Spell integration verification remain open. Native runtime/concurrent files are not owned or committed here.