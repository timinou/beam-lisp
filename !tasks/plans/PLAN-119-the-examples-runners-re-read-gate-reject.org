#+TITLE: the examples runner's re-read gate rejects valid source files
#+STATE: ITEM
#+SESSION_ID: 158190337cc00a7b
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-16T07-08-17-780Z_158190337cc00a7b.jsonl]]
#+CUSTOM_ID: PLAN-119-the-examples-runners-re-read-gate-reject

* Initial Message

bro wtf, please commit everything semantically (even if it's not your work)

* Initial Message

`bl examples` fails its RE-READ step on files the compiler accepts and the test suite greens: priv/lib/mcp/tools.bl and priv/lib/system/core.bl ("map literal has an odd number of forms"), priv/lib/z3corpus.bl ("unterminated collection, expected )"), examples/system/14_discover_invariant.bl ("unterminated collection, expected }"). The runner's re-read uses a reader configuration stricter than the compiler's (or a stale one) — the files are untouched and the reader is untouched, so the divergence lives in the example runner's re-read path.

Measured 2026-09-17 post-:arith-cutover: 221 passed, 1 skipped, 11 failed — of the 11, 3 are deliberate negative fixtures ("no range scan", "no fixed-width column", "no schema validation"), 1 is environmental (wry_hello argument error — webview needs a display), and 5–7 are these re-read false negatives. The suite's `bl test` battery (36 files, all ANF engines) is green on the same tree.

Not verified: whether the count changed recently — no pre-cutover examples run exists this session.

* Next step

Find where the example runner re-reads sources (priv/std/bl/examples*), reproduce one failure in isolation, and align its reader configuration with the compiler's (or drop the re-read in favour of the compiler's own parse).
