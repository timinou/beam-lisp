#+TITLE: Wave 12: clojure.test port — deftest/is/run-tests + mix task
#+STATE: DONE
#+SESSION_ID: 154d7065511fb2de
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-08-06T19-28-19-268Z_154d7065511fb2de.jsonl]]
#+CUSTOM_ID: PLAN-008-wave-12-clojure-test-port-deftest-is-run
#+COMPLETION_HASH: 45043f9ad8001f09

* Initial Message

what's next? what's left? what else could be done?

Testing beam-lisp in beam-lisp: port clojure.test's core.

`deftest` (registers a test fn under {:test, ns, name} in ETS), `is` (asserts, on failure records {:expected, :actual, :form} and prints, does not halt), `testing` (context string for failures), `are` (table-driven, macro over is), `run-tests` (discovers all tests in ns or all nses, runs, prints summary, returns pass/fail counts). `mix beam_lisp.test` task running test/**/*.bl.

Fixture support (use-fixtures) only if cheap.

Depends on: try/catch (wave 5 part A — is needs it to report exceptions as failures), docstrings/meta optional. Files: priv/test.bl or lib/beam_lisp/test.ex + core test ns file, lib/mix/tasks/beam_lisp.test.ex, examples or test/bl/ sample tests. Acceptance: write beam-lisp's own prelude tests in beam-lisp and run them via the mix task; a deliberately failing test reports form + expected/actual.

NOTE [2026-08-06]: DONE — commit 3f1ae09. deftest/is/testing/are/run-tests written in beam-lisp (priv/test.bl), registry + counters in the shared :beam_lisp_vars ETS table under {:test_registry, ns} / {:test_run, ns} / {:test_ctx} (no new process — matches how Env stores per-ns data). priv/test.bl evaluates into ns core so the macros resolve from any test ns via the core fallback. Key detail: is/deftest capture (BeamLisp.Env/current_ns) at MACRO-EXPANSION time — current_ns is global and drifts to the last-loaded file by run time. is passes each form twice: (quote ~form) for failure detail and (fn [] ~form) for evaluation, wrapped in beam-lisp try/catch so exceptions report as errors; (= a b) special-cased to report expected/actual separately. are substitutes argv symbols into the template via a datum walker in TestRT. mix beam_lisp.test globs test/**/*.bl (accepts a path arg), exits 1 on any failure/error — verified with a scratch failing suite (output recorded in the agent artifact). test/bl/prelude_test.bl: 11 tests / 50 assertions. No application.ex change needed: cli_task? already returns true for any non-empty argv, so the task never starts Tidewave.