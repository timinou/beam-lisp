#+TITLE: HARD CUTOVER: delete Elixir compiler/reader genesis — bl entry points, seed staging
#+STATE: DOING
#+SESSION_ID: 156e98a98c8113c8
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp--self-hosted/2026-09-01T13-34-07-410Z_156e98a98c8113c8.jsonl]]
#+CUSTOM_ID: PLAN-083-hard-cutover-delete-elixir-compiler-read

* Initial Message

Please think critically.

The Core Erlang seed _is_ bootstrappable on its own

Please just go through one round of bootstrapping from the Core Erlang output, provide the appropriate tooling, then HARD FUCKING CUTOVER NO EXCUSES.

You will always have the commit history to come back to. Role A Elixir MUST GO

Supersedes the "defer S-E" decision in PLAN-081/PLAN-082. User directive: the
Core seed IS bootstrappable on its own; do ONE round of staged bootstrap from
Core output, provide the tooling, then HARD CUTOVER — Role-A Elixir MUST GO.
Git history is the recovery floor; the oracle's job is done.

=== GROUND TRUTH (verified this session, not assumed) ===
1. compile/2 (compiler.ex:123) delegates EVERY form to BeamLisp.Ns.Compiler
   whenever the `compiler` ns is interned. That ns interns FROM THE SEED BEAM
   (enable_bl_backend → Loader.ensure_loaded → __bl_init__ replays def VALUES;
   no recompile, no genesis). ∴ the staging ladder already runs on the SEED,
   not genesis. My earlier "only genesis can rebuild the seed" was WRONG.
2. Genesis (compile_elixir + do_compile/* + compile_special/* + compile_fn/
   destructure/guard helpers, compiler.ex ~L217–3671; reader.ex
   read_string_elixir + all defp parsers) is reached in exactly TWO cases:
   (a) primordial build with NO seed at all;
   (b) compiler_key MISMATCH: Bootstrap.install! SKIPS the seed and the build
       falls back to genesis.
3. PROVEN: with compiler+reader interned from their beams, forcing
   :compiler_backend=:bl / :reader_backend=:bl, arbitrary forms compile with
   genesis FORBIDDEN — `(reduce + 0 (map inc (filter even? [1..6])))`=15,
   `(do (def x 41)(+ x 1))`=42. Genesis is NOT needed once interned.
4. The ONE ordering gap: BeamLisp.init evals @data_readers (starts with
   `(ns data-readers)`, needs compiler/special-forms) BEFORE interning the
   compiler. Under :auto that silently used genesis. Fix = intern first.
5. Caller surface is SMALL: lib/ calls BeamLisp.Compiler.{compile,
   macroexpand_1, eval_string, eval_form, new_env, read_data, enable_bl_backend}
   and BeamLisp.Reader.{read_one, read_all, enable_bl_reader} — ~10 sites across
   rt.ex, aot.ex, ward.ex, beam_lisp.ex, reader.ex. Tests: ~110 sites, mostly
   eval_string/new_env.
6. compiler.bl ALREADY defines: new-env, reset-fresh!, compile, macroexpand-1,
   prelink-defn, special-forms, and emits under name Elixir.BeamLisp.Compiler
   (var-context). Missing driver entry: eval-string, eval-form, read-data,
   read-all-data. reader.bl defines read-string, read-all (need read-one).

=== PRINCIPLE (user) ===
Anything compiled to BEAM is callable from anywhere. The Elixir "driver" is NOT
a requirement — eval_string/eval_form/new_env/read_data are host-glue whose
logic can live in .bl, compile to a beam, and be called by the few Elixir
callers. compiler.ex / reader.ex get DELETED.

=== STEPS ===
S1 boot-reorder + genesis-forbidden proof
  Reorder BeamLisp.init: intern compiler+reader from beam BEFORE @data_readers
  eval (and before any prelude eval on a fresh tree — but fresh tree has no seed;
  see S3). Prove a FULL genesis-forbidden build + boot + regression.
S2 driver → .bl
  Add eval-string, eval-form, read-data, read-all-data, read-one to the .bl
  compiler/reader (emit as BeamLisp.Compiler / BeamLisp.Reader module names so
  existing callers resolve with ZERO edits — the beam owns the name). eval-form
  hosts Module.create via erlang/'Elixir.Module':create or an RT shim; verify
  TCO + signature-inference-off behaviour preserved (the 93s→63ms hazard).
S3 seed-staging tool (kills genesis case 2b)
  Bootstrap.install! on key MISMATCH must STAGE: load the committed seed
  compiler (gen-N), compile the current .bl toolchain closure with it into a
  scratch dir, install THOSE beams, then proceed — never fall to genesis. This
  is "one round of bootstrap from Core output." Prove: edit compiler.bl (key
  drifts) → clean build succeeds with genesis DELETED.
S4 HARD CUTOVER — delete
  Delete lib/beam_lisp/compiler.ex genesis (keep NOTHING Elixir — move the last
  driver bits to .bl in S2), delete lib/beam_lisp/reader.ex, retire
  priv/self/oracle.bl + its gate. Rewire the ~10 lib callers + tests to the
  surviving module names (should be no-ops if S2 kept names). mix compile clean.
S5 full green
  Clean clone sim (mix clean + single compile), full regression = baseline,
  reproducibility 330/330, escript/release boot, all bl gates. Regenerate seed.
S6 docs
  Update AGENTS/docs: genesis is gone; the seed is the floor; staging ladder.

=== GATES ===
- Every step: `TMPDIR="$PWD/.tmp" mix test test/beam_lisp/ 2>&1 | tail -3`
  regression stays at baseline (1345/1366, 21-22 known).
- 5 bl gates green: core_backend anf core_anf core_switch core_aot.
- S3: edited-compiler.bl clean build with lib/beam_lisp/compiler.ex genesis
  physically removed (or :compiler_backend forced to a non-existent genesis).
- S5: reproducible 330-beam build, escript boots.

=== RISK / REVERSIBILITY ===
Deletion is the point, but each step commits separately so any prefix is a safe
stopping point. Git history is the ultimate revert. The oracle is retired only
in S4 AFTER the staging ladder (S3) proves genesis is unreachable.

=== PACKAGING DIRECTIVE (user, this session) ===
escript is DEPRECATED as a packaging target unless it can carry native
extensions — it cannot: an escript is a single BEAM archive with no native
priv-artifact packaging, so a full beam-lisp (defnative → Rust crates, rt/vector
natives, drop-packed binaries) can't ship in one. ONLY release packaging that
packages the FULL beam-lisp counts: `mix release` (+ drop pack), which carries
priv/ and native artifacts. Already PROVEN this session: `mix release bl`
succeeds on Core beams and the released `bl` boots + evals (`(+ 2 3)=5`).

∴ S5 gate AMENDED: replace "escript boots" with "mix release bl boots + evals
on Core beams (+ drop pack if configured)". escript is not a cutover gate and
any escript-only boot path is legacy, not load-bearing for this work.

=== TEST-MIGRATION DIRECTIVE (user, this session) ===
Move the ~110 Elixir test sites to .bl too, where feasible. The suites currently
exercise the compiler via BeamLisp.Compiler.eval_string / new_env / read_all from
ExUnit .exs files — but beam-lisp HAS a good test framework (test/bl/*.bl gates
already use it: `mix beam_lisp.test`). Keeping the compiler's own tests in Elixir
fails to dogfood bl-as-test-language, which it is good at. So: port the .exs
suites that test LANGUAGE behaviour to .bl test files, showcasing bl testing.

Scope nuance (choose most correct):
  - Language-behaviour suites (eval a form, assert a value/raise) → port to .bl.
  - Suites that test ELIXIR-side host internals (Emit topology, AOT cache keys,
    reproducibility harness, is_map lint of lib/*.ex) → stay .exs (they assert on
    Elixir modules, not language behaviour). Porting those would be forced.
  - The differential-oracle suites (genesis-vs-bl parity) are RETIRED by S4, not
    ported — there is no genesis to compare against post-cutover.

New step S2b added to the roster (after S2 driver, can overlap S5).

=== S3 DESIGN REFINEMENT (verified, not assumed) ===
The seed today holds ONLY compiler + reader-node. But the .bl compiler, to RUN,
resolves core vars (`list`, `reduce`, …) through the Env var table — proven:
with only RT.seed_core (no prelude), the compiler INTERNS (:bl, interned=true)
but compiling `(+ 1 2)` fails `undefined var: compiler/list`. So the boot
prelude (core, multi, sugar) MUST be loaded before the compiler can compile, and
genesis is what compiles them on every clean build today (they are not seeded).

∴ To delete genesis the SEED must be self-sufficient for the whole boot closure
the compiler depends on:
  seed = {core, multi, sugar, compiler, reader-node} + their Body/Init companions.
Then init's prelude loop (AOT.ensure_loaded, which honours staging trust) interns
core/multi/sugar FROM the staged beams (no compile), the compiler interns and
has core/list, and it compiles the rest of the tree — genesis untouched.

Mechanism now in place:
  - Bootstrap.install! STAGES the seed on key mismatch (installs anyway) and sets
    :bootstrap_staging to the namespaces the seed provides.
  - AOT.stale?/2 trusts a staged namespace's beam for interning (values replay;
    bytes need not match current source).
Remaining S3 work: expand the committed seed to the full boot closure, extend the
staging-trust ns set accordingly, prove a genesis-SABOTAGED clean build is GREEN.