#+TITLE: P21: synthesis and repair — the checker run backwards
#+STATE: ITEM
#+SESSION_ID: 15699e91a40f236f
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp--type-inference/2026-08-28T16-47-31-472Z_15699e91a40f236f.jsonl]]
#+CUSTOM_ID: PLAN-053-p21-synthesis-and-repair-the-checker-run

* Initial Message

ok my friend, can you tell me more about this?

The honest remaining depth — z3-sequence content invariants beyond length, a richer discovery vocabulary, multi-collection capacity — is depth on a shipped foundation, not a missing floor.


From zero context in simple language. FOld in more stuff if it needs to be talked about

GOAL: the inference phase built a RELATION (typeo/verify), not a one-way checker. It runs forwards (check), sideways (refute), and BACKWARDS (synthesize). R5 (synthesize-capacity) proved this for one constant. Generalize: from a VIOLATED invariant, synthesize the GUARD that would make the transition safe. Program repair for free.

THESIS: "what guard preserves this invariant?" is the same backward run as "does this guard preserve it?". abduce already synthesizes a MISSING precondition (shipped). Point it at a broken transition and it proposes the fix.

DESIGN:
- repair-transition: given a process whose transition T breaks invariant Inv, find a guard G such that (Inv ∧ G ∧ next) ⇒ Inv'. Search abduce's candidate vocabulary (the mk `candidates` relation, relational (op a b) over state vars ∪ consts) for the weakest G that makes preservation unsat-of-negation.
- weakest-precondition flavour: prefer the most permissive G (fewest states excluded) that still preserves — via the same monotone search R5 used.
- connect to R5: capacity synthesis is the degenerate case (synthesize the CONSTANT); guard synthesis is the general case (synthesize the PREDICATE).
- connect to abduce (shipped): abduce finds a missing precondition on an unprovable box; repair finds the guard on a provably-broken transition.

STRIKING DEMOS:
- a bank withdraw with no balance guard → repair proposes (>= balance amt)
- a use-after-close protocol bug → repair proposes (= phase :open) on send
- a queue overflow → repair proposes (< (count items) K) with K from R5

VERIFY: for each broken machine, the synthesized guard, when inserted, makes verify-process hold. The repair is CHECKED by the forward checker (round-trip: synthesize backward, verify forward). Reachable≠decidable caveat: some repairs are :unknown → report honestly. Graduate as tests + demos.

DEPENDS: leans on the mk candidates relation (core.bl:81) + R5 monotone search + abduce (all shipped).
