#+TITLE: Resolve LazySeq memo ownership before completing core iteration verification
#+STATE: DOING
#+SESSION_ID: 15751ba43523b193
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-06T14-57-15-988Z_15751ba43523b193.jsonl]]
#+CUSTOM_ID: PLAN-092-resolve-lazyseq-memo-ownership-before-co
#+DECISION_STATUS: Awaiting user decision after requested explanation
#+PARENT: PLAN-089-fast-core-iteration-fine-grain-the-toolc

* Initial Message

you are  the only agent working on this. make it work.

* Problem
Latest normal mix test fails before ExUnit while compiling anf.bl with LazySeq.ForceError {:lazy_cache_budget_exceeded,536870912}; evidence artifact://15751ba43523b193/main/bash/140.txt. A diagnostic forced 20,000 discarded sequences; ETS retained 20,001 rows after GC. Twenty concurrent callers correctly executed one shared thunk once. ETS strongly roots results/tails and cannot detect last-handle loss.

* Decision boundary
User requested an explanation of the bolder native-resource alternative; explanation delivered. Implementation NOT approved or started. Asking for explanation is not authorization to replace the runtime ownership contract.

Proposed solution: GC-owned shared memo handles using a small Rustler resource; BEAM continues running Lisp. Native code manages only short state transitions and lifetime. Preserve shared exactly-once execution, failure replay, and owner-death behavior. Explicitly address cycles: native resources do not automatically trace arbitrary cyclic graphs. Alternative, only if approved: explicit scoped ownership with publication rules. Neither raising the cap nor evicting live values is acceptable.

* Investigation and scope
Read lib/beam_lisp/lazy_seq.ex, lib/beam_lisp/loader_server.ex, test/beam_lisp/wave23_lazyseq_test.exs, existing native build integration and Env publication boundaries. Preserve dirty changes. Design result storage, handle liveness during force, wait notification, owner monitoring, recursive forcing, sharing, captures and cycles before editing. No blocking scheduler NIFs, Lisp evaluation under native locks, arbitrary ETS resets, or silent semantic downgrade.

* Acceptance
- Existing LazySeq tests pass, including cross-process exactly-once side effects, owner death, failure replay and captures.
- Repeated creation/discard/GC has bounded memory, verified through instrumentation.
- Copied and globally captured values remain valid while referenced.
- Cycles and recursive forcing have explicit tested semantics.
- Normal cache-disabled build and full mix test complete under default settings.
- Re-run PLAN-089 AOT/cache/drift/reproducibility/ANF/boot-barrier tests on final tree. Earlier 29-pass and isolated clean-build evidence predates current blocker.
- Seed files stay unchanged during iteration; regeneration remains the commit-time gate.

* Deferred PLAN-089 verification
Implemented: coherent Core AOT publication, serialized boot with parallel ordinary waves, bounded cache cleanup, hardlink-safe beam writes, Fix2/3 investigation. Removed unsafe prelude-interface shortcut. Full current-tree verification is incomplete pending this ownership decision. Do not close PLAN-089 until final gates pass.

* User authorization and integration requirement
User explicitly requested implementation of the whole fix and asks whether it applies wherever useful. Implement the shared LazySeq ownership primitive (not a compiler-only workaround), audit constructors and consumers across RT, Vector, compiler, Loader and cross-process paths. Preserve successful shared memoization and explicit failure semantics. Do not blanket-replace persistent caches with value-lifetime semantics. Require runtime reclamation/concurrency tests, complete build/regression verification, native build/deployment integration, and update docs/memory-policy/shared-lazy-values.bl.md to actual shipped behavior with limitations stated.

NOTE [2026-09-06]: Coordination from PLAN-090 implementing session 15729b4fce888940: our BUG-028 ETS repair overlaps your new GC-owned LazyMemo cutover. Observed current lazy_seq.ex now calls LazyMemo.create/1; our current-source startup gate fails because its native module is not built/loaded yet. We will NOT overwrite your new runtime source or commit these shared files. Please own lazy_seq.ex/loader_server.ex/native build/wave23 tests to completion and attach commands/results here. PLAN-090 continues compiler canonical emission and dev.change independently; its full build gate must consume your final native integration. Our earlier ETS concurrency tests passed 24 aggregate; later cycle fixes are preserved by your working source if applicable. Current user authorizes ALL PLAN-090 and commits; coordinate Mix/native build serialization.

NOTE [2026-09-06]: PLAN-090 resumed: Mix gate waited behind PID 1708815 (mix test) and timed out at 600s after reaching canonical host cutover; journal peak 379.3M, no OOM. I will not terminate that process or run another Mix build. Continuing an isolated elixir source-upgrade proof (fresh temporary seed, explicit old/new host modules) without build-lock/disk emission into shared _build. Please attach native integration/full-suite results here when ready.

NOTE [2026-09-06]: PLAN-090 owns canonical compiler + oracle migration right now: compiler2.bl/anf.bl/lower.bl (eager-transform worker), test/beam_lisp/anf_direct_emit_test.exs, test/bl/{anf,core_anf,core_backend,core_switch}_test.bl, priv/self/anf.bl, new anf_interpreter.ex, and anf_canonical_corpus fixtures. Observed concurrent changes around AOT/oracle normalization while running gates; please avoid these files during this wave and record any required fix here. Current focused host gate58passed, BL oracle29tests110assertions pass; remaining snapshot37 mismatches are legacy :struct pairs vs canonical fields, being migrated once from frozen fixtures, not recomputed from current compiler. Native memo is now loaded in gate VM; baseline compiler alone creates1630 LazySeq handles per10units (3 repeats), zero loaded modules. Eager worker addresses this separately, no native runtime edits.

NOTE [2026-09-06]: Native and host cutover implemented centrally, including resource-owned metadata and immutable eager-input cursors. Atomic publication includes waiter notifications; normal scheduler work moved to dirty CPU NIFs; native reclaimer has explicit unload/drain/join; cycles through visible memo handles/closures/metadata are rejected. First integrated build and 89 focused tests passed (bash/199). Cursor benchmark 8k map/count changed from 130064us to 24060us, with native live_cells/retained_bytes/pending_reclaims all zero after worker exit (bash/201, bash/212). Full suite reached 1467/1472 (bash/214): remaining compiler infrastructure regressions being resolved, not skipped. Corrupt printable corpus replaced by lossless frozen-input/seed-ANF ETF fixtures; 280/280 inputs independently compiled+normalized using committed seed (bash/227), with no current-compiler evaluation used to generate expected outputs. Added SHA-256 cache-entry validation/versioning and application metadata loading for stable compiler keys; forced rebuild and startup provenance matched (bash/237). Current final targeted job bg_15757aebb4161660 includes source compile-info propagation and bounded post-GC resource cleanup grace. Still owe final focused/full suite pass, normal tutorial run, release smoke, and final document delivery.

NOTE [2026-09-07]: Status reconciliation requested by user: the earlier approval request/Decision boundary is historical and superseded by explicit authorization. Native migration is implemented and committed in d785007; current HEAD c88b942 includes PLAN-090 integration and published seed. Read !tasks/artifacts/PLAN-090-final-verification.md and actual final-policy-regression-result.log: recorded final suite 1487 passed. Integration receipt records 141-source uncached build, independent byte-identical 30-module seed rebuild and executed documentation; these supersede this session's earlier failed full build and 33-test snapshot. This session also measured three batches of 20000 discarded forced values: each settled to exactly one intentionally retained live cell, 80 retained bytes, zero pending reclaims, with shared live value preserved. Remaining confirmed defect BUG-029: CompilerData eager_to_list exposes large Vector trie internals (1024 inputs become five fields), reproduced now against current source. Fix and regression/rebuild gates remain. Reconcile historical decision text, leftover untracked older corpus artifacts and current ownership before final plan closure. No new implementation approval is needed.

NOTE [2026-09-07]: Lane split committed as f1cba00 (perf: two scheduler lanes for MemoCell). Clean full run on committed tree: 1492 passed, seed 419179, 479.3s (load avg ~12-16 from unrelated repos' BEAMs; the 3+8 failures seen in two earlier runs did not recur and were characterized as environmental: wave23 stats-convergence flakes at identical rate under a forced pre-change dirty-only lane A/B; Refs noproc examples all pass in isolation and Refs has no LazyMemo coupling). Measured: swap 10158->587ns, read 4151->91ns, CAS 5718->333ns; B went from 9x slower than Agent to 2.6x faster. Follow-on plan PLAN (MemoCell unification) created separately.