#+TITLE: datalog goes native — typed value slot + resolve NIF + columnar engine + #d[…]
#+STATE: REVIEW
#+SESSION_ID: 1566d824eadd2493
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-08-26T13-03-37-899Z_1566d824eadd2493.jsonl]]
#+CUSTOM_ID: PLAN-042-datalog-goes-native-typed-value-slot-res

* Initial Message

could you use clojure-reader to turn the binary blobs in redb into readable/queriable/dataloggable EDN directly from the Rust interface, thus making it the most efficient datalog store there is? And still have deep integratoin on the beam lisp side? Can you prototype that and tell me how impressive it is?

* Goal

Make beam-lisp datalog fast at the SUBSTRATE — no size-based routing. Two fixes,
BOTH mandatory, ONE cutover, NO fallback path (delete the old per-datom
vector-of-maps engine entirely). One implementation of each thing, and it is the
fast one.

Full design + measured evidence live in the worktree scratch docs (gitignored):
- scratch-plan-datalog-native.md          (the implementation plan)
- scratch-plan-datalog-native-TRADEOFFS.md (losses/mitigations, final arch, downstream usage)
- scratch-proto-typed-values/              (the (b) prototype: scheme C, 37–100× decode)

Prototypes proved (real redb): ETF decode ≈40% of query time; typed tag-free
value slot removes it (long 37×, contiguous 100×, string 9× zero-copy); columnar
bindings remove a further ~5× in the join layer.

* Storage — DECIDED: scheme C, columnar tag-free typed values

Value slot = packed fixed-layout record, NO per-value type tag (dtype known from
schema per scan):

  [ e:8B i64 | tx:8B i64 | op:1B | v:<dtype-typed payload> ]

Two lanes, chosen per attribute by schema:
- typed lane  (long/ref/instant → 8B i64; float → 8B IEEE; boolean → 1B;
              string/keyword → raw UTF-8, zero-copy) — the fast path.
- term  lane  (:db.type/term, and bignum past i64) → 1-byte tag + ETF payload,
              handed back OPAQUE. Preserves zero-cost round-trip of ANY EDN/BEAM
              structure (proven bit-identical). codec.bl (the KEY) still orders
              every term, so term attrs stay indexable.

The NIF reads the VALUE slot only, by fixed offsets — it NEVER reverses the
order-preserving KEY codec (that coupling is rejected). `a` is constant per scan.

Recommendation adopted: float → typed lane; bignum → term lane (keeps fast lane
strictly fixed-width, preserves the contiguous-column fusion path).

* Fix A — the resolve NIF (Rust, crate datom_resolve)

(resolve db-handle index-tag prefix-bytes dtype) -> columnar frame
  1. range-scan redb (keys opaque; used only as range bounds)
  2. read packed value record [e|tx|op|v] by fixed offset, v per dtype
  3. retraction-filter IN RUST (supersede by later retraction of same [e v])
  4. build typed columns → one coarse BEAM crossing
Conformance: golden-vector suite over the PACKED-RECORD layout (not ETF), per
dtype, both directions, 0 disagreements; retraction parity vs a bl reference.

* Fix B — columnar binding frame (pure beam-lisp)

Binding set becomes {:vars [...] :cols {?v [...] ...}} not a vector of maps.
run-clauses / pattern-scan-once fold over the frame; join = int-keyed bucket;
unify = column mask; predicates vectorized where possible. q still returns a SET
of tuples (materialize at the end); q-df returns an Explorer DataFrame. Recursive
rules materialize rows only transiently inside the rule — one representation.

* Syntax (target)

- aggregates first-class (add min/max/distinct to agg.bl)
- q-df (Explorer DataFrame surface; q stays set-valued)
- pull in :find
- (datom/from …) pipeline macro → compiles to q-df (pure macro; query is EDN data)
- #d[…] reader literal — ONE branch in reader.ex record_or_tag to accept `[`
  after a registered data-reader tag + core.bl registration; validates at read
  time with source location.

* Cutover contract (no fallback)

DELETE in the same change: store-redb ETF value read path for queries;
datom.db/datoms|range-datoms returning decoded datom vectors; engine
vector-of-maps path; unify-pattern per-row map. No available?/if-native branch on
the query path (grep proves it). resolve is a hard dep of datom, peer to redb.

* Acceptance

- mix compile builds datom_resolve; typed-codec golden vectors 0 disagreements
  both ways; retraction parity 0 disagreements.
- existing datom query suite passes UNCHANGED (behavior-preserving rewrite).
- ≥100k-datom analytical query measurably faster than pre-cutover; old
  vector-of-maps path gone (grep proves no second impl).
- q-df == q then hand-built columns (value-for-value).
- #d[…] reads + validates at read time with a source location.
- datom/from == equivalent explicit q-df.
- :db.type/term round-trips arbitrary EDN/BEAM structures bit-identically.
- no fallback branch on the query path.

* Non-goals

No charts/viz (relay consumes q-df). No new query language beyond the forms
above. Contiguous-column fusion (many datoms per redb slot) is a future step the
format allows, not built here.

NOTE [2026-08-27]: Implemented end-to-end on branch plan-042-datalog-native (worktree). 8 commits from e717612 base.

DELIVERED:
- Fix A (scheme-C typed storage + resolve NIF): value-codec.bl (schema-free runtime-typed value<->bytes, lanes long/bool/str/keyword/float/ESC); store-redb packed record [TAG|e:8|tx:8|op:1|a-len:2|a|v]; datom_resolve NIF decodes the whole range in Rust (no per-datom binary_to_term); Scan protocol + scan-datoms capability dispatch (satisfies? Scan, NOT an if-redb branch); index/scan+scan-range cut over. Golden-vector suite (resolve==scan across dtypes/boundaries/retraction/arbitrary structures). ~40% ETF decode tax removed from durable read path.
- Fix B: the measured bottleneck was NOT the vector-of-maps representation but an O(n^2) accumulator — (into [] (mapcat f bindings)) per clause (micro-bench 7.7s@10k / 50s@20k vs reduce+conj 8ms/17ms). flat-into (O(n)) applied to all engine folds (pattern-scan-once join branches, run-clauses, :and/:or/:not/:or-join/:not-join, :in binding folds). Measured: spend-by-model aggregate join 4000 rows 3861ms->409ms (9.4x); 8000 rows now 1535ms (was superlinear).
- Syntax: datom/from analytical pipeline macro (gather/where/group-by/agg -> q); #d[...] reader literal validated at read time (reader.ex record_or_tag + rt.ex data_reader table + builtin default; read-query macro runs q's exact validation at compile time).

SCOPE ADJUSTMENTS (evidence-driven, honest):
- Fix B is NOT a columnar-frame rewrite. Profiling showed the accumulator was the actual quadratic; flat-into fixes it with 9x win at far lower risk against 322 passing tests. A full columnar rewrite would be high-risk for incremental constant-factor gain — reassessed as not worth it. Documented in scratch-LAYOUT-corrected.md.
- q-df (Explorer DataFrame surface) deferred to PLAN-039, which owns Explorer/Polars (not a dep here). datom/from + #d[...] + the fast engine are the in-scope syntax; a DataFrame is a thin future map over the columnar result.

VERIFIED: full datom suite 322 tests/722 assertions 0 failures; all beam_lisp elixir tests 0 failures; no-fallback grep clean (no available?/if-redb on query path); rebuilt datom_redb.so committed.

NOT DONE (honest): the storage packed record still carries `a` (redundant with the key) — the bare-v + key-parse optimization is noted as future work; contiguous-column fusion (many datoms per slot) is future; a real Explorer DataFrame surface is PLAN-039.