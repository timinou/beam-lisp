#+TITLE: FUP: complete Core cutover — emit → flip → seed → delete genesis
#+STATE: ITEM
#+SESSION_ID: 156e98a98c8113c8
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp--self-hosted/2026-09-01T13-34-07-410Z_156e98a98c8113c8.jsonl]]
#+CUSTOM_ID: PLAN-081-fup-complete-core-cutover-emit-flip-seed

* Initial Message

Look at the original big plan on this worktree to make this fully self-hosted, and critique/review it. find incomplete tasks and new tasks revealed by this work. let's be thorough

Follow-up to PLAN-079. S-A..S-D.aot are DONE (Core backend exists, reads bl-ANF,
builds+persists+loads real namespaces reproducibly, a switch routes to it,
default :elixir). This FUP is the remaining CUTOVER chain — each step gated, the
last two irreversible, so they are staged deliberately rather than rushed.

=== 1. S-D.aot integration (contained, reversible) ===
Have `mix compile.beam_lisp` (aot.ex `emit_module`) build body modules through
the Core path (self.core/ns-body-beam) behind :bl_backend, default :elixir.
GATE: `mix test test/beam_lisp/` stays 1344/1366 under :core; AOT
reproducibility 303/303 byte-identical with Core-PRODUCED beams (a Core beam is
already per-build reproducible — cba344c). Touches aot.ex (.ex) + a build flag.

=== 2. S-D.emit (LARGE, multi-session; an OPTIMISATION, not a 0-Elixir gate) ===
Make priv/boot/compiler.bl emit bl-ANF DIRECTLY, retiring the Elixir-quoted
layer + the self.anf/normalise bridge. Emit surface is 7 constructors
(ast-node, ast-alias, ast-dot, ast-mod-call, ast-fetch, ast-invoke,
ast-remote-fun) — the only sites that build quoted nodes — but threaded through
hundreds of call sites. compiler.bl is 3731 lines, byte-parity-locked under the
oracle (priv/self/oracle.bl), so this is a compiler rewrite that must stay green
at EVERY step: its own sequenced sub-plan, not a single turn. NB: because the
normalise bridge already lets Core consume compiler.bl's output, S-D.emit only
removes one data-translation (quoted is bl DATA, not Elixir semantics) — it is a
cleanup/perf win, NOT a prerequisite for zero-Elixir-in-the-compiler.

=== 3. S-D.flip (IRREVERSIBLE — default change) ===
Flip the :bl_backend default from :elixir to :core (the switch's default value in
link.ex core_backend?/0, and the AOT branch). GATE: full suite + reproducibility
green under default-core; escript AND drop boot green; corpus gate stable.
Depends on step 1 (and ideally 2). This changes what the booted system compiles
with — do only with the whole suite proving it.

=== 4. S-SEED (commit the Core boot seed) ===
Compile the boot .bl toolchain (reader, reader-node, compiler, core, sugar,
source-graph, build-plan, ns-interface) to Core → .beam, COMMIT as the checked-in
seed, loaded at startup instead of genesis-compiling. GATE: a clean clone boots
and runs `bl eval` / `mix beam_lisp.test` with reader.ex/compiler.ex RENAMED
AWAY (proving they are never invoked). The seed regenerates from source via the
Core backend itself, so R3 "regrow from a clean clone" is preserved WITHOUT
Elixir. Depends on step 3.

=== 5. S-E (IRREVERSIBLE — delete genesis) ===
Delete lib/beam_lisp/reader.ex + compiler.ex (role-A Elixir); retire
priv/self/oracle.bl (its Elixir-differential job is over — the behavioural
value-identity gate from S-A/S-B is the standing regression, Core vs the
reference interpreter, no Elixir). GATE: grep shows no compile-path reference;
clean-clone boot green. Depends on step 4. Role-B function-call Elixir
(rt/vector/env/link) stays until PLAN-057 P10/P11 — that is the LAST Elixir and
a separate benchmark-gated decision.

=== State proven so far (why this is de-risked) ===
- bl-ANF neutral IR exists + faithful: value-identity vs Elixir across the
  corpus + 430-form structural census (compiler.bl normalising itself), 0
  rejected (PLAN-079 S-B, commit 49624d8).
- Core builds+persists+loads real recursive multi-clause namespaces
  reproducibly (S-D.aot, f6e2b2c; real-ns recursion across body/shim verified).
- System switch routes defvar to Core, default inert (S-D.part2, 0213154);
  full regression unchanged (1344/1366 = baseline).
- MLIR/LLVM (PLAN-080) reads the SAME bl-ANF — this cutover and the native
  track share the neutral IR.

Gates + commits are enumerated in PLAN-079's tail (org PLAN-079).

* EXECUTION LOG (this session)

Steps 1, 3, 4 DONE and committed. Step 2 (emit) and Step 5 (delete) deferred
with precise scoping below.

** DONE — S-D.aot integration (commit d7bcc7d)
Route AOT body modules through Core behind :aot_backend. Found+fixed 3 real Core
bugs the earlier cache-warm probe had masked:
  1. lower-anf :if exponential blowup (else lowered twice; cond nests it) —
     45489ms→25ms via a single guarded clause (anf-if->case).
  2. lower-anf-pattern struct/map key lowered a raw atom via lower-anf → "unknown
     op :items" on every vector pattern in receive/case — fixed with
     lower-anf-pattern-key.
  3. Non-reproducible beams: defs->module-anf grouped by atom-keyed map (iterates
     in atom-TABLE order, varies run-to-run) — fixed by sorting groups on fname
     STRING + arity; fresh counter + current-module made per-process pdict.

** DONE — backend-aware cache key (commit 749951d)
AOTCache.aot_backend/0 single resolver; compiler_key folds in the backend and
(under :core) hashes priv/self/ sources. A Core beam and an Elixir beam of the
same source get different keys — no cache cross-contamination.

** DONE — Core struct lowering fills defstruct defaults (commit bea4ce5)
lower-anf :struct built a plain map of only WRITTEN fields, so a Core %Vector{}
was missing meta:. The compiler's own data->form does map_get(:meta, v) →
"key :meta not found", which broke every file compiled while a Core compiler.bl
was loaded (system/core.bl in a cold all-Core build). Fixed by calling the
struct module's __struct__/1 with the written fields (applies over defaults),
mirroring the quoted `lower`.

** DONE — S-D.flip: default → :core (commit afaefde)
Full-tree Core build: clean, byte-reproducible (serial==parallel, 330 beams, 0
diff), boots, runs; regression 1345/1366 = SAME baseline as Elixir. Elixir stays
available via config :beam_lisp, :aot_backend, :elixir. Also fixed: inline pdict
key atoms (were value vars unreachable from a throwaway eval env → undefined
var); Link.core_backend?/0 now requires Env.loaded_ns?("self.core") so the
runtime switch falls back to Elixir when self.core isn't interned instead of
raising on a sibling call.

** DONE — S-SEED: Core-built bootstrap seed (commit 0004a54)
The committed seed (compiler + reader-node closure) is now Core-built; manifest
records the :core compiler_key, so Bootstrap.install!/1 accepts it. PROVEN
genesis-independent: with :compiler_backend forced :bl (genesis forbidden), a
booted-from-seed compiler compiles non-trivial forms; mix clean + single mix
compile from the seed works first-try, boots, runs.

** DEFERRED — S-D.emit (step 2)
Unchanged from the plan: compiler.bl emits bl-ANF directly, retiring the quoted
layer + normalise bridge. LARGE (3731-line byte-parity-locked compiler.bl, 7
emit constructors threaded through hundreds of sites). An OPTIMISATION, not a
0-Elixir gate — the normalise bridge already lets Core consume compiler.bl
output. Its own sequenced sub-plan.

** DEFERRED (deliberate) — S-E delete genesis (step 5)
KEY FINDING: "delete reader.ex/compiler.ex" is NOT literally achievable as a
cutover step, and deleting the genesis LOWERING now would be wrong:

  - Both files are ~40% genesis lowering, ~60% ESSENTIAL driver/API + the
    delegation seam: new_env, eval_string, eval_form, compile/2 (the seam that
    delegates to BeamLisp.Ns.Compiler), macroexpand_1, prelink_defn, read_data,
    reset_fresh!, read_one/read_all. Every caller and the whole runtime depend
    on these. They are NOT genesis and cannot be deleted until the compiler
    DRIVER itself (not just codegen) migrates to .bl and every caller is rewired
    — a project, not a step.

  - The genesis lowering (compile_elixir + compile_special/*, read_string_elixir)
    IS the deletable genesis, but it is also (a) the bootstrap-of-last-resort and
    (b) the differential oracle (oracle.bl grades the .bl frontend against it).
    Deleting it removes the safety net exactly when the Core backend is one
    session old.

  - IRREPLACEABLE ROLE confirmed: a compiler.bl EDIT invalidates the committed
    seed (compiler_key changes), and then ONLY genesis can rebuild the new seed.
    This is the bootstrap ladder. Deleting genesis needs a staged-bootstrap tool
    that rebuilds seed N+1 from seed N via Core — real, careful tooling.

  ∴ S-E is correctly gated on: (1) compiler.bl frontend stable, (2) a
  Core-only staged-seed-rebuild path (no genesis), (3) a soak period with the
  oracle active to catch Core regressions, (4) the driver/API migrating to .bl
  for the literal file deletion. Keeping dormant genesis costs nothing and is
  the recovery floor; deleting it trades robustness for a purity the API-shell
  Elixir doesn't even grant yet.

* NET RESULT
The compiler's SEMANTIC backend is now Core Erlang by default: compiler.bl →
bl-ANF → Core Erlang → .beam, no Elixir compiler on the codegen path, proven
reproducible + behaviourally identical + genesis-independent (via the Core
seed). The remaining Elixir is the compiler DRIVER/API shell (Role A residue)
and Role-B runtime calls — both out of scope for the backend cutover and
tracked separately.