#+TITLE: P18: Richer state models — type-directed SMT + invariant discovery
#+STATE: ITEM
#+SESSION_ID: 15699e91a40f236f
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp--type-inference/2026-08-28T16-47-31-472Z_15699e91a40f236f.jsonl]]
#+CUSTOM_ID: PLAN-050-p18-richer-state-models-type-directed-sm

* Initial Message

this is amazing!

Please explain what's left from zero context in simple language, and what that would enable. I am particularly curious about the richer state models, what is missing? what's your vision? let's take all the time we need to flesh it out

Moreover, please tell me what work has been done so far about implied invariants and implied everything else.

* Goal
Lift the system-model tier's state model from ONE integer to the full tag-lattice
shape — multi-var, records, collections, sum types — and discover invariants
without annotation. The mechanism is ONE indirection: type-directed SMT
translation (smt.bl consults the tag lattice for each state var's z3 sort instead
of hardcoding Int). Design doc: docs/typing/richer-state-models.typ (7pp).

Two load-bearing claims PROVEN with runnable spikes (research/p18_state_models/):
- spike_multivar.bl PASS: relational invariant (balance >= reserved >= 0) proven
  for 3 transitions, reservation-ignoring bug caught. Rung 1 mechanism verified.
- spike_discover.bl PASS: Houdini fixpoint over abduce's candidate vocabulary
  discovers (>= balance 0) from an UNANNOTATED machine, refutes bogus candidates.
  Invariant discovery verified. Key subtlety: establishment filter (holds-at-init)
  must run BEFORE induction, else contradictory candidates make the antecedent
  unsat and induction is vacuous.

* Non-goals
- No new engine. Strict engine division preserved (lattice=structure, z3=arith,
  miniKanren=synthesis, datalog=codebase). All four already exist.
- No JVM. z3 bundled (priv/z3/bin/z3), no PATH.
- TLA+ export out of scope (prior user decision).
- Not touching the auth.* pre-existing breakage (out of scope, foreign-session).

* Contract (graduation)
Every capability graduates as TESTS *and* a demo/example (the tier's standing
contract). Priv APIs land in priv/system/. Non-auth regression stays green.

* Constraints / conventions learned
- preserves? in core.bl uses svar + svar2 suffix priming (NOT apostrophe — ' is
  awkward through the shell and not the codebase convention). Multi-var = declare
  the whole tuple, prime each with "2" suffix, join the next-eqs.
- state-vars ALREADY collects the SET; verify-process throws all but (first) away.
  Rung 1 = declare+prime the whole set, generalize inv-fn to a renamer.
- smt.bl passthrough-ops already ARE valid SMT-LIB (prefix bl ~ prefix SMT-LIB).
  The Int assumption lives in the (declare-const … Int) lines + the fact that
  translate emits bare symbols. Type-directed = pick the sort per var.
- Houdini seed MUST be consistent: filter to holds-at-init FIRST, then iterate
  induction to fixpoint. Order pinned in spike_discover.
- Test harness: elixir $(for d in _build/test/lib/*/ebin; do echo -pa $d; done)
  -e 'Application.ensure_all_started(:explorer); BeamLisp.init(); for p <-
  ["priv","priv/datom","examples"], do: BeamLisp.Env.push_load_path(p);
  BeamLisp.TestRT.cli(["test/bl/system/..."])'
- Dev/demo harness: elixir $(…dev…) -e 'Application.ensure_all_started(:beam_lisp);
  BeamLisp.init(); BeamLisp.Env.push_load_path("priv"); BeamLisp.run_file("…")'

* Tasks (dependency-ordered)

** p18-multivar — Rung 1: multi-variable relational invariants
Generalize verify-process (priv/system/core.bl) from (first svars) to the full
set. Declare every state var + prime each with "2"; inv becomes a fn of a renamer
(v -> current name) so it primes jointly; preserves? already N-ary-ready underneath.
next-state map: a transition updates a SUBSET, unmentioned vars carry through
(vi2 = vi). state-vars already collects the set — stop truncating it.
Accept: spike_multivar's reservation account verifies end-to-end through
verify-process (not hand-written SMT); good transitions :holds true, buggy
withdraw warns.
Demo: examples/system/10_multivar_invariant.bl. Test: test/bl/system extends.

** p18-records — Rung 2: record / mixed-sort state  [blocked: p18-multivar]
State is a product of typed fields {:balance int :frozen bool}. Route: PROJECTION
— each field its own z3 var of its own SORT (Int, Bool), pulled from the tag
lattice (typed/walk / sigs-from-env infer the field types). smt.bl stops assuming
Int: a declare-sort helper maps :int->Int, :bool->Bool. Invariant like
(or (not frozen) (= balance 0)). A field access (:balance state) reads as that
field's projection var.
Accept: mode machine — frozen ⇒ balance=0 proven; a pay-out-while-frozen caught.
Demo: examples/system/11_record_state.bl. Test added.

** p18-collections — Rung 3: collection state (bounded)  [blocked: p18-records]
z3 theory of arrays/sequences/sets for queue/set/map state. Crosses the
decidability line: some invariants become BOUNDED not exact. Register results in
the guarantee-catalog as :approximate with :bound (machinery already ships:
register-guarantee! / approximate-guarantees / exact-guarantees). Translator maps
(conj q x) -> seq append, (count q) -> seq length, (contains? s x) -> set member.
Accept: bounded work queue — "pending never exceeds capacity" as a bounded
guarantee, honestly catalogued.
Demo: examples/system/12_collection_state.bl. Test added.

** p18-sumtypes — Rung 4: sum-typed protocol state  [blocked: p18-records]
State is a tagged variant :idle | (:running job) | :closed. z3 algebraic datatype
with a discriminant; translator reads the variant tag. Invariants about LEGAL
transitions (can't :send from :closed). Recruits all four engines; connects to
existing simulates? / migration-preserves? (session types / protocol conformance).
Accept: connection FSM — illegal :send-while-:closed caught.
Demo: examples/system/13_protocol_state.bl. Test added.

** p18-discover — invariant discovery (no annotation)  [blocked: p18-multivar]
Houdini fixpoint as a graduated API in priv/system/ (e.g. system.discover):
candidates (reuse system.core/candidates) -> establishment filter (holds-at-init)
-> induction fixpoint (drop conjuncts a transition breaks under the current set)
-> the discovered invariant. Seed the vocabulary from the state vars (Rung 1+).
When a machine has NO ^{:invariant}, verify-process can OFFER the discovered one
(sound: it's proven inductive + established) rather than staying silent.
Accept: un-annotated server -> checker PRINTS the discovered invariant, then proves
it. spike_discover promoted to graduated API + test.
Demo: examples/system/14_discover_invariant.bl. Test added.

** p18-verify-all — regression + numbers + doc  [blocked: all above]
examples/system/15_type_directed.bl: ONE server, three state shapes (int/record/
sum), the single translator adapting by consulting the lattice — the mechanism
demo. Full non-auth regression (target: all system tests + prior 635 green),
measure numbers per rung, update docs/typing/richer-state-models.typ "Where it
stands" with results, GRADUATION.md note.

* Demos produced (the proof surface)
- 10_multivar_invariant.bl — reservation account, relational invariant
- 11_record_state.bl — mode machine, mixed-sort (int+bool)
- 12_collection_state.bl — bounded work queue, approximate guarantee
- 13_protocol_state.bl — connection FSM, sum-typed protocol conformance
- 14_discover_invariant.bl — un-annotated server, discovered invariant (headline)
- 15_type_directed.bl — one translator, three shapes (the mechanism)

* Evidence already in tree
- docs/typing/richer-state-models.typ (7pp, compiles)
- research/p18_state_models/spike_multivar.bl (PASS)
- research/p18_state_models/spike_discover.bl (PASS)

NOTE [2026-08-29]: p18-multivar DONE (Rung 1 shipped, commit ab7fe0c). verify-process in priv/system/core.bl now handles N state variables jointly: ordered-svars reads the tuple shape from the init binding vector; next-map maps each transition's :next vector positionally; the invariant is a z3 (define-fun Inv ((v Int)...) Bool ...) so priming needs no textual substitution; preserves-joint? asserts Inv(s) & guard & (AND vi2=nexti) & not Inv(s2). N=1 is the special case (one param) — P17 single-var seam unchanged. Reservation account (balance>=reserved>=0) proves end-to-end; reservation-ignoring withdraw caught. Demo examples/system/10_multivar_invariant.bl. Tests seam_test.bl +2/+6 (11t/31a). Regression non-auth 637t/1736a/0F0E. Doc docs/typing/richer-state-models.typ updated (Rung 1 = shipped). Remaining: p18-records, p18-collections, p18-sumtypes, p18-discover, p18-verify-all. Discovery already spiked (spike_discover, PASS) — p18-discover is graduation of the Houdini fixpoint.

NOTE [2026-08-29]: P18 COMPLETE (2026-08-29). The whole ladder graduated — state is no longer one integer. ALL rungs shipped in the real checker with tests + demos:
- Rung 1 (multivar, commit ab7fe0c): verify-process handles N state vars jointly via z3 define-fun. 21ms.
- Rung 2 (records, 7239080): mixed-sort {int×bool}; state-shape reads per-field sorts from init map; false/true→Bool. 23ms. Root-fixed delaborate :map/:keyword case.
- ★ discovery (23cadaa): Houdini fixpoint discovers invariant from nothing; establish-before-induct; state vars derived from transitions (input=bound by pattern, state=in guards/nexts not pattern). 99ms.
- Rung 3 (collections, d03bdd0): capacity via length abstraction (smt/translate-len: count→_len, conj→+1, rest→-1); verify-capacity registers :exact; content stays untranslatable = bounded companion. Catalog keeps exact/approximate apart. 19ms.
- Rung 4 (sumtypes, add3032): variant=phase=integer-coded enum; protocol conformance via existing verify-process. 17ms. Root-fixed free-syms shadowing true/false as Int inputs (soundness bug hitting any bool-literal assignment).
- verify-all (407e93c, 0f35464): mechanism demo 15 (one checker, 5 shapes + discovery); doc updated to shipped; GRADUATION.md P18 section.
Tests: seam_test.bl 9→21 tests (25→55 assertions). Demos examples/system/10-15, all 15 clean. Regression non-auth 647t/1760a/0F0E. Doc docs/typing/richer-state-models.typ (7pp). INFRA lesson: priv/*.bl compile to ebin — edits need `mix compile.beam_lisp` (+MIX_ENV=test) before the harness sees them. Remaining depth (non-blocking, honestly out of scope): z3-sequence CONTENT invariants beyond length; richer discovery vocabulary; multi-collection verify-capacity.