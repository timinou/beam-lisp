#+TITLE: Wave 10: lazy sequences
#+STATE: DONE
#+SESSION_ID: 154d7065511fb2de
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-08-06T19-28-19-268Z_154d7065511fb2de.jsonl]]
#+CUSTOM_ID: PLAN-006-wave-10-lazy-sequences
#+COMPLETION_HASH: e8d8ff319f69634d

* Initial Message

what's next? what's left? what else could be done?

Clojure's seq model is lazy; beam-lisp's prelude map/filter/… are strict (Enum-based, list in → list out). `(take 5 (map f (range 1000000)))` realizes everything.

Design: `lazy-seq` macro wrapping a thunk in %BeamLisp.LazySeq{fun, memo: Agent|:atom_cache} — realized once, cached. first/rest/seq/nth/conj/count on LazySeq force as needed (RT dispatch on struct). Prelude map/filter/concat/iterate/repeat/cycle/take-while return LazySeq; take/drop force minimally. `doall`/`dorun` force fns. `range` with no end → infinite lazy.

BEAM-specific: infinite lazy seqs + processes are fine (no stack issues — forcing is a call, not a build-up), but caching must not leak memory on hot loops: memoize via a mutable cell (Agent or :atomics per seq — measure).

Interop: Enum functions on LazySeq — implement Enumerable protocol for the struct so Elixir interop works.

Files: lib/beam_lisp/lazy_seq.ex (new), rt.ex (first/rest dispatch), priv/core.bl (map/filter/take/drop/… rewrite), tests + example with infinite sequences. Acceptance: `(take 5 (map inc (range)))` → (1 2 3 4 5) without hanging; realized-once test; all existing seq tests green.

NOTE [2026-08-07]: DONE (with a recorded deviation) — commit e65fd7b. %BeamLisp.LazySeq{} = chain of deferred cells, each realized at most once, memoized in the shared :beam_lisp_vars ETS table keyed {:lazy, ref} (:atomics was tried first and rejected — it stores integers only, verified at runtime). Enumerable + bounded Inspect (printing an infinite seq terminates). map/filter/concat/iterate/repeat/cycle/take-while/drop-while compose lazily; take/drop force minimally; doall/dorun force fully and ITERATIVELY (100k force asserted not to grow the stack). = (RT.eqv) realizes lazy operands element-wise with short-circuit, so (= (range) '(1 2)) terminates.

DEVIATION — seq fns are HYBRID, not fully lazy: a realized input (list/vector) takes the strict Enum path and returns a realized list; only a LazySeq input composes lazily, and only no-arg (range) is infinite. Forced by the existing suite: prelude_test.exs/wave2_test.exs assert on beam-lisp results with Elixir's own == (e.g. eval("(range 5)") == [0,1,2,3,4]), and a struct cannot overload ==. Making bounded range lazy requires = to normalize list-vs-vector, which would flip the deliberate wave-3 contract that vectors and lists are structurally distinct ([] ≠ ()). Consequence: (take 5 (map f (range 1000000))) still realizes the 1M range (186ms measured). Closing this properly is now PLAN-010.

MEASURED: lazy 100k doall (map inc (range)) = 663ms vs strict 100k = 46ms (≈6.6µs/element for per-cell struct + ETS + RT.invoke); lazy take-5 of infinite (range) = 17ms. Also fixed in this wave (found by beam-lisp's own suite): (get m k) 2-arity raised BadArity (linked 3-arity only) → now multi_fn(%{2,3}); assoc added (map/vector polymorphic, variadic); even?/odd?/zero?/pos?/neg? added; drop flipped to Clojure order (drop n coll) via RT.drop_clj/2, with priv/test.bl's are macro updated (its only call site).