#+TITLE: Unify mock/fault/fuzz into the veritas.* family
#+STATE: DONE
#+SESSION_ID: 156c3fa9cf7f5fb2
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-08-30T17-48-19-133Z_156c3fa9cf7f5fb2.jsonl]]
#+CUSTOM_ID: PLAN-065-unify-mock-fault-fuzz-into-the-veritas-f
#+COMPLETION_HASH: 4d3631e57d8cbf65

* Initial Message

- Deferred, not skipped: migrating mock.bl/fault.bl/fuzz.bl to thin wrappers is FUP-015 — those tools have green tests today, and rewriting
 tested code in the same session to satisfy a cleanup risked breaking working behavior for no functional gain. The cutover lands deliberately
 with those tests as the safety net.

=> what? no they are used almost nowhere.

what's still using those modules?????

* Goal

One quantifier core, zero capability lost, coherent namespaces. Fold priv/mock.bl,
priv/mock/fault.bl, priv/fuzz.bl into a =veritas.*= family of SEPARATE files, each
built ON TOP of veritas's core (Gen algebra + check escalator + modal verdict),
preserving every unique capability the three modules have that veritas lacks.

Decision: option A (rewrite-in-terms-of + re-namespace), separate files. Chosen by
user 2026-08-30. Supersedes the "thin wrapper / delete" framing in FUP-015 (both of
whose premises were false — see that item's CORRECTION).

* Target layout

#+begin_src
priv/veritas.bl            core (exists/for-all/covers/faults, Gen, check, modal verdict) — EXISTS
priv/veritas/mock.bl       ns veritas.mock     — synth-seeded server + synth-boundary (z3 optimize)
priv/veritas/fault.bl      ns veritas.fault    — the datalog fault-space ANALYSIS layer
priv/veritas/property.bl   ns veritas.property — hypothesis (output property + shrink)
#+end_src

Old files priv/mock.bl, priv/mock/fault.bl, priv/fuzz.bl are REMOVED once their
capability + demos + tests are re-pointed at the new family. No parallel impls left.

* What must be PRESERVED (veritas has no equivalent — verified)

- mock:        synth-seeded (deterministic keyed mock SERVER), synth-boundary (min/max via z3 optimize).
- mock.fault:  build-db + faults-per-clause + redundant-mutants + uncovered-clauses (not-join) +
               independent-laws; serve/error-for/errors (named fault injection). Violations MEASURED.
- fuzz:        hypothesis — call the target fn, check a predicate over its OUTPUT `result`, SHRINK.

* What is REDUNDANT (delete on cutover — the core, written 4×)

- veritas/exists  already ≡ mock/synth + synth-invalid + satisfiable?
- veritas/faults  already ≡ mock.fault/isolate + independent-laws (z3 half)
- veritas/covers  already ≡ fuzz/check + prove + lint
⟹ the new veritas.mock/fault/property call veritas core for these; they DON'T reimplement them.

* Contract: how each layer sits on the core

- A generator/contract seam. veritas core speaks {:q :gen :var :pred} and Gen records.
  The mock contract ({:fields :ensures}) must be expressible as a veritas query or a
  thin adapter. Decide during Phase 1: either (a) veritas.mock keeps the {:fields :ensures}
  contract shape and lowers to raw z3 like today but REUSES veritas/parse-model + timeout +
  smt seam, or (b) contract becomes sugar over Gen. Prefer (a) for synth-seeded/boundary
  since they need z3 optimize + field pinning that Gen does not currently model.
- veritas.fault builds on veritas/faults for the z3 half (isolated mutants + implied laws
  with modality), then adds the datom db + queries as its ANALYSIS half. serve/error-for
  build on veritas.mock's server.
- veritas.property builds on veritas generators (int-of/mixed via any-of/pairs via tuple-of)
  for the domain, adds the call-the-fn + output-predicate + shrink machinery.

* Phases

** Phase 1 — veritas.mock (priv/veritas/mock.bl, ns veritas.mock)
- Port synth / synth-seeded / synth-boundary / synth-invalid / satisfiable? / check /
  contract-from. REUSE veritas/parse-model (already moved in), veritas timeout, smt seam.
- Public server: make-mock (contract + seed-field) → deterministic input→response.
- Acceptance: examples/veritas/mock demo green; test/bl/veritas/mock_test.bl covers
  synth-seeded determinism (same key → same value) + synth-boundary (min/max) + synth-invalid.

** Phase 2 — veritas.fault (priv/veritas/fault.bl, ns veritas.fault)
- Build on veritas/faults for isolated mutants (modality-tagged). Add SCHEMA + build-db +
  MEASURED violated-clauses + uncovered-clauses/faults-per-clause/redundant-mutants/
  independent-laws. Add serve/error-for/errors on veritas.mock server.
- Acceptance: examples/veritas/fault demo green (redundant law PROVEN implied; a coverage
  gap found by not-join); test/bl/veritas/fault_test.bl covers redundant-mutants grouping +
  uncovered-clauses + error-for named injection.

** Phase 3 — veritas.property (priv/veritas/property.bl, ns veritas.property)
- Port hypothesis + shrink + holds-at? (call target fn, bind `result`, check predicate).
  Domains come from veritas generators (any-of ≡ mixed, tuple-of ≡ pairs).
- Acceptance: examples/veritas/property demo green (a refuted property shrinks to minimal
  witness; a crash reported as a first-class outcome); test/bl/veritas/property_test.bl
  covers hold / refute+shrink / crash.

** Phase 4 — cutover + docs
- Delete priv/mock.bl, priv/mock/fault.bl, priv/fuzz.bl and their examples/{mock,fuzz}.
  Re-point any demo worth keeping to the veritas.* family.
- Update docs/one-verb-over-generators.md "Relationship" + "Where it lives" sections to the
  new family; fold docs/a-contract-is-a-mock.md into it or point it at veritas.mock.
- Global gate: mix beam_lisp.test (whole suite) green; every veritas.* demo ends :ok.

* Non-goals
- No behaviour change to the CORE (exists/for-all/covers/faults keep their modal contract).
- No new theories or engines — this is consolidation, not expansion.

* Global acceptance
- No quantifier-core logic in more than one place (grep: one definition each).
- synth-seeded, synth-boundary, the datalog fault analysis, serve/error-for, and hypothesis
  all run AND are covered by test/bl suites (not just demos).
- mix beam_lisp.test green end to end; all examples/veritas/* end :ok.
- Old ns mock / mock.fault / fuzz gone; no dangling :require to them anywhere.

* Notes / gotchas (from prior sessions)
- Edit tool WORKS this session (verified) — use it; python3 heredoc no longer needed.
- Every z3 call carries (set-option :timeout 3000). Nonlinear int UNSAT can hang.
- env/eval leaks caller ns if given (ns X) → use bare lambda application ((fn [v] pred) v).
- lit: quote strings explicitly, pr-str keywords (str drops colon), (quote …) collections.
- datom: every tx entity needs :db/id; not-join is [:not-join [?v] …] (bracket-keyword).
- MODALITY DISCIPLINE: never :proven from a sampled run (only z3 or finite exhaustion).
- Run demo: mix beam_lisp.run --path priv examples/…/NN-*.bl ; clean end = :ok.
- Related: PLAN-064 (veritas core, DONE), PLAN-062 (stateful mocks → target veritas.mock),
  FUP-015 (this supersedes its framing).

NOTE [2026-08-31]: All 4 phases shipped + green. The veritas.* family:
- priv/veritas.bl (core, +samples helper, parse-model owned) — 11 tests/24 assert
- priv/veritas/mock.bl (ns veritas.mock): synth-seeded server (make-mock/answer), synth-boundary optimize, synth-invalid, contract-from, contract-decls — 6 tests/18 assert
- priv/veritas/fault.bl (ns veritas.fault): datalog analysis (build-db, uncovered-clauses not-join, faults-per-clause, redundant-mutants, independent-laws), serve/error-for on the mock server, isolate with modality — 6 tests/21 assert
- priv/veritas/property.bl (ns veritas.property): hypothesis + shrink over a fn's OUTPUT, domain from veritas generators via samples — 7 tests/13 assert
Total: 30 tests / 76 assertions / 0 failures. 6 examples/veritas demos end :ok.
DELETED: priv/mock.bl, priv/mock/fault.bl, priv/fuzz.bl + examples/mock/*, examples/fuzz/* (empty dirs removed). No dangling requires. Docs re-pointed (one-verb-over-generators.md family section; a-contract-is-a-mock.md renamed to veritas.mock/veritas.fault + fixed make-server 2-step sig).
One quantifier core; each layer adds only what the core cannot express. Zero capability lost.
NB: whole-tree mix beam_lisp.test is blocked by a PRE-EXISTING semantic.shape load failure in test/bl/datom/shape_test.bl (verified reproduces in isolation, unrelated to this cutover).