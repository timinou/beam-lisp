#+TITLE: Wave 21: optics — lenses in beam-lisp, and code-as-data rewriting
#+STATE: DONE
#+SESSION_ID: 154d7065511fb2de
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-08-06T19-28-19-268Z_154d7065511fb2de.jsonl]]
#+CUSTOM_ID: PLAN-013-wave-21-optics-lenses-in-beam-lisp-and-c
#+COMPLETION_HASH: d0b865762fa327ee

* Initial Message

ok keep going. let's start with some reflection. after wave 19, what would be next?

** Premise, verified by prototype
Two things were prototyped in beam-lisp *before* writing this plan, and both ran:

*** 1. Lenses need no language support at all
A lens is a getter/setter pair. Full van Laarhoven-style composition works in ~15 lines of beam-lisp with nothing but `fn`, `get` and `assoc`:
: (defn lens [getter setter] {:get getter :set setter})
: (defn view [l s] ((:get l) s))
: (defn over [l f s] ((:set l) s (f ((:get l) s))))
: (defn *> [a b] (lens (fn [s] (view b (view a s)))
:                      (fn [s v] (setv a (over b (fn [_] v) (view a s)) s))))
Composed three deep over `{:app {:users [{...} {...}]}}` this correctly viewed, updated (`:hits` 7 → 8) and left the original untouched. **∴ optics ship as `priv/optics.bl`, in beam-lisp — not as an Elixir module.** This is the project's stated philosophy (as much of beam-lisp in beam-lisp as possible) and here it costs nothing: no compiler change, no new special form.

*** 2. Code-as-data rewriting works today
A macro that walks its own argument form and rewrites it:
: (defn walk [f form] (if (list? form) (f (map (fn [x] (walk f x)) form)) (f form)))
: (defmacro rewritten [f] (walk plus->times f))
: (rewritten (+ 2 (+ 3 4)))  ; => 24
Every `+` became `*`, at every depth. **∴ "lenses for automated bug resolution" is a real capability, not a metaphor** — a codemod is a lens whose target is a form.

** What this wave builds
*** `priv/optics.bl` — the optics library
`lens`, `view`, `setv`, `over`, composition, and the primitive optics: `in` (map key), `idx` (vector index), `fst`/`snd`. Then the ones that earn their keep:
- `traversed` — an optic focusing *every* element, so `(over (*> (in :users) traversed (in :hits)) inc data)` increments every user's hits in one expression. This is where lenses beat `update-in`, which cannot express "all of them".
- `filtered pred` — focus only matching elements.
- `optional`/`maybe` — a lens that no-ops on a missing key instead of creating nil holes.
Prefer defining the derived optics *in terms of* the primitives; if a definition needs an Elixir escape, that is a finding worth recording.

*** `priv/rewrite.bl` — code-as-data on top of the same abstraction
`walk`/`prewalk`/`postwalk` (Clojure's `clojure.walk` names, so knowledge transfers), then `match?`/`subst` for pattern-directed rewriting, and `defrule` sugar:
: (defrule nil-punning (= ~x nil) => (nil? ~x))
A rule is data, so a ruleset is a collection, so rulesets compose. This is the honest form of "automated bug resolution": **mechanical, reviewable rewrites** — not a model guessing at a fix.

** Design decisions — DECIDED, with reasons
- *Optics in beam-lisp, not Elixir.* Proven to cost nothing; the philosophy pays here.
- *Lens = map `{:get :set}`, not a record or protocol.* Keeps it inspectable data, and `deftype` was deliberately skipped in wave 11 anyway.
- *`clojure.walk` naming.* Do not invent names where Clojure has them.
- *No `swap!`-style mutation API.* Optics stay pure; refs already own mutation.

** DECIDED by user: composition is outside-in
`(*> outer inner)` reads as a path — `:app` then `:users` — matching `get-in` and Specter. Settled, because reversing it later would silently change the meaning of existing code rather than break it loudly.

** DECIDED by user: Specter is the north star
The user's steer: *ideally make Clojure's Specter run as-is*; if that is not straightforward, ship the custom lens library first and defer vendoring Specter.

This is the fidelity programme pointed at a library instead of a stdlib, and it is the right instinct — running someone else's real code has found nine bugs so far. Sequence it that way deliberately:
1. Ship `priv/optics.bl` (below) — it is small, it works, and it makes the concepts real.
2. THEN attempt Specter as a measured fidelity target, same method as jank: vendor verbatim with a sha256, load, then CALL with upstream's own examples, classify every failure.
Specter is a serious target — it leans on protocols, `deftype`, rich metadata, inline caching and macro-heavy path compilation. `deftype`/`defrecord` were deliberately skipped in wave 11, so expect that to be the first wall. Treat a low initial score as information, exactly as wave 16 did.

** Acceptance
- `examples/optics.bl` runs: a nested update, a `traversed` bulk update, and a side-by-side with `update-in` showing what optics add.
- `examples/rewrite.bl` runs a `defrule` codemod over a quoted form and prints before/after.
- Optics tests in `test/bl/optics_test.bl` — **written in beam-lisp**, using the self-hosted test framework; that suite exists precisely for this.
- Lens laws asserted as tests: view-after-set, set-after-view, set-after-set. These are the definition of a lens; a "lens" that fails them is a pair of functions with pretensions.
- `mix test` green, `mix beam_lisp.test` exit 0, all examples exit 0, `--warnings-as-errors` clean.

** Note
Prototyping this already found bug #9: `(str {:a 1})` raised `Protocol.UndefinedError` because `to_str/1` fell through to `Kernel.to_string/1`, which no BEAM collection implements. Fixed in `81ed814` by delegating to the existing `print_str/1`. That is the pattern worth repeating — **writing real programs in the language finds what tests for the language do not**.

NOTE [2026-08-07]: DONE. Both libraries shipped in beam-lisp, and Specter was measured twice.

SHIPPED
- `priv/optics.bl` — lenses and traversals, written in beam-lisp, **no compiler
  change needed**. Representation generalized from the prototype's `{:get :set}`
  to `{:view :over}`, because a traversal has many foci and a getter cannot say
  that. Primitives `in`/`idx`/`fst`/`snd`, variadic outside-in `*>`, plus the
  optics that earn their keep: `traversed`, `filtered`, `optional`.
- **Lens laws asserted on every primitive** (get-put, put-get, put-put). A
  "lens" that fails them is a pair of functions with pretensions.
- `priv/rewrite.bl` — clojure.walk names, `?x` pattern variables that unify,
  rules as ordinary data so rulesets compose by `concat`, and a **bounded**
  fixpoint: two rules that undo each other raise `{:non-terminating true}`
  rather than hanging the build.
- `test/bl/optics_test.bl` and `test/bl/rewrite_test.bl` — in beam-lisp, on the
  self-hosted framework, which is what that framework exists for.
- `examples/optics.bl`, `examples/rewrite.bl`.

USER DECISION HONOURED: composition is outside-in. `(*> outer inner)` reads as
a path, matching `get-in` and Specter. Stated explicitly in the source so it
cannot be flipped later — reversing it would silently change the meaning of
existing code rather than break it loudly.

SPECTER — measured, per the user's steer, as a fidelity target
31 slices vendored verbatim from commit 6119462, Apache-2.0, sha256-guarded.

  first measurement:  load 11/31, behave  1/31
  after gap-closing:  load 23/31, behave  4/31

**The load/behave gap IS the finding.** Building the syntax the first ranking
named (defn-, reify, identical?, defprotocol docstrings, `#?` conditionals,
deftype/defrecord) doubled the load score and moved behaviour by three. That
says Specter's remaining distance is not special forms but its own impl
machinery — `i/NONE`, compiled path caching, mutable cells, RichNavigator
internals — which is a different and larger kind of work. Knowing that before
starting it is worth the whole exercise.

Prediction-vs-outcome, checked honestly:
- `defprotocol` docstring: fully materialised (both slices load AND behave).
- `defn-`: unlocked all 7 slices it promised — but at LOAD only, 1 of 7 behaves.
- `reify`: did NOT materialise. The navigator slices were blocked one level
  earlier, at a quoted-symbol map-destructure key. The ranking had
  mis-attributed the cause.
- `identical?`: necessary, not sufficient.

BUG FOUND — worth more than the score: an unresolved qualified name compiled to
a **usable value**. Because a lowercase prefix means Erlang interop, `i/NONE`
with nothing aliasing `i` produced a handle to a module that was never loaded —
comparing unequal to everything, exploding much later blaming a module the
author never wrote. Every NONE sentinel check silently returned false. Now
raises at the reference. Found only because we ran someone else's real code.

Also found while prototyping: bug #9, `(str {:a 1})` raising
Protocol.UndefinedError — fixed in 81ed814.

REMAINING (recorded, not hidden): Specter is 4 of 31. `docs/specter-compat.md`
ranks what would move it, led by the quoted-symbol map-destructure key (5
slices) and the missing core prims (~10 slices).