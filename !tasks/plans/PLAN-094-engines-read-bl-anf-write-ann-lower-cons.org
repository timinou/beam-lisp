#+TITLE: Engines read bl-ANF, write :ann, lower consumes :ann
#+STATE: ITEM
#+SESSION_ID: 15729b4fce888940
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-04T16-19-37-402Z_15729b4fce888940.jsonl]]
#+CUSTOM_ID: PLAN-094-engines-read-bl-anf-write-ann-lower-cons

* Initial Message

Please, let's imagine what this work can unlock! Go back to the documentation, read beam lisp a bit, then come back to me with your insights/realisations

* Goal

One meaning for a program: the proof engines under priv/lib/system/ (and
priv/std/typed.bl, priv/std/termination.bl) read the bl-ANF the backend emits,
write their findings into each node's :ann slot, and lower.bl consumes those
annotations for proof-directed emission. Today the engines re-implement a
fraction of the compiler over surface reader-node trees; they are conservative
wherever a macro is involved and unsound in a few local-naming cases.

* Evidence (prototype, research/anf-engines/)

- research/anf-engines/fp-anf.bl: system.footprint over bl-ANF, same output
  contract. Differential over 28 forms: 16 agree, 9 surface :opaque-world →
  ANF precise (all macro forms: -> ->> doto if-let as-> some-> cond-> for),
  3 ANF more sound (locals are not resources; aliases resolve; shadowing).
- Cost: surface walk ~105 µs, compile ~210 µs, ANF walk ~250 µs (one 6-line
  body). z3-dominated consumers unaffected.
- research/anf-engines/cost_and_model.bl: :ann with {:footprint :rung}
  survives anf/module validation, descriptor->beam, load, run (returns 42).
  lower.bl reads only :ann :line/:file — zero backend change needed to write.
- Gate hit and respected: annotate must produce eager lists (FEAT-039).
- Full write-up: research/anf-engines/README.md.

* Non-goals

- No change to the seventeen bl-ANF ops or descriptor version.
- No new proof theories. Same facts, computed from the IR.
- No native offload changes; PLAN-087 SIMD stays independent.

* Contract

** Engine entry points keep their signatures

Every consumer today hands a reader node (typed/parse-clause, read1) or a
source string. Entry points keep accepting those; internally they compile to
ANF via compiler/compile-node with a scratch env and walk the ANF. New
ANF-native entry points are added alongside (fp-anf, model/from-descriptor)
for callers already holding IR (the compiler pipeline, dev.change verify).
∴ ~60 consumer files need no signature change.

** Answers change only in the documented direction

More precise (macro forms no longer :opaque-world) or more sound (locals are
not named resources). Any test asserting :opaque-world for a threaded or
macro form asserts a limitation; updating it is the acceptance criterion.

** :ann keys owned by engines

:footprint {resource → mode} · :rung · :terminates {:measure :dir} ·
:type sort · :bound closed-form heap bound. Engines only add keys; :line/:file
remain the compiler's. Values must be finite eager data (anf/module refuses
LazySeq).

** lower.bl consumption is opt-in per key

lower-anf reads :ann and may choose emission (e.g. unboxed arithmetic when
:type ⊆ Int with :bound). Absence of a key = today's emission. Never a
correctness dependency on an annotation; a wrong proof is a catastrophe, a
missing proof is the ordinary path.

** Alias environment

ANF names every intermediate. Engines carry {local → aliased global};
computed locals are :opaque-world; :case/:recv pattern vars and let
rebinding shadow. Reference implementation: fp-anf.bl.

* Steps

1. Engine harness: research/anf-engines → priv/lib/system/anf.bl helper ns
   (alias env, eager node walker, annotate). Tests: differential corpus from
   the prototype as a frozen fixture; annotate output validates via anf/module.
2. system.footprint over ANF. Keep fp-walk signature (compiles internally);
   add fp-anf. Key op-mode by [mod fun] instead of strings. Update tests that
   assert :opaque-world for macro forms. Consumers: effects.bl op-rung,
   reload/ward capability checks, research/lowerers_spike lower2.
3. termination over ANF. measure-dir / upper-bounded-vars over :let/:if/:remote
   erlang :< etc. and :apply of the loop fn. Keep check-loop signature.
4. system.model from descriptor. extract-defserver / extract-defn /
   extract-defmethods become projections over :defn-clause maps
   ({:label :pattern :guard :next}). next-state matches ANF reply/recur/
   noreply/ok shape (RT.invoke of the global, or the server-body ctor local).
   Expose compile-defserver's descriptor (currently private) through a
   compiler API that returns the descriptor without publishing.
5. system.smt/translate over ANF. veritas properties compile first, then
   translate; translate-len over :remote RT count. Widest consumer — run
   test/bl/system/smt_*, smt_parity, veritas suites.
6. typed over ANF. Theory selection from :lit/:map/:pstruct/:case patterns
   instead of surface shapes. docs/typing/sum-types-are-shapes examples are
   the acceptance corpus.
7. Compiler pipeline hook: after compile-node/compile-defserver, run engines
   that are cheap and total (footprint, termination) and write :ann. Gate by
   env flag first (BEAM_LISP_PROOFS=on), measure build time on the 141-source
   uncached build, then default on if < 10% cost.
8. lower.bl consumes :ann. First consumer: :footprint = {} + :terminates on a
   counted loop → tag the Core function with a proven attribute the native
   gate reads (docs/memory-policy/05). Second: :type Int + :bound → erlang
   arithmetic without RT dispatch.
9. codebase.bl :call/* facts from ANF :remote/:apply/:global instead of
   surface scanning (calls through macros appear). Impact/reachability tests.
10. Delete surface walkers in the engines once every consumer's tests pass
    on ANF. No parallel implementations survive.
11. Docs: docs/from-source-to-silicon/06 claims engines write :ann — make it
    true and show a runnable cell. Update memory-policy README engine column.

* Dependencies / order

1 → 2 → {3, 4} → 5 → 6 → 7 → 8; 9 independent after 1; 10 after all; 11 last.

* Tradeoffs recorded

- Engine tier depends on compiler (seeded; safe) instead of reader only.
- Post-macro: engines cannot see which macro was used. Nothing wants that.
- Resource keys may become ns-qualified; changes frame-independent? across
  namespaces. Decide in step 2; default keep author names for parity, add
  ns-qualification as a separate opt-in.
- ~5× walk cost when compiling on demand; microseconds.

* Verification

- Per step: the engine's own test/bl/system/* suite + differential fixture.
- Steps 7–8: full suite (1487 baseline), uncached build time delta, seed
  reproduction byte-identical when proofs are off; documented delta when on.
- research gate (census/oracle) unchanged: interp ignores :ann.
