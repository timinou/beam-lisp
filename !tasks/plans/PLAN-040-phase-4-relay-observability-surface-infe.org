#+TITLE: Phase 4 — Relay observability surface (inference-id, feedback, export, admin, rate-limit)
#+STATE: DOING
#+SESSION_ID: 15660dc6528e6c24
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-08-25T22-19-27-946Z_15660dc6528e6c24.jsonl]]
#+CUSTOM_ID: PLAN-040-phase-4-relay-observability-surface-infe

* Initial Message

go for it autonomously until the end. Set up the right todo tool and remember you have unlimited budget

Parent: PLAN-040 Session B, Phase 4. Builds on committed base (gate/keys/store/http/spend).

## Scope (5 pieces)
1. INFERENCE ID on every completion. store.bl already has :inference/{id,model,provider,key,status,at,
   feedback,rating} (id unique-identity). On each completed request (stream + non-stream), assert one
   :inference datom with a generated stable id, and surface it: response body already has "id"; ALSO
   return header x-relay-inference-id and store the SAME bare id on the datom so feedback/replay join.
2. FEEDBACK endpoint. POST /v1/feedback {inference_id, rating?, comment?} → upsert :inference/feedback
   + :inference/rating by :inference/id unique identity. Gate-auth; a key may only feed back its own
   inferences (:inference/key = key id, else 404 not 403). Returns {ok true}.
3. JSONL EXPORT. GET /admin/logs.jsonl?since=&key= → LAZY stream (Stream/*, eager/lazy trap) of spend
   +inference datoms as JSONL, one object/line. Admin-token gated. Reuse the lazy body the bridge writes.
4. KEY ADMIN. GET /admin/keys (list w/ live spend+limit+hit-rate), GET /admin/keys/:id, DELETE
   /admin/keys/:id (revoke exists). Admin-token gated.
5. RATE LIMITING per key. Token-bucket/fixed-window per key id, checked in gate AFTER budget. 429
   rate_limit_exceeded. In-memory (Agent/ETS) — durable facts are the store's job, not per-second counters.

## Contracts
- Inference id: SAME bare string in response "id" (prefix chatcmpl- only in the OpenAI body),
  x-relay-inference-id header, and :inference/id datom. Join key = bare id.
- Feedback upsert relies on :inference/id :db.unique/identity (in schema) — same-id tx updates.
- Export lazy end-to-end; 100k rows O(1) memory.
- Rate limit per-key, in-memory, window-reset; restart resets (acceptable).

## Acceptance
- Completion returns x-relay-inference-id; :inference datom exists, status "ok".
- POST /v1/feedback records rating+comment on the same entity; wrong key → 404.
- GET /admin/logs.jsonl streams valid JSONL lazily (body is a lazy seq).
- GET /admin/keys lists keys w/ spend+hit-rate; DELETE revokes.
- N+1 rapid requests under a rate-limited key → 429.
- Full suite green.
