#+TITLE: P23: the engine closing on itself — native datom greatest-fixpoint
#+STATE: ITEM
#+SESSION_ID: 15699e91a40f236f
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp--type-inference/2026-08-28T16-47-31-472Z_15699e91a40f236f.jsonl]]
#+CUSTOM_ID: PLAN-055-p23-the-engine-closing-on-itself-native

* Initial Message

ok my friend, can you tell me more about this?

The honest remaining depth — z3-sequence content invariants beyond length, a richer discovery vocabulary, multi-collection capacity — is depth on a shipped foundation, not a missing floor.


From zero context in simple language. FOld in more stuff if it needs to be talked about

GOAL: pull the greatest-fixpoint + z3-aggregation INTO datom natively, so EVERY fixpoint — least (reachability), greatest (Houdini, CTL AG), mixed (liveness) — is one engine with one optimizer, one provenance trail, one incrementality story.

CURRENT STATE: system.gfp (P19 R2, shipped) is a bl DRIVER beside datom. It hosts Houdini/CTL/bisimulation but does NOT get datom's semi-naive optimizer, magic-sets, native Rust kernel, or since/watch incrementality. The spike (spike_gfp.bl) proved the shape; spike_fixpoint_z3.bl proved datom's derive-semi-naive CAN call z3 mid-recursion (Q1✓) but the precise Houdini is a GREATEST fixpoint with recursion-through-negation (Q3✗ for least-fixpoint datom).

DESIGN (three layers, cheap→deep):
- Layer B: a `gfp` rule MODE in datom.query.rules — dualize derive-semi-naive (seed FULL, monotone kill, downward delta). Pure relational, validated vs system.gfp/safe-region + bisimulation closed forms. Independently valuable (CTL model-checking + minimization) before Houdini touches it.
- Layer C: recursive monotone AGGREGATION — a rule folding the current relation → one z3 conjunction mid-fixpoint (what strong Houdini needs). Principled home: Datalog-with-lattices/semirings (Flix, Datafun, Datalog°), Van Gelder alternating fixpoint / well-founded semantics.
- Then discover-invariant's Houdini loop moves INTO the engine.

THE PAYOFF (the reason, beyond "Houdini works" which it already does):
- VERIFICATION AS A SUBSCRIPTION: datom has since/watch/time-travel. A fixpoint IN the engine ⇒ re-checking an invariant after a code edit only recomputes the AFFECTED slice — not from scratch. SOUNDNESS BOUNDARY (from P19 R2 spike): warm-start is sound only when the edit SHRINKS the fixpoint (stricter spec/more bad states); a growing edit must re-seed.
- PROVENANCE for free: datom tracks why each tuple exists → a discovered invariant carries its derivation (which transition forced which conjunct).
- ONE optimizer accelerates ALL fixpoints.
- fixpoints stop LEAKING out of datom — the engine-purity argument that motivated the whole refactor.

VERIFY: gfp rule-mode reproduces system.gfp results; incremental re-verify does provably less work on a shrinking edit (measure checks, like gfp-delta); Houdini-in-engine matches the bl loop. Graduate as tests + demos + a real engine RFC. This is the biggest single project; Layer B is the honest first PR.
