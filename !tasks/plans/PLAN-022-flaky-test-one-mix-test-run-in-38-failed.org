#+TITLE: Flaky test: one `mix test` run in 38 failed, not reproduced
#+STATE: ITEM
#+SESSION_ID: 155610dbcb70f8c8
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-08-13T12-17-14-541Z_155610dbcb70f8c8.jsonl]]
#+CUSTOM_ID: PLAN-022-flaky-test-one-mix-test-run-in-38-failed

* Initial Message

Be honest, what else would you need to flag? Please answer honestly, this is a safe space. Do a list explaining what actually got done, what got deferred, and what's next, from zero context, including your confession

* Observed

One `mix test` run reported `Result: 989/990 passed · Failed: 1 test`. Every
subsequent run passed. Total evidence: **1 failure in 38 runs.**

The identity of the failing test was LOST: the capturing command piped through
`tail -3`, so only the summary line survived. The unfiltered artifact
(`artifact://155610dbcb70f8c8/main/bash/1716.txt`) contains the same truncation —
the detail was discarded before it was ever written.

** Reproduction attempts (all green)
- 3x `mix test --seed N` (1117 / 2234 / 3351)
- 6x bare `mix test`
- 3x `mix beam_lisp.test` immediately followed by `mix test` (the exact sequence
  that produced the failure, in one shell, same working dir)
- 12x the four timing-sensitive suites together
  (wave22_otp, wave23_codeversion, wave6_errors, wave7_refs)
- 12x the three shell-out suites together
  (st_edn, view_roundtrip, wave9_aot)

** Candidates, by mechanism
The suites that CAN be nondeterministic, from a scan of the tree:

1. *Timing* — 23 `Process.sleep` / `assert_receive` sites, all in
   wave22_otp_test.exs (14), wave23_codeversion_test.exs (3),
   wave6_errors_test.exs (4), wave7_refs_test.exs (2). A sleep-based
   assertion under load is the classic 1-in-N.
2. *Shell-out* — st_edn_test.exs, view_roundtrip_test.exs, wave9_aot_test.exs
   invoke the `spacetime` binary via `System.cmd`. If the binary is being
   rebuilt concurrently, or a temp path collides between async suites, these
   fail nondeterministically.
3. *Machine load* — the failing run happened while `cargo test --lib` (3214
   tests, ~200s, all cores) was running in the background from a prior command.
   That is the single strongest signal: it is the one run that had heavy CPU
   contention, which is exactly what turns a marginal `Process.sleep` into a
   failure.

Hypothesis (3) + (1) is most likely, and matches host notes about zram/CPU
contention on this machine. **Not confirmed** — no reproduction.

** Next
- Re-run `mix test` under deliberate CPU load
  (e.g. `stress-ng --cpu 16` or a concurrent `cargo test --lib`) — the condition
  present during the only failure and absent from all 37 green runs.
- When it reproduces, replace the sleep-based assertion with a deterministic
  synchronisation primitive (monitor/receive, or polling with a deadline)
  rather than lengthening the sleep.
- Never pipe a test run through `tail` again — keep the full log, filter after.

** Note
This is filed as an honest known-unknown. The suite is NOT green-by-assertion:
it is green in 37 of 38 observed runs, and the one failure is unexplained.
