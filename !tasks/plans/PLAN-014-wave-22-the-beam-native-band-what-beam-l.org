#+TITLE: Wave 22: the BEAM-native band — what beam-lisp can do that no other Lisp can
#+STATE: DONE
#+SESSION_ID: 154d7065511fb2de
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-08-06T19-28-19-268Z_154d7065511fb2de.jsonl]]
#+CUSTOM_ID: PLAN-014-wave-22-the-beam-native-band-what-beam-l
#+COMPLETION_HASH: 7b9fc4f2e64f98e1

* Initial Message

ok keep going. let's start with some reflection. after wave 19, what would be next?

** Framing
Waves 1–19 answered "is this a real Clojure?" — 62 of 64 upstream slices say yes. This wave answers a better question: **what can a Lisp do when it is native to the BEAM that it cannot do anywhere else?** Clojure has the JVM, jank has LLVM. Neither has preemptive scheduling, supervision trees, or live code replacement. That is the differentiator, and it is currently unexploited: `examples/processes.bl` reaches OTP through raw interop and nothing more.

** Verified by prototype: live hot-swap already works
Redefining a fn while a process loops on it, with no restart:
: before: v1
:   running process sees: v1
:   running process sees: v1
: redefined mid-flight
:   running process sees: v2-HOTSWAPPED
:   running process sees: v2-HOTSWAPPED
The var-linking layer means a redefinition is visible to already-running processes. **∴ the hard part is done** — what is missing is the surface that makes it usable and safe.

** Build, in value order
*** 1. `defprocess` / `defserver` — OTP as a first-class form
A GenServer in beam-lisp today means hand-rolling interop. It should be a form:
: (defserver counter
:   (init [] 0)
:   (handle-call :inc [_ state] (reply (inc state) (inc state)))
:   (handle-cast :reset [_ state] (noreply 0)))
Compiles to a real GenServer module — real `:gen_server` callbacks, real supervision compatibility, inspectable by `:observer`. Non-negotiable: it must be a *real* OTP behaviour, not a lookalike, or supervisors and `:sys.get_state` won't work on it.

*** 2. `supervise` — supervision trees as data
: (supervise :one-for-one [(worker counter) (worker logger)])
A supervision tree is a nested data structure; Lisp is the language where that is literally true. Restart strategies as keywords.

*** 3. A REPL that attaches to a *running node*
The existing REPL evaluates locally. The BEAM's actual superpower is connecting to a production node and inspecting it live. `mix beam_lisp.repl --node app@host --cookie …`, evaluating in the remote node's context. This is the feature that makes people take a language seriously for operations.

*** 4. `trace` — built on `:dbg`, exposed as data
: (trace counter/handle-call (fn [call] (println call)))
The BEAM can trace any function call in a live system without instrumentation. Expose it, with the safety rails (`:dbg` can take down a production node under load — bound the match spec and the rate).

*** 5. Distribution: `(remote node (expensive-fn args))`
Compile a form, ship the module to another node, run it there. beam-lisp compiles to bytecode already, so this is mostly plumbing — and it is *code mobility*, the thing homoiconicity and the BEAM are jointly good at.

** Design decisions — DECIDED
- *Real OTP behaviours, never lookalikes.* If `:observer` and `Supervisor` don't recognise it, it doesn't ship.
- *Hot-swap stays explicit.* It works now as a consequence of var linking; wave 22 makes it a documented, tested guarantee with a story for in-flight state, not an emergent accident.
- *`:dbg` gets rails.* A tracing API that can kill a production node is a footgun, not a feature.

** DECIDED by user: faithful core now, ergonomics layered later
Build the faithful OTP mapping first — "mostly Elixiry, kinda Lisp": `handle-call`/`handle-cast`/`handle-info` with real `{:reply v state}` returns, covering `handle_continue`, timeouts and hibernation. Ergonomic macros come **on top of** that core once it is proven, never instead of it.

The ordering matters and must not be inverted: a pretty surface built first would fix the wrong semantics in place, and every advanced callback would then be a special case fighting the abstraction. A faithful core is a base other things compose onto; a lossy wrapper is a ceiling.

** Acceptance
- `examples/server.bl`: a supervised counter that survives a deliberate crash — printing state before, the crash, and the supervisor's restart.
- `examples/hotswap.bl`: a running process picking up a redefinition, as prototyped.
- A test asserting the generated module is a genuine GenServer (`:sys.get_state/1` works on it).
- Tracing demo bounded so it cannot run away.
- `mix test` green, `mix beam_lisp.test` exit 0, all examples exit 0, `--warnings-as-errors` clean.

** Why this ranks where it does
After wave 20 (source locations), because debugging a supervision tree without line numbers is worse than not having one. The fidelity programme proves beam-lisp *is* a Clojure; this wave is the argument for *why put a Clojure here at all*.

NOTE [2026-08-07]: DONE. beam-lisp now exploits the platform it runs on.

SHIPPED
- `defserver` → a GENUINE :gen_server. Generated module declares @behaviour :gen_server; :sys.get_state/1 works; a real Supervisor adopts it. Asserted with OTP's own tooling, never with beam-lisp's client fns — that distinction was the premise of the wave. Full return surface: replies, timeouts, :hibernate, {:continue, term}, {:stop, …}.
- Client side (`server-start-link`/`server-start`/`server-call`/`server-cast`/`server-stop`) — a server reachable only through raw interop would defeat the form.
- `supervise`/`worker` → a real Supervisor. :one-for-one, :one-for-all, :rest-for-one; :permanent/:transient/:temporary.
- Hot code replacement promoted from emergent accident to tested guarantee, with limits documented honestly: the next call runs new code; a process inside the old body finishes it; in-flight state does not migrate.
- Tracing over :dbg with MANDATORY rails (count cap, default 1000, enforced in the tracer process). Asserted, not trusted — :dbg can take down a production node.
- examples/server.bl, examples/supervision.bl, examples/hotswap.bl — all terminate, all exit 0.

USER DECISION HONOURED: faithful core first, ergonomics later. Every OTP return shape is expressible today. A prettier surface built first would have fixed lossy semantics in place and made handle_continue a special case fighting the abstraction.

BUGS FOUND (all by testing, none by reading)
1. Module.create/3 rejects a nil location — so EVERY defserver/defmulti/defprotocol compiled from a string (the REPL, eval_string without a path) crashed with FunctionClauseError. Wave 20 introduced it; wave 22 exposed it. Fixed with a real fallback location at all 3 sites.
2. `trace` resolved a name against Env.current_ns(), which is a process-global Agent that outlives any single evaluation — so tracing worked from a file (one file = one ns) and failed anywhere else. Now falls back to the ns that actually holds the var; added Env.namespaces/0.
3. `untrace` raised when nothing was traced, so a cleanup path could fail and mask the error that made cleanup necessary. Now idempotent.
4. :gen_server.start_link has no "no name" value — passing [] as a name is an ArgumentError. Anonymous servers must use the /3 arity.

MY OWN TEST WAS THE BUG, TWICE — worth recording, because both were the same shape: a test that reaches for a process-global. (a) The hot-swap test spawned a process calling eval_string in a loop, which fought every other test for current_ns. Fixed by calling the linked fn directly — which is what a real long-running process does anyway, and the path hot-swap must work on. (b) The :temporary test asserted the child lingers as :undefined; OTP drops the spec entirely.

GATES: mix compile --warnings-as-errors clean · mix test 530 passed · mix beam_lisp.test 0 failures · 28 examples all exit 0 · 57 jank slices still behave.

Commits: 75db330 (the band), README follow-up.

FOLLOW-UP: no code_change/3 story — a release upgrade with state migration is not attempted, and the docs say so rather than implying otherwise. `defserver` needs `(handle-info PAT [state] …)` (pattern-shaped) not `(handle-info [msg state] …)`; the error message names the file and line, which is how it was caught, but the asymmetry with `init`/`terminate` is a papercut worth revisiting when the ergonomic layer lands.