#+TITLE: Bundles build: steps 2–8 (verbs, fence, invariant, registry, bus, supervisor, capstone)
#+STATE: DONE
#+SESSION_ID: 156d56787b666447
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-08-31T14-06-26-797Z_156d56787b666447.jsonl]]
#+CUSTOM_ID: PLAN-085-bundles-build-steps-28-verbs-fence-invar

* Initial Message

ok explain the primitives and patterns that we are claiming represent the whole domain space, explain them and their relationships from zero context in simple language, and then each step and the .bl.md guidebook that will also be given to an audience new to beam lisp, for each step. Make sure you are up to date regarding the folder hierarchy, and then finish with your full in-chat report

Build the five bundles (server verbs, fence, registry, bus, supervisor) per `docs/the-five-bundles.md` §7. Each step: working code + green example + guidebook chapter `docs/guidebook/NN-topic.bl.md` + commit.

Steps:
2. Generic verbs (call/cast/start/start-link/stop prelude cutover from server-*)
3. fence — priv/std/fence.bl
4. defserver (invariant …) + system/verify
5. defregistry + (name …) + [:reg key] resolution
6. defbus on flow protocol + flow/broadcast
7. defsupervisor + child/pool + super/* + tree system/verify; supervise/worker as skin
8. examples/bundles/00-all-five.bl + ledger update

DAG: 2∥3 → 4 → {5,6} → 7 → 8.

* DONE — all eight steps landed and verified (2026-09-02)

- step 2 (7c9b19c): generic verbs, server-* cutover
- step 3 (8496156): fence bundle
- step 4: invariant clause + sys/verify from source; tree verify over defsupervisor; example 28 green (guards/account :ok, binder substitution, buggy-wallet caught, honest :unchecked)
- step 5: reg.bl defregistry + name-as-value resolution in the verbs; examples/registry.bl green
- step 6 (a9b8063): bus bundle
- step 7: super.bl defsupervisor + pools (MFA children — bare-fn children of gen_servers restart-loop, found empirically); examples/supervisor.bl green
- step 8: examples/bundles/00-all-five.bl green end to end; pattern ledger trued; guidebook chapters 01-08 all present

Deferred with honesty notes: (name …) self-naming clause (needs compiler.bl, blocked on PLAN-088); vitals cutovers to fence+registry (vitals is atom-local by design for its dashboard; cutover questionable value, left as-is); flow/distribute, flow/merge; ^pin reader form; Tick/Heartbeat/Snapshot stdlib fns.

Carry-over gotchas discovered this session: `match` does not exist (cond + contains? is the idiom); repeatedly is arity-1; the verify dialect wants scalar state or bare-field meta — `(:field s)` accessor style silently fails proof (holds=false, no warnings — empty-warning unsafe is a verifier UX gap worth its own plan); supervisor children of defservers must be MFA specs via BeamLisp.Supervisor/server, never bare fns.