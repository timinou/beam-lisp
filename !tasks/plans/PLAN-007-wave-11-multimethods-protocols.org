#+TITLE: Wave 11: multimethods & protocols
#+STATE: DONE
#+SESSION_ID: 154d7065511fb2de
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-08-06T19-28-19-268Z_154d7065511fb2de.jsonl]]
#+CUSTOM_ID: PLAN-007-wave-11-multimethods-protocols
#+COMPLETION_HASH: e2bad061c1c57822

* Initial Message

what's next? what's left? what else could be done?

Clojure's polymorphism, BEAM-flavored.

Multimethods first (simpler): `defmulti` (name dispatch-fn), `defmethod` (name dispatch-val [args] body). Registry: ETS {:multi, ns, name} → %{dispatch_fn, methods: %{dispatch_val => fn}, default: fn|nil}. Call: apply dispatch-fn to args → lookup method → apply. `derive`/isa? hierarchy: defer unless cheap (parents ETS table).

Protocols (bigger): `defprotocol` + `extend-protocol`/`extend-type`. Two implementation routes: (a) Elixir Protocols under the hood (dispatch on struct — but beam-lisp fns aren't structs; need a wrapper type or any→Any impl with per-type fn map lookup), (b) pure ETS dispatch table keyed by (protocol, type-tag) — slower but uniform and redefinable at runtime, matching beam-lisp's dynamic var model. Route (b) fits the runtime-redefinition story; route (a) fits AOT (wave 9). Decide at pickup; prototype (b).

deftype/defrecord: likely out of scope — Elixir structs already cover the need via interop; document the position instead.

Acceptance: examples/multimethods.bl (area of shapes with defmulti/defmethod + :default), tests including method redefinition; protocol PoC only if multimethods land cleanly.

NOTE [2026-08-07]: DONE — commit 2dd5505. Both halves landed, not just multimethods. defmulti/defmethod/defprotocol/extend-type/extend-protocol are COMPILER SPECIAL FORMS (chosen over a prelude library so they intern into the current ns and need no load), backed by BeamLisp.Multi with all state in the existing :beam_lisp_vars ETS: {:multi,ns,name}, {:hierarchy,ns}, {{:protocol,ns,name,method},type_tag}. A multimethod is an ordinary var holding {:"$blfn", %{}, {0, fn}} — min-0 variadic, because a fixed-arity closure would receive only ONE arg through RT.invoke's apply/2 spread. Dispatch order: exact key → isa? hierarchy → :default → error naming multi + value. Dispatch keys normalize vectors to {:"bl-vector", to_list} in both add_method and dispatch so structurally-equal vectors (incl. the ≤32 raw-tuple form) hit one entry. defmulti redefinition follows CLJ-1351 (MultiFn reused, methods survive, dispatch fn swaps) — verified against modern Clojure rather than assumed. Hierarchies (derive/isa?/parents/ancestors/underive) came free and are wired into dispatch. Protocols use route (b), the ETS table keyed by (protocol, type_tag) from one central Multi.type_of/1 — Elixir Protocols were rejected because beam-lisp fn values are not structs. deftype/defrecord deliberately NOT done: Elixir structs cover user types via interop. priv/multi.bl loads at startup after core.bl (lib/beam_lisp.ex), so the prelude is layered — core.bl is the language, multi.bl the dispatch library. 18 tests; examples/multimethods.bl + protocols.bl. AOT caveat: capture_value_def only recognizes plain def, so multi/protocol registrations are not persisted into __bl_init__/0 — files compile fine, but the defs must be re-evaluated at runtime.