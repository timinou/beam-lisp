#+TITLE: FUP: retire compiler genesis — driver→.bl, staged seed, then delete
#+STATE: ITEM
#+SESSION_ID: 156e98a98c8113c8
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp--self-hosted/2026-09-01T13-34-07-410Z_156e98a98c8113c8.jsonl]]
#+CUSTOM_ID: PLAN-082-fup-retire-compiler-genesis-driver-bl-st

* Initial Message

Go for PLAN-081 thoroughly and rigourously

Follow-up to PLAN-081. The Core Erlang BACKEND is done and default (steps
1/3/4): compiler.bl → bl-ANF → Core → .beam, proven reproducible, behaviourally
identical, genesis-independent via a Core-built seed. Two pieces remain, both
larger than a cutover step.

=== A. S-D.emit — compiler.bl emits bl-ANF directly (OPTIMISATION) ===
Rewrite compiler.bl's 7 emit constructors (ast-node, ast-alias, ast-dot,
ast-mod-call, ast-fetch, ast-invoke, ast-remote-fun) to build bl-ANF nodes
directly instead of Elixir-quoted, retiring self.anf/normalise (the quoted→ANF
bridge) and the quoted `lower` path in self.core. compiler.bl is 3731 lines and
byte-parity-locked under oracle.bl; the constructors thread through hundreds of
call sites. Its own sequenced sub-plan, oracle-gated at every step. NOT a
0-Elixir gate — the normalise bridge already lets Core consume compiler.bl
output today; this only removes one data-translation (a perf/cleanliness win).

=== B. S-E — retire compiler genesis (CONSEQUENTIAL, staged) ===
"Delete reader.ex/compiler.ex" is not literally achievable: both files are ~60%
essential DRIVER/API + the delegation seam (new_env, eval_string, eval_form,
compile/2, macroexpand_1, prelink_defn, read_data, reset_fresh!, read_one/all)
that every caller and the runtime depend on. Only the genesis LOWERING
(compile_elixir + compile_special/*, read_string_elixir) is genesis, and it also
serves as the bootstrap-of-last-resort and the differential oracle.

Hard prerequisites before deleting genesis lowering:
  1. compiler.bl frontend stable (few/no edits) — because a compiler.bl edit
     invalidates the committed seed's compiler_key, and TODAY only genesis can
     rebuild the new seed. This is the bootstrap ladder.
  2. A Core-only staged-seed-rebuild path: build seed N+1 from seed N using the
     Core backend itself (no genesis), so R3 (regrow from a clean clone) holds
     without Elixir. Needs tooling + a "compile the compiler with the previous
     compiler" harness.
  3. A soak period with the oracle (oracle.bl) active, to catch any Core-backend
     regression against genesis before removing the yardstick.
  4. For the literal file deletion: migrate the compiler DRIVER/API (the ~60%
     that is not genesis) to .bl and rewire every caller — a project of its own
     (Role-A full self-host), separate from the backend cutover.

Until then, dormant genesis costs nothing and is the recovery floor. Deleting it
early trades robustness for a purity the API-shell Elixir does not yet grant
(eval_string et al. stay Elixir regardless).

=== Also tracked ===
Drop/escript packaging under :core not yet validated (mix bl.build chains mix
compile → mix release → cargo → drop pack; the Core beams boot in dev, release
packaging untested). Role-B runtime-call Elixir (rt/vector/env/link) stays until
PLAN-057 P10/P11 — the last Elixir, a separate benchmark-gated decision.

See PLAN-081 execution log for the full evidence chain and commit list.

* SUPERSEDED IN PART BY PLAN-083 (2026-09-04)

Section B (S-E — retire compiler genesis) is DONE via PLAN-083's hard cutover:
compiler.ex/reader.ex genesis deleted (now ~230-line facades), oracle retired,
a Core-Erlang boot-tier seed + staging ladder replace genesis as the bootstrap
floor. My earlier prerequisites here were resolved rather than waited on: the
seed IS bootstrappable on its own (proven with genesis sabotaged), so no soak
period was needed.

Section A (S-D.emit — compiler.bl emits bl-ANF directly, retire the normalise
bridge) REMAINS open and unchanged: still an optimisation, not a correctness
gate — Core consumes compiler.bl output through the quoted→ANF bridge today.
This is the only remaining item of this FUP.