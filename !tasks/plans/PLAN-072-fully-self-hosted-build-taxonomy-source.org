#+TITLE: Fully self-hosted build: taxonomy, source-graph.bl, O(V+E) gate, interface hash, .bl build driver, bl daemon
#+STATE: DOING
#+SESSION_ID: 156e98a98c8113c8
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp--self-hosted/2026-09-01T13-34-07-410Z_156e98a98c8113c8.jsonl]]
#+CUSTOM_ID: PLAN-072-fully-self-hosted-build-taxonomy-source

* Initial Message

My friend, you are now in a worktree that lets you experiment with this 'fully self-hosted' thing. Look at the modules that can help you achieve this goal. Show me how you'll go about this change.

The goals:
1. Faster compilation (SourceGraph, O() guarantee, macro-typed edges and maybe you can surprise me with some techniques you find, and make them available as modules). I will want .bl.md or .bl.org files for the juicy ones, I am curious.
2. Clean development loop (no duplicate parsers, no fucked up locks when many things are using beam lisp)
3. Self-hosted (i.e. I do want the whole language to 
4. maybe a clearer separation between the nature of the .bl file? core vs. extra vs. stdlib? That may be out of scope if you want.

Go explore and come back with your own understanding and propositions

* Goal

Make beam-lisp's BUILD a beam-lisp program, make incremental compilation
provably cheap, and make the dev loop lock-free. Branch: feat/fully-self-hosted
(worktree /home/user/code/undefine/beam-lisp--self-hosted).

* Ground truth (verified 2026-09-02, commit 1497328)

- Language core is ALREADY self-hosted: priv/reader.bl, reader-node.bl,
  compiler.bl (3514 LOC) → BeamLisp.Ns.Compiler. compiler.ex compile/2 is a
  thin delegator. Oracle (priv/self/oracle.bl) proves 932/932 prelude forms
  byte-identical. bl escript has no Elixir floor (priv/bl/cli.bl).
- Still Elixir ("the build system", ~2800 LOC): aot.ex 1090,
  mix/tasks/compile.beam_lisp.ex 565, loader.ex 506, aot_cache.ex 390,
  source_graph.ex 218. Plus substrate (env/emit/link/rt/vector…) which stays
  per FUP-016's floor rule (migrates only when a benchmark earns it).
- Hard floor that CANNOT be .bl: AOTCache.compiler_key/0 + Bootstrap.install!
  run BEFORE BeamLisp.init/0 (no Env, no reader). Mix.Task.Compiler behaviour
  must be Elixir.
- Duplicate parser: SourceGraph.header/1 (regex, Env-free) ∥
  compile.beam_lisp.ex order_and_deps/1 (Reader.read_all + primary_ns/requires).
- Build loop is serial: compile.beam_lisp.ex:171 Enum.reduce over `ordered`.
- Manifest freshness is O(N·k): up_to_date? calls closure_hash per path, each
  re-walking a closure the DAG builder already has.
- Cross-ns calls: compiler.bl compile-call → linked-call bakes the callee's
  BODY MODULE + fname + arity into the caller (Env.link → {mod, fixed, variadic}).
  Body module is deterministic per ns (aot.ex stabilise_body_modules/ns_body_module).
  ∴ caller bytes depend on callee INTERFACE (names, arities, variadic min,
  private?, macro expansions) — never on callee function bodies.
- Macros: vars tagged {:"$macro", fn}; compiler.bl macro?/macro-lookup;
  expansions inlined at compile time (cross-ns).
- Emitted beams carry __bl_provenance__/0 = {source_hash, compiler_key}.
- Locks: (a) Mix.Sync.Lock on _build — every mix entry point (run/test/
  watcher/MCP demo) serialises; (b) :global.trans({:bl_load, mod}) aot.ex:196
  — per-VM, correct, keep.
- Any aot.ex edit rotates compiler_key → full prelude rebuild (~511s).
  Batch aot.ex edits per wave.
- Literate dialects .bl.md/.bl.org are live (loader.ex @doc_extensions,
  reload_watcher) but AOT discover/1 only globs **/*.bl.

* Waves

** W0 — taxonomy: priv/{boot,std,lib,self,build}
- git mv: boot = reader-node reader compiler core sugar data-readers;
  std = env errors multi interop effects deferred flow optics test typed
  termination promote rewrite deodorant codebase reload(+dir) executor(dir)
  clojure(dir) specter(dir); lib = datom(+dir) auth(+dir) live(+dir) loom
  veritas(+dir) minikanren z3 wry web system(dir); self = self/; build = new.
- Loader ns→path: search-path list over the tier dirs (loader.ex:309 area);
  compile.beam_lisp source_dirs; AOTCache @toolchain_seeds → hash priv/boot/**
  by directory; Bootstrap seed manifest paths; mix.exs escript/release
  include lists; any hard-coded "priv/x.bl" paths in lib/test/research/tooling.
- Gate: mix compile clean; mix test (per-file if FUP-014 flake); oracle
  932/932; bl escript builds.

** W1 — priv/build/source-graph.bl + delete Elixir SourceGraph + dedupe parser
- Port header/1, closure/2, closure_hash/3, hash_lines/1 to .bl. Pure: no Env.
  Exposed as AOT module BeamLisp.Ns.Build.SourceGraph; Mix task calls via
  apply after Bootstrap.install!+init (seed floor guarantees presence).
- compile.beam_lisp order_and_deps/1 uses it instead of Reader.read_all.
- compiler_key computes closure via the SAME .bl (post-install) — verify the
  pre-init constraint: compiler_key is called from Bootstrap.install! BEFORE
  init. Resolution: seed-verification path keeps a tiny Elixir hasher for the
  toolchain closure ONLY (priv/boot/** by directory — no graph walk needed
  after W0), so no graph code is needed pre-init.
- Gate: existing source_graph_test.exs cases ported to a .bl test; identical
  hashes before/after (record manifest hashes pre-change, assert equal).

** W2 — O(V+E) gate + wave-parallel emission
- Closure hashes computed once in DAG post-order (build-plan.bl); manifest
  check does zero re-walks. Test: instrument source reads, assert
  reads == |sources| for a full freshness check.
- Emit topological waves under Task.async_stream (leaves in parallel).
  Env is global ETS + Loader.Server: verify emission of independent
  namespaces is safe concurrently (ns_defs isolation) — if not, parallelise
  only the READ side (form capture) and keep emit serial.
- Gate: same manifest/beams as serial build (byte-compare); wall-time delta.

** W3 — interface hash (ns-interface.bl) + literate doc
- Interface of a ns = sorted [(var, kind, arities, variadic-min, private?,
  macro? → macro expansion-fn hash)]. Emit writes __bl_interface__/0.
- Dependent key = own content hash + Σ interface hashes of direct requires
  (transitively via interface, not content). Fall back to closure hash when
  a require has no interface (foreign/older beam).
- Gate: edit a defn body in datom.vector → 0 dependents rebuild, and re-emitting
  a dependent yields byte-identical beam (oracle-style). Edit a macro → only
  consumers rebuild. Edit an arity → consumers rebuild.
- docs/build/interface-hash.bl.md, docs/build/source-graph.bl.md (executable).

** W4 — build driver in .bl (aot-driver.bl, build-plan.bl) + cutover
- Move stale?/provenance/manifest read-write/discover/order/up_to_date? to
  priv/build/. compile.beam_lisp.ex shrinks to: install seed → init →
  apply(BeamLisp.Ns.Build.Driver, :run, [opts]).
- Discover includes .bl.md/.bl.org (literate AOT).
- Delete the moved Elixir. Keep: bootstrap.ex, compiler_key, Mix shell, emit/link.
- Gate: full mix test, oracle, fresh-clone cold boot (CHECKPOINT 4 of FUP-016).

** W5 — bl daemon (lock-free dev loop)
- `bl daemon` owns _build in one VM (distribution node); `bl run/test/eval`
  attach if a daemon is up, else run standalone. Watcher runs inside the daemon.
- Gate: two concurrent `bl run` against the same tree do not block on
  Mix.Sync.Lock; watcher + test + REPL coexist.

** Spike — content-addressed body modules (no commitment)
- Name body modules per (ns, var, body-hash) so one defn edit re-emits one
  body module. Risk: BEAM keeps 2 versions/module (emit.ex). Measure, report.

* Non-goals
- Substrate (rt/vector/set/env/emit/link) migration — FUP-016 P10/P11 rule.
- Changing language semantics.

* Standing constraint (user, 2026-09-02)
Any touch of / need for the `system` module (priv/system.bl, priv/system/):
every AUTHORING surface must declare mathematical content (SMT-LIB assertions,
sorts, constraints) in the QUOTED form — SMT as beam-lisp data forms — never
as SMT-LIB strings. Applies to new code, docs (.bl.md/.bl.org), examples, and
any existing string-form call site encountered while working here.

** W6 — SMT quoted authoring (from the standing constraint)
- Today: priv/system/smt.bl `translate` renders SMT-LIB STRINGS; core.bl (19),
  veritas.bl (13), veritas/fault.bl (2), veritas/mock.bl (4), z3.bl build
  assertions by `(str "(assert " …)` splicing.
- Target: SMT is DATA. Authoring uses syntax-quote:
  `(assert (= ~(sym svar "2") ~next))`. ONE printer (smt/emit data → SMT-LIB
  text) at the z3 port edge (z3/check). translate returns data, not strings.
- Rewrite all string-builder call sites; update docs/examples/tooling that
  show the string form. Gate: existing system/veritas/z3 tests pass; z3
  receives byte-identical SMT-LIB for the corpus (snapshot before/after).

NOTE [2026-09-02]: W0 W1 W2 DONE (commits 21abe90 → b58c05d + gate). Measured: cold build 7m50s→1m52s (jobs=8); no-op 7.5s→2.4s; 303/303 beams byte-identical across serial/parallel/perturbed VM (was 247/303 differing run-to-run BEFORE this work — the pre-existing AOT was not reproducible at all). Four nondeterminism sources found+fixed in both compilers: baked template gensyms, forward-ref link timing, atom-order map literals, worker-owned ETS. Also: Code.compiler_options save/restore race → CompilerOptions.ensure! once VM-wide. Gates: aot_reproducible_test.exs, build_plan_test.bl (7/25), AOT suites 23/23, bl self-hosting 87 tests (2 pre-existing baseline failures: try/defserver, same on main). Baseline: datom.store edit → 35 rebuilds (W3 interface hash target).

NOTE [2026-09-02]: W3 DONE (20bb359 + doc). ns-interface.bl + coverage-gated interface keys in build-plan.bl; ONE key fn (key-for) for build stamp + runtime gate. Measured: body edit datom.store 35→1, vector 24→1, typed 24→1, interop 29→1; interface edits unchanged (sound). Cold build now 58s (jobs=8) from 7m50s; no-op 1.5s from 7.5s. Two executable docs under docs/build/. Gates: ns_interface_test.bl 7/7, AOT suites 25/25, docs run clean. Next: W4 (driver → .bl) is now mostly a shell already; W5 daemon; W6 SMT quoted.
NOTE [2026-09-03]: W6 DONE (SMT as quoted data, one printer at the z3 edge). New tier lib priv/lib/smt.bl (ns `smt`, aliased `smtlib` by consumers): `emit` term→SMT-LIB text (symbols/keywords by name, negatives→(- n), Real floats, String escaping via doubled quote, realises lazy seqs via vec), `script` commands→text (skips nils, `[:smt/raw text]` splices verbatim), `raw`/`sym`/`declare-const`/`assert`. THE RULE: mathematical content is written as syntax-quoted forms upstream; the ONLY form→text crossing is emit/script at the z3 port edge; `raw` is the legacy-string boundary adapter (tolerance branches `(if (string? t) (smtlib/raw …) (smtlib/assert t))`), never program-logic authoring.
- Cutover surfaces (all author forms now): system.smt translators; z3.bl check/check-model/prove-equiv (render via script; strings pass through); system.core (79 string sites → form scripts + helpers decl-cmds/asserted/neg-asserted/eq-asserted/and-asserted + term-atom normalising a repair domain member to its FORM atom — numeric string→int, name string→symbol, the ONE names→terms crossing, in `candidates`); veritas.bl (generators constrain via form commands; TupleGen/param-decls concat form vectors, never join ""); veritas/{fault,mock,property} (form command vectors + `:clause/text` renders an ensures FORM at the datom string column); datom/time.bl (overlap-smt/overlap-verdict/consistent? → form commands); reload/migrate.bl (invariant-var + subst-var walk FORMS, not string split).
- Infra: lib/beam_lisp/z3_port.ex bundled_exe now uses BeamLisp.Tiers.priv_root/0 (not :code.priv_dir) — the escript code path answers with an in-archive path that is not a dir on disk; the checkout priv/z3/bin/z3 is the truth. Unblocks all z3-backed suites under `./bl`.
- Gates (fresh escript + mix, authoritative): smt_test 6/28, smt_parity_test (z3 proves old-string ≡ new-form for 14 corpus entries), system 112 tests 0/0, veritas 55 0/0, reload 44 0/0 (all under mix; escript shows the documented datom-NIF-in-escript :nif_not_loaded on seam/system + the live-module-reload limitation on reconcile/watcher — both pre-existing, pass under mix). repair/upgrade/migrate/fault/property/hypothesis-symbolic/datom-time all 0/0. self-hosting 2 pre-existing failures (try/defserver AST parity, same on main). Corpus gate FILES 34 OK 939 FAIL 66 (was OK 933 FAIL 65 at W4): fails are the known gensym-numbering / try→cond parity gap (diagnose.bl confirms identical AST, only x_NNNN counter differs because W6 added forms), not a semantic W6 regression. mix compile cold clean, exit 0.
- Test expectation updates reflecting the contract: `-1` renders `(- 1)` (SMT-LIB spec, parity-proven); translator-output comparisons wrap in `(smtlib/emit …)`; `:discovered` is now a form.
