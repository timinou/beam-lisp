#+TITLE: Wave 5: fidelity sprint — try/catch, refs, docstrings, destructuring
#+STATE: DONE
#+SESSION_ID: 154d7065511fb2de
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-08-06T19-28-19-268Z_154d7065511fb2de.jsonl]]
#+CUSTOM_ID: PLAN-001-wave-5-fidelity-sprint-try-catch-refs-do
#+COMPLETION_HASH: 9b088c2a91c3e15d

* Initial Message

what's next? what's left? what else could be done?

Active wave. Four parts, dispatched to subagents in two batches (disjoint file ownership; orchestrator runs global suite + commits).

**Part A — error handling** (compiler.ex, lib/beam_lisp/ex_info.ex, priv/core.bl):
- `try/catch/finally` special form → Elixir try quoted. Clojure shape: `(try expr… (catch e body…) (catch :throw e body…) (finally body…))`. Untyped catch = catch-all (catch normalized error value); typed catch `(catch SomeModule e …)` → rescue clause matching that struct. `throw` special form: map/keyword payload → raise BeamLisp.ExInfo.
- BeamLisp.ExInfo exception struct (message, data) with module fns ex_info/2, ex_data/1, ex_message/1.
- core.bl defns: ex-info, ex-data, ex-message wrapping the module fns via interop.

**Part B — reference types + concurrency** (reader.ex, lib/beam_lisp/refs.ex, rt.ex):
- `atom` (Agent-backed, %BeamLisp.Atom{pid} struct), `deref`, `swap!`, `reset!`, `compare-and-set!` (Agent.get_and_update for CAS).
- `@x` reader sugar → (deref x).
- `future` macro (Task.async) + `future?`/future-cancel; `promise`/`deliver` — blocking deref w/ optional timeout `(deref p ms timeout-val)`. Simple Agent+poll impl acceptable; document as debt.
- Seed all into core in rt.ex seed_core; register links (BeamLisp.Refs module fns) where arity-exact.

**Part C — compiler fidelity** (compiler.ex, env.ex, priv/core.bl):
- Docstrings: defn/def/defmacro accept optional docstring after name (`(defn f "doc" [x] …)`); store in ETS under {:meta, ns, name}; `doc` fn in core.bl prints name + doc.
- Map destructuring in let/fn/loop params: `{:keys [a b] :strs [s] :as whole :or {a 1}}` + general `{binding key}` form. Lenient like Clojure (missing keys → nil). Compile to nested get calls via RT helpers. Sequential destructuring already exists and is lenient — reuse that pattern.

Acceptance: each part has tests in test/beam_lisp/waveN_*_test.exs + a runnable example in examples/; `mix test` fully green; examples run via `mix beam_lisp.run`.

NOTE [2026-08-06]: DONE — all four parts landed, 204/204 green, all examples exit 0. Commits: cd72657 (5a errors), 427a5df (5b refs), 673d69d (try do-first fix), 331ffc4 (reader-macro table), 2313db3 (5c docstrings+destructuring), de5cae4 (future+@ into core.bl), fa4ac39 (README). Notables: (1) Elixir 1.20 ParallelChecker crash root-caused to keyword ORDER in hand-built try AST — do: must lead; checker was right, no inference opt-out needed. (2) User directive: as much as possible in .bl — @ mapping now registered from core.bl via a rebindable reader-macro table (ETS {:reader_macro, char}); future is a prelude defmacro using ` + ~@; Elixir keeps only substrate. (3) W5c fixed a pre-existing loop-destructuring scoping bug. (4) promise = waiter-list process (no polling); swap! retry-free via Agent serialization. Next waves: PLAN-002..PLAN-009.