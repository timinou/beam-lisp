#+TITLE: Wave 8: HAMT-backed persistent vectors
#+STATE: DONE
#+SESSION_ID: 154d7065511fb2de
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-08-06T19-28-19-268Z_154d7065511fb2de.jsonl]]
#+CUSTOM_ID: PLAN-004-wave-8-hamt-backed-persistent-vectors
#+COMPLETION_HASH: fb0c96fee01d6f5b

* Initial Message

what's next? what's left? what else could be done?

Current: BeamLisp.Vector is a tuple wrapper — O(1) read, O(n) write (conj/assoc copy the tuple). Fine at demo sizes, wrong at real ones.

Design: HAMT (hash array mapped trie), branching factor 32, 5-bit chunks of :erlang.phash2 — same shape as Clojure's PersistentVector. Keep the %BeamLisp.Vector{} wrapper so all call sites (RT first/rest/conj/count/nth, reader vector literal, receive patterns, print_str) keep working; only the internal repr changes. Path-copying persistence (no transients initially).

Perf gate: benchmark conj-build of 100k-vector before/after; seq access patterns must not regress (bench.bl exists for hot loops).

Files: lib/beam_lisp/vector.ex (rewrite internals), possibly lib/beam_lisp/hamt.ex, rt.ex touchpoints (conj/nth/etc. call Vector API — keep the API surface identical), test/beam_lisp/vector_hamt_test.exs (persistence: old version unchanged after conj/assoc).

Edge: receive pattern matching on %BeamLisp.Vector{} (compiler.ex emits two clauses for [p q] patterns — tuple AND vector) must still work; print path walks elements — needs an efficient iterator. Acceptance: all existing vector tests green unchanged + persistence tests + bench numbers recorded in this item.

NOTE [2026-08-06]: DONE — commit 6e49cd7. Implemented as Clojure's PersistentVector (32-way bit-partitioned INDEX trie: cnt/shift/root/tail + tail buffer), not a hash trie — the plan's "HAMT" name was imprecise; index tries are what vectors want, hash tries are for maps. Plan assumption corrected on contact: compiler.ex both constructs AND pattern-matches %Vector{items: tuple} by field, and compiler_test asserts eval("[1 2 3]") == Vector.new([1,2,3]) — so a fields-only rewrite was impossible. Design: items holds EITHER a raw element tuple (all vectors ≤32, i.e. Clojure's tail-only state, which is also exactly what literals produce) OR a tagged {:bl_vec, cnt, shift, root, tail} for >32. Dispatch disambiguates on tuple_size==5 ∧ elem(0)==:bl_vec ∧ cnt>32. to_list/Enumerable.reduce walk leaves in chunks, not nth-in-a-loop. BENCH (real, mix run): conj-build 100k 9219ms → 12ms (756×); random nth 100k 7ms → 13ms (~130ns/elem, the expected constant-factor cost of trie descent vs bare tuple elem). Boundaries tested: 0,1,31,32,33,1023,1024,1025,1025,32769,100000. 27 tests + examples/vectors.bl. Follow-up available: transients for bulk build; assoc at i==count appends (Clojure assocN).