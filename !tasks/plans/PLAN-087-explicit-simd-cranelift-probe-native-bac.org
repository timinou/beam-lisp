#+TITLE: Explicit-SIMD cranelift probe → native backend decision (MMORPG vs expert-swarm pressure test)
#+STATE: ITEM
#+SESSION_ID: 15729b4fce888940
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-04T16-19-37-402Z_15729b4fce888940.jsonl]]
#+CUSTOM_ID: PLAN-087-explicit-simd-cranelift-probe-native-bac

* Initial Message

Check that PLAN-074 is done and if it is, understand all the things written about MLIR/LLVM IR and beam lisp in this repo. Rebuild your understanding from scratch to make it your own and then come back to me with your own assessment. You should also read docs/memory-policy and cross it with the other work on compiling beam lisp to MLIR/LLVM IR

* Directive (user, 2026-09-04)

Run the explicit-SIMD probe (j1_probe), THEN decide the native backend by
pressure-testing two usecases: MMORPG server, and a swarm of expert language
models acting as one LLM endpoint.

* Probe

cranelift has explicit vector types (i8x16, f32x4, …) but no auto-vectorizer.
The lowerers_spike widening experiment measured manual SCALAR widening (nothing
gained) — true vector IR emission was never tried. The probe: for a counted
pixel-class loop with provable trip count and lane-divisible bounds (decidable
structurally — same doctrine as the fragment), emit explicit SIMD IR through
cranelift and measure vs the rustc auto-vectorized twin (blend ref: 1.94 ms /
5.89× gap today).

- GATE A: blend-class kernel within 1.5× of rustc twin → cranelift+explicit-
  SIMD suffices; LLVM tier shelved.
- GATE B: still >1.5× → LLVM tier justified for SIMD-class kernels.

* Usecase pressure-test (decides the backend split)

- MMORPG: per-tick kernels are counted scalar loops (movement integration,
  interest management, checksums) → checksum-class → cranelift suffices
  (measured 1.29× hand-Rust). Serialization/encode loops: fold/store class.
- Expert-swarm LLM endpoint: routing scores, embedding dot-products, softmax,
  top-k over f32 vectors → SIMD-class → this usecase is what GATE B serves.
  If the swarm is the product, vector throughput is the deciding metric.

* Output

A decision note: native backend = {cranelift-only | cranelift + LLVM opt tier},
justified by probe numbers + usecase mapping. Records into PLAN-080 follow-up 2.
