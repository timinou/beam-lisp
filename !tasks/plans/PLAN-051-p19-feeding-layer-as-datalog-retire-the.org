#+TITLE: P19: feeding layer as datalog — retire the string-walking, obey datom=codebase-as-data
#+STATE: ITEM
#+SESSION_ID: 15699e91a40f236f
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp--type-inference/2026-08-28T16-47-31-472Z_15699e91a40f236f.jsonl]]
#+CUSTOM_ID: PLAN-051-p19-feeding-layer-as-datalog-retire-the

* Initial Message

ok my friend, can you tell me more about this?

The honest remaining depth — z3-sequence content invariants beyond length, a richer discovery vocabulary, multi-collection capacity — is depth on a shipped foundation, not a missing floor.


From zero context in simple language. FOld in more stuff if it needs to be talked about

GOAL: the P18 feeding layer (state-var discovery, sort assignment, next-field mapping, candidate-invariant generation, collection handling) is currently hand-rolled AST re-walking + string lexing in priv/system/core.bl. Replace it with datom facts + datalog queries, matching the pattern codebase.bl and model.bl already established. Doctrine: datom = codebase-as-data; the extracted state model IS codebase-as-data.

RECEIPTS (the smell):
- core.bl:549 free-syms — "a cheap lexer: strips ( ) [ ] { } then splits on space" to distinguish state var vs message input. This is a JOIN over facts done as string surgery.
- core.bl:303 state-vars — re-walks nodes, subtracts a hardcoded op blocklist.
- core.bl:335 replace-var — textual (join to (split s from)) substitution.
- core.bl:361 state-shape / lit-sort — guesses Bool/Int from init LITERALS instead of asking the tag lattice (typed/walk).
- core.bl:615 candidate-invariants — hardcoded (op v k), consts (list 0 1), single var only. This is Gap 2 (poor discovery vocabulary).
- core.bl:740 collection-vars + (first …) — one collection only (Gap 3).

PRECEDENT ALREADY IN REPO:
- model.bl asserts transitions as :edge/to facts, expresses reachability as datalog rule REACHABLE, evaluated by datom/query/fixpoint. Its comment: the fixpoint engine "cannot tell a computed edge from a stored one." The newer core.bl helpers REGRESSED past this to string-walking.
- codebase.bl: source→FACTS→datalog QUERIES, sees through macroexpand-1, recursive rules (reaches), :not-join negation, arbitrary bl predicates inside [(f ?x)] clauses (e.g. [(codebase/interop-name? ?callee)]). This is the exact template.

DESIGN:
1. New system/facts.bl (sibling to codebase.bl): model.bl clauses → datom facts. Schema:
   :machine/name
   :svar/machine :svar/name :svar/sort :svar/position   (sort from typed/walk tag lattice, NOT init literal)
   :input/trans :input/name                             (bound by the pattern)
   :trans/machine :trans/label :trans/guard
   :next/trans :next/svar :next/expr
   :elem/coll :elem/val                                 (collection contents as datoms)
   This is projection over model.bl's normalized {:label :pattern :guard :next :body} — not re-parsing.
2. Replace feeding helpers with queries:
   state-vars       → [?s :svar/machine ?m] [?s :svar/name ?n]
   state-shape sort → [?s :svar/name ?n] [?s :svar/sort ?sort]   (lattice-sourced)
   free-syms        → var vs input = presence in :svar/name vs :input/name (a join)
   next-fields      → [?n :next/trans ?t] [?n :next/svar ?v] [?n :next/expr ?e]
   collection-vars  → [?s :svar/sort "coll"]  (returns the SET → Gap 3 gone)
3. Discovery as a datom fixpoint (the punchline): the Houdini loop is a stratified datalog program with one z3-computed predicate.
   [(cand ?p)   [?a :svar/name ?x] [?b :svar/name ?y] [(compat-sort ?a ?b)] [(mk-pred ?x ?y ?p)]]   ; relational candidates = self-join over svars
   [(inv ?p)    (cand ?p) (est ?p) (not (killed ?p))]                                                 ; establishment = base
   [(killed ?p) (cand ?p) [?t :trans/machine ?m] [(breaks? ?t ?p)]]                                    ; breaks? = z3, computed predicate like interop-name?
   Stratification (forced by negation) IS the establish-before-induct ordering the manual loop enforced. datom/query/fixpoint evaluates it. z3 stays DECISIONS; datom drives the fixpoint.

GAPS DISSOLVED:
- Gap 2 (vocabulary): relational candidates are a self-join; single-var (op v k) is the degenerate case. Widen vocabulary = add a rule, not edit a list. = typeo running backward, seeded by queried state vars.
- Gap 1 (collection content): contents are datoms; "no duplicate ids" / "sorted" = datalog safety queries with negation, NO z3-sequences for discrete/finite content. z3-sequences only for unbounded arithmetic content.
- Gap 3 (multi-collection): collection-vars is a set once queried; (first …) was the only limiter.

CONSTRAINTS PRESERVED: strict engine non-overlap (tag=STRUCTURE, z3=DECISIONS, miniKanren=RELATIONS/synthesis, datom=codebase-as-data). Sound-warnings-only; untranslatable dropped not falsely proven. Fix at root. Cutover not parallel impl (retire the string helpers, don't leave both). Graduate as tests + demos.

INFRA: priv/*.bl compile to _build ebin — after editing, mix compile.beam_lisp (+ MIX_ENV=test). .bl files have no tree-sitter profile → edit by line range. Non-auth regression baseline: 647t/1760a/0F0E. Direct-elixir harness (see memory).

VERIFY: all 15 examples/system demos still clean; seam_test.bl green; equivalence — the datalog feeding layer reproduces every P18 result (reservation invariant, record frozen⇒balance=0, capacity exact, protocol use-after-close, discovery of (>= balance 0)); THEN Gap 1 (a content invariant demo) and Gap 2 (a relational DISCOVERED invariant, e.g. discover balance>=reserved unannotated) as NEW capability the string layer could not reach.

NOTE [2026-08-29]: ═══ P19 PROGRESS UPDATE (2026-08-29) — three spikes green + theory functor SHIPPED ═══

SCOPE GREW during investigation. Original P19 = "feeding layer as datalog". Along the way three foundational spikes proved a much larger opportunity, and one increment SHIPPED. Full state:

── SPIKES (all RAN GREEN, research/p19_datalog_feed/) ──
1. spike_fixpoint_z3.bl — CAN the datom fixpoint drive z3 as a predicate mid-recursion? Q1 MECHANISM ✓ (derive-semi-naive runs [(z3-pred ?x)] on a recursive var), Q2 ESTABLISHMENT ✓ (datalog+z3 filter), Q3 HOUDINI ✗-with-nuance (precise mutual-support Houdini is a GREATEST fixpoint w/ recursion-through-negation → NOT datom's least-fixpoint; weakened each-alone version IS datalog+z3 and finds the right answer on the account machine).
2. spike_gfp.bl — the GREATEST-FIXPOINT primitive (~8-line dual of derive-semi-naive) + gfp-delta (semi-naive) + gfp-explained (provenance). Hosted THREE capabilities from ONE driver: D1 Houdini×z3 (discovered mutual-support invariants x>=0,x>=y,y<=x the weakened loop cannot), D2 safe-set/CTL-AG×delta (incremental re-verify 7 vs 10 checks on edit), D3 bisimulation×transition-model (state minimization 4→2 classes). SOUNDNESS BOUNDARY: warm-start incremental is sound ONLY when edit SHRINKS the fixpoint.
3. spike_theories.bl — 15/15 GREEN through the UNCHANGED z3/check port: Real, BitVec, Array, String, Seq, Datatype/ADT, Set, quantifiers∀, Optimize, UF, and MIXED-theory record solved jointly. PROVED the Int-only limit was OURS (smt.bl), never the port. z3 4.16.0 bundled.

── SHIPPED (commit 4e8284a): the z3 SORT FUNCTOR ──
Scalar theories (Int/Real/Bool/String) now flow through verify-process end-to-end:
- smt.bl: sort-of-value = tag-lattice→z3-sort functor (:int→Int :float→Real :bool→Bool :string→String) via typed/lit-type; translate emits Real literals + String theory (str→str.++, str-len→str.len, str-contains?/prefix?/suffix?→str.contains/prefixof/suffixof).
- core.bl: retired lit-sort (2 hardcoded cases) → node-sort (functor); state-shape reads per-field sorts from lattice; INPUT SORTS inferred BY FLOW (an amount inherits the sort of the field its next feeds).
- errors.bl ROOT FIX: delaborate now re-quotes string literals (bl pr-str doesn't quote unlike Clojure → "evt:init" re-parsed as SYMBOL, broke establishment). Same class as earlier :map/:keyword gaps.
- Demos 16 (Real interest rate), 17 (String log prefix — content not length), 18 (one verify-process across 4 theories, all green). seam_test 21→28 tests, 55→74 assertions incl delaborate round-trip regression.
- Non-auth regression 654t/1779a/0F0E (was 647/1760). Docs: docs/typing/z3-is-not-an-int-solver.bl.md + docs/typing/the-greatest-fixpoint.bl.md (zero-context .bl.md convention established).

── ROADMAP (dependency-ordered, NOT yet built) ──
R1 [next] COMPOUND theories in the functor: :vec/:seq→(Seq T)/(Array Int T), :set→(Array T Bool), :map→(Array K V), :kw/sum→(declare-datatypes). Wires Gap-1 (Array CONTENT invariants: no-duplicate, membership) and Rung-4-native (ADT sum types w/ discriminant+payload, replacing integer-coded enum). smt.bl sort-of-tags currently returns :unknown for these; the spike proved all reachable.
R2 gfp LAYER B in datom.query.rules: dualize derive-semi-naive as a `gfp` rule mode (seed=full, monotone kill, downward delta). Pure relational, validated vs D2 safe-set + D3 bisim closed forms. Independently valuable (CTL model-checking + state minimization) before Houdini touches it.
R3 gfp LAYER C: recursive monotone AGGREGATION (fold current relation → one z3 conjunction mid-fixpoint) — what strong Houdini specifically needs. Research surface: Datalog-with-lattices/semirings (Flix, Datafun, Datalog°), Van Gelder alternating fixpoint. Then the P18 discover-invariant loop moves INTO the engine.
R4 the ORIGINAL feeding-layer cutover (system/facts.bl): model.bl clauses → datoms; state-vars/free-syms/next-fields become datalog queries; discovery redirects candidate-invariants → the mk `candidates` already in core.bl:81 (relational Gap-2, net line loss). Establishment filter → datalog+z3 (spike Q2 ✓). Houdini iteration consumes R2/R3.
R5 OPTIMIZE-as-synthesis: z3 (maximize) turns verify-capacity from checker into bound-SYNTHESISER (discover tightest capacity, not annotate).

CAVEAT threaded throughout: quantifier/string/nonlinear fragments can return :unknown (undecidable) → register :approximate in the exact/approximate catalog, never call unknown a proof. Reachable ≠ decidable.

Commit chain P19: (spikes uncommitted until 4e8284a) → 4e8284a (sort functor + demos 16-18 + delaborate fix + docs). Worktree feat/type-inference, clean except gitignored .spell/memory/.

NOTE [2026-08-29]: ═══ P19 UPDATE 2 (2026-08-29) — smell-b fixed, R1a shipped ═══

SMELL-B FIXED (commit dbf976e): the real node→string→node round-trip. model.bl clause now carries reader NODES (:pattern-node/:guard-node/:next-node) as source of truth; smt.bl gained of-node/of-node-len (translate a node directly, no re-read); core.bl SMT hot path (next-fields, keep-translatable-multi, state-shape, verify-process, verify-capacity, discover-invariant) all translate nodes. Deleted dead single-var string helpers (inv-fn, replace-var, parse1, keep-translatable, input-decls). NB the demo/test defserver-as-STRING is NOT the smell and is correct — verified quote yields a POSITION-LESS scalar (node-pos nil, name→content-hash), so read_string is the only source of position-bearing meta-carrying nodes the checker needs.

DOCS FIXED (41cf36a): the two narrative .bl.md docs violated docs/AGENTS.md (record what-was, not how-it-is). Replaced with docs/typing/how-state-picks-its-theory.bl.md — timeless, universal truths only.

R1a SHIPPED (commit 0451a8e): NATIVE ENUM (sum-type) state over z3 DATATYPES.
- A keyword-init field is a finite enum → z3 algebraic datatype. Write (= phase :open) not (= phase 1); no idle=0 legend to drift; z3 no-junk axiom fences the field to exactly its tags (Int encoding admits phase=99, datatype does not).
- smt.bl: translate threads a variants map (tag→SORT-PREFIXED constructor E_phase-open — bare names collide across datatypes, probed); keyword→constructor; unknown keyword→untranslatable; variants-map/datatype-decl/variant-ctor. Empty variants ⇒ Int/Real/Bool/String byte-identical.
- core.bl: state-shape marks keyword-init field sort E_<field>; verify-process gathers universe (init+next+guards+invariant = complete set, user's choice) → prelude + variants threaded through inv-define-fun/preserves-joint?/establishes-joint?/next-fields/keep-translatable-multi. Constructors EXCLUDED from z3 inputs (values not inputs — same class as true/false; declaring one desyncs the datatype — this was the false-positive bug found + fixed).
- PHASE GRAPH as code-as-data (the user's datom=code emphasis): phase-edges extracts (from→to) from guard-tag+assigned-tag; phase-analysis returns reachable/dead(declared-but-unreachable=spec-impl mismatch)/terminal(sink) via injected datalog reachable-from wired to the shipped datom.query.fixpoint REACHABLE rule. z3 decides arithmetic invariant; datalog reasons structure.
- Demo 13 rewritten to native tags + datalog phase analysis. seam_test 28→33 tests (74→87 assertions). Spike spike_adt_enum.bl. Regression 659t/1792a/0F0E (was 654/1779).

ENGINE-DIVISION AUDIT (user challenge "do your tiers include invariants from model.bl clauses — dispatch/overloading/pattern/guards?"): HONEST ANSWER — model.bl extraction is still 32 sites of hand-walking, NOT datalog. Coverage (handled-labels/unhandled-messages/deadlock) IS datalog. The clause STRUCTURE (dispatch siblings, guard preconditions, pattern shadowing, arity overloading) is not yet datom facts. R1a's phase graph is the first clause-structure-as-datalog slice (guards+assignments→edges→reachability). Fuller clause-topology-as-facts remains the deferred R4 (facts.bl).

REMAINING ROADMAP: R1b payload variants ((running j)→(E-running j) constructor, (job c) accessor, (running? c) discriminant — session types with carried data); R2 gfp Layer B (dualize derive-semi-naive); R3 gfp Layer C (Houdini into engine); R4 clause-topology-as-datom-facts (facts.bl — dispatch coverage, guard-precondition graph, shadowed-clause as datalog queries); R5 Optimize-as-synthesis. CAVEAT: reachable≠decidable, register :approximate for unknown.

Commit chain P19: 4e8284a(functor)→242dcd1(docs)→dbf976e(smell-b node path)→41cf36a(docs fix)→0451a8e(R1a enum).