#+TITLE: P20: composition flywheel — every guarantee as one queryable relation
#+STATE: ITEM
#+SESSION_ID: 15699e91a40f236f
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp--type-inference/2026-08-28T16-47-31-472Z_15699e91a40f236f.jsonl]]
#+CUSTOM_ID: PLAN-052-p20-composition-flywheel-every-guarantee

* Initial Message

ok my friend, can you tell me more about this?

The honest remaining depth — z3-sequence content invariants beyond length, a richer discovery vocabulary, multi-collection capacity — is depth on a shipped foundation, not a missing floor.


From zero context in simple language. FOld in more stuff if it needs to be talked about

GOAL: today the guarantees (types, effects, footprints, process names, discovered invariants, phase graphs, clause facts) exist but as SEPARATE function calls. Project every one into a single datom relation store so cross-cutting questions become datalog joins. The moment each capability is a relation, it joins with all prior ones: N capabilities → N² answers. R4's system.facts is the first tile.

THESIS: the project's own recurring bet — "the right primitive answers questions you never posed" — cashed in. Each new capability projected joins with EVERY prior one for free, because they're already nearly datom-shaped.

DESIGN: a system.datalog (or system.knowledge) layer that projects, per process/codebase:
- :fn/type, :fn/ret, :fn/effect (from typed + effects)
- :fn/footprint, :fn/pure?, :fn/monotone? (from footprint)
- :process/name (content-hash), :process/invariant (annotated OR discovered)
- :clause/label, :clause/guard, :clause/guarded (from system.facts — SHIPPED)
- :phase/edge (from phase graph — SHIPPED in R1a)
- :call/callee, :call/msg (from codebase — SHIPPED in coverage)
All into ONE conn with a unified schema, dependency-injected like coverage/facts.

KILLER QUERIES (unanswerable today, ~free once relational):
- "which processes SEND a message that could violate the RECEIVER's invariant, given the caller's capability footprint?" (types + effects + protocol + invariant, one join)
- "which discovered invariant depends on which transition?" (provenance join)
- "which handler's guard precondition is never established by any sender?" (facts + coverage anti-join — the dead precondition)
- "impact of changing a guard: which downstream invariants rest on it?" (clause facts + invariant + call graph)

BUILD: R4 facts.bl is the template. Add projectors for each existing capability (they already return the data — this is projection not re-derivation), unify the schema, write the cross-cutting rules as datalog. Graduate as tests + demos. Reuse codebase.bl / datom idioms (examples/typing/02, examples/datom/12).

VERIFY: each projector reproduces its source capability's answer; then at least 3 cross-cutting queries that NO single existing function can answer. Non-auth regression stays green (baseline 680t/1833a/0F0E).
