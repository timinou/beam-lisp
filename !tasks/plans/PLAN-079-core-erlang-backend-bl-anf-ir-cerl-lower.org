#+TITLE: Core Erlang backend: bl-ANF IR, cerl lowering, behavioural oracle (priv/self → priv/boot)
#+STATE: ITEM
#+SESSION_ID: 156fbf164fa5cf22
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-02T11-00-29-118Z_156fbf164fa5cf22.jsonl]]
#+CUSTOM_ID: PLAN-079-core-erlang-backend-bl-anf-ir-cerl-lower

* Initial Message

would `veritas`, `codebase`, and `system` enable us to compile directly to BEAM and manage garbage collection and other concerns through formal proof? I am open to ideas

* Goal

Replace the Elixir-quoted backend of priv/boot/compiler.bl with a Core Erlang
lowering owned in .bl, via one IR (bl-ANF) that the whole toolbench reads.
Design: docs/core-erlang/how-beam-lisp-uses-elixir.md (the three roles of
Elixir; role A is the only semantic dependency) and
docs/core-erlang/targeting-core-erlang.md (ANF, Core, the rewrite, the gains,
the modules). This plan builds it behind a research gate; no cutover without
the gate.

* Ground truth (verified 2026-09-02, branch feat/fully-self-hosted, HEAD 688805d)

- Pipeline today: reader.bl → compiler.bl → Elixir quoted → Module.create /
  :elixir_compiler.quoted (emit.ex build_module, aot.ex compile_block!) →
  elixir_expand → Module.Types (disabled: infer_signatures false) → elixir_erl
  → :compile.forms → Core → SSA → beam.
- Emitted Elixir node census over priv/boot/compiler.bl: :else 35 :when 23
  :__block__ 19 :do 15 :__aliases__ 8 :require 7 :fn 7 :def 5 :after 3 :try 2
  :throw 2 :case 2 :receive 1 :raise 1 :__MODULE__ 1 :cond 1 :catch 1 :alias 1;
  Macro.escape ×7; @behaviour :gen_server on defserver. No Elixir macros,
  with/for/defstruct, protocols, or interpolation are emitted.
- emit.ex owns the shim/body topology (body module BeamLisp.Ns.Fn.M<n> never
  reloaded; ns module holds guarded shims). Both runtime and AOT consume it.
  MUST survive verbatim — the two-version purge hazard is VM-level.
- aot.ex bypasses Code.compile_quoted for :elixir_compiler.quoted/3 (private
  API) to dodge ParallelChecker (13+ min blowup). Elixir-version coupling risk.
- priv/self/oracle.bl: differential oracle on TREES (932/932 prelude forms).
  Elixir-shaped; cannot grade a Core backend.
- Cross-ns calls bake callee body module + fname into caller (Env.link →
  linked-call). Interface-hash design in PLAN-072 depends on this; Core
  lowering must emit the same `call 'Elixir.BeamLisp.Ns.Fn.M…':'f'(…)`.
- compiler.bl calls back into Compiler.macroexpand_1 (Elixir). Stays until
  macroexpansion is .bl (PLAN-072 scope), independent of this plan.
- OTP public APIs available: cerl (c_module c_fun c_let c_case c_clause c_call
  c_apply c_receive c_try c_primop c_literal set_ann), compile:forms/2 with
  [from_core binary return], code:load_binary/3. All callable via erlang/ interop.

* Placement (tiers per PLAN-072 W0)

During the spike and until cutover: priv/self/ (compiler's own tier, beside
oracle.bl): anf.bl match.bl guard.bl cerl.bl beam.bl oracle2.bl opt.bl.
At cutover: anf/match/guard/cerl/beam → priv/boot/ (they become the toolchain
closure AOTCache hashes); oracle/opt stay in priv/self/. Nothing in boot may
require std/lib — anf/cerl read only reader-node + core.
Spike gate under research/sh14_core_erlang/ (continues the shN series).

* Waves

** W1 — bl-ANF IR (priv/self/anf.bl)
- Node kinds (≈12): let · fn · apply · call{:remote|:var|:local} · case
  (clauses w/ pattern+guard) · receive · try · do · literal · var · letrec ·
  primop. Every subexpression let-bound; every symbol resolved to
  {:local v} {:var ns name} {:remote mod fun} {:literal x}.
- Input: the resolved form compiler.bl already produces before lowering
  (identify the seam in compile-* fns; the :clause/:-> ast-node constructions
  are where lowering starts).
- Output is plain bl data; pr-str round-trips.
- Gate: anf of every prelude form (priv/boot/** + std/** + lib/**) succeeds;
  a `(anf/eval …)` reference interpreter over bl-ANF agrees with the running
  value for research/sh12_selfanalysis/end_to_end.bl cases.

** W2 — cerl lowering + beam load (priv/self/cerl.bl, beam.bl)
- cerl.bl: fold bl-ANF → cerl terms. Table in targeting-core-erlang.md §2.
  cond → nested case; raise → erlang:error on the exception map BeamLisp
  already uses; aliases → atoms; Macro.escape → c_literal.
- beam.bl: port of emit.ex topology — body module per ns (deterministic name,
  reuse aot.ex ns_body_module scheme), guarded shims, __bl_provenance__/0
  chunk; compile:forms → code:load_binary. :global.trans({:bl_load, mod})
  lock kept.
- defserver: c_module attributes [{behaviour, [gen_server]}]; callbacks as
  today; start/stop still route through BeamLisp.Server.
- Gate: examples/hello.bl, control.bl, destructuring.bl, guards.bl,
  processes.bl, supervision.bl run identically through both backends.

** W3 — behavioural oracle (priv/self/oracle2.bl)
- For each prelude form: eval via Elixir backend and via Core backend; compare
  values (=) and footprints (system.footprint on both emitted trees is not
  possible — compare observable effects: io captured, atoms created, processes
  spawned). Property-shaped: veritas.property over fn vars with generator from
  typed sigs where available.
- Gate: 932/932 forms value-identical; 0 effect divergences; full
  mix beam_lisp.test green with BL_BACKEND=core.

** W4 — measure
- REPL def latency and hot-reload latency: Elixir vs Core backend, 3 samples
  each, on priv/std/typed.bl and priv/lib/datom/query/magic.bl (the two
  incident files). Record spread.
- Full prelude AOT build time both backends.
- Gate: Core ≤ Elixir on both; document numbers in research/sh14 README.

** W5 — match + guard (priv/self/match.bl, guard.bl) — the first bl-owned pass
- match.bl: clause list → decision tree; emit exhaustiveness/redundancy facts
  for typed (clause reachability) — one analysis, two consumers.
- guard.bl: guard-safe BIF set; a system.smt-translatable predicate that is
  also guard-safe lowers as a Core guard.
- Gate: typed clause-reachability warnings unchanged; proven-exhaustive case
  emits no match_fail branch (inspect Core via cerl_prettypr).

** W6 — cutover (separate session; per docs/self-hosting.md "runtime cutover")
- Move anf/match/guard/cerl/beam → priv/boot/; AOTCache toolchain hash covers
  them; emit.ex reduced to the Elixir-side seed path or deleted; aot.ex
  compile_block! → beam.bl.
- Delete oracle.bl (tree oracle) after oracle2 is the gate.
- Gate: mix test, oracle2, fresh-clone cold boot (CHECKPOINT 4 of FUP-016),
  drop pack + `bl-bundle run examples/hello.bl`.

** W7 — opt as optics over bl-ANF (priv/self/opt.bl)
- Port research/sh12b_optimizer queries (inline-once, dead-fn) to bl-ANF
  facts in codebase; rewrites as optics; consumed before cerl.
- Enables PLAN-078 W4 (tuple state, iolist strings) and W5 (native lowering
  reads bl-ANF).

* Non-goals
- Role B (lib/beam_lisp/rt.ex, vector, env, link, refs …) untouched; substrate
  migration is FUP-016's floor rule, not this plan.
- Role C (application, supervisor, mix tasks, drop) untouched.
- No BEAM-asm or raw-bytecode target: Core keeps sys_core_fold, beam_ssa_type
  and beam_validator; below Core we would re-implement them and lose JIT trust.

* Risks
- Elixir exceptions/structs semantics in try/raise must be reproduced exactly
  (BeamLisp.ExInfo.raise, CompileError) — W2 gate covers.
- Line/position annotations: cerl set_ann with {file, line}; error-reporting
  tests key on BeamLisp.Ns.* frames — keep module naming.
- PLAN-072 is concurrently restructuring build/loader; coordinate the
  toolchain-hash set (boot/**) and the loader's backend switch.

* Relations
- Unlocks PLAN-078 W4 (repr) and W5 (native): both read bl-ANF / land in
  cerl.bl.
- Depends on PLAN-072 W0 (tiers — done) and benefits from its interface-hash
  work (unchanged: Core emits the same linked-call shape).

* DESIGN REVISION [2026-09-03] — direct emit, neutral IR for MLIR/LLVM (user directive)

Two constraints from the user reshape this plan:

1. DIRECT EMIT, NO QUOTED LAYER. The destination is compiler.bl → Core Erlang
   with the Elixir-quoted intermediate DELETED — not CE1's "second reader of the
   quoted output". CE1 (research/ce1_core_erlang, proven: 7897 forms lowered,
   142 tests / 583 assertions zero divergence, 1.7–2.0× faster) is the SHORT
   PATH that de-risks the lowering; the deep path replaces compiler.bl's
   `ast-node`/`ast-mod-call` quoted constructors with bl-ANF emission, and
   cerl.bl lowers bl-ANF → Core. When done, no `Module.create` /
   `:elixir_compiler.quoted` / `Code.eval_quoted` on the compile path.

2. bl-ANF IS THE NEUTRAL IR — it must feed BOTH Core Erlang AND the native
   MLIR/LLVM path (PLAN-080), which already lowers a bl-ANF-shaped vocabulary
   through cranelift at 76–145× (research/j1_probe, lowerers_spike). So bl-ANF
   is the single IR the whole toolbench reads (typed/footprint/system/codebase/
   opt) and BOTH backends consume. Two IR levels, one pivot:
     - bl-ANF  = HIGH IR: let·fn·apply·call·case·receive·try·do·literal·var·
                 letrec·primop, every subexpr let-bound, every symbol resolved.
                 → cerl.bl lowers ALL of it to Core Erlang.
     - j1/MLIR = LOW IR: scalar SSA (imm/add/mul/load/store) for counted numeric
                 loops. native.bl lowers the PROVEN-ELIGIBLE bl-ANF fragment
                 (footprint ∅ ∧ terminating ∧ sorts ⊆ nif-sorts) to it.
   MLIR-readiness requirements on bl-ANF shape (design so as not to preclude):
     - SSA/named: every intermediate has a name (ANF gives this).
     - per-node annotation slot `:ann` (types from typed, effects from footprint,
       source pos) — mirrors cerl:set_ann; MLIR attributes map onto it.
     - structural regions: let-nesting = MLIR region nesting; case = a region-op.
   Do NOT build the MLIR attrs now; only keep the node shape annotation-carrying.

** Sequencing (each step independently green against the oracle)

S-A. Graduate CE1 lowering → priv/self/core.bl (short path): lower/defs->module/
     server->module/core-defvar/eval-core minus gates. Wire a backend switch
     (Env key :backend ∈ {:elixir,:core}) at Compiler.eval_form + Link.defvar.
     Gate: mix beam_lisp.test under :core = green. This makes Core a REAL,
     default-able backend reading quoted — Elixir compiler still present but
     bypassable. LOW RISK, proven by CE1.
S-B. bl-ANF vocabulary + normaliser (priv/self/anf.bl) + reference interpreter +
     corpus gate (anf of every prelude form; interp value == running value).
     The neutral IR now EXISTS — PLAN-080 can start lowering it to MLIR/LLVM.
S-C. cerl.bl reads bl-ANF (not quoted): re-point the S-A lowering at bl-ANF.
     Gate: value-identity oracle unchanged.
S-D. compiler.bl emits bl-ANF directly (delete ast-node/quoted constructors).
     Gate: value-identity oracle; census 0 rejected; then Core = DEFAULT.
S-E. Delete role-A Elixir from the compile path (Module.create/eval_quoted seam);
     keep reader.ex/compiler.ex ONLY as the genesis+oracle (per PLAN-057 R3).

Original W1–W7 below remain the content; S-A..S-E is the risk-ordered path to
them with the two new constraints. W1≈S-B, W2≈S-C, W3=oracle (spans), W6=S-D/S-E.
