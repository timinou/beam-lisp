#+TITLE: Native execution review + JIT fragment probe (P0)
#+STATE: ITEM
#+SESSION_ID: 1570167797c6d81f
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-02T17-22-15-263Z_1570167797c6d81f.jsonl]]
#+CUSTOM_ID: PLAN-080-native-execution-review-jit-fragment-pro

* Initial Message

look at @docs/memory-policy/05-proof-directed-native-offload.bl.md and tell me how Rust can read the anf IR and what it could do within the language and what kind of beam-lisp could be lowered that way safely and with the guarantees of beam lisp/the beam

* Goal

Review verdict of the "LLVM JIT for beam-lisp" proposal against repo evidence, and the first prototype (P0) it implies. Anchors: PLAN-042 (datalog native, REVIEW, implemented on worktree branch plan-042-datalog-native), PLAN-002 (loom-shell pure-bl Wayland bar, DOING), PLAN-079 (bl-ANF IR, ITEM), docs/memory-policy/05-proof-directed-native-offload.bl.md, research spike native/datom_datalog (61-160x measured).

* Review verdicts

** Wrong
1. Wayland conflated with wry/WebView. The wayland work is loom-shell: PURE bl renderer writing ARGB frames to a wl_shm buffer (wayland_shm crate = memfd/pwrite only). Its hot loops (glyph blit, alpha blend, tree-diff to pixels) ARE doc-05 fragment shape (loops over binaries, provable bounds). JIT applies DIRECTLY to loom-shell's renderer, unlike wry. "JIT is the wrong tool for wayland" was drawn from the wrong module.
2. "Intern values to i64 + dictionary" contradicts the decided storage design: PLAN-042 chose typed-lane columnar value slots (zero-copy UTF-8, term lane for EDN), implemented. Interning remains valid ONLY for a native recursive-rules wire (spike), not for datom storage.
3. BeamAsm "0-130%" is interpreter-to-JIT headroom; bl already runs on BeamAsm. Real headroom is boxed-to-unboxed = spike-measured 61-160x.
4. "Engine in bl over columns fits the fragment once columns exist" — false: fragment has no growable local buffers/hash tables. Needs a fragment extension (local scratch allocation, purity-compatible since unshared native memory is not a VM resource, but new proof obligations).

** Overcomplicated
5. Plan quietly replaced PLAN-042 (implemented, in REVIEW) with a longer JIT path. Datom's next win = land PLAN-042, then decide on native semi-naive rounds for recursive rules. JIT is orthogonal.
6. Dual-tier (cranelift + LLVM) from day one — pick one backend first.
7. "Polls widen the eligible set" — automates chunking; no new guarantee class.
8. doc 05's 5-50x is a PREDICTION; only measured native numbers in-repo: 61-160x (recursive datalog spike), 37-100x decode + 9.4x join (PLAN-042). JIT numbers: none.

** Survives
J1/J2/J3 taxonomy (reject J2 fork, J3 new runtime) · theorems are backend-independent · REPL-pace is the JIT's real differentiator — AND IT IS UNMEASURED, making it the first thing a prototype must test · no statepoints needed for the fragment · crates stay for systems.

* P0 — JIT fragment probe (this prototype)

Standalone Rust (research/j1_probe), no bl plumbing. Defines the bl-ANF node vocabulary as a Rust enum and lowers through cranelift, so when bl-ANF (PLAN-079) lands the lowering plugs in.

Kernels, chosen from the named beneficiaries:
- checksum over 64 MiB (doc 05's own dirty-cpu example): acc = acc*31 + byte
- const-alpha blend over two 16 MiB buffers (loom-shell glyph composite): d[i] = (s*a + d*(255-a))/255

Measured: (a) compile ms (median of 31 fresh module builds) = the REPL-pace claim; (b) exec JIT vs handwritten Rust (median of 21) = codegen-quality claim.

Acceptance gates: compile <= ~50 ms median per kernel; exec <= 1.5x hand Rust. Pass => JIT backend is viable for the doc-05 fragment (vs gen.rs+cargo AOT). Fail => cargo AOT at def-time wins for static defns; JIT only earns its complexity for query-shaped programs.

* Follow-ups (ordered)

1. Land PLAN-042 (in REVIEW) — independent of JIT.
2. P0 verdict -> choose fragment backend for doc 05.
3. If wayland route is taken: JIT or hand-NIF the blend/blit kernel; protocol/event machinery stays pure bl (already right).
4. Fragment extension design: local scratch allocation (needed by any engine-in-bl path).
5. Native semi-naive rounds (chunked, cancel flag) for recursive rules — spike productization, wire = typed lanes not interning.

NOTE [2026-09-02]: P0 probe RAN (research/j1_probe, cranelift 0.116.1). Measured: compile 0.04-0.05ms median per kernel (gate <=50ms: PASS by 1000x — REPL-pace native confirmed; cargo AOT is ~5 orders slower per defn). Exec vs rustc twin: checksum 1.29x (PASS), blend 5.89x (FAIL). Blend gap decomposed by measurement: literal udiv-255 = 18.0x slower; strength reduction (x*0x8081)>>23 = 5.9x; remainder is rustc auto-vectorization. VERDICT: JIT backend viable (compile-pace landslide); cranelift alone insufficient for blend-class kernels (loom-shell renderer path) — LLVM opt tier or fragment-owned peephole required. Probe IR = bl-ANF node vocabulary, plugs into PLAN-079 when it lands.

NOTE [2026-09-03]: lowerers_spike COMPLETE (research/lowerers_spike + j1_probe --run-ir). L-IR lowerer: quoted (REAL compiler output) → j1 IR in bl, ANF fused into the walk, ~1-2ms lowering pace on the BEAM. Value oracle: JIT(bl-lowered IR) == rustc ref == 8383099488808122916 over the same 1 MiB LCG bytes; bl-on-BEAM baseline 135 ns/byte vs JIT 1.1-1.7 ns/byte = 76-120x (spike band 61-160x confirmed on an isolated kernel). Refusals demonstrated: :impure (dot-product/nth), :not-shrinking (parse-header recur buf), :not-a-counted-loop (ackermann) — total structural predicates, decidability by construction. Widening experiment: cranelift blendW8 4.72x vs scalar 4.90x — manual IR widening buys ~nothing; SIMD is the residual gap ⇒ LLVM tier needed only for pixel-class kernels (loom-shell renderer), cranelift sufficient for checksum-class. Encoding finding for PLAN-079: compiler/compile returns bl-DATA with EVERY tuple {:"{}" [] [k m args]}-wrapped (outermost real), module calls as :apply nodes, recur loop-var threaded through the callee position.

NOTE [2026-09-03]: PIXEL-CLASS EXTENSION COMPLETE (user ask: "extend lowerable patterns to at least pixel-class"). Approach = extend the SHIPPED proof engines (not hand-roll), then delegate. TWO additive engine completions: (1) system.footprint pure-ops += binary/at + byte/bit ops (immutable-binary index = pure, value semantics of nth); checksum/blend now infer {} not {:opaque-world :W}. (2) termination check-loop: replaced ∀-vars-shrink with ∃-ranking (Floyd) + count-UP measure — added measure-dir (:down|:up|nil) and upper-bounded-vars (harvests (< v E)/(> E v) guards); count-up bounded loops now provably terminate. Both verified: all accept/reject cases pass, impure ops still caught. NEW lowerer lower2.bl (~260 lines): reads the READER-NODE tree (same clean tree the engines read, no {:"{}"} wrapping), routes eligibility through check-pure!→footprint/pure? and check-terminates!→termination/check-loop, refusals cite engine verdict + doc-05 theorem. Lowers TWO shapes: fold (acc init 0 → ret) and STORE (acc init [] via (conj out EXPR) → native store loop). FRAME-RULE INSIGHT: bl binaries immutable so pixel idiom is functional (conj into fresh out []); footprint/frame-independent? proves the {out:W} disjoint from input reads → observationally pure → lowers to in-place store with no aliasing hazard. MEASURED: blend 1 MiB JIT 2.0 ns/byte vs bl-on-BEAM 290 ns/byte = 145×, output BYTE-IDENTICAL to BEAM ref AND rust twin. Also value-verified invert (out=255-src). Fragment GENERALISES: scale/brighten/invert/bdot all accept unchanged. JIT bug fixed: parser aliased "sub"→Add (subtraction did addition!) — added real Stm::Sub(isub) + store/udivimm/subimm/zext IR ops + --run-store mode (3/4/5-param). Frontier doc: research/lowerers_spike/INSIGHTS.md. Remaining widening (decidable, ranked): tuples/multi-channel, min/max clamp (smt already lowers to ite), nested loops (lexicographic measure); SIMD is the only real LLVM-tier need. Toolchain note: ./bl escript embeds FROZEN stdlib — iterate via (env/eval (File/read edited-source)); mix escript.build ~20min under memory pressure (load 20).