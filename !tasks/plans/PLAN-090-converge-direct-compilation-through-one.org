#+TITLE: Converge direct compilation through one module contract and executable editing assessment
#+STATE: DOING
#+SESSION_ID: 15729b4fce888940
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-04T16-19-37-402Z_15729b4fce888940.jsonl]]
#+CUSTOM_ID: PLAN-090-converge-direct-compilation-through-one
#+COMPLETION_HASH: ff2a44b7b8e0aaa8

* User request and deliverables
Turn the compiler convergence assessment into zero-context implementation instructions in FEAT/BUG/PLAN files, then investigate structural editing in beam-lisp and /home/user/code/undefine/spell and describe a runnable .bl.md-centered coding conversation. This parent tracks preparation and assessment only. Its completion does NOT mean compiler implementation or the proposed editing companion is complete.

* Plain-language architecture
Beam Lisp currently translates language forms into its own intermediate representation (bl-ANF) and lowers that to Core Erlang, which Erlang compiles to BEAM executable code. Some surrounding generated modules still pass through Elixir syntax trees. The goal is one complete module emitter, not merely ANF function bodies. Generated forwarders, initialization, macro replay, behavior attributes and provenance are part of that contract. Existing Elixir host implementation modules may remain; the forbidden dependency is using Elixir syntax/compiler paths to generate Beam Lisp programs.

* Preserve the verified progress
Committed E2 is 440fb3b. Current E3 work in compiler2.bl/lower.bl/tests is uncommitted and useful. Do not restart or discard it. Other sessions own aot_cache.ex/prelude_interface.ex and may change adjacent files. Inspect current git state and coordinate ownership before editing. Historical test totals and old PLAN comments are not fresh evidence. The new work replaces PLAN-086's remaining feature-by-feature sequencing, not its original one-IR/one-backend goal.

* Why the previous sequence stalls
The producer changes representation before every consumer does. AOT emit_module constructs quoted bodies before backend selection and still compiles shims/init/provenance via Elixir. Runtime lower has parallel old/new/mixed builders. Boot generations can leave mixed representations in a namespace. Consequently tests expose a new seam after each local repair. Language behavior (notably local/global call selection) and lazy memo semantics have also shifted during the migration. Fix the full module boundary, pin semantics and generation, then retire routes one by one.

* Executable work graph
1. [[id:FEAT-036-pin-compiler-generation-and-characterize][Generation identity and semantic characterization]]. Establish actual running-generation receipts and a behavior baseline. No incidental semantic redesign.
2. [[id:FEAT-037-define-and-validate-one-complete-anf-mod][Canonical complete module contract]]. Depends on 1. One schema and validator, one emitter, one temporary bootstrap import boundary if necessary. Adopt current :params spelling consistently.
3. [[id:FEAT-038-route-live-and-disk-compilation-through][Complete vertical source-to-module slice]]. Depends on 2. Live evaluation, redefinition, AOT/fresh load and bootstrap share emission, including initialization and provenance.
4. [[id:PLAN-091-complete-generated-forms-and-retire-ever][Remaining generated forms and deletion]]. Depends on 3. Each syntax family migrates end-to-end, then its legacy route disappears. Final seed/full-suite/reproducibility gates.
Parallel runtime correctness: [[id:BUG-028-lazy-memo-eviction-replays-effects-and-c][Lazy memo replay and concurrent force]]. Preserve at-most-once successful realization; do not promise transparent GC that ETS cannot provide.
Parallel compiler retention after 2: [[id:FEAT-039-make-finite-compiler-transformations-eag][Eager finite compiler data]]. Coordinate lower.bl edits with 2/3; report compiler temporary retention separately from executable module retention.
Final closure of PLAN-086 requires all six child items complete, including correctness and retention proof. Runtime correctness may proceed independently; shared-file compiler changes must serialize. Do not assign an entire package to a worker: inspect owners, then dispatch 3–5 exact files per implementation task.

* Decisions implementers must not reopen implicitly
- One ANF/Core emitter for all generated Beam Lisp modules; no new reverse bridge as an AOT fix.
- Keep existing module topology where needed for closure survival; one emitter does not mean one module.
- Preserve currently implemented local/global dispatch during cutover. A different language rule needs a separately explicit decision.
- Use strict tagged data validation, not is_map as a type check. Tuples inside literal values are legitimate.
- Legacy seed input is converted once at import, never accepted throughout the backend.
- Remove compiler-owned lazy intermediates, not user laziness. Memory pressure cannot authorize replay of effects.
- Hotpatch is an inner-loop optimization, not a fresh-bootstrap proof. Reuse the current harness, do not regenerate seed every iteration.

* Acceptance and evidence policy
Each child includes behavior and failure tests, targets, non-goals and completion criteria. Before committing a milestone, run focused tests followed by the relevant whole-system gate. Capture command, source revision/diff identity, loaded compiler receipt, exit status, actual test counts, skips and reasons, memory cap and OOM status. Preserve live functionality tests. User requested pre-existing failures discovered along the way be repaired, not hidden. Environment-specific tests have a separately recorded enabled-environment gate; a skip is not a pass. No claims of performance improvement without comparable repeated measurements.

* Operational safety
Serialize mix processes. Run memory-risk commands asynchronously via systemd-run --user --scope with MemoryMax and MemorySwapMax, captured output and an identifiable unit; join rather than polling. Inspect unit journal and leftovers. No git stash/reset/revert/checkout/clean to discard work. Use structural edits and preserve unrelated changes. Follow docs/AGENTS.md before documentation changes. Update child status and attach evidence as work lands; this planning task itself performs no compiler modifications.

* Second deliverable: executable editing assessment
Read existing bl.doc literate execution, structural editing and transaction APIs in both repos. Ground the proposed conversation in exact source APIs. Distinguish result-span updates, source-file commits and runtime hot reload: they are different transactions. A .bl.md artifact is executable code, not inherently sandboxed or safe. Prototype only in isolated fixtures unless explicitly authorized; preview must not write source, application must reject stale inputs, and a failed verification must not blindly overwrite concurrent edits. Record gaps as a separate feature, not imaginary working API.

* Executable editing deliverable and evidence
[[id:FEAT-040-executable-bl-md-change-proposals-with-g][FEAT-040]] specifies the proposed local dev.change helper and later Spell integration. Actual runnable proof: [[file:../artifacts/literate-edit-session.bl.org][literate-edit-session.bl.org]], target [[file:../artifacts/literate-edit-fixture.bl][literate-edit-fixture.bl]]. Both runs used the installed /home/user/.local/bin/bl (binary file dated 2026-09-02), not the current uncommitted compiler. Commands: systemd-run --user --scope -p MemoryMax=4G -p MemorySwapMax=1G --collect --unit bl-literate-edit-poc-{1,2} timeout 180 /home/user/.local/bin/bl doc run '!tasks/artifacts/literate-edit-session.bl.md'. Each run reported 1 cell, 0 errors. Eight explicit checks covered source basis, exact rewrite, unchanged preview, behavior, pre-write basis, final bytes and repeat no-op. The first run changed the fixture using existing deodorant/fix-source; the second preserved fixture and complete document bytes (compared directly). The document contains the runner-owned verified result. Journal peaks: 125.6M and 131.7M; no OOM indication; both scopes collected with zero remaining units. This is a real single-user source-edit demonstration, not proof of proposed conflict/crash-safe APIs. No production compiler source changed for this task.

NOTE [2026-09-06]: Independent read-only review identified underspecified module schema, bootstrap importer boundary, initialization/publication failure semantics, lazy force attempt transitions and compiler memo lifetime. These are now binding sections in FEAT-037, FEAT-038, BUG-028 and FEAT-039. In particular, source/image/registry effects are not falsely described as one atomic transaction; normal lazy thunk exceptions retain explicit retry semantics while ambiguous owner death is terminal. FEAT-040 owns the formerly separate editing integration scope. Org validation scanned the entire repository despite an id argument and reported 9 existing missing-CUSTOM_ID errors in PLAN-031/039/081/085 and FEAT-035; none in newly created instructions. Those unrelated/concurrently owned files were not changed.

NOTE [2026-09-06]: Preparation complete: seven implementation children (FEAT-036..040, BUG-028, PLAN-091), linked replacement sequencing for PLAN-086, reviewed binding contracts, two-repository editing assessment, and a twice-executed .bl.md source-edit proof with byte-identical rerun. Child items remain ITEM: compiler convergence and the guarded editing companion are not claimed implemented. Final in-chat walkthrough accompanies the artifacts.

NOTE [2026-09-06]: User authorizes implementation of ALL child tasks and incremental commits. Default executable change artifact is now .bl.org with Babel RESULTS boundaries; .bl.md remains accepted for feature guides. Reopened parent to track implementation, not just assessment. Preserve inherited/concurrent changes; commit explicit owned file sets at verified milestones. Implement FEAT-036..040, BUG-028 and PLAN-091, closing PLAN-086 only after final gates.