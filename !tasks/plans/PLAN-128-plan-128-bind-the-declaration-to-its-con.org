#+TITLE: PLAN-128 — bind the declaration to its consumer, or the wrong copy runs
#+STATE: DOING
#+SESSION_ID: 15839d044a6586d8
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-17T21-21-15-049Z_15839d044a6586d8.jsonl]]
#+CUSTOM_ID: PLAN-128-plan-128-bind-the-declaration-to-its-con

* Initial Message

can you explain what got done and what you discovered?

#+TITLE: PLAN-128 — bind the declaration to its consumer, or the wrong copy runs

* The finding

Six of this session's defects are **one defect**: a single source of truth was
restated somewhere else, and the restatement drifted. Not "similar bugs" — the
same bug, six times.

| restated fact | where it was declared | where it was restated (wrongly) | defect |
|---|---|---|---|
| the artefact's installed name | `vm.native/installed-path` | `build.bl` literal `(str crate ".so")` | BUG-070 |
| how to install without disturbing a mapping | `vm.native/build!` (rename) | `build.bl` `File/cp!` | BUG-074 |
| how to prune beams for gone files | `build-ordinary` | `build-substrate` (absent) | BUG-067 |
| the daemon readiness handshake | `tooling/drop/src/daemon.rs` | `launcher.rs` (a second copy) | BUG-066 |
| what the native key hashes | `native-key`'s docstring ("plus the cargo version") | the code, which hashed `"cargo"` | key bug |
| the loader's path | `vm.native/installed-path` | `native_test.bl`'s literal assertion | BUG-070 fallout |

The last row is the tell. A **test** restated the path — so when BUG-070 moved
the truth, the test kept asserting the lie, and went red behind the fix.

* Why it keeps happening

The contracts in `vm/native.bl` are already well named: `host-module` is
documented as "THE rustler::init! contract", `lib-path` as "the path
`erlang/load_nif` wants", `installed-path` as "Where the built .so lives". The
declaration is not the problem.

**Nothing binds a declaration to its consumer.** A name in a docstring and a
literal in another module look equally authoritative to a reader, and only one
of them runs. `build.bl` does not require `vm.native`, so it could not have used
the contract even if its author wanted to — the module boundary made the
duplication the path of least resistance.

* The four classes

  A. re-derivation — a fact named once, restated elsewhere. Six instances above.
  B. unbound declaration — a contract is stated; nothing checks observance.
     (`defnative` declares a boundary; nothing asserts the install matches it.)
  C. the wrong artifact measured — tests assert a restated path; a build is
     verified against a dirty tree, not the commit a release is cut from.
  D. out-of-band death — a boundary kills the VM with no stack (BUG-074).

* The program

Each fix must be **enforceable by a test**, or it is documentation that will rot
the same way.

**1. One name, one module — and the consumer requires it.**
`vm.native` gains a root-aware path constructor; `installed-path/1` delegates to
it. `build.bl` requires `vm.native` and calls it. The build can then no longer
disagree with the loader, because it asks the loader's module. Delete
`build.bl`'s literals — do not keep them "in sync".
*Kills:* BUG-070's class. *Proves it:* the test asserts via the same function.

**2. A gate that fails on a restated contract.**
A test in the shape of the existing `sunset_gate_test`: `"priv/native"` and
`".so"` may be constructed only in `vm/native.bl`. Any other module naming the
artefact layout fails the gate.
*Kills:* the whole class, not one instance — including the next one nobody has
written yet.

**3. Tests derive, never restate.**
An assertion about a path calls the function that computes it. An assertion
about a name calls the function that names it. Where a test must hardcode
something, that hardcoding is a finding, not a convenience.
*Kills:* BUG-070 fallout, structurally.

**4. The release is a value, not a ritual.**
`bl release` computes and cross-checks: the tag, the changelog, the artefact
hash, and the set of fixes it claims to ship — asserting every fix is an
**ancestor of the tag**, and that the artefact was built from a clean tree at
that commit. PLAN-127's landmine (a tag 35 commits stale, silently rebuilding a
fixed bug) is precisely a missing assertion.
*Kills:* the stale-tag class.

**5. Verify the artefact, not the tree.**
A release gate builds in a clean worktree at the tag and runs the produced drop
from a directory with no beam-lisp sources. This is already how the drop smoke
test works; it was not how the fix was verified.
*Kills:* class C.

**6. Instrument the declared boundaries.**
`defnative` already declares every point where beam-lisp can die without a
stack. Extend the declaration to carry the recorder, so the instrumentation
travels with the boundary instead of being an env var a human must remember.
`BeamLisp.NifTrace` exists; it needs attaching.
*Kills:* class D's diagnosis cost.

* Sequencing

1 and 3 first — small, and they remove the class that produced most of this
session. 2 immediately after, because it is what makes 1 and 3 permanent.
4 and 5 before the next release attempt; they are release-machinery, not
language work. 6 is independent and can land whenever.

* Not in scope

The pre-existing non-green suites (`code.semantic`, `browser/kernel`,
`loom/parts`) are unrelated to this class and are recorded on PLAN-127.

Evidence for every row of the table, with commands and observed output, is on
BUG-074-bug-071-bl-build-segfaults-a-null-call-i and in the commits named there.

* CORRECTION (same day): the barrier decides which fix is available

Fix 1 said "delete the second copy — do not keep them in sync". Attempting it
proved it impossible for this contract, and the attempt is the evidence.

Making `build.bl` require `vm.native` was **refused by the build**:

    beam-lisp AOT: toolchain source priv/build/build.bl requires ordinary
                   source priv/std/vm/native.bl

`partition-plan` defines toolchain as `priv/boot/` + `priv/build/` and rejects a
toolchain source requiring an ordinary one. That is sound — the driver must not
depend on what it builds. The other two directions are closed as well: the
runtime must not require a build source (a drop ships no `priv/build/`), and
`priv/boot/` is codegen, where a NIF filename is a category error.

**So a contract that spans the toolchain/ordinary barrier provably cannot have
a single definition.** The barrier did not cause the duplication as an accident;
it made the duplication the only available shape. The choice is between a copy
and a copy that is *checked*.

∴ **For barrier-crossing contracts the fix is a CHECK, not a require.**
Landing today as `c57a8570`: `vm.native/artifact-name` (loader's half),
`build/nif-artifact-name` (installer's half, carrying the barrier in its
docstring), and `test.native/the-installer-and-the-loader-agree-on-the-name`
(the gate). Proven to fail rather than asserted to pass — diverging the two
deliberately fails 6 assertions.

* What this changes about the program

- Fix 1 is narrowed: **collapse duplicates only within a layer.** Across the
  barrier, gate them. Anything else is either an illegal require or a category
  error.
- Fix 2 is promoted from a nicety to **the only available enforcement** for
  this whole class, and should be written as a general gate over
  contract-bearing literals rather than one fact at a time.
- The 6 rows in the finding table must be split by this rule: which restate
  something in a *requirable* layer (collapse them), and which cross the
  barrier (gate them). BUG-074's install rule and BUG-067's pruning are
  same-layer and collapse for real; BUG-070's name and BUG-066's handshake
  cross a boundary and need gates.

* Worth recording about the barrier itself

The build system already had the enforcement machinery — it just had it pointed
at *imports* and not at *duplication*. `partition-plan` proves the project is
willing to refuse a build over a layering violation. The same willingness,
pointed at a restated contract, is Fix 2.

* CORRECTION 2: I had row 1 wrong, and the split is now verified not asserted

The first correction said "BUG-074's install rule ... same-layer, collapses for
real". **That was wrong**, and checking each row against the source is what
showed it. Verified split:

| row | both sides live in | verdict |
|---|---|---|
| BUG-070 artefact name | `vm/native.bl` vs `build.bl` | **barrier → GATE** (landed, `c57a8570`) |
| BUG-074 install rule | `vm/native.bl` vs `build.bl` | **barrier → GATE** (was wrongly called collapse) |
| BUG-067 gone-file pruning | `build-ordinary` + `build-substrate`, **both in `build.bl`** | same file → collapse |
| BUG-066 handshake | `hello` + `daemon_ready`, **both in `daemon.rs`** | already collapsed |
| native-key toolchain | docstring vs body, same function | already collapsed |
| BUG-070 fallout test | restated the path | collapsed by deriving (`ba3987ff`) |

So **two** of the six cross the barrier, not one — and the one I mis-filed is the
most dangerous of them, because it is the bug that segfaulted the build.

* The install gate — landed, `56e98e61`

`BUG-074`'s invariant is "installing an artefact REPLACES the file; it never
rewrites it in place". Both installers sit on opposite sides of the barrier, so
the rule is pinned as a **behaviour**: the inode must change on a reinstall.

The observable was verified before being relied on, against `stat`:
`cp` over an existing file leaves the inode alone; temp-then-rename gives a new
one. In-place rewrite keeping the inode *is* the thing that killed the VM.

**Proven to bite**: reverting `install-artifact!` to `File/cp! src dest` fails
the test with a message naming BUG-074.

* Two errors of mine, both while building the gate

  1. The first version changed `{ 42 }` to `{ 43 }` and expected new bytes. It
     produced a **byte-identical cdylib** — same md5 from two different keys —
     so the skip branch fired, correctly, and the inode rightly did not move.
     The test was wrong, not the code. I only found this by inspecting the
     store, not by reasoning about it.
  2. Adding an export did not help: the test crate has no `#[no_mangle]`, so a
     plain `pub fn` is mangled and dropped and the linker emits identical bytes
     whatever the body says. `setup!` now emits `#[no_mangle] extern "C"`.

Both are the same lesson the closure reminder keeps making: a self-authored test
is evidence of coverage, not of correctness — and here the coverage was wrong
twice in a row while the assertions looked reasonable.

* Coverage gap, stated rather than implied

The gate exercises the BUILD's installer. The runtime's (`vm.native/build!`) is
the same two-step shape but installs into the live app's priv, which a test
cannot retarget at a temp root — so it is asserted by inspection, not by
execution. A gate that covered both would need `build!` to take a destination.

* Fix 2 is now the load-bearing one

Two of six rows cross the barrier, so the general gate over contract-bearing
literals is not a nice-to-have; it is the only enforcement available for a third
of this class. That is the next piece of work.

* CORRECTION 3: "provably cannot" was wrong — it is possible, but the direction is wrong

Corrections 1 and 2 said the contract **provably cannot** have a single
definition. That is false, and it was false for two reasons I had not checked.

**1. A drop DOES ship `priv/build/`.** I asserted it did not. It does —
`.bl/lib/beam_lisp-0.1.0/priv/build/` carries `build.bl`, `build-plan.bl`,
`claim.bl` and the rest, and the packer has no exclusion list for it. The claim
was load-bearing (it was half my argument) and I had not looked.

**2. The barrier is not symmetric.** Read properly, `partition-plan` iterates
`tc` — the toolchain sources — and only asks whether *they* require something
outside the toolchain:

    (map (fn [n] ...) tc)   ; only toolchain sources are examined

So `toolchain → ordinary` is rejected, and **`ordinary → toolchain` is never
checked at all**. A contract in `priv/build/` would be requirable by
`priv/std/vm/native.bl`. The single source is available.

* So why keep the gate

Because the dependency direction is wrong, not because the language forbids it.
`vm.native` is runtime substrate: every native-backed library declares through
it at runtime. `priv/build/` is the driver that *produces* the runtime. Making
the substrate depend on its own producer is the mirror of what the barrier
already forbids — the driver must not depend on what it builds, and the runtime
should not depend on what builds it. A drop that trims the build driver would
take the loader down with it.

The gate is therefore a **choice with a reason**, not a necessity. That is a
weaker claim than the plan made, and the honest one.

* What the probe cost and taught

I wired `vm.native` to a new `priv/build/nif.bl` to test this live. It failed —
`namespace not found: build.nif` — **on staging, not on the barrier**: the
runtime resolved the require from `.bl/lib/.../priv/build`, not the source tree,
so a new `.bl` in the build layer is invisible until the image is re-staged. The
experiment did not settle the barrier question; reading the barrier code did.
Note the operational cost it exposed: adding a file to `priv/build/` breaks the
build until re-staged, which is itself an argument for not putting a two-line
contract there.

Reverted cleanly — `priv/build/nif.bl` removed, `vm/native.bl` byte-identical to
HEAD, suite and build green.

* What stands

The gate is right and proven to bite. The claim "impossible" is withdrawn and
replaced by "possible, deliberately declined, here is why". The rows that
collapse remain collapsed; the two native contracts remain gated.

* The asymmetry is now OBSERVED, not read

Correction 3 rested on reading `partition-plan`'s source, and I said so — the
live probe had failed on staging and settled nothing. A claim that load-bearing
should not sit on a reading.

Now pinned and executed, `ae031fb1`, in `test/bl/build_test.bl`:

    the-toolchain-barrier-is-one-directional
      toolchain source requiring an ordinary one  -> :errors NON-empty
      ordinary source requiring a toolchain one   -> :errors EMPTY

Observed: `build.test 39 passed`. The two assertions constrain each other — the
first requires the check to fire, the second requires it not to — so a constant
cannot satisfy both and the test discriminates rather than passing vacuously.
Paths are real locations under the live priv root because `toolchain_source?`
decides by inode-beneath (`boot_source? or build_source?`), not by string prefix.

If anyone symmetrises the barrier, this goes red — and the two docstrings and
the reasoning above become false with it, which is exactly when someone should
be told.

* What is still inference, plainly

  - The runtime's installer (`vm.native/build!`) is asserted by inspection, not
    execution: it installs into the live app's priv, which a test cannot
    retarget at a temp root. Closing it needs `build!` to take a destination.
  - Whether an unpacked RELEASE drop ships `priv/build/` — I checked the staged
    image and the packer, not a release artifact.
  - The barrier test bites by construction (mutually-constraining assertions),
    not by a live break-test the way the other two gates were proven.