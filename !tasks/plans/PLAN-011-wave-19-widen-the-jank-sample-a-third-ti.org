#+TITLE: Wave 19: widen the jank sample a third time, and close the last two
#+STATE: DONE
#+SESSION_ID: 154d7065511fb2de
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-08-06T19-28-19-268Z_154d7065511fb2de.jsonl]]
#+CUSTOM_ID: PLAN-011-wave-19-widen-the-jank-sample-a-third-ti
#+COMPLETION_HASH: 99870d515c35d9bc

* Initial Message

do as much of the recorded PLANs as you can, maximally leveraging subagents and running efficiently. keep recording your work in examples and documentation

** Goal
Continue the fidelity programme that has driven every recent wave: measure beam-lisp against jank's real `core.jank`, let the gap list rank the work, build the top of the list, re-measure.

Current state: **62 of 64 attempted slices load and behave**. Trajectory 7/21 → 13/21 → 21/21 → 36/64 → 38/64 → 62/64.

** Why widen rather than finish
A sample that nearly all passes has stopped being informative — exactly the reasoning that drove wave 16 (which tripled the sample from 21 to 64 and correctly dropped the score to 36). Both previous widenings paid for themselves in real bugs found. See `docs/jank-compat.md`, section "What to build next".

** Task 1 — widen (primary)
Vendor the next tranche from `core.jank` at the SAME commit `30285949933065417c6311a91902b7866ab60f87` (internal consistency: every existing fixture header pins that commit). Target areas deliberately chosen to be harder than the current sample:
- transducers and the `reduce`/`into`/`transduce` family
- `volatile!`/`vswap!`, `reduced`, `cat`
- the arithmetic and bit-operation layer
- `deftype`/`defrecord`/protocol users
- anything whose header uses `^{}` metadata (currently unreadable — that is a legitimate reader-level FAIL and a useful finding)

Method is established and must be followed exactly (`docs/jank-compat.md`, "Method"): copy verbatim with a provenance header (URL, commit, upstream line range, sha256 of the code portion), load in a throwaway ns, then CALL with upstream's own docstring examples. **Load ≠ behave.** Never edit a fixture to make it pass; a checksum test asserts they are byte-for-byte upstream.

Expect the score to fall. That drop is the deliverable.

** Task 2 — `& [pattern]`, the last real compiler gap
`for` (slice 44) is blocked by a rest argument that is itself a destructuring pattern:
: [[[bind expr & mod-pairs] & [[_ next-expr] :as next-groups]]]
`split_variadic/1` in `lib/beam_lisp/compiler.ex` requires a bare symbol after `&` ("& in params must be followed by exactly one symbol"). The destructuring machinery it needs already exists — `destructure_steps/3` handles vectors, maps, `:as`, and nesting. This is plumbing: bind the rest to a fresh temp, then run the pattern against that temp.

Watch: the exhausted-rest case must still bind **nil** (Clojure semantics, fixed in an earlier wave and load-bearing for every recursive upstream fn), so a destructured empty rest must destructure nil leniently rather than raise.

** Not in scope
`with-open` (slice 60) is an upstream TODO stub whose body is commented out — it throws by construction and cannot pass anywhere, including in jank. It stays a recorded failure. Do not special-case it.

** Files
`test/fixtures/jank/**` (new slices), `test/beam_lisp/jank_compat_test.exs`, `docs/jank-compat.md`, `examples/jank_slice.bl`; for task 2, `lib/beam_lisp/compiler.ex` + a test file.

** Acceptance
- `mix test` green; `mix beam_lisp.test` exit 0; every `examples/*.bl` exits 0; `mix compile --warnings-as-errors` clean.
- `docs/jank-compat.md` reports **N of M** over the widened sample, every new failure classified by the existing taxonomy, build-next re-ranked by unlock count.
- Any newly-behaving slice is in `@accepted` with its checksum AND a behavioral test calling it with upstream's own examples.
- `for` behaves, or the precise reason it still does not is recorded.

** Standing lesson (read before starting)
Every wave of this programme has found runtime bugs that beam-lisp's own tests missed — eight so far, two silent and one a hang. The recurring root cause is that **a struct is a map on the BEAM**, so any `when is_map(x)` clause silently swallows `%Vector{}`, `%LazySeq{}`, `%Set{}` and `%Transient{}`. When adding any new collection type, audit every `is_map` guard in `lib/beam_lisp/rt.ex` and put the struct clause first.

NOTE [2026-08-07]: DONE. Both tasks shipped, and the widening did what widenings are for.

TASK 1 — WIDEN (done, twice over)
Sample doubled: 64 → 120 slices, vendored verbatim from core.jank@3028594,
sha256-guarded, none edited. Trajectory:

  7 → 13 → 21 → 36 → 38 → 62 → 63 → 89 → 109 → **115 of 120**

The score fell to 89 on widening exactly as predicted, then climbed as the
refilled gap list was worked. Of the five that remain, **four are upstream TODO
stubs** whose bodies are commented out (with-open, instance?, rseq,
unchecked-inc-int) — they throw by construction and cannot pass in jank either.
The fifth is `into`, recorded as partial and deliberately NOT promoted.

TASK 2 — `& [pattern]` (done)
A rest argument may now be any destructuring pattern. The load-bearing edge
held: an exhausted rest still binds **nil** leniently rather than raising,
which every recursive variadic fn in upstream jank depends on at its base case.
A map in rest position raises a clear error instead of mis-binding every key.

Slice 44 `for` behaves fully — nested bindings, `:when` modifiers, lazy over an
unbounded range. It cost **three unrelated bugs**, which is the finding: the
rest-pattern gap, a lazy-seq thunk returning a bare LazySeq (crashing the seq
walk), and `when-first` simply missing from the prelude. None would have
surfaced by reasoning about the compiler.

THE PREDICTION LOOP — the most valuable thing this plan produced
Each measurement now ends with a prediction-vs-outcome check, because a ranked
gap list that is never checked against results is astrology. Checking it was
uncomfortable and therefore useful:
- The cpp/* shim, reader `^{}`, and volatile! predictions landed.
- `take-nth`'s real blocker was `rem`, not volatile! — mis-attributed.
- The small-core-gaps backlog (transientable?/reduce-kv/rem/float?) had been
  ranked in a previous round and **simply never built**. The doc now says that
  out loud rather than quietly re-listing it. It has since been built, and it
  closed all 7 remaining genuine failures.

REAL BUGS FOUND BY RUNNING SOMEONE ELSE'S CODE (this wave alone)
- `interleave` crashed on lazy infinite input — hidden because the accepted
  interleave slice only ever saw finite lists. This is the argument for
  widening in one sentence: a passing test proves the case it tests.
- The lazy-seq bare-thunk invariant (two workers hit it independently).
- An unresolved qualified name silently compiling to a phantom module handle.

NOT IN SCOPE, CORRECTLY: `with-open` stays a recorded failure. It was never
special-cased.

GATES: compile clean · `mix test` 767 passed · `mix beam_lisp.test` 0 failures ·
31 examples exit 0 · checksum test covers all 115 accepted fixtures.

The sample is once again effectively exhausted — only `into` and unpassable
stubs remain. The informative next move is a fourth widening, and the doc says
so plainly rather than pretending the list is still full.