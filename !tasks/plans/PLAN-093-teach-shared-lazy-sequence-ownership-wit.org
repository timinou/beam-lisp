#+TITLE: Teach shared lazy sequence ownership with executable syntax
#+STATE: DONE
#+SESSION_ID: 15751ba43523b193
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-09-06T14-57-15-988Z_15751ba43523b193.jsonl]]
#+CUSTOM_ID: PLAN-093-teach-shared-lazy-sequence-ownership-wit

* Initial Message

you are  the only agent working on this. make it work.

* User request
Show syntax for the proposed GC-owned lazy memo design, and deliver a full tutorial for a curious beginner as a .bl.md file in docs/. This authorizes documentation, not native runtime implementation.

* Deliverable
Create docs/memory-policy/shared-lazy-values.bl.md. Follow docs/AGENTS.md: explain present concepts from zero context, not a session changelog. Clearly distinguish current executable syntax and semantics from proposed native ownership/reclamation. No invented public resource/free/publish syntax. Include delayed evaluation, shared successful memoization, finite observation of infinite streams, cross-process sharing, retained heads/captures, exceptions/retries/owner loss, chunking, native state/lifetime design and cycle limitations, and verification criteria. All clojure fences concatenate into one finite executable program; architecture pseudocode uses text fences.

* Verification
Use the repository literate extractor and reader/compiler to execute all runnable fences in an isolated fresh VM using already-built bootstrap artifacts if normal compilation is blocked. Do not regenerate seeds, alter runtime, or silently raise memory caps. Verify expected values and counters with executable checks. Record exact command and any limitations. Review accuracy against core.bl, LazySeq runtime and native API docs if claims require external support. Finish with a link to the delivered .bl.md file.

* Delivered and verified
Created docs/memory-policy/shared-lazy-values.bl.md: full beginner tutorial with nine executable beam-lisp fences, 18 explicit value/counter checks, finite infinite-stream example, factory identity, Task sharing, captures, chunking, failure/retry distinction, reference retention, native memo state/lifetime, releasing captured recipes, cycle constraints, and proposed-runtime acceptance criteria. Proposed native reclamation is explicitly not shipped or proved by these examples. No runtime implementation changes in this documentation task.

Important verification correction: Loader recognizes beam-lisp fences, not clojure fences. All executable fences use beam-lisp, and the runner asserts at least 1000 extracted source bytes to prevent a vacuous pass.

Old isolated bootstrap runtime failed cross-task sharing, so it was not accepted as current-runtime evidence. Final successful command source-loads current Loader.Server and LazySeq into a fresh VM using already-built bootstrap artifacts:
MIX_BUILD_PATH=/tmp/beam-lisp-clean-zIcrsa MIX_ENV=test mix run --no-compile --no-start -e 'Code.compile_file("lib/beam_lisp/loader_server.ex"); Code.compile_file("lib/beam_lisp/lazy_seq.ex"); BeamLisp.Bootstrap.install!(Mix.Project.compile_path()); source = BeamLisp.Loader.read_source("docs/memory-policy/shared-lazy-values.bl.md"); if byte_size(source) < 1000, do: raise("tutorial cells were not extracted"); BeamLisp.eval(source); System.halt(0)'
Result: exit 0, all 18 check labels ok and final all-checks-passed sentinel. Artifact artifact://15751ba43523b193/main/bash/152.txt. Compilation warnings from source-loading host modules remain; no full project build or native-resource implementation was verified. Final prose-only addition does not change executable cells.