#+TITLE: W2: one verb run — read/classify source, kill the sync turn and the no-loop fallback
#+STATE: ITEM
#+SESSION_ID: 155b996b2e53d8b8
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp/2026-08-17T19-26-17-273Z_155b996b2e53d8b8.jsonl]]
#+CUSTOM_ID: PLAN-028-w2-one-verb-run-read-classify-source-kil

* Intent

The user: "define makes no sense, there should just be a run that does it all." machine/repl/agent = one GenServer (BeamLisp.Spell.Live, renamed Loop) with ONE verb: run.

* What changes

1. The tool the model gets: name `run`, schema `{source, rationale}`. The model writes the SAME source a human author writes: `(defcontract …)` / `(defview …)`.
2. spell.define → spell.run: the JSON-proposal machinery (proposal-keys, proposal->contract/view, template-form, bind-form, event-clause stubs) is replaced by READ + CLASSIFY + SANITIZE:
   - read exactly one top-level form (reader only — never eval; eval-in-image is W4's fence)
   - classify: head must be defcontract|defview
   - sanitize: `(on …)` handler bodies are SHAPE, not behaviour — extract reply tags, replace bodies with inert stubs (identical security boundary to the deleted JSON path: a model must not write server code through a rendering tool)
   - feed `(parse …)` / `(parse-view …)` — downstream cannot tell a model's definition from an author's (unchanged property)
3. Kill the sync turn (Live.ask/turn) and the no-loop fallback in Spell.Server.maybe_ask: the browser path REQUIRES the loop; absent loop = loud error, never toolless prose.
4. Spell.Live → Spell.Loop; define/2 → run/2 taking source text; ask_async stays the browser entry.
5. demo.exs drives the async path.

* What does not change

Rungs 2-4 (machine joins, verse candidate check, ghosts) are untouched. The atom-table safety of read (PLAN-009) is the reader's, already tested.

* Acceptance

mix test 1097+ green with proposals rewritten as source strings; no `define` left as a public verb; review swarm passes.

NOTE [2026-08-18]: W2 implemented and review-swarms run (3 reviewers, 20m). Fixed before commit: (1) on-info clauses in model source REFUSED (they execute via spell.server walker; JSON boundary never exposed them); (2) non-symbol definition names (map as name) crashed the loop GenServer — now rung-1 refusals; (3) reader metadata `^:kw` bypassed the atom guard entirely — now sampled, plus Compiler.read_data does an eager AtomGuard.check! per read (reader resets its sampling counter per read; the model path is many small reads); (4) assert-enumerable! now runs on the ORIGINAL body before stubbing (a dynamic constructor previously sailed through with an invented reply surface); (5) inert stubs carry the EXACT reply-tag set ([], [ok], [err], both) — widening [err] to [ok err] would grow arms the source never answers (also fixed a seq-vs-vector = bug that made the exact match never fire); (6) machine.bl gains undeclared-template-holes as an ERROR (hole root must be a declared param, an assign, the fx channel, or a page-local signal — loop vars are NOT exempt; this is the blank-render bug from the JSON era, re-guarded for author-source); view-signals/view-scoped/view-locals moved run.bl→machine.bl (one home, errors computes signals itself); (7) loop_test warning test re-strengthened (kind + assign names); schema test pins string types; quoted driver-key EDN test ported to contract_edn_test; demo.exs --live collector gates on ✓ verdict text and waits 240s (loop's own deadline). Startup-publication judged covered by host_regen init-publish path.