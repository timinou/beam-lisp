#+TITLE: P22: theories and time — content invariants, liveness, the second half of TLA+
#+STATE: ITEM
#+SESSION_ID: 15699e91a40f236f
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-beam-lisp--type-inference/2026-08-28T16-47-31-472Z_15699e91a40f236f.jsonl]]
#+CUSTOM_ID: PLAN-054-p22-theories-and-time-content-invariants

* Initial Message

ok my friend, can you tell me more about this?

The honest remaining depth — z3-sequence content invariants beyond length, a richer discovery vocabulary, multi-collection capacity — is depth on a shipped foundation, not a missing floor.


From zero context in simple language. FOld in more stuff if it needs to be talked about

GOAL: two orthogonal widenings, both foundations proven.

THEORIES (all spike-proven reachable through the UNCHANGED z3 port, spike_theories.bl 15/15):
- Array → collection CONTENT invariants (no-duplicate ids, sorted, every-elem-valid) — Gap 1 done RIGHT, not length-abstraction. z3 (Array Int Int), select/store.
- BitVec → bit-exact capability-mask/flag invariants + REAL machine overflow.
- Seq/String → session words, message-format on actual content (partly shipped R1 String scalar; extend to Seq collections).
- Quantifiers ∀ → invariants over ALL indices/inputs (∀i. sorted a[i]≤a[i+1]); lifts discovery past ground predicates.
- UF → reason about opaque/:opaque-world effects abstractly (equal inputs ⇒ equal outputs) without modelling the body.
Design: extend the sort functor (smt.bl) — :vec/:seq→(Seq T)/(Array Int T), :set→(Array T Bool), :map→(Array K V). translate the collection ops (conj→store/seq.++, get→select, contains?→select). The functor pattern (R1) generalizes; each tag→its natural theory.

TIME (the second half of TLA+; everything shipped so far is SAFETY □):
- LIVENESS ◇ (eventually) — we have find-lasso (bounded BMC). The lfp/gfp duality (P19 R2) IS the CTL/LTL machinery: AG=safety=gfp (SHIPPED via safe-region), AF/EG=liveness=mixed fixpoint.
- fairness, progress, termination-under-scheduling.
Design: gfp (shipped) + a least-fixpoint "eventually" over the transition graph = CTL model checking. AF φ = μZ. φ ∨ (enabled ∧ □Z next). Reuse system.gfp + the phase/transition graph.

CAVEAT: quantifiers/strings/nonlinear can return :unknown (undecidable fragments) → register :approximate in the exact/approximate catalog (shipped). Reachable≠decidable.

STRIKING DEMOS: no-duplicate work queue (Array); a mutex that EVENTUALLY grants (liveness); a sorted-buffer invariant (∀); an overflow-free counter (BitVec).

VERIFY: each theory demo proves a property length-abstraction/Int could not express; each liveness demo proves an ◇ property safety cannot. Graduate as tests + demos. Regression green.

NOTE [2026-08-29]: P22 COMPLETE (2026-08-29, commit c61e156). Two widenings past Int safety.
THEORIES: verify-content (core.bl) proves a collection stays DUPLICATE-FREE over z3 Array theory. Distinctness ∀i<j a[i]≠a[j] via define-fun Distinct; established by empty init; preserved by freshness-guarded append (recognises (not (includes? coll x))/(not (contains? coll x))). appends-to? detects (conj coll _); unguarded append flagged. Registered :approximate (quantified array = sound-not-always-decidable). Gap 1 done right: length=how many, Array=what.
TIME: verify-liveness (core.bl) + af/af-holds? (system.gfp). AF goal = every path from init eventually reaches a goal phase = LEAST fixpoint over the phase graph, dual of safe-region greatest fixpoint. Starvation loop (cycle avoiding goal) → holds false. gfp.bl now has BOTH: safe-region (AG/gfp) + af (AF/lfp).
Demos 25 (no-dup set) + 26 (job completes vs starves). theories_test 5t/13a incl AF/AG duality direct. Regression 692→702.

P23 COMPLETE (2026-08-29, commit fcd675c). The engine closing on itself.
datom.query.rules: derive-gfp + materialise-gfp = greatest-fixpoint dual of derive-semi-naive, IN the engine, with semi-naive delta (tuple newly falls only if dependency fell last round; :checks measures economy). Result is a relation.
system.gfp: gfp/gfp-delta now DELEGATE to derive-gfp — safe-region/bisimulation/af are thin wrappers over the native primitive. Every tier fixpoint (least reachability + greatest safety) is one engine, one optimizer, one shape.
PAYOFF beyond "works": fixpoint no longer leaks out of datom → inherits delta today, incrementality next (safe region re-checkable on edit, sound when edit SHRINKS the fixpoint — the boundary the engine already draws for least fixpoints). NOT YET: Layer C (recursive z3-aggregation folding the relation into one z3 conjunction mid-fixpoint = Houdini INTO engine; needs Datalog-with-lattices) — derive-gfp is Layer B (pure relational shrink), the honest first PR. The bl gfp driver's shape is preserved but now native.
Engine test test/bl/datom/gfp_test.bl 5t/7a. Demo 27 (both fixpoints one engine). Doc one-engine-two-fixpoints.bl.md. Regression 702t/1878a/0F0E.
ALL FOUR FUTURE DIRECTIONS NOW DONE: P20 composition (907c9b7), P21 repair (9b11d7e), P22 theories+time (c61e156), P23 native-gfp (fcd675c).