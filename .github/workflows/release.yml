name: release

# Self-contained `bl` drops (docs/native-bundler.md) for Linux and macOS on
# both processor families.
#
# Each target is built NATIVELY on its own runner. That is deliberate, not a
# convenience:
#
#   * every NIF (native/datom_*, lazy_memo, wry_webview) is compiled against
#     the runner's own libc/ABI, so it always matches the ERTS that the release
#     assembly copies into the payload. Cross-packing a release onto the
#     beam-machine "any" linux bundles would instead impose the musl libc rule
#     (docs/native-bundler.md 11): the Rust NIFs and the pinned z3 binary would
#     have to be rebuilt for musl, and z3 publishes glibc-only linux assets.
#     Deferred (FUP).
#   * `bl install z3` resolves its pinned asset per OS/arch itself.
#   * explorer's precompiled NIF (rustler_precompiled) matches the runner
#     triple by construction.
#
# NOTHING here needs Mix: dependencies come from `bl deps`, the pinned assets
# from `bl install`, the tiers from `bl build`, and the drop from `bl self-build`
# sealed with a launcher built by cargo (cargo is not Mix).
#
# Cost note: `ubuntu-24.04-arm` and the macOS runners are billed minutes on
# private repositories. Drop a matrix leg if that matters more than coverage.

on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      version:
        description: "Version to stamp when not building a tag (e.g. 2026.0 → 2026.0.0)"
        required: false
        default: ""

permissions:
  contents: write

jobs:
  build:
    name: ${{ matrix.artifact }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 120
    strategy:
      fail-fast: false
      matrix:
        include:
          - artifact: linux-x86_64
            runner: ubuntu-24.04
            # z3's pinned linux x64 asset is built against glibc 2.39, so the
            # runner must be ≥ 24.04 (its `bl install z3` smoke-runs
            # the binary). This sets the drop's glibc floor in turn.
          - artifact: linux-aarch64
            runner: ubuntu-24.04-arm
          - artifact: macos-arm64
            runner: macos-15
          - artifact: macos-x86_64
            # macos-13 (the last Intel image) was retired 2025-12;
            # macos-15-intel is its supported replacement (until 2027-08).
            runner: macos-15-intel
    steps:
      - uses: actions/checkout@v4

      - name: Set up OTP 29 + Elixir 1.20
        uses: erlef/setup-beam@v1
        with:
          otp-version: "29"
          elixir-version: "1.20"

      - name: Resolve version
        id: version
        run: |
          set -eu
          v="${{ github.ref_type == 'tag' && github.ref_name || inputs.version }}"
          v="${v#v}"
          [ -n "$v" ] || v="0.1.0-dev"
          # Elixir's Version is major.minor.patch, so a CalVer tag like `v2026.0`
          # is stamped as `2026.0.0`. The GitHub release keeps the tag's name;
          # only the app version (what `bl version` reports) is normalized.
          case "$v" in
            *.*.*) ;;
            *.*) v="$v.0" ;;
            *)   v="$v.0.0" ;;
          esac
          echo "BL_VERSION=$v" >> "$GITHUB_ENV"
          echo "value=$v" >> "$GITHUB_OUTPUT"
          echo "building beam-lisp $v for ${{ matrix.artifact }} (ref ${{ github.ref_name }})"

      # `priv/native` is a build OUTPUT — `.gitignore` says so, and the native
      # stage installs it — but the boot needs `priv/native/lazy_memo` before
      # anything that could build it can run.
      # `BeamLisp.LazyMemo.load_nif!/0` loads from ONE path with no fallback and
      # the prelude interns LazySeq, so a runner without it dies at the FIRST
      # `bl` call: "BeamLisp LazySeq requires the lazy_memo native runtime".
      # No step order fixes that — building it needs the language, and the
      # language needs it first — so the cache is what carries it across runs,
      # and it has to be listed here to do that.
      - name: Cache deps, builds, and cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/beam_lisp
            build
            ~/.cargo/registry
            ~/.cargo/git
            native/*/target
            tooling/drop/target
            priv/embed
            priv/native
          key: ${{ matrix.artifact }}-${{ hashFiles('bl.lock', '**/Cargo.lock') }}
          restore-keys: |
            ${{ matrix.artifact }}-

      # wry_webview is a Linux-only capability (gtk + webkit2gtk +
      # wlr-layer-shell). Its Cargo.toml target-gates those deps and lib.rs is
      # crate-level `cfg(target_os = "linux")`, so the macOS legs compile it to
      # an empty cdylib and `wry/*` reads as ABSENT — the doctrine for a native
      # capability that cannot be built here. On Linux the headers must exist.
      - name: Install WebKitGTK toolchain (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libgtk-3-dev libwebkit2gtk-4.1-dev libgtk-layer-shell-dev

      # The version lives in the DECLARATION (env.bl `:app :vsn`) — that is what
      # the app resource is written from, and the app resource is what the
      # release assembly reads — so the release stamps it there instead of
      # passing it through a build tool's environment. Both occurrences: the
      # application's own vsn and the release's.
      - name: Stamp the version into env.bl
        run: |
          set -eu
          perl -pi -e "s/:vsn \"0\.1\.0\"/:vsn \"$BL_VERSION\"/" env.bl
          grep -n ':vsn' env.bl

      # Dependencies, with no Mix: `bl deps fetch` obtains every locked tarball
      # into the content-addressed store (verifying the digest the lock names),
      # and `bl deps compile` builds each one for THIS runtime.
      - name: Dependencies
        run: |
          ./bin/bl deps fetch
          ./bin/bl deps compile

      # The tiers and the Rust NIFs. The launcher's cold path has already
      # elixirc'd the Elixir substrate and built the natives it needs to boot at
      # all; this is the coherent rebuild on top of that, into `build/`.
      - name: Build (AOT + NIFs)
        run: ./bin/bl build --force

      - name: Fetch the pinned z3 solver
        run: ./bin/bl install z3

      # The embedding weights travel INSIDE the drop, so they are fetched into
      # the BUNDLED copy here (`bl install embed DIR` writes into DIR) — and for
      # the cache: a miss costs one 33 MB download per leg, and a stale entry is
      # re-verified against the pinned sha256 and refetched.
      - name: Fetch the code-embedding weights (bundled into the drop)
        run: ./bin/bl install embed priv/embed/potion-code-16M-v2

      # Apple Silicon refuses to exec unsigned Mach-O pages. Cargo/ld ad-hoc
      # sign what they produce, but the z3 release archive is not necessarily
      # signed — sign it (and its dylib) before the release copies priv/ into
      # the payload. The signature rides in the file, so extraction preserves it.
      - name: Ad-hoc sign native binaries (macOS)
        if: runner.os == 'macOS'
        run: |
          set -eu
          find priv/z3 -type f \( -name z3 -o -name '*.dylib' \) -exec codesign --force --sign - {} \;
          find priv/z3 -type f \( -name z3 -o -name '*.dylib' \) -exec codesign --verify --verbose=1 {} \;

      # cargo builds the STOCK launcher — the one non-beam-lisp piece a drop
      # needs, and cargo is not Mix. `bl self-build` then assembles the release
      # tree from THIS checkout, compiles the staged tier sources, seals the tree
      # with that launcher, and verifies the compound against its own trailer.
      - name: Build the drop
        run: |
          set -eu
          cargo build --release --manifest-path tooling/drop/Cargo.toml
          ./bin/bl self-build \
            --bin tooling/drop/target/release/drop-launcher \
            --out "bl-${{ matrix.artifact }}" \
            --jobs 6

      # A drop gets launched in parallel more often than it looks — an editor
      # opening two workspaces, `bl daemon start` beside a `bl run`, two jobs
      # on one machine. The first launch of a version extracts ~100 MB, and
      # that staging path used to be shared and deterministic: racers deleted
      # each other's work mid-unpack ("first-run extraction failed: failed to
      # unpack … into …/drop/<sha8>.tmp/…"). This step is that wave, on a
      # runner whose install dir is EMPTY — it must run before the smoke
      # below, because that one extracts this same payload and would leave the
      # directory warm.
      - name: Smoke test (concurrent first launch)
        run: |
          set -eu
          cp "bl-${{ matrix.artifact }}" "$RUNNER_TEMP/bl-race"
          chmod +x "$RUNNER_TEMP/bl-race"
          cd "$RUNNER_TEMP"
          pids=""
          for i in 1 2 3 4 5 6 7 8; do
            ( BL_DAEMON=off ./bl-race version >"race-$i.out" 2>&1 ) &
            pids="$pids $!"
          done
          fail=0
          for p in $pids; do wait "$p" || fail=1; done
          for i in 1 2 3 4 5 6 7 8; do
            echo "race-$i: $(cat "race-$i.out")"
            grep -q "beam-lisp " "race-$i.out" || fail=1
          done
          if [ "$fail" -ne 0 ]; then
            echo "FAIL: a concurrent first launch did not answer" >&2
            exit 1
          fi

      # The drop must run with NO daemon and NO checkout: this is the artifact
      # a user downloads, exercised exactly as they would. It is copied out of
      # the workspace first, so a namespace that the payload is missing cannot
      # be papered over by the checkout's own sources. Every assertion is a
      # live native surface — the language, a Rust NIF (fjall), the core
      # lazy_memo runtime, Explorer/Polars, and the z3 port — so a payload that
      # silently lost its native tier fails here rather than on a user's machine.
      - name: Smoke test (cold, no daemon, no checkout)
        env:
          BL_DAEMON: "off"
        run: |
          set -eu
          cp "bl-${{ matrix.artifact }}" "$RUNNER_TEMP/bl"
          chmod +x "$RUNNER_TEMP/bl"
          cd "$RUNNER_TEMP"
          BL="./bl"
          expect() {
            got="$("$BL" eval "$3" 2>&1 | tail -n1)"
            if [ "$got" != "$2" ]; then
              echo "FAIL $1: expected '$2', got '$got'" >&2
              exit 1
            fi
            echo "ok  $1 → $got"
          }
          got="$("$BL" version | tail -n1)"
          if [ "$got" != "beam-lisp $BL_VERSION" ]; then
            echo "FAIL version: expected 'beam-lisp $BL_VERSION', got '$got'" >&2
            exit 1
          fi
          echo "ok  version → $got"
          expect language   3      '(+ 1 2)'
          expect datom_fjall true  '(datom.store-fjall/available?)'
          expect explorer   true   '(datom.frame/available?)'
          expect lazy_memo  65536  '(BeamLisp.LazyMemo/fast_lane_bytes)'

          # The oracle answers a STATUS MAP on the oracle tier and a bare string
          # on the older port. This check used to demand the string and so failed
          # a release whose solver was working — observed on all four targets:
          #
          #     FAIL z3: expected 'sat', got '{:status :sat, :core [], :why nil,
          #     :tier :oracle, :model {}, …}'
          #
          # `bl doctor` already learned this (its probe reads whichever shape is
          # there, with the measurement written down). Ask for the status and
          # accept both, so a shape change cannot fail a build again.
          z3_out="$("$BL" eval '(let [p (z3/open) r (z3/check p "(assert true)")] (or (:status r) r))' 2>&1 | tail -n1)"
          case "$z3_out" in
            *sat*) echo "ok  z3 → $z3_out" ;;
            *) echo "FAIL z3: expected a status naming sat, got '$z3_out'" >&2; exit 1 ;;
          esac

          # The two stdio servers, from a cwd that has no beam-lisp sources in
          # it at all — which is the point: everything they need comes from the
          # installed priv tier. `bl mcp` mounts a codebase, and a mount that
          # resolved "priv/std/codebase.bl" against the cwd answered nothing
          # here (the reader got `:enoent` as source text) while working fine
          # from a checkout.
          mcp_req='{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"code/list","arguments":{}},"_meta":{"io.modelcontextprotocol/protocolVersion":"2026-07-28"}}'
          got="$(printf '%s\n' "$mcp_req" | "$BL" mcp 2>/dev/null | head -n1)"
          case "$got" in
            *'"codebases"'*'"functions":'*)
              echo "ok  mcp → served a mounted codebase" ;;
            *)
              echo "FAIL mcp: expected a code/list result, got '$got'" >&2; exit 1 ;;
          esac

          lsp_body='{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"capabilities":{}}}'
          got="$(printf 'Content-Length: %s\r\n\r\n%s' "${#lsp_body}" "$lsp_body" \
                 | "$BL" lsp serve 2>/dev/null | head -c 200)"
          case "$got" in
            *'"capabilities"'*)
              echo "ok  lsp serve → answered initialize" ;;
            *)
              echo "FAIL lsp serve: expected an initialize result, got '$got'" >&2; exit 1 ;;
          esac

      - name: Checksum
        run: |
          set -eu
          f="bl-${{ matrix.artifact }}"
          if command -v sha256sum >/dev/null 2>&1; then
            sha256sum "$f" > "$f.sha256"
          else
            shasum -a 256 "$f" > "$f.sha256"
          fi
          cat "$f.sha256"

      - uses: actions/upload-artifact@v4
        with:
          name: bl-${{ matrix.artifact }}
          path: |
            bl-${{ matrix.artifact }}
            bl-${{ matrix.artifact }}.sha256
          # The payload is already gzip; re-zipping ~100 MB per leg buys nothing.
          compression-level: 0
          retention-days: 7

  publish:
    name: publish release
    if: github.ref_type == 'tag'
    needs: build
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Verify the Linux drop runs on this host
        run: |
          set -eu
          ls -la dist
          chmod +x dist/bl-linux-x86_64
          BL_DAEMON=off ./dist/bl-linux-x86_64 version
          BL_DAEMON=off ./dist/bl-linux-x86_64 eval '(+ 1 2)'

      - uses: softprops/action-gh-release@v2
        with:
          files: dist/*
          body: |
            Self-contained `bl` — ERTS + language + native tier (datom Rust
            crates, z3, Explorer). No Erlang/OTP needed on the host.

            | file | runs on |
            |---|---|
            | `bl-linux-x86_64` | Linux x86_64 (glibc ≥ 2.39) |
            | `bl-linux-aarch64` | Linux arm64 (glibc ≥ 2.39) |
            | `bl-macos-arm64` | macOS on Apple Silicon |
            | `bl-macos-x86_64` | macOS on Intel |

            ```sh
            chmod +x bl-linux-x86_64
            ./bl-linux-x86_64 version
            ./bl-linux-x86_64 eval '(+ 1 2)'
            ```

            The binaries are not code-signed. On macOS, Gatekeeper may require
            `xattr -d com.apple.quarantine ./bl-macos-arm64` after a browser
            download; `gh`/`curl` downloads are not quarantined.

            Checksums ship alongside each file as `<name>.sha256`.

            ## License

            Eclipse Public License 2.0. These binaries are Distributed under it,
            so the Source Code is available under the same Agreement:
            [`LICENSE`](${{ github.server_url }}/${{ github.repository }}/blob/${{ github.ref_name }}/LICENSE)
            · [${{ github.repository }}](${{ github.server_url }}/${{ github.repository }})
