; tooling/run-mcp-demo.bl — runnable entry for the codebase-MCP prototype demo.
;
; Run:  mix beam_lisp.run --path tooling tooling/run-mcp-demo.bl
;
; Two-line file on purpose: tooling/mcp/* is the library (protocol core +
; codebase tools), and mcp.demo drives seven real exchanges against it.

(ns tooling.run-mcp-demo
  (:require [mcp.demo]))
