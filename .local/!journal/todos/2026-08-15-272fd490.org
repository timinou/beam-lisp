#+TITLE: Session 1558a47369aead86 Todos
#+DATE: 2026-08-15
#+TODO: ITEM DOING BLOCKED | DONE

* review

** DONE Map target commits and changed validation code
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Inspect git log and git show for 3b53730, 7de53e2, d773129, 76c06e8; identify changed lines in spell/src/spell/define.bl and test/bl/spell/define_test.bl. Acceptance: every candidate finding is anchored to a line introduced by these commits.

Observed 3b53730 introduces define.bl and its suite; 7de53e2 splits view-signals/view-scoped and extends the ladder. Target lines are all newly introduced in review range.

** DONE Trace schema builders and machine findings
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Read schema-problem, proposal->contract, proposal->view, bind-form, view-signals, view-scoped, view-locals, machine-findings, validate, and define in spell/src/spell/define.bl plus related registration semantics. Acceptance: enumerate rung-1 checks and decide each requested malformed/orphan/redefinition/rollback case from concrete code paths.

Rung 1 checks only top-level kind/name/rationale, kind enum, nonempty template html, style selectors, and bind selectors; late builder exceptions catch missing bind variant/action. It does not validate nested names/types/replies/template references/uniqueness. Immutable assoc registration makes tentative rollback sound; keyed redefinition replaces and recomputes findings. Cross-view union is unsound specifically for view-scoped each variables, not necessarily page-global st/value signals.

** DONE Audit define tests for meaningful failure
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Read all tests in test/bl/spell/define_test.bl and their helpers. For each target feature, verify assertions distinguish broken behavior rather than comparing emitter against itself or passing without exercising the branch. Acceptance: name any vacuous test with exact lines and mechanism, otherwise record coverage gaps only when tied to a provable bug.

All 21 tests exercise a discriminating condition; none is wholly vacuous. The redefinition test proves non-duplication but not replacement content/removal, so it is a coverage gap rather than a vacuous assertion. Missing coverage directly maps to supported findings: nested contract enums, template reference integrity, server/local ownership, and cross-view scoped-local isolation.

** DONE Validate candidates and submit review verdict
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:DEPENDS: task-3
:END:

Cross-check each candidate against commit diff and full source context; report only provable introduced defects with an exact proposal, observed code-path verdict, expected verdict, and diff-overlapping line. Acceptance: report_finding called per valid issue, then submit_result exactly once.

Reported five patch-anchored P2 defects: reply-tag normalization, unchecked assign enum, missing template integrity validation, cross-view leakage of each-scoped locals, and server/local ownership collision. Static proof cross-checked against machine/page emitters and Verse E0408/E0938 behavior. Rollback and replacement are sound; no wholly vacuous test found.
