#+TITLE: Session 155ff40a70ebed22 Todos
#+DATE: 2026-08-21
#+TODO: ITEM DOING BLOCKED | DONE

* review

** DONE Trace planner patch and index contracts
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Inspect commit e58ba8b diff plus full priv/datom/query/plan.bl, priv/datom/index.bl, and engine call sites. Enumerate every index-for branch and verify returned prefix order against index-components/index population. Acceptance: branch-by-branch contract map with diff anchors and identified runtime probe shapes.

Observed prefix order: EAVT [e a], EAVT [e], AVET [a v], AEVT [a], VAET [v], EAVT [] all match index-components. Defect candidate: value-only branch selects VAET without proving the unknown attribute is ref; indexes-for writes VAET only for ref attrs.

** DONE Probe index and greedy-order edge cases
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Run read-only beam-lisp query/planner expressions covering bound non-ref value with unbound attribute, all index-for prefixes, duplicate equal clauses, empty :where, unbound predicates, and a demonstrably bad greedy order. Acceptance: observed outputs/counts; quantify B1 by scan invocations or wall time if constructible.

Observed: all prefix component orders match indexes. `[?e _ "London"]` plans VAET and returns empty vs EAVT expected entity 1. Equal duplicate patterns shrink 2→1; duplicate ready predicates shrink where 3→plan 2 and swap! runs once. Empty order returns []. Never-bound `(= ?missing nil)` terminates then silently keeps rows. Greedy cardinality-blind example at 500 entities: chosen order 501 scans/candidate rows and 1,882,095µs/20 runs vs legal reverse 2 scans/rows and 5,098µs.

** DONE Report only patch-introduced planner defects
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Correlate runtime evidence to changed lines in priv/datom/query/plan.bl. Call report_finding once per provable actionable defect, then submit structured verdict. Acceptance: every finding overlaps the e58ba8b diff, names exact trigger/impact, and excludes intentional/known limitations.

Reported three patch-anchored defects: incomplete VAET selection (P1), unbound predicate silent success (P1), and equality-based duplicate clause loss (P2). Deliberate statistics-free greedy weakness was quantified but not filed as correctness defect.
