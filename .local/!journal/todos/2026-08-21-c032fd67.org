#+TITLE: Session 1560a7a60ad94508 Todos
#+DATE: 2026-08-21
#+TODO: ITEM DOING BLOCKED | DONE

* Review

** DONE Map changed datom code and tests
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Inspect git diff/status/log plus priv/datom/ and test/bl/datom/ structure. Identify patch lines and newest feature symbols; acceptance: exact modified files, commits, and diff anchors recorded.

Observed source patch is origin/main...HEAD: 31 new datom/docs/example/test files, 8,383 lines. Newest feature sequence mapped through commits e08901d, f39a89a, 2575290, 3b7fb7b, e40ee55, 29fb870; range pushdown diff anchors are engine.bl 228-333 and plan.bl 64-125.

** DONE Audit newest feature invariants deeply
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Read full context for range pushdown, :in bindings, joins, pull recursion, transaction expansion/reification, and schema-as-datoms under priv/datom/. Trace callers with find graph edges; acceptance: candidate failures tied to concrete patch lines and reachable API constructions.

Concrete reachable candidates: relation rows are zipmapped without width validation (engine.bl bind-inputs); or-join passes all outer bindings into branch-local scope; reverse recursive pull switches to values-of on a reverse attr; expand-store-ops reads only pre-tx state so retractEntity misses earlier same-tx additions/upserts; unique validation reads only store so same-tx duplicates may pass; enabling AVET after existing data does no backfill; Conn/index discard Store mutator return values despite immutable-store contract.

* Verification

** DONE Execute focused datom regression probes
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Run at most one mix process at a time: baseline `mix beam_lisp.test test/bl/datom --path priv`, then timeout-120 focused `/tmp/*.bl` probes for each viable candidate. Never run `mix test`; acceptance: actual expected/observed outputs or candidate disproved.

Baseline passed 245 tests/507 assertions. Executed focused probes. Range negatives, many, empty windows, as-of, dual patterns, refs, :or and :not all returned expected answers. Reproduced: malformed relation row accepted; or-join local-shadow answer lost; reverse recursion stops after one hop; same-tx upsert+retractEntity leaves fields; same-tx unique/value creates 2 holders; enabling index hides old fact from q; existing-store reopen basis=999999/entity=nil; immutable Store direct returned value works but Conn entity=nil; explicit-id identity duplicate creates 2 holders.

** DONE Review docs against executable behavior
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:DEPENDS: task-3
:END:

Read examples/datom/01..05 and docs/datomic-in-beam-lisp/report.typ claims, compare with source and focused runs where suspicious. Acceptance: every reported mismatch is executed; unexecuted claims explicitly excluded.

Examples 01-04 executed successfully. Example 05 cost tutorial exceeded its documented 120s window with no output under current load, so its runtime tables remain unexecuted; source and report were reviewed. Docs explicitly promise same-store backend interchangeability, schema evolution, relation tables, scoped joins, recursive reverse shape, and retractEntity completeness; focused probes falsified the corresponding edge cases. Report also states half-open -range while Store contract/tests are inclusive.

* Closure

** DONE Submit patch-anchored review verdict
:PROPERTIES:
:TASK_ID: task-5
:STATUS: completed
:DEPENDS: task-4
:END:

For each proven introduced bug call report_finding with an absolute path and <=10-line diff-overlapping range, then call submit_result once. Acceptance: verdict includes only actionable reproducible defects and states review confidence.

Reported 10 reproduced, patch-anchored issues: five P1 data/storage invariant failures and five P2 query/pull/input/contract defects. No unexecuted hypothesis was reported. Full datom suite passed; cost tutorial runtime remained unexecuted after its prescribed 120s timeout.
