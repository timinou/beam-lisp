#+TITLE: Session 155fff50a5ebed4f Todos
#+DATE: 2026-08-21
#+TODO: ITEM DOING BLOCKED | DONE

* source-trace

** DONE Locate PLAN-033 target symbols and changed lines
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Inspected absolute target sources, range call sites, ETS implementation, and query tests. Exact symbols/lines recorded for engine, plan, db, index, store, pull, time, agg.

Source files use .bl without language profile; #raw and line-aware searches provided exact source ranges. Workspace diff qualifier had no results.

** DONE Trace Store range calls through query operations
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Derived datoms→index.scan→Store/-range chain and two-clause formula: one initial open-column range plus one entity-bound range per first-clause surviving binding, yielding N+1 for N matching entities. Bulk current/as-of filtering adds no Store range; nested pull recursively adds scans.

Observed source: engine eval-pattern invokes db/datoms once per binding; db/datoms invokes index/scan once; index/scan invokes Store/-range once. Inference: N+1 assumes first clause is open-column and second clause shares ?e and is evaluated per binding.

** DONE Validate scaling expectations and batching alternative
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Separate planner, scan, visibility, dedupe, pull, and aggregate costs; identify Store protocol has only single-range reads, so batching requires a new protocol/API or server-side multi-prefix operation. Acceptance: review-ready source-grounded findings with absolute paths and exact lines.

Observed no multi-range/batch-read method in Store protocol lines 29-137; -commit batches writes only. Pull wildcard repeats values-of per datom; q final set dedupes projected tuples; aggregate folds raw bindings without reads.
