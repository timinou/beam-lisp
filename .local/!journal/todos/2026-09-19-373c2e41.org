#+TITLE: Session 15864b1da8c4421a Todos
#+DATE: 2026-09-19
#+TODO: ITEM DOING BLOCKED | DONE

* core

** DOING deodorant: migration tier + requires-ns guard + ensure-require
:PROPERTIES:
:TASK_ID: t1
:STATUS: in_progress
:END:

priv/std/deodorant.bl: (a) header describes a 4th tier :migration; (b) defsmell validates pattern via rewrite/check-pattern! and treats :requires-ns/:ensure-require values as DATA (quote them); (c) var-resolves? + rule-available?; smell-match returns nil when the prerequisite is unmet; (d) skipped-rules + migration-rules queries; (e) ensure-requires fn that appends a require entry to the ns :require vector, refusing (no write) when there is no :require; (f) fix-source-preserving tracks which rules fired, applies their :ensure-require, returns :requires-added/:refused; fix-file-preserving! does not write on refusal. Acceptance: ./bin/bl test test/bl/deodorant_test.bl green.

** ITEM deodorant: register the FUP-094/095 migration smells
:PROPERTIES:
:TASK_ID: t2
:STATUS: pending
:END:

priv/std/deodorant.bl: defregistry→registry-clause, defbus→bus-clause (2- and 1-arity), and srv/defserver→defserver (rest pattern). Each :migration, opt-in via every-rule ONLY, gated by :requires-ns on the real successor (reg/registry-clause, bus/bus-clause, core/defserver) so they are inert today and reported skipped. defregistry/defbus carry :ensure-require [proc.server :as srv]. all-rules (default) must NOT include them.

** ITEM span-rewrite: rest pattern in node-match + render
:PROPERTIES:
:TASK_ID: t3
:STATUS: pending
:END:

priv/std/span-rewrite.bl: node-match accepts a trailing ?*name in a list/vector pattern (binds remaining nodes; >= arity; refuse non-tail/duplicate); template->render splices a rest binding's slices joined by space; driver capture-ok? and binds->data handle a rest binding (a list of nodes). Zero behaviour change for exact-arity rules. Needed so bl fix applies a rest-pattern migration.

* report

** ITEM bl.lint learns skipped rules + migration findings
:PROPERTIES:
:TASK_ID: t4
:STATUS: pending
:END:

priv/std/bl/lint.bl.md: lint-paths adds :skipped (rules whose prerequisite is unmet) as JSON-safe strings; render prints a ⊘ skipped line per inert rule and, for a migration finding, its tier [migration] plus the :ensure-require it implies. Default (no --tier) output byte-identical: no migrations in all-rules.

* verify

** ITEM tests: migration tier, guard, second edit, rest pattern, report
:PROPERTIES:
:TASK_ID: t5
:STATUS: pending
:END:

test/bl/deodorant_test.bl: new deftests covering (1) migrations are their own tier, not in all-rules; (2) an unmet :requires-ns rule is skipped and does not match; (3) a met gate fires; (4) ensure-requires appends / no-ops / refuses without :require; (5) fix lands the second edit; (6) a rest-pattern migration rewrites 0/1/7-clause forms through the smell's own rule; (7) a report labels a migration differently from safe/idiomatic. Acceptance: ./bin/bl test test/bl/deodorant_test.bl test/bl/rewrite_test.bl green.
