#+TITLE: Session 155fde6abdebed0d Todos
#+DATE: 2026-08-21
#+TODO: ITEM DOING BLOCKED | DONE

* review

** DONE Inspect target commits and modified code
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Run git diff/show for commits cb7bac7 and cd7f9de; identify exact modified lines in priv/datom/{db,tx,index,conn,store-ets}.bl and tests; acceptance: complete patch map with patch-anchorable line numbers.

Observed both target commits. db.bl and tx.bl are wholly new in cb7bac7; conn.bl is wholly new with the cd7f9de schema-aware write-datoms call at line 129; index.bl is wholly new with cd7f9de changes at require and lines 137-176. Current worktree changes are unrelated local journals only.

** DONE Audit visibility semantics and history behavior
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Trace priv/datom/db.bl visible?, current?, retracted-later?, keep-datom?, entity, datoms and transaction cardinality-one outputs; run reproductions for reassertion, same-tx retract/assert, since-t nil, and history entity; acceptance: observed outputs and any patch-introduced defect anchored to changed lines.

** DONE Audit snapshot immutability and keyspace isolation
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-1
:END:

Inspect every -put/-delete/-commit in priv/datom/, counter storage, all range scans, and conn transaction ordering; acceptance: prove whether snapshots can change, counters enter index scans/collide, or basis can outrun durable datoms, with exact code paths.

** DONE Measure entity visibility scan performance
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:DEPENDS: task-1
:END:

Run a temporary non-mutating /tmp .bl probe using mix beam_lisp.run --path priv for 1000 entities, measuring transaction/load and entity read timings plus scaling where feasible; acceptance: actual measured numbers and confirmation that repository files remain unchanged.

** DONE Synthesize patch-anchored review verdict
:PROPERTIES:
:TASK_ID: task-5
:STATUS: completed
:DEPENDS: task-2 task-3 task-4
:END:

Cross-check agent findings against runtime evidence and git diff; call report_finding only for provable introduced bugs, then submit_result once; acceptance: every finding overlaps the diff and final correctness verdict excludes known issues.

Reported five patch-anchored defects: stale assertions suppress re-adds; realization ignores earlier ops in the same transaction; retraction lookup scans e+a instead of exact e+a+v; history entity folds retractions as values; same-tx assertion/retraction keys collide. Verified 91 tests/230 assertions pass despite these gaps. Counter key isolation, snapshot basis publication, default nil since-t, and separate-transaction cardinality-one replacement are safe under documented single-writer assumptions.
