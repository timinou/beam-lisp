#+TITLE: Session 1560312848d944f5 Todos
#+DATE: 2026-08-21
#+TODO: ITEM DOING BLOCKED | DONE

* Review

** DONE Inspect patch and cross-layer interaction surfaces
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Run read-only git diff in /home/user/code/undefine/beam-lisp; inspect modified priv/datom/*.bl and test helpers. Acceptance: identify exact APIs and result shapes needed for all eight requested constructions.

Patch range c52d6d3..6360fd8. APIs confirmed from conn/time/pull/query tests; two static audits completed without mix.

** DONE Design one compact eight-case probe
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Create probe only under /tmp (not repository) covering time×aggregate, time×pull, historical lookup-ref, cardinality-many time, stale-db upsert, same-tx replacement query, nil aggregates, wildcard/reverse refs. Acceptance: each case prints observed value with transaction points and no dataset over tens of entities.

Designed one 3-entity stdin probe; added aggregate duplicate/empty and same-fact same-tx audit checks from static review. Uses /dev/stdin to obey no-file-edit review constraint.

** DONE Run and interpret all interaction constructions
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Run exactly one foreground mix beam_lisp.run --path priv /tmp probe at a time; never mix test. Acceptance: executed observed output for all eight constructions, reduced rerun only if parser/runtime issue blocks a case.

All 8 requested cases executed in one 3-entity probe. Three sequential reduced probes total; no mix test. Clean: time aggregates/history rows, future-safe nested pull, historical lookup moves, many snapshots, stale-db/current-store upsert, same-tx cardinality-one replacement, nil folds, wildcard reverse exclusion. Confirmed 3 anomalies with targeted repros.

** DONE Trace anomalies and submit review verdict
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:DEPENDS: task-3
:END:

For every mismatch, trace exact cross-layer source path and diff line; reject known/accepted or pre-existing issues. Acceptance: report_finding once per actionable patch-introduced defect, then submit_result exactly once with verdict.

Reported 3 executed, patch-anchored findings: P1 index op collision; P2 aggregate basis-set overcount; P2 pull history-view incoherence. No accepted/prior fixed defects re-reported.
