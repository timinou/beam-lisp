#+TITLE: Session 155ff566776bed2e Todos
#+DATE: 2026-08-21
#+TODO: ITEM DOING BLOCKED | DONE

* Review

** DONE Inspect planner patch and conventions
:PROPERTIES:
:TASK_ID: InspectPatch
:STATUS: completed
:END:

Read commit e58ba8b diff for priv/datom/query/plan.bl and inspect query tests/conventions. Acceptance: identify changed lines and planner invariants; no file edits or builds/tests.

Planner is entirely new in e58ba8b. Equality-based filterv removal at lines 191 and 199 can delete duplicate equal clauses. Unready predicates are emitted at lines 197-199 despite comment claiming engine reports unbound vars.

** DONE Probe duplicate empty predicate cases
:PROPERTIES:
:TASK_ID: ProbeCases
:STATUS: completed
:DEPENDS: InspectPatch
:END:

Use existing compiled tree and ephemeral files outside repo to probe duplicate identical patterns/predicates, empty clauses, never-bound predicates. Acceptance: exact plan/output/termination evidence.

Runtime /tmp/plan_probe.bl: duplicate patterns plan [[?e :p/name "Ada"]], count 1 from input 2; duplicate predicates plan [[?e :p/name ?n] [(> ?n "A")]], count 2 from input 3; empty -> []; never-bound predicate -> [(> ?a 40)], cost 1. Runtime /tmp/never_probe.bl against one Ada fact returned result=#{["Ada"]}, proving unbound predicate silently passes.

** DONE Assess measurable greedy ordering regression
:PROPERTIES:
:TASK_ID: AssessGreedyOrdering
:STATUS: completed
:DEPENDS: InspectPatch
:END:

Construct simple legal query where greedy order may be worse; quantify scan calls/rows/wall time without modifying repository. Acceptance: report only severe patch-anchored regression if observed.

No severe regression reported: heuristic can prefer entity-bound over AVET lookup by design, but no scan instrumentation was available and this is explicitly documented as non-statistical. No nontermination observed; all probes terminated.
