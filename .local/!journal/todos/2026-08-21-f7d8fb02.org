#+TITLE: Session 15603160139944fb Todos
#+DATE: 2026-08-21
#+TODO: ITEM DOING BLOCKED | DONE

* trace

** DONE Inspect transaction and connection semantics
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Read AGENTS.md if applicable, then inspect priv/datom/tx.bl and priv/datom/conn.bl plus callers. Acceptance: document exact stale-db/current-store and same-tx cardinality paths with source anchors.

tx realization threads pending datoms; conn captures db-before but resolves against mutable store. No stale-db defect observed under single-writer contract.

** DONE Trace database and query aggregate semantics
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Inspect priv/datom/db.bl, priv/datom/query/agg.bl, and priv/datom/query/engine.bl. Acceptance: trace missing bindings, zero rows, history/as-of aggregate behavior and identify only source-grounded hypotheses.

Found empty-result aggregate drops global zero-row group; history mode exposes retractions to query engine; aggregate folds raw bindings without set dedup despite set-semantics claim. Missing binding is rejected for OR shape and ordinary patterns never create absent vars.

* review

** DONE Cross-check patch anchors and report findings
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Compare relevant source against patch range c52d6d3..6360fd8 using read-only inspection. Acceptance: final structured report lists concrete bugs with construction, expected/observed semantics, or explicitly clean constructions; no accepted limitations.

Final findings: global empty aggregate group omitted; history retractions enter pattern rows and duplicate tuples collapse under set projection; aggregate folds raw duplicate derivations despite set-semantics claim. Stale-db/upsert, same-tx cardinality-one, as-of current filtering, and ordinary missing bindings trace clean.
