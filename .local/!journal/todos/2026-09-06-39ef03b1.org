#+TITLE: Session 15755763cf864de0 Todos
#+DATE: 2026-09-06
#+TODO: ITEM DOING BLOCKED | DONE

* implementation

** DONE Lower compiler metadata as plain data
:PROPERTIES:
:TASK_ID: meta-lowering
:STATUS: completed
:END:

priv/boot/compiler2.bl: replace four a/normalise metadata call sites with a/lit; make var-meta-map return a plain data map and lower-meta-value only datum-if-reader plus strip-one-quote; preserve explicit :doc/:private precedence and quoted metadata semantics; remove ast constructors/map-head/var-context only if no remaining uses. Acceptance: targeted source inspection finds no metadata a/normalise and no dead constructors.

Replaced four metadata normalise calls with literals; var-meta-map now plain map; lower-meta-value no Macro.escape; dead map-head/var-context removed after scoped use search.

** DOING Centralize validated descriptor reload sequence
:PROPERTIES:
:TASK_ID: descriptor-reload
:STATUS: in_progress
:DEPENDS: meta-lowering
:END:

priv/boot/lower.bl and priv/boot/compiler2.bl: add public reload-descriptor!(descriptor), compile/validate before purge/delete, load and accept exactly {:module expected}, error includes module and result; compiler2 compile-defserver calls API via existing mod-body-lower atom. Preserve wrapper ordering and server defaults. Acceptance: source inspection confirms one install implementation and wrapper remains at file end.

* verification

** ITEM Add canonical metadata and reload coverage
:PROPERTIES:
:TASK_ID: coverage
:STATUS: pending
:DEPENDS: descriptor-reload
:END:

Use the existing canonical upgrade gate test file among scoped files; add assertions for def/defn/defmacro rich :arglists/:tag/custom metadata, macro-provided metadata, source annotations unchanged, no normalise invocation, and reload contract. Do not run tests/build. Acceptance: static inspection shows all requested cases and canonical gate totals 16 tests if that is the established fixture.

* documentation

** ITEM Update canonical compiler feature guide
:PROPERTIES:
:TASK_ID: feature-guide
:STATUS: pending
:DEPENDS: coverage
:END:

Update the existing scoped .bl.org feature guide following docs/AGENTS.md: explain current plain-data metadata lowering and validated descriptor reload as universal current behavior, concise and from zero context; do not narrate changes. Acceptance: guide reflects safe literals, metadata semantics, validation-before-purge, exact load-result contract, and bootstrap wrapper ordering.
