#+TITLE: Session 156125da5ad94519 Todos
#+DATE: 2026-08-22
#+TODO: ITEM DOING BLOCKED | DONE

* Review

** DONE Map six commits and target diffs
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Run git log --oneline -6 and inspect patches touching lib/beam_lisp/rt.ex, priv/datom/codec.bl, and relevant tests. Acceptance: identify exact changed lines and surrounding contracts without modifying files.

Target substrate changes are in d1882f3, immediately before the requested six visible commits; a40ad8c adds the existing 1,225-pair terms suite. Exact changed compare ranks/cmp_same and codec branches identified.

** DONE Trace compare equality codec implementations
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Read full target symbols plus sorted collection/hash call paths and codec helpers. Acceptance: derive concrete boundary cases for total order, equality/hash, numeric unification, and bignum encoding.

Derived adversarial matrix: rank chain/triples; opaque within/cross-type pairs; sorted tuple/list/vector/map/ref collisions/order; hashed composite keys; lazy-vector equality bridge; canonical map/set and nested mutation; 2^53± cases including representable even integers; ±2^63/±2^64 and last-byte bignums.

** DONE Validate and submit review findings
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:DEPENDS: task-3
:END:

Tie every reproduced mismatch to a changed diff line, verify impact on target contract, call report_finding per actionable introduced bug, then submit_result once. Acceptance: verdict excludes known/accepted items and unproven hypotheses.

Seven patch-anchored, executed P1 findings reported. Excluded passing controls, known accepted items, and pre-existing equality/hash behavior not introduced by this patch.

* Verification

** DONE Execute focused adversarial runtime probes
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Create only temporary /tmp probe scripts and run one `mix beam_lisp.run --path priv /tmp/probe.bl` process at a time, within timeout 120. Cover rank transitivity, antisymmetry, sorted collections, equal/hash keys, codec mutation/rebuild, 2^53 numerics, and bignum boundaries. Acceptance: record construction, expected, and actual for every requested category; no repository edits and no mix test.

Three sequential bounded probes executed. Observed 18 failed contract checks and 26 passing controls; transitivity/antisymmetry, composite hashed keys, mutation/rebuild, negative zero, near-boundary distinct-int collision check, and integer-only bignum ordering passed. Reproduced mixed numeric, opaque tag, lazy equality, sorted projection, map/set order failures plus sorted-set codec crash.
