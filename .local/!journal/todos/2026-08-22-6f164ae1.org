#+TITLE: Session 1561d5142cd64d77 Todos
#+DATE: 2026-08-22
#+TODO: ITEM DOING BLOCKED | DONE

* evidence

** DONE Inspect compiler guard implementations and patch diff
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Read lib/beam_lisp/compiler.ex around @guard_bifs and guard_special(:guard_map), plus current uncommitted diff. Acceptance: record exact changed lines and guard semantics.

@guard_bifs maps fn? to erlang is_function/1 at line 2927; map? expands to is_map and not is_map_key(:__struct__) at lines 3100-3107. No repository diff view was exposed by find, so validation uses current source plus live tests/docs.

** DONE Trace runtime predicates and live tests/docs
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Inspect lib/beam_lisp/rt.ex fn?/1 and map?/1 and relevant tests/docs/callers. Acceptance: establish ordinary predicate semantics for tagged functions, records, structs, SortedMap.

RT.fn?/1 accepts native functions plus {:"$blfn",_,_} and {:"$remote",_,_}; RT.map?/1 accepts SortedMap and registered record structs, while plain maps use is_bl_map. Tests/docs explicitly assert record and SortedMap map? true.

* conclusion

** DONE Validate candidates and identify patch anchors
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Compare changed guard behavior against runtime semantics and patch scope. Acceptance: return true/false per candidate, exact static reason, and best source anchor; no edits or builds/tests.

Both candidates are valid under the stated comparison: guard fn? only accepts BEAM funs; guard map? only accepts plain maps. RT explicitly broadens fn? to tagged tuples and map? to registered records plus SortedMap. Anchors are compiler.ex @guard_bifs fn? entry and guard_special(:guard_map) body.
