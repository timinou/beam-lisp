#+TITLE: Session 15754d923e064dd0 Todos
#+DATE: 2026-09-06
#+TODO: ITEM DOING BLOCKED | DONE

* implementation

** DONE Cut compiler APIs to direct ANF
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Files: priv/boot/compiler2.bl, priv/boot/compiler.bl, lib/beam_lisp/compiler.ex. Make public compile return ANF, remove quote readback and consolidate facade/delegation on canonical compiler while preserving host APIs. Acceptance: no live compiler2 quoted helper uses; compiler facade exposes one implementation.

Public compile now returns compiler2 ANF; compiler namespace reduced to canonical delegates; host facade targets Compiler2.

** DONE Migrate defserver module producers directly
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Files: priv/boot/compiler2.bl only. Convert callback/client/default clauses to :defn-clause maps and canonical ANF→Lower descriptor→beam→reload chain; preserve guards, invariants, dispatch, raw_raise/frame behavior. Acceptance: no quoted constructors in defserver route.

Defserver callbacks/defaults/client arities are canonical defn-clause maps; descriptor->beam then checked code load.

* tests

** DONE Adapt direct compiler behavior coverage
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Files: test/beam_lisp/anf_direct_emit_test.exs, test/beam_lisp/direct_forms_test.exs. Compare canonical semantics against stable pre-cutover snapshots, retain guard coverage, add server guard/invariant/reload tests. Acceptance: test source exercises direct server path without legacy compiler comparisons.

Corpus oracle compares public canonical ANF directly; direct_forms adds guard, invariant, reload/client-default coverage.

* audit

** DONE Audit residual generated form producers
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:DEPENDS: task-3
:END:

Files: priv/boot/compiler2.bl, priv/boot/compiler.bl. Identify exact residual quoted producer sites for protocol/record/multi/native/reify/ns after edits. Acceptance: final report lists symbols/sites/contracts for next wave; no out-of-scope edits.

Audit: compile-defmulti/defmethod/defprotocol/extend-type/extend-protocol, compile-defnative, compile-reify, compile-defrecord, and compile-ns already return direct a/* ANF. Residual quoted construction is metadata-only ast-node/Macro.escape in var-meta-map/lower-meta-value, not module production. Lower lacks descriptor reload/install API; direct server currently uses descriptor->beam then purge/delete/load_binary with checked {:module, mod}. Requested sibling contract: reload-descriptor!(descriptor) -> module atom, validate+compile before purge, load checked, explicit failure payload.
