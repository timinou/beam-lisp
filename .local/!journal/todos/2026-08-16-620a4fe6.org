#+TITLE: Session 155a06817d74a965 Todos
#+DATE: 2026-08-16
#+TODO: ITEM DOING BLOCKED | DONE

* review

** DONE Inspect W1 ordering patch and context
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Read git shows 55210e5 and dc0ef48 plus full current spell/src/spell/live.bl, spell/src/spell/contract.bl, and emit golden test context. Acceptance: identify exact changed ordering logic and contracts.

Observed W1 changes css-rule to `(sort-by name (keys decls))`; W0 added exact-byte goldens, and concurrent seed/contract changes added driver params emission.

** DONE Trace emitted map iteration ordering paths
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Audit every `(keys ...)` in spell/src/spell/*.bl, especially machine-css, machine-locals, machine-hosts, edn-forms, view-form, bind-form, and json-fields. Acceptance: prove whether each path reaches emitted output deterministically and assess namespaced/custom/vendor key behavior and CSS cascade semantics.

25 key-iteration references audited. CSS model keys remain strings, so `name` is lexical and preserves `--brand`, vendor prefixes, slash/colon text; contract JSON/scopes use entry-name. Registration vectors and explicit sorts stabilize other targeted emitters. Newly added driver-params-field alone emits map keys unsorted. Pre-existing st/st_edn and machine fallback paths are outside patch and do not prove changed CSS semantics.

** DONE Validate golden fixtures against emitter inputs
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Compare test/beam_lisp/spell/emit_golden_test.exs expectations and test/fixtures/golden/* with current seed/live/page/contract sources and git history. Acceptance: establish whether goldens are current and emitter-output changes cause test failures without running builds.

Current seed-style.css is lexical per css-rule and seed source; seed-view.edn contains current keydown + driver_params form. W1 history updates exactly those bytes. Golden tests compare all three emitter results byte-for-byte to fixed fixture files, so a seeded emitter change fails. No process tests run because review procedure restricts Bash to git reads; supplied background reports full suite passing.

** DONE Report proven ordering defects and verdict
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:DEPENDS: task-3
:END:

Anchor each proven introduced defect to changed diff lines, call report_finding per issue, then submit overall correctness. Acceptance: verdict covers correctness/completeness and any unstable emitted ordering; no planned-later work reported.

Filed P2 for unsorted driver parameter keys and P2 for unescaped driver parameter values. CSS lexical sort is correct for model-supplied free-form string keys and keeps custom/vendor spellings; fixture regeneration matches current seed and exact comparisons are effective.
