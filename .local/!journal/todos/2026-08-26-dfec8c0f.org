#+TITLE: Session 1566dd679e4e6c44 Todos
#+DATE: 2026-08-26
#+TODO: ITEM DOING BLOCKED | DONE

* Review

** DONE Verify req_llm Anthropic option contracts
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Read /home/user/code/ora/relay/deps/req_llm/AGENTS.md, then locate req_llm 1.9.0 declarations and provider handling for anthropic_prompt_cache, anthropic_prompt_cache_ttl, anthropic_cache_messages, prompt_cache_key, and prompt_cache_retention. Acceptance: record exact accepted keys, consumption paths, and behavior of Anthropic-only keys on OpenAI.

Observed req_llm 1.9.0 lock at relay/mix.lock:26. Anthropic schema accepts exact keys anthropic_prompt_cache, anthropic_prompt_cache_ttl, anthropic_cache_messages at deps/req_llm/lib/req_llm/providers/anthropic.ex:79-96; consumers at 653-748. OpenAI schema at providers/openai.ex:116-200 has none of these and repo-wide req_llm search has zero prompt_cache_key/prompt_cache_retention references. Options.process auto-hoists only current provider schema keys then validates with NimbleOptions; therefore Anthropic keys passed to OpenAI remain unknown top-level options and non-stream prepare_request returns validation error, not ignore/warn.

** DONE Trace request cache normalization paths
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Inspect commit d8b00be diff plus full /home/user/code/ora/relay/src/relay/request.bl and cache-intent tests in /home/user/code/ora/relay/test/relay/caching-test.bl. Acceptance: enumerate concrete request params → call-opts outputs, including explicit 1h/5m/invalid TTL and OpenAI prompt cache fields, with changed-line anchors.

Observed request.bl:40-93 and all cache tests. cache_ttl "1h" -> {:anthropic_prompt_cache true, :anthropic_prompt_cache_ttl "1h", :anthropic_cache_messages true}; explicit "5m", "1hour", or "3600" -> cache enabled/messages true with TTL omitted (5m default), matching the documented explicit-only-1h contract. prompt_cache_key + prompt_cache_retention -> only Anthropic enable/messages opts; key and retention are absent. retention alone -> no cache opts. relay.http routes these opts to both completion/generate and completion/stream. OpenAI non-stream validates/rejects Anthropic keys; OpenAI streaming skips schema validation but its Chat/Responses body builders ignore them, so prompt_cache_key/retention never reach wire body.

** DONE Validate and submit material findings
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Cross-check changed request.bl paths against req_llm provider option schemas and tests; report only provable introduced behavioral or billing bugs via report_finding with absolute path and diff-overlapping line range. Acceptance: submit exactly one final correctness verdict; do not run tests or edit files.

Reported two patch-anchored findings: P1 OpenAI non-stream cache hints become Anthropic-only unknown options and fail req_llm validation; P2 OpenAI prompt_cache_key/retention are absent from opts and streaming wire bodies. Confirmed Anthropic option names are exact and explicit-only "1h" TTL mapping matches documented contract; no TTL coercion finding because fallback is deliberate.
