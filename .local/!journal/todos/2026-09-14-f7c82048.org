#+TITLE: Session 157f2f865dc66311 Todos
#+DATE: 2026-09-14
#+TODO: ITEM DOING BLOCKED | DONE

* recon

** DONE Record pre-change baselines for differential
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Observed PRE-change (BL_ORACLE_SESSIONS=off) on this machine:
  discover unannotated-account -> checked=true discovered=(and (>= balance 0)) conjuncts=((>= balance 0))
  discover leaky -> checked=true discovered=(and (<= n 0) (<= n 1))
  synth q5 -> bounded=true synthesized=5
  synth qinf -> bounded=false synthesized=nil
  examples/system/14 account -> DISCOVERED (and (>= balance 0)); leaky -> (and (<= n 0) (<= n 1))
  examples/system/22 -> capacity 5; qinf nil
POST-change both lanes (ON and off) reproduce these exactly. These are the pinned values in oracle_test.

post-change sessions ON and BL_ORACLE_SESSIONS=off both reproduced the same values

* core

** DONE Change A: sessions in candidate loops
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

priv/lib/system/core.bl: discover-invariant + synthesize-capacity open ONE oracle/session per machine (prelude = decls+prime-decls / len decls+nonneg+len2); helpers holds-at-init? and inductive-under? and len-bound-inductive? become scope builders; one loop body with an ask1 closure selecting oracle/scoped vs oracle/ask driven by BL_ORACLE_SESSIONS (off => one-shot legacy). Skip session when candidate set empty.

** DONE Change C: establishment refusal carries witness
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

verify-process: establishment check returns a verdict; when REFUTED build :failures entry {:label :init :counterexample (oracle/witness v svars)}; keep :holds. Add :blame from dual only when refuted and session available. Warnings derived from combined failure list.

** DONE Change B: repair unrepairable branch blames culprit
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:DEPENDS: task-2
:END:

repair-process: new blame-obligation (dual: g_guard/g_next/g_inv2=Inv(s2), expect UNSAT) asked in its own session with here-verdict {:core? true}; unrepairable entry gets :blame (oracle/explain) and :counterexample (witness of break). Polarity documented in comment. Handler-safe: skip core lane when no conversation held.

* tests

** DONE Tests: oracle_test differential + blame
:PROPERTIES:
:TASK_ID: task-5
:STATUS: completed
:DEPENDS: task-2 task-3 task-4
:END:

test/bl/system/oracle_test.bl: add differential tests pinning :discovered/:conjuncts/:capacity/:checked for examples 14/22 machines and two seam fixtures, plus 3 blame assertions via real engine machines (one per blame keyword).

oracle_test 37 → 80 assertions (differential values, lane-switch mechanism, 3 blames, init witness)

* examples

** DONE Examples: 07-sessions, 09-blame, 05-sorts
:PROPERTIES:
:TASK_ID: task-6
:STATUS: completed
:DEPENDS: task-2 task-3 task-4
:END:

Create examples/z3/07-sessions.bl (sessions off/on differential, z3pool/current inside/outside, lease prose) and examples/z3/09-blame.bl (4 machines, failure maps side by side, polarity lesson, measured extra-question cost). Update examples/z3/05-sorts.bl to print the :init failure + witness.

07-sessions.bl + 09-blame.bl created; 05-sorts.bl updated; README 05 row fixed + 07/09 rows added

* verify

** DONE Run all gates
:PROPERTIES:
:TASK_ID: task-7
:STATUS: completed
:DEPENDS: task-5 task-6
:END:

Run the 4-file test suite; examples 07/09/05; examples/system 14/22/02. Report assertion counts and observed output.

4-file suite: oracle 80 / sorts 8 / system 30 / verify_undecided 7 — 4 passed, 0 failed. Consumers: seam 105, repair 11, corpus 23, migrate 20, ward 42, z3_stats 46. z3-adjacent: explain 41, z3_conversation 15, prove 13, verify_tier 10, incremental 11 — all green. Examples rc=0, 0 error lines: 05-sorts (init failure + witness + blame), 07-sessions (77039→2687 µs, 7703→268 µs/q, discovered identical), 09-blame (3 blames + maps), 14/22/02.
