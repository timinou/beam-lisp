#+TITLE: Session 155a067f4c34a95f Todos
#+DATE: 2026-08-16
#+TODO: ITEM DOING BLOCKED | DONE

* review

** DONE Inspect conversion patch and governing contracts
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Read git shows for 55210e5 and dc0ef48, PLAN-027, and full lib/beam_lisp/spell/data.ex context. Acceptance: enumerate changed conversion APIs, modes, and diff-anchored lines.

Observed dc0ef48 adds Data.to_bl/2 + from_bl/1, replaces eval_string paths in Live/Server/Page, and sources proposal vocabulary from spell.define/proposal-keys. Diff anchors identified in new data.ex and modified caller lines.

** DONE Trace callers and attacker-controlled key origins
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Read server.ex, live.ex, page.ex, spell/src/spell/define.bl, spell/src/spell/server.bl, and semantic callers. Acceptance: classify every to_bl mode and assign_all name as closed vocabulary, source literal, or wire/model controlled.

Classified to_bl sites: proposal vocabulary receives model JSON; all_strings receives browser JSON and atom-keyed socket assigns normalized to strings; as_written receives only literal-key maps built by define_tool/conversation. assign_all names come from declared contract assigns/current socket keys; bind-params never promotes payload keys to assign names.

** DONE Attack recursive conversions with exact values
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Inspect conversion implementation and tests for nested vectors, tuples, improper lists, pids/functions, vocabulary collisions, non-binary keys, and injection shapes. Acceptance: each candidate has a concrete execution path and patch-line anchor or is dismissed with evidence.

Attacks: nested JSON keys remain binaries unless in existing vocabulary; non-binary key collisions can overwrite but are outside the documented JSON-shaped model/browser boundary and do not intern/evaluate; as_written key provenance is literal-only. from_bl leaves tuple/pid/function and raises on improper lists, but normal provider/model/browser paths produce JSON shapes, and baseline plain/bl_json had the same behavior. Arbitrary {:failed,id,pid} crashes the LiveView before this patch (old printer FunctionClauseError) as well as after, so not introduced. Recorded injection is inert because target paths contain no evaluator, not merely because the exact key is tested.

** DONE Validate findings against diff and tests
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:VERIFY_REVIEW: Every finding is diff-anchored, actionable, introduced, and evidence-backed; no later-wave planned work is reported.
:DEPENDS: task-3
:END:

Cross-check candidates against baseline e9be8a6 and targeted existing tests; use only read-only git commands per review procedure. Acceptance: report only introduced, reproducible correctness/security defects; otherwise return correct verdict naming attacked invariants.

Cross-checked every candidate against e9be8a6 and changed-line anchors. No reportable patch-introduced defect remains. The only real atom-growth candidate—keyword/symbol creation from model-provided proposal names—exists unchanged in the baseline and is outside the modified proposal-keys hunk. git diff --check returned clean; no builds/tests rerun per review-mode prohibition and user-supplied verified state.
