#+TITLE: Session 15561256a170f8d2 Todos
#+DATE: 2026-08-13
#+TODO: ITEM DOING BLOCKED | DONE

* mcp

** DONE Map MCP tools and source ingress
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Inspected MCP tool registry, dispatch, st_tab_open, st_fn_put, st_mount, LiveServer put path, and bundle compile pipeline; recorded source-text boundaries and parser call.

Primary source-text tools: st_tab_open.code, st_fn_put.source, st_mount.source; shared sink LiveServer::put_source[_with_entry] -> bundle::compile_function -> compile_pipeline -> parser::parse.

* state

** DONE Trace LiveStore and function entities
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:END:

Inspected FunctionRecord, InstanceRecord, LiveEvent, LiveStore fields and mutations; inspected functions/registry.rs first-class built-in function types and confirmed no serde derives.

LiveStore is Arc<Mutex<LiveStore>> with function/name/instance maps, counters, bundle cache, port. FunctionRecord stores source, compiled outputs, contract, diagnostics, roots/origin/contract metadata. InstanceRecord stores function linkage, input, URL/region, event cursor/history, composition maps. functions/registry.rs defines compile-time built-in FunctionDef/FunctionCall entities; no Serialize/Deserialize derives.

* surface

** DONE Map CLI and external injection surfaces
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:END:

Mapped Command enum and main dispatch, MCP HTTP/browser action source ingress, dev_server WebSocket edit boundary, and ranked all observed process-boundary representations.

CLI verbs are Command enum in src/main.rs lines 70-469; dispatch is main match lines 582 onward. Highest-ranked injection remains MCP tools -> LiveServer::put_source_with_entry -> bundle compile pipeline; browser workbench actions add source edit/create/open-tab paths; dev_server receives JSON edit protocol but edits disk through sync handlers.
