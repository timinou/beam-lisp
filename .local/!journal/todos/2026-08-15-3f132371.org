#+TITLE: Session 1558bf84a06ead9e Todos
#+DATE: 2026-08-15
#+TODO: ITEM DOING BLOCKED | DONE

* Review

** DONE Map patch and changed control flow
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:VERIFY_REVIEW: Patch and relevant full-file context are both inspected before conclusions.
:END:

Inspect `git diff`, recent commits, and full context in `lib/beam_lisp/spell/live.ex` plus `spell/src/spell/provider.bl`; identify exact added/changed lines and review baseline. Acceptance: patch hunks and relevant symbol bodies are mapped with line anchors.

Observed no working-tree diff. Review baseline is 2c0c2e2..HEAD; both target files are new in that range. Relevant lines: live.ex turn 159-206, apply/ladder 216-268, publish/report 272-314, provider_messages 334-345, parse/to_bl 352-410; provider.bl request 62-129 and turn decoding 272-423.

** DOING Attack recursion and multi-call budgeting
:PROPERTIES:
:TASK_ID: task-2
:STATUS: in_progress
:VERIFY_REVIEW: Every claimed defect names the exact reachable branch and patch line.
:DEPENDS: task-1
:END:

Trace `turn/3`, `apply_proposal/2`, and `run_ladder/1` for repeated rejection and multiple tool calls; construct exact transcript/call ordering that proves any budget or control-flow defect. Acceptance: either a patch-anchored finding with trigger+impact or a reasoned no-finding result.

** ITEM Attack proposal printer data boundary
:PROPERTIES:
:TASK_ID: task-3
:STATUS: pending
:VERIFY_REVIEW: Security finding requires a concrete serialized payload that crosses the code/data boundary.
:DEPENDS: task-1
:END:

Trace `to_bl/1` and `bl_json/1`; test mentally and, only via permitted read-only runtime path if available, exact strings containing quotes, backslashes, `)`, interpolation-like `#{}`, newlines, and template HTML. Acceptance: prove whether model strings remain literals or can emit executable forms, with exact input/output and patch line.

** ITEM Trace failed publish state consistency
:PROPERTIES:
:TASK_ID: task-4
:STATUS: pending
:VERIFY_REVIEW: Any finding proves post-failure fields and browser-visible behavior from source.
:DEPENDS: task-1
:END:

Trace accepted machine commit, version bump, `publish/1`, `write_report/2`, page write/build, and browser reload behavior for build failure. Acceptance: identify exact persisted state/report/page mismatch and user impact, or establish consistency from code.

** ITEM Attack provider malformed response handling
:PROPERTIES:
:TASK_ID: task-5
:STATUS: pending
:VERIFY_REVIEW: Malformed-input findings require a specific input shape and exact failing operation.
:DEPENDS: task-1
:END:

Trace `extract-turn`, `tool-calls`, `ask-turn`, `request-body`, `parse_arguments/1`, and `provider_messages/2` with truncated JSON, missing function, invalid arguments JSON, long transcripts, and rejected-proposal feedback. Acceptance: exact crash/error behavior and patch anchor for each genuine defect; distinguish bounded selection from unbounded retained state.

* Synthesis

** ITEM Validate findings and submit verdict
:PROPERTIES:
:TASK_ID: task-6
:STATUS: pending
:VERIFY_REVIEW: No speculative, pre-existing, style-only, or non-diff-anchored findings remain.
:DEPENDS: task-2 task-3 task-4 task-5
:END:

Cross-check candidate issues against full context, git history, existing tests, and changed lines; report each qualifying defect with absolute path and ≤10-line diff range, then submit final correctness verdict. Acceptance: every finding is introduced, actionable, observed/proven, and patch-anchored; submit_result called exactly once.
