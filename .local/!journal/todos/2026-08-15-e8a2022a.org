#+TITLE: Session 155899377c6ead7b Todos
#+DATE: 2026-08-15
#+TODO: ITEM DOING BLOCKED | DONE

* Review

** DONE Inspect loader and Env path implementation
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Read git show c35b82f plus full lib/beam_lisp/loader.ex and lib/beam_lisp/env.ex. Trace exact search_dirs/extra_dirs/env_paths ordering, Env state construction/startup, mutation semantics, and cwd-relative expansion. Acceptance: every requested implementation concern has source-backed conclusion.

Observed search_dirs is exactly load_paths ++ cwd ++ Env.search_paths ++ parsed env ++ priv. Env Agent state is constructed only in Env.start_link; OTP supervisor and embedded ensure_env both call it. Map.get default supports a live pre-upgrade Agent/iex process whose old map lacks :search_paths. Env values expand once when added; BEAM_LISP_PATH expands on each loader search. No repo code mutates global cwd.

** DONE Audit mix task path ordering
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Read modified Mix.Tasks.BeamLisp.Run and Mix.Tasks.BeamLisp.Test in commit c35b82f and full files. Trace CLI --path insertion order and spell/src auto-add interaction through Env.add_search_path/1. Acceptance: prove whether explicit path precedes auto-added spell/src and note any concrete breakage.

Observed both tasks parse repeatable --path/-p and call Env.add_search_path in option order. Test task then appends BeamLisp.Spell.src_path; add_search_path appends first-seen absolute paths, while Loader searches Env.search_paths left-to-right. Therefore every explicit path outranks auto-added spell/src; spell/src outranks BEAM_LISP_PATH. Run task has no auto root.

** DONE Audit loader tests honesty and isolation
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Read test/beam_lisp/loader_path_test.exs plus relevant loader/namespace behavior. Check each assertion is non-vacuous, uniq_ns prevents global poisoning, filesystem cleanup survives assertion/load failures, and shared Env state is restored. Acceptance: each test classified as feature-proving or concrete false-green/order-dependent scenario identified.

All seven tests exercise observable behavior: missing-before-add, both env entries, cwd-vs-configured value, configured-vs-priv value, priv fallback, second ambient load, and exact idempotent ordering. uniq_ns uses VM-unique names with distinct prefixes, so loaded registry entries cannot shadow siblings; real optics is untouched. setup/on_exit serializes and clears global path/env state. Priv fixture assertions are protected by try/after. Concrete defect: cwd test deletes unrelated repo tmp/loader_path_test and registers cleanup only after assertions, so the regression it is meant to catch leaves a root .bl file and temp tree.

** DONE Synthesize patch-anchored correctness verdict
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:DEPENDS: task-3
:END:

Cross-check all candidate issues against commit diff lines and criteria: introduced, provable, actionable, unintentional. Report qualifying findings via report_finding; then submit one final verdict. Acceptance: no speculative/style findings; clean conclusion explicitly covers all six requested checks.

Reported one P2 cleanup defect at loader_path_test.exs:101-103. No implementation defect found: actual precedence is pushed > cwd > explicit insertion order > env > priv; cwd-before-configured is deliberate though explicit --path users may initially expect override semantics. Env Map.get default is valid live-Agent upgrade compatibility. Env parsing drops empty segments (semantically redundant because cwd is already higher), expands relative values at lookup cwd, and expands ~ per Elixir Path.expand. Explicit --path is appended before test task's spell/src, so it wins on a fresh task VM. Tests are behavior-based and unique namespaces avoid sibling poisoning; priv assertion cleanup is try/after.
