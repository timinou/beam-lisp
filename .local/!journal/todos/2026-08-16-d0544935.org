#+TITLE: Session 155a14cd12f4a97b Todos
#+DATE: 2026-08-16
#+TODO: ITEM DOING BLOCKED | DONE

* Review

** DONE Map provider reassembly and routing changes
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Inspect commits 0546b49 and 3911f1e plus full spell/src/spell/provider.bl context; trace stream-tool-calls, sse-frames, raw-field, assemble-tool-calls, assembled-document, tool-calls, and stream/4. Acceptance: identify exact changed lines and executable contracts without editing files.

W2 adds text-scanning assembly at provider.bl lines 623-721; stream/4 chooses tool-calls over all deltas at lines 754-780. Recorded cassette contains one `tool_calls` frame with its full `arguments` value despite the split-across-frames test description.

** DONE Probe hostile escaped and ambiguous fields
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Construct exact SSE inputs covering escaped quote/backslash/unicode/newline and misleading `name` text/fields; evaluate through provider functions using a no-edit temporary probe or equivalent. Acceptance: observed output demonstrates survival or a patch-introduced mismatch, with exact triggering SSE body retained.

Observed no-build probe: a 5-frame arguments string containing escaped quote, doubled backslash, split `\u00e9`, newline escape, and literal escaped `\"name\":\"` returned byte-for-byte as expected. A top-level `name`/`id` before choices is incorrectly selected; argument-embedded `name` text is not.

** DONE Probe two calls and mixed prose
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-1
:END:

Construct exact two-tool-call SSE body and mixed prose-then-tool body; verify assembled arguments stay separated and inspect stream/4 emitted message choice. Acceptance: concrete observed behavior and determination whether prose handling violates existing consumer contract.

** DONE Validate findings against tests and patch
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:DEPENDS: task-2 task-3
:END:

Read provider cassette tests and stream-tool-call fixture context; run only permissible targeted no-build verification if needed; ensure every finding is introduced by reviewed commits, patch-anchored, actionable, and non-duplicative. Acceptance: final verdict with exact line anchors and no speculative findings.

No-build probes confirmed: hostile escaped quote/backslash/split unicode/newline and argument-embedded `name` survive; sequential two-call frames survive. Proven patch defects reported: per-frame/global scans drop same-frame calls, misroute indexed continuations, and capture outer metadata; stream branch drops valid prose whenever calls exist. Both anchors originate in 0546b49.
