#+TITLE: Session 15637146403514ec Todos
#+DATE: 2026-08-23
#+TODO: ITEM DOING BLOCKED | DONE

* review

** DONE Map commit trust-boundary patch precisely
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Read `git show 99cb7ef` and modified-file context for spell/src/spell/server.bl and lib/beam_lisp/spell/server.ex. Acceptance: identify exact changed line ranges and data flow from handler walk result to performer execution.

Observed path: walker records `{:op (name op) :payload payload}` into :intents; handle/handle-info return it; host Data.from_bl conversion occurs in apply_bl before apply_result; apply_result assigns walker state, then performs registry lookup and invokes performer.

** DONE Trace walker authority and op naming
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Exhaustively trace `walk`, every special form, `pure-fns`, symbol/parameter resolution, and all runtime values accepted by `(name op)` in spell/src/spell/server.bl plus interpreter primitives. Acceptance: prove or disprove reachability of registry/performers and attacker-controlled op collisions with exact forms and lines.

Observed: walk dispatch is closed; unknown heads throw and no special/pure form accesses @performers or register_performer. `name_of/1` accepts binary unchanged, keyword atom or quoted symbol with namespace stripped, and returns nil otherwise. Params resolve directly from env, so `(do! title {})` lets wire data select any exact binary registry key. Namespaced keyword/symbol ops can collapse onto another bare op. This is indirect performer selection by design surface, not direct callable/registry access; assess reportability after host audit.

** DONE Audit host conversion and atom safety
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-1
:END:

Trace intent values through Data.to_bl/from_bl, Map.get(intent, "op"), lookup, exceptions, and any atom interning in lib/beam_lisp/spell/server.ex and data conversion modules. Acceptance: determine whether wire-controlled strings create atoms or alter lookup authority, citing exact lines.

Confirmed ordinary op path does not intern: event payload keys remain strings; name returns binary; Data.from_bl preserves as binary; Map.fetch and inspect do not atomize. `(keyword wire)` can intern through pre-existing guarded whitelist primitive, not introduced by patch. New performer result path is unsafe: any returned plain map is Data.from_bl-normalized then assign_all atomizes every top-level key, invalidating assign_all's preexisting closed-declared-assign invariant. A performer returning/forwarding attacker-derived payload keys causes unbounded String.to_atom.

** DONE Audit performer registry lifecycle concurrency
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:DEPENDS: task-1
:END:

Trace every register_performer/2 caller and persistent_term update behavior. Acceptance: establish boot-only vs runtime registration, lost-update race conditions, and whether any concern is patch-introduced/actionable with exact code paths.

Observed unsynchronized get→Map.put→persistent_term.put can lose concurrent distinct registrations; replacing the map triggers persistent_term replacement/global-GC costs. However repo has no production runtime/event caller, docs/error explicitly instruct boot registration, walker cannot invoke the public API, and the same pattern is established in register/2 and Record.register. No patch finding: only trusted host misuse or unsupported concurrent boot writes trigger it.

** DONE Compare do and ask execution guards
:PROPERTIES:
:TASK_ID: task-5
:STATUS: completed
:DEPENDS: task-2 task-3 task-4
:END:

Compare do! recording/performance with ask!, maybe_ask, loop_running?, apply_result ordering, and LiveView callback contexts. Acceptance: identify any patch-introduced execution outside intended loop/process guarantees, or document parity with exact lines.

Event/info execution parity is sound: both effects are recorded by walker and run synchronously in caller; ask checks its process dependency while do checks registry membership. Found mount-specific defect: apply_result now performs do! from run_mount_event on both LiveView's disconnected and connected mount passes, duplicating writes and invalidating the claimed owning-process lifetime for async performer work. Independent validator upheld this and atom finding at P1.
