#+TITLE: Session 155ff40a34ebed20 Todos
#+DATE: 2026-08-21
#+TODO: ITEM DOING BLOCKED | DONE

* Review

** DONE Inspect engine diff and runtime fixtures
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Read commit e58ba8b diff plus full priv/datom/query/engine.bl, query tests, and test/runtime setup. Identify exact public APIs and data-loading forms needed to execute the requested cases. Acceptance: executable query harness and patch line anchors are known.

Commit adds engine from scratch. `mix beam_lisp.run --path priv /dev/stdin` executes non-mutating inline harnesses. Untyped schema attributes accept nil and codec supports nil, so nil is a legitimate datom value.

** DONE Cross-check findings and submit verdict
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:DEPENDS: task-2 task-3
:END:

Compare observations with engine semantics and existing tests, use a read-only reviewer subagent for an independent engine pass, report each actionable introduced defect with a ≤10-line diff overlap, then submit verdict exactly once. Acceptance: every finding is runtime- or source-proven and patch-anchored; no known issues are re-reported.

Independent reviewer confirmed the single engine defect at line 85 and found no additional reproducible join issue. Reported P1 for unbound predicate arguments resolving to nil and silently widening results. `git diff --check e58ba8b^ e58ba8b` passed; review made no repository edits.

* Verification

** DONE Execute eight join edge cases
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Create no files; run ephemeral /tmp .bl scripts via `mix beam_lisp.run --path priv` covering self-join, repeated variable, Cartesian product, empty intermediate, nil value, duplicate find rows, hidden bindings dedupe, and input shadowing. Record query, hand-computed expected set, actual set/error. Acceptance: all eight cases observed at runtime.

Observed all eight cases: self-join #{[1 1] [1 2] [2 1]}; repeated variable #{[10]}; Cartesian 9 exact rows; empty middle #{}; nil join #{[1 nil]}; duplicate projection #{[London] [New York]}; hidden bindings dedup same two rows; input shadow match #{[Ada]} and miss #{}. All matched hand-computed expectations.

** DONE Verify unbound predicate failure behavior
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-1
:END:

Trace priv/datom/query/{engine,plan}.bl and execute a query where a predicate consumes a variable before binding if planner permits. Determine whether nil comparison crashes or silently changes answers. Acceptance: observed ordering and runtime output/error with patch anchor.

Observed accepted malformed query `[:find ?n :where [?e :p/name ?n] [(> ?a 40)]]`: validate => []; plan => pattern then predicate; core evaluates `(> nil 40)` as true (with only a compile warning); q returns all three names instead of rejecting unbound ?a. This is a silent wrong-answer defect anchored at engine.bl resolve-term/eval-predicate.
