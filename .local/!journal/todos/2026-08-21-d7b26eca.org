#+TITLE: Session 15604dcf46194504 Todos
#+DATE: 2026-08-21
#+TODO: ITEM DOING BLOCKED | DONE

* review

** DONE Map changed Datom performance paths
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Inspect `git diff` plus full context in `priv/datom/{tx,db,pull}.bl` and `priv/datom/query/{engine,plan,agg}.bl`; identify patch-introduced scan loops and exact diff lines relevant to seven requested operations. Acceptance: candidate cost functions and range-call path mapped with patch anchors.

Target patch is HEAD f84de1f. `realize` calls `current-values` once per op and globally re-filters pending datoms; new `net-datoms` calls `held-before?` once per surviving fact. Read path maps through `datom.db/datoms` to one `datom.index/scan`/`-range` per clause-binding.

* measure

** DONE Run single three-size benchmark probe
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Create only `/tmp/perf.bl`; execute exactly one sequential `timeout 200 mix beam_lisp.run --path priv /tmp/perf.bl` for N=100,300,600, timing bulk transact, entity, column query, join, nested pull, count aggregate, and historical as-of column query using monotonic milliseconds. Acceptance: one complete numeric result set, no `mix test`, no concurrent process.

Initial tempid-based run produced N=100/300 then timed out before N=600 (1.188s→20.311s); final isolating run used explicit positive ids to exclude tempid resolution and completed: bulk 633/4262/14367ms; repeated read totals captured with correct cardinalities 100/300/600.

* analysis

** DONE Compute ratios and range-call count
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Compute 300/100, 600/300, and 600/100 cost ratios from probe output; inspect exact query engine/index/store call graph to determine `datom.store/-range` count for the two-clause join and label measured versus inferred. Acceptance: numeric ratios, complexity classification, named functions/lines, and range-call formula/count.

Normalized ms/op and ratios computed. Inferred from `run-clauses`→`eval-pattern`→`datom.db/datoms`→`datom.index/scan`: join performs 1 full AEVT range plus N bound EAVT ranges = N+1 (101/301/601). Other inferred ranges: entity 1, column 1, pull 5, aggregate 1, as-of 1; positive-id bulk 6N total, of which new net pass adds 3N.

* report

** DONE Rank regressions and submit verdict
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:DEPENDS: task-3
:END:

Rank patch-introduced superlinear/network-round-trip risks by measured impact and Hobbes amplification; call `report_finding` only for criteria-satisfying issues with changed-line anchors in `/home/user/code/undefine/beam-lisp`, then submit structured verdict. Acceptance: concise measurement table and ranking captured in review findings where actionable, or correct verdict if no qualifying patch bug.

Reported P1 at tx.bl:343-345. Ranking: (1) fuse realization/net state to remove quadratic pending scans plus added 3N reads; (2) address inferred N+1 join ranges before Hobbes; (3) defer fixed-count/single-range read paths.
