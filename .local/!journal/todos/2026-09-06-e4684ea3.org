#+TITLE: Session 157532228c23b1a2 Todos
#+DATE: 2026-09-06
#+TODO: ITEM DOING BLOCKED | DONE

* Investigation

** DONE Trace ANF adapters and tuple assumptions
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Inspect lib/beam_lisp/emit.ex, lib/beam_lisp/aot.ex, lower.bl adapters, and link.ex semantics. Identify representation boundaries and all tuple assumptions affecting body modules/init. No mutations outside three allowed targets.

lower.bl already owns mixed payload semantic lowering via defs->module-anf-any and shim-def-any. AOT Core body seam consumes Emit.body_modules. Remaining Elixir namespace shim must structurally adapt ANF payload; one unguarded forwarder per fname/arity correctly delegates clause/guard selection to Core body. Legacy quoted clauses retain existing guarded shims.

* Implementation

** DONE Implement structural ANF emission compatibility
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Modify only lib/beam_lisp/emit.ex and lib/beam_lisp/aot.ex. Reuse existing lowering/link semantics; accept ANF compiler output plus legacy Elixir AST seed defs. Stabilize body-module and init handling without seed regeneration or hotpatching.

Emit.shim_defs now dispatches by payload shape. ANF clauses produce one neutral Elixir namespace forwarder per fname/arity, delegating guards and patterns to the lower.bl-built Core body module. Legacy quoted AST guard shims remain unchanged. Existing AOT body quoted construction remains confined to false backend branch; tuple indexing in init/stabilisation already supports five-field attached defs.

* Tests

** DONE Add guarded multi-arity ANF regressions
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:VERIFY_REVIEW: Tests directly exercise representation compatibility; inspect diagnostics/diff and only use isolated commands that do not trigger builds.
:DEPENDS: task-2
:END:

Create test/beam_lisp/anf_emit_test.exs only. Cover ANF def representation, guarded clauses, multiple arities, body modules/init assumptions, and legacy Elixir AST compatibility where feasible. Do not run mix compile or mix test.

Added structural ANF shim dedup/arity test, legacy guarded AST preservation test, and end-to-end isolated-output AOT test exercising guarded same-arity clauses plus a second arity and __bl_init__. Verification run: all three changed files parse via Code.string_to_quoted!; git diff --check passes; standalone elixir -r emit.ex ANF shim probe passes. Per instruction, mix compile/test not run.
