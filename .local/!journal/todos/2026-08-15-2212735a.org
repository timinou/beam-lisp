#+TITLE: Session 1558a4738b2ead88 Todos
#+DATE: 2026-08-15
#+TODO: ITEM DOING BLOCKED | DONE

* review

** DONE Map emitter changes and module conventions
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Inspect commits 3b53730, 7de53e2, d773129, 76c06e8 plus full spell/src/spell/live.bl, lib/beam_lisp/spell/page.ex, and spell.contract module-name logic. Acceptance: identify exact changed lines and data flow from machine to emitted page.

Mapped W4 additions. live.bl extracts sections from contract.bl EDN strings; Page emits Verse-printed seam/forms plus hand-serialized styles. Contract module mapping uppercases only segment initials; Page uses String.capitalize. Machine registries are name-keyed maps exposed via vals.

** DONE Attack merge and naming fidelity invariants
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Construct/reason through bracket/string attacks, two-contract/two-view merge, host naming, module-name parity, and deterministic repeat emission using target files and existing compiled/runtime artifacts without changing repo files. Acceptance: each attack has observed or source-proven outcome and affected diff lines.

Observed exact scanner simulation: ordinary `'a]b'` stays complete; EDN `<i data-x=\"a]b\">` truncates constructs after `a`; later `:st/forms [` in template HTML does not shadow the real forms key. A legal earlier bind selector containing `:st/scopes [` shadows the later key and extracts only `'`. Normal two-contract/two-view sections all join; host and seam names are identical for multi-hyphen names; requested lowercase module names agree. Immutable map traversal is repeat-stable but not registration-ordered.

** DONE Compare CSS paths and finalize findings
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Compare machine-css serialization with verse emit CSS semantics and reconcile all evidence against patch scope. Acceptance: report each actionable introduced bug with ≤10-line diff anchor, or return correct verdict naming checked attacks.

Canonical style path maps `(inject @x)` to `[:st/inject "$x"]` / `<-`; machine-css raw stringification loses it. Normal string property names, units, and quoting in existing seed output match. Repeat emit is stable, but map-backed views do not retain the claimed registration precedence. Four patch-anchored findings reported.
