#+TITLE: Session 155a2a332974a9a6 Todos
#+DATE: 2026-08-16
#+TODO: ITEM DOING BLOCKED | DONE

* audit

** DONE Inspect Data conversion and changed diff
:PROPERTIES:
:TASK_ID: task-a
:STATUS: completed
:END:

Read lib/beam_lisp/spell/data.ex and diff e9be8a6..HEAD; enumerate to_bl modes and changed lines. Acceptance: exact mode semantics and patch anchors recorded.

Observed Data.to_bl/2 lines 109-136: list whitelist, :all_strings, :as_written. Workspace diff qualifier unavailable; current changed implementation and caller lines are observable.

** DONE Enumerate and classify every caller
:PROPERTIES:
:TASK_ID: task-b
:STATUS: completed
:DEPENDS: task-a
:END:

Locate every Spell.Data.to_bl/1,2 call in lib/, scripts/, tests/ using semantic/find plus alternate search; trace each caller's source provenance and mode. Acceptance: complete caller table with exact paths/symbols.

Direct call count: lib 8 (schema 1, all_strings 4, as_written 3); scripts 1 helper (as_written, invoked at 82/83/105/127); tests 23 (schema 16, all_strings 6, as_written 1). Total direct calls 32: schema 17, all_strings 10, as_written 5.

** DONE Check concrete regressions and security boundary
:PROPERTIES:
:TASK_ID: task-c
:STATUS: completed
:DEPENDS: task-b
:END:

Evaluate nested/mixed/unknown/transport/contract/config inputs against :as_written; perform cheap focused repro only if available, no full suites/builds. Acceptance: every conclusion anchored to observed input/path and diff line, or explicit safe rationale.

No reportable defect: all production model/browser/wire inputs use schema whitelist or :all_strings; :as_written receives locally rebuilt literal-key maps. Nested recursion preserves this invariant. Plan's unknown-key wording says dropped, but current implementation intentionally retains unknown keys as strings and tests assert that behavior; not a concrete regression.
