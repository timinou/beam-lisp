#+TITLE: Session 1563de4d9cb51510 Todos
#+DATE: 2026-08-24
#+TODO: ITEM DOING BLOCKED | DONE

* Review

** DONE Trace store writes and handle exposure
:PROPERTIES:
:TASK_ID: trace-store
:STATUS: completed
:END:

Inspect src/reel/store.bl and priv/datom*.bl. Map every write/read entry, transact! ownership, conn/store lifetime, DB value shape. Acceptance: concrete ownership/lifetime conclusions with exact anchors.

Writes funnel through reel.store/transact! and transact-as!; app read paths use db values. Conn API is exposed, and Db record embeds store handle, so db snapshots retain redb handle beyond server call.

** DONE Trace corpus transaction and partial states
:PROPERTIES:
:TASK_ID: trace-corpus
:STATUS: completed
:DEPENDS: trace-store
:END:

Inspect src/reel/corpus.bl and related datom APIs. Determine validation ordering, transaction atomicity, partial-load possibilities, stale/empty DB behavior. Acceptance: exact call/interleaving traces and anchors.

problems runs before tx-data; valid load calls one transact-as!. datom validation rejects before commit; redb write-datoms is one atomic commit. Empty/new DB has schema tx then corpus tx; commit failure leaves no corpus (counter gap only). Existing corpus remains on failed update.

** DONE Review boot rescue and startup semantics
:PROPERTIES:
:TASK_ID: trace-boot
:STATUS: completed
:DEPENDS: trace-corpus
:END:

Inspect lib/reel/boot.ex and relevant callers/supervision. Analyze validation/transaction rescue, performer registration, stale state, brutal store death, supervised linkage, DB/corpus versions. Acceptance: candidate findings only if provable, exact anchors and impacts.

Rescue logs and returns :error, then init registers all performers and reports ready. Fresh DB after validation/storage failure has schema but no corpus; existing DB retains stale prior corpus. Brutal store death leaves registry/store handle; linked Boot supervisor retries start and redb lock can prevent restart.
