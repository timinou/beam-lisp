#+TITLE: Session 156038211e5944ff Todos
#+DATE: 2026-08-21
#+TODO: ITEM DOING BLOCKED | DONE

* review

** DONE Inspect datom patch and public APIs
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Run git diff in /home/user/code/undefine/beam-lisp; read modified priv/datom/*.bl and relevant test context. Acceptance: identify changed lines and exact callable API signatures needed for probes.

Observed origin/main...HEAD adds full priv/datom stack; public paths/signatures confirmed in conn.bl, db.bl, pull.bl, query engine/agg, tx.bl, time.bl. Working-tree diff contains journal only; review baseline is committed PLAN-033 series.

** DONE Validate findings against changed source
:PROPERTIES:
:TASK_ID: task-6
:STATUS: completed
:DEPENDS: task-5
:END:

Trace every observed wrong answer/hang/unclear error to modified diff lines in /home/user/code/undefine/beam-lisp/priv/datom; reject accepted/pre-existing/design limitations. Acceptance: each retained finding has executed reproducer, provable impact, ≤10-line diff anchor, priority and confidence.

Retained four executed, patch-anchored wrong-answer defects: unqualified reverse refs; value-only ref tempids; final add after same-tx retract; unsupported string/keyword entity IDs accepted. Dropped 50k latency as non-blocking performance (roundtrip correct) and all documented/sensible behaviors.

* runtime

** DONE Probe empty and recursive read paths
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Create no files in repo; run temporary /tmp/*.bl probes via timeout 20/120 mix beam_lisp.run --path priv covering cases 1-3: empty q/pull/entity/datoms/changes/aggregates/lookup-ref and empty tx forms, self-reference pull, two-entity cyclic pull. Acceptance: record concrete outputs/timings and classify each path.

Empty paths returned #{}/nil/[] as appropriate; [] transaction returned empty datoms/tempids; [{}] raised clear missing :db/id. Self and A↔B finite nested pulls returned correct finite trees. One 20s wrapper lingered after :ok under host load; explicit erlang/halt confirmed cyclic pull itself completed inside guard.

** DONE Probe large Unicode and keyword values
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Run sequential temporary probes for cases 4-6: ~50k string transact/read timing, UTF-8 including non-BMP roundtrip and key sort order, unqualified :name with pull/reverse-attr path. Acceptance: observe bounded runtime, exact returned values/order/errors, and reason from UTF-8 byte construction about embedded NUL.

50k ASCII string round-tripped; encode-value measured 8053ms, transaction 14653ms, read 0ms. UTF-8 values (2/3/4-byte incl. non-BMP) round-tripped and AVET order matched sort; encoded bytes contained no NUL. Unqualified forward attrs worked; reverse :_friend silently omitted an existing incoming :friend ref.

** DONE Probe unusual queries and type mismatches
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:DEPENDS: task-3
:END:

Run sequential temporary probes for cases 7-8: 20 clauses, small Cartesian product, throwing predicate, nil function clause, nested :not; nonexistent forward/reverse refs and string ref value. Acceptance: capture exact output/error/timeout and compare behavior with parser/engine/pull contracts.

20-clause and 2×2 Cartesian queries returned expected sets; arithmetic predicate propagated clear badarith; nil function bound nil; nested double-not returned blocked eid. Dangling int ref stored; nested pull returned nil value and missing-root reverse pull nil per pull contract. String ref rejected with structured wrong-type problem.

** DONE Probe unusual transactions and schema mismatches
:PROPERTIES:
:TASK_ID: task-5
:STATUS: completed
:DEPENDS: task-4
:END:

Run sequential temporary probes for cases 9-10: value-only tempid, same-datom assert/retract, 200 operations, keyword/string :db/id, wrong valueType, undeclared attribute. Acceptance: capture exact outputs/errors and verify state via public reads.

Value-only ref tempid stored raw -99 with empty tempids report; add→retract same tx ended absent, reverse retract→add on an existing fact also ended absent despite final add; 200 ops wrote/read 200 in 212ms; keyword/string ids accepted; wrong value type and undeclared attr rejected with structured problems.

* closure

** DONE Submit final robustness review verdict
:PROPERTIES:
:TASK_ID: task-7
:STATUS: completed
:DEPENDS: task-6
:END:

Call report_finding once per retained issue, then submit_result exactly once with overall correctness, 1-3 sentence explanation, confidence, and auto-populated findings. Acceptance: ticket closed via structured tool output only.

Four P2 findings recorded with executed reproducers and exact diff anchors; final verdict ready.
