#+TITLE: Session 15561255d830f8d0 Todos
#+DATE: 2026-08-13
#+TODO: ITEM DOING BLOCKED | DONE

* metadata

** DONE Inspect registry and property metadata
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Read /home/user/code/ora/verse/src/syntax/registry.rs, stdlib_registry.rs, property_types.rs outlines and targeted bodies. Identify core form/property types, declarative tables, and parser-facing consumers; acceptance: cite exact symbols and paths.

RegisteredForm wraps FormClause + MacroDefAst + specificity; SyntaxRegistry indexes forms and capture types; property_type_map derives scoped property types from live registered forms.

* pipeline

** DONE Trace parser visitor and generation consumers
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Inspect form_match.rs and form_scoring.rs via grep/outline only, plus bootstrap.rs, visitor.rs, conversions.rs, treesitter_gen, parser/loader/compiler references. Acceptance: establish registry authority versus validation/LSP artifact and generic traversal availability.

Registry is parser authority through events::parse_matches; FormMatch is serde-enabled universal AST; Visitor supports recursive traversal. treesitter_gen derives rules from MacroDefAst/FormClause, not registry directly; scoring is runtime disambiguation over FormClause.

* report

** DONE Synthesize source-verified architecture verdict
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Cross-check findings against syntax architecture and call/reference edges; answer all five questions with exact type names, paths, and explicit verdict on registry-driven tree↔EDN mapping. Acceptance: structured report identifies gaps and risks without code changes.

All requested answers source-verified. Verdict: parsing is registry-driven; mechanical EDN mapping can use FormMatch + Visitor + serde, but lossless text round-trip and registry-driven construction require hand-written policy for spans/docs/raw expressions, macro semantics, and formatting.
