#+TITLE: Session 15640809d1b5151d Todos
#+DATE: 2026-08-24
#+TODO: ITEM DOING BLOCKED | DONE

* review

** DONE Establish Reel patch and target context
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Inspect git status/log/diffs for Reel commits f54d39b..7179eeb and read projects/reel/lib/reel/{application,boot,web/endpoint}.ex, projects/reel/src/reel/{store,intent}.bl, projects/reel/config/*.exs, and relevant docs. Acceptance: exact changed-line anchors and intended lifecycle are identified.

Nested projects/reel repo is clean at 7179eeb; all target files are new relative to the empty tree. Full target source, config, README, and e2e evidence read.

** DONE Trace multi-tab boot and failures
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Trace LiveView state propagation, PubSub usage, Boot.init ordering/ready? callers, publish behavior without Spacetime, corpus/generate failures, store lock lifecycle, and performer exception mapping. Acceptance: each requested scenario has evidence-backed user-visible behavior and expected behavior.

Verified: endpoint starts only after Boot; ready?/0 has no repository callers and cannot currently self-deadlock; generate/store-lock failures abort before endpoint; publish and corpus failures are swallowed; no board broadcast/subscription exists; CAS/schema/native errors are distinct upstream but catch-all discards them.

** DONE Validate config portability and findings
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Inspect projects/reel/config/*.exs and path/port resolution, cross-check documentation, prove patch-introduced impact, then issue report_finding calls with diff-overlapping lines only. Acceptance: all supported defects are reported and unsupported concerns explicitly excluded from verdict.

Reported five patch-anchored defects. Excluded unsupported concerns: no ready?/0 caller during init; generate and redb-open/lock exceptions prevent endpoint startup by child order; spell_site_dir is checkout-absolute; ports have env overrides; path dependency is intentional.
