#+TITLE: Session 158823020c6ed79f Todos
#+DATE: 2026-09-22
#+TODO: ITEM DOING BLOCKED | DONE

* defects

** DONE Diagnose both drop-rebuild defects at source
:PROPERTIES:
:TASK_ID: recon
:STATUS: completed
:END:

Both are ONE defect: the toolchain reads the drop's tiers, not the checkout's. (a) Tiers.priv_root/0 answers :code.priv_dir -> the drop, so the checkout's priv/std is on no path. (b) selfbuild/stage-sources! stages the running payload, so self-build cannot absorb a checkout change. (c) loom.token/realize in the drop is private while the workspace fixed it. Mixing generations is measured-impossible (source-graph/cycles). Filed as BUG-096.

Evidence: loader.ex:845 search_dirs; priv_root returns drop; three BEAM_LISP_PATH variants all fail differently.

** BLOCKED ~~Fix ns resolution so the build sees its own std~~
:PROPERTIES:
:TASK_ID: fix-index
:STATUS: abandoned
:VERIFY_CMD: ./bl self-build
:DEPENDS: recon
:DEFERRED_TO: BUG-096
:END:

The self-build reads workspace priv/std sources but resolves against the drop's priv/std, so any NEW std module (vm/index, vm/ports, bl/json) is invisible. Chicken-and-egg.

NOT FIXED — deliberately. Every partial path fix is measured-unsafe (namespace diag missing; source-graph/cycles). A real fix is a build-system change that can produce a broken drop. Deferred to BUG-096 with the wiring named.

** BLOCKED ~~Fix loom.token/realize visibility~~
:PROPERTIES:
:TASK_ID: fix-loom
:STATUS: abandoned
:VERIFY_CMD: ./bl check priv/lib/live/
:DEPENDS: recon
:DEFERRED_TO: BUG-096
:END:

priv/lib/live/socket.bl:205 calls t/realize but the compiler says the var is not public. Either export it or use the public entry point.

NOT FIXED — the workspace ALREADY fixed it (defn realize, public at loom/token.bl:273). The DROP is stale, so the compiler sees the private copy. Not a socket.bl bug. Same root as fix-index; deferred to BUG-096.

** BLOCKED ~~Rebuild the drop green~~
:PROPERTIES:
:TASK_ID: rebuild
:STATUS: abandoned
:VERIFY_CMD: ./bl run examples/... (or a probe requiring bl.json)
:DEPENDS: fix-index fix-loom
:DEFERRED_TO: BUG-096
:END:

After both fixes, self-build to completion; confirm bl.json and vm.ports resolve from a workspace lib.

NOT DONE — bl self-build fails on loom.token/realize and vm.index, and structurally cannot absorb the checkout. Deferred to BUG-096.

* credential

** DONE Fix flow test hang, get loopback E2E green
:PROPERTIES:
:TASK_ID: flow-test
:STATUS: completed
:VERIFY_CMD: ./bl test test/bl/credential/
:END:

Two real bugs found, both invisible to a passing-looking test: (1) erlang/spawn does NOT carry a closure's env across the process boundary, so the fake browser never ran and accept blocked its full 5-min fence; (2) :inet.port/1 answers {:ok,Port}, so the URL carried a url-encoded Erlang tuple. Test is now race-free — ring before accept, one process, no sleeps.

19 flow / 42 oauth2 / 19 env / 7 store = 87 green.

** DONE Real token exchange over the wire
:PROPERTIES:
:TASK_ID: post-http
:STATUS: completed
:VERIFY_CMD: ./bl test test/bl/credential/
:DEPENDS: recon
:END:

post-form! added (RFC 6749 form POST, not JSON) as finish!'s 2-arity default, plus id-claims for the OIDC sub. Two bugs found ONLY by hitting Google: content-type in both headers and the request tuple sends it twice -> 400 HTML; and decode-error raised {:invalid_byte,60} on that HTML. Both fixed; decode is now tolerant (:unparseable/:config-error).

Live: Google returns 400 {"error":"invalid_grant","error_description":"Malformed auth code."} — proper JSON, so transport+headers+body are correct.

* showcase

** DONE Build the .bl authorize app that opens the browser
:PROPERTIES:
:TASK_ID: showcase
:STATUS: completed
:VERIFY_CMD: ./bl run examples/connectors/01-authorize.bl
:DEPENDS: flow-test post-http
:REF: org://PLAN-137
:END:

Runnable app: reads .env, begin! prints/opens the URL, user clicks, callback lands, tokens stored 0600. User connects themselves.

examples/connectors/01-authorize.bl. Live run reached the Google consent screen; process listening on 127.0.0.1:8000 awaiting the human click.
