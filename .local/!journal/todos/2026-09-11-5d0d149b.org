#+TITLE: Session 157bd76d4e6112f7 Todos
#+DATE: 2026-09-11
#+TODO: ITEM DOING BLOCKED | DONE

* verbs

** DONE Write bl.doctor + doctor_test.bl
:PROPERTIES:
:TASK_ID: doctor
:STATUS: completed
:END:

priv/std/bl/doctor.bl.md: probes fn (>=8: language* otp elixir beam-lisp search-paths code-paths datom_fjall explorer lazy_memo* z3 wry daemon src bl-check-edn; * = required), render aligned table, run emits + exit per ok. test/bl/system/doctor_test.bl: >=8 entries each :name :ok :detail, language ok, render contains 'otp'.

** DONE Write bl.serve + serve_test.bl
:PROPERTIES:
:TASK_ID: serve
:STATUS: completed
:END:

priv/std/bl/serve.bl.md: port atom + (port default) helper, run registers paths, resets atom from (:port st), run_file with argv, prints 'bl serve: <file> running — Ctrl+C to stop', parks in Process/sleep :infinity, returns 1 on raise. usage 2 when no FILE. test/bl/system/serve_test.bl: port helper default + override.

** DONE Write bl.monitor
:PROPERTIES:
:TASK_ID: monitor
:STATUS: completed
:END:

priv/std/bl/monitor.bl.md: DIR required, ensure_loaded reload + reload.monitor, trap_exit, ReloadWatcher/start_link with on_result repaint = clear+home+render-text(inspect), own watch-error mirroring cli's (do not edit cli.bl), park. Port from lib/mix/tasks/beam_lisp.reload.monitor.ex.

** DONE Write bl.examples + examples_test.bl
:PROPERTIES:
:TASK_ID: examples
:STATUS: completed
:END:

priv/std/bl/examples.bl.md: globs default examples/**/*.bl, Path/wildcard + distinct + sort, add examples to search path, ensure_loaded reload + reload.ward, preload datom/auth/reload.migrate guarded, entries {'path' p 'src' (Loader/read_source p)} string keys, call reload.ward/run-examples directly, emit {:passed :skipped :failed :ok :files} via plain, text = report-examples, exit per :ok. test/bl/system/examples_test.bl: tmp dir one trivial example -> exit 0 + :passed 1; throwing example -> exit 1.

* cleanup

** DONE Delete mix tasks + fix stale refs
:PROPERTIES:
:TASK_ID: cleanup
:STATUS: completed
:END:

Delete lib/mix/tasks/beam_lisp.examples.ex and lib/mix/tasks/beam_lisp.reload.monitor.ex. Fix examples/reload/00-README.md:75 (mix beam_lisp.reload.monitor DIR -> bl monitor DIR).

** DONE Remove BL_SERVE guards from 8 examples + docs
:PROPERTIES:
:TASK_ID: deserve
:STATUS: completed
:END:

examples/loom/{box,token,ui,form,transform}-gallery.bl, examples/reload/09-web-monitor.bl, examples/data/{studio,explorer}.bl, docs/data/studio/00-the-studio.bl.md. Each main starts its server and returns (no sleep, no headless branch); headers -> 'Run: bl serve <path>'. Acceptance: grep -rn BL_SERVE examples priv lib docs empty.

* verify

** DONE Compile, run tests, verify end-to-end
:PROPERTIES:
:TASK_ID: verify
:STATUS: completed
:DEPENDS: doctor serve monitor examples cleanup deserve
:END:

mix compile once; mix bl test on the 3 new test files; mix bl doctor (+--json); mix bl examples examples/hello.bl; timeout 8 mix bl serve examples/tooling/vitals.bl (expect 124 + URL, curl 4043); timeout 5 mix bl monitor examples/reload (124); grep checks; ls lib/mix/tasks.

mix compile EXIT=0; mix bl test 3 files → 15 tests/33 assertions 0 failures; doctor text+json exit 0; examples/hello.bl passed 1 exit 0; monitor frame + 124; serve URL+curl 200 on 4043 + 124; --port override verified source and AOT; BL_SERVE grep empty; no beam_lisp.* mix tasks; 4 new verbs AOT-compile in isolation.
