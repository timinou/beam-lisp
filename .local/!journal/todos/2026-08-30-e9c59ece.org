#+TITLE: Session 156c280c1f9848e6 Todos
#+DATE: 2026-08-30
#+TODO: ITEM DOING BLOCKED | DONE

* investigate

** DONE Inspect Cluster A tests and implementations
:PROPERTIES:
:TASK_ID: task-a
:STATUS: completed
:END:

Read test/beam_lisp/wave15_macro_test.exs assertions for the three named tests; inspect lib/beam_lisp/compiler.ex and priv/compiler.bl around &env, macro expansion, &form metadata. Acceptance: verbatim assertions, genesis functions/error logic, current .bl functions, and exact port sketch captured.

Genesis macro_clauses injects &form/&env; macro_env maps symbol nodes to names; datum strips positional metadata, preserving only vector author metadata; .bl equivalents are present. Main parity risks are compile metadata path and attr-map handling.

** DONE Inspect Cluster B attr-map handling
:PROPERTIES:
:TASK_ID: task-b
:STATUS: completed
:DEPENDS: task-a
:END:

Read test/beam_lisp/wave25_reader_test.exs named assertions; inspect genesis parse_fn_clauses/defn metadata and .bl parse-fn-clauses, compile-defn, var-meta-map, name-author-meta. Acceptance: verbatim test code, exact implementation quotes, failure explanation, beam-lisp port sketch.

Genesis compile_def_special calls split_defn_attr after split_docstring; .bl compile-defn* only splits doc then feeds attr map to parse-fn-clauses, which treats it as parenthesized clause and crashes while counting map tuple.

** DONE Inspect Cluster C compatibility and locations
:PROPERTIES:
:TASK_ID: task-c
:STATUS: completed
:DEPENDS: task-b
:END:

Inspect jank_compat, wave20_locations, wave26_atom_guard tests and relevant genesis/.bl code. Acceptance: verbatim assertions and failure/source slices; runtime location mechanism gap; atom guard path comparison; concrete port sketches.

bit-not fixture has docstring + attr map, so .bl parse-fn-clauses misreads map and crashes; distinct? is defined verbatim and should work after function compilation. Runtime location gap: .bl threads env positions but never stamps generated AST nodes with line metadata. Novel keyword gap: .bl uses binary_to_atom directly instead of BeamLisp.AtomGuard/to_atom.
