#+TITLE: Session 15754ccf4763b1bc Todos
#+DATE: 2026-09-06
#+TODO: ITEM DOING BLOCKED | DONE

* Audit

** DONE Map LazySeq construction and structural access
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Search lib/, test/, native/, mix.exs for BeamLisp.LazySeq constructors, field access, pattern matches, and references. Use code graph where indexed. Acceptance: exact-path inventory distinguishing automatic cutover vs required edits.

Mapped constructors/accessors. Structural coupling outside lazy_seq: meta.ex key rewrites; loader_server monitor callbacks; wave23 tests inspect key/tid. RT/core consumers route through public API and benefit automatically.

** DONE Classify ETS caches and lazy abstractions
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Search runtime/compiler/vector/lib/test paths for direct ETS memo access, Stream/list lazy abstractions, and cache ownership. Acceptance: classify shared lazy computation caches vs permanent registries that must remain separate, with exact actions.

Only LazySeq ETS is shared lazy computation cache being cut over. Env vars/per-env instances, Native declarations, Record metadata, AOT persistent/disk caches, Sandbox bases, tiers/compiler flags, PerfProbe, test registries are lifecycle-scoped registries/caches with distinct semantics and must remain.

** DONE Audit native packaging and lifecycle integration
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Inspect native crates, build/compiler packaging, seed/cache/release integration, and tests. Acceptance: exact requirements for lazy_memo NIF availability and integration without running builds.

Native compiler auto-discovers crate and installs priv/native/lazy_memo.so; release carries priv. Mandatory runtime changes absent-cargo behavior: lazy_memo must not silently remain unavailable. Hex package needs explicit native source inclusion if publishing. No seed regen/API syntax change; compiler key need not rotate for runtime-only semantics.

* Deliverable

** DONE Write comprehensive ownership audit artifact
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:VERIFY_ARTIFACT: !tasks/artifacts/PLAN-092-lazyseq-ownership-audit.md
:VERIFY_REVIEW: Comprehensive against contract; exact paths and explicit action classifications.
:DEPENDS: task-3
:END:

Create one artifact under !tasks/artifacts/ covering all findings, exact paths/actions, automatic beneficiaries, out-of-runtime edits, deliberately separate registries, native integration and test impacts. Acceptance: artifact is self-contained and no other files modified.

Created 192-line self-contained audit artifact. Verified file exists and includes automatic beneficiaries, required sibling edits, separate cache classifications, native/package/seed/release integration, tests, and risk matrix.
