#+TITLE: Session 156031284a5944f7 Todos
#+DATE: 2026-08-21
#+TODO: ITEM DOING BLOCKED | DONE

* Review

** DOING Map patch and Datomic public APIs
:PROPERTIES:
:TASK_ID: task-1
:STATUS: in_progress
:END:

Inspect `git diff`, `priv/datom/**/*.bl`, and existing `test/bl/datom/*_test.bl` to identify exact transaction/query/pull/entity/datoms/changes/aggregate call shapes. Acceptance: every requested attack has a concrete executable expression and modified-line provenance is understood.

* Execution

** ITEM Run empty and guarded recursion attacks
:PROPERTIES:
:TASK_ID: task-2
:STATUS: pending
:DEPENDS: task-1
:END:

Run serial short-lived `mix beam_lisp.run --path priv` probes for empty read paths/transactions, self-reference nested pull, and A↔B nested pull. Guard recursion probes at ~20 seconds and keep each process isolated. Acceptance: observed output/exit/timeout for attacks 1-3.

** ITEM Run value query and transaction attacks
:PROPERTIES:
:TASK_ID: task-3
:STATUS: pending
:DEPENDS: task-2
:END:

Run serial compact probes covering 50k strings, UTF-8/NUL construction, unqualified keywords, 20 clauses, cartesian products, throwing predicates, nil fn clauses, nested `:not`, dangling refs/reverse refs, value-only tempids, assert+retract same datom, and 200 operations. Acceptance: observed output and timings for attacks 4-9 without `mix test`.

* Analysis

** ITEM Trace failures and submit review verdict
:PROPERTIES:
:TASK_ID: task-4
:STATUS: pending
:DEPENDS: task-3
:END:

For every abnormal result, inspect exact implementation and patch lines, rerun a minimal reproducer where safe, then call `report_finding` only for actionable patch-introduced bugs. Acceptance: every requested attack summarized in evidence, findings patch-anchored, and exactly one final `submit_result` call.
