#+TITLE: Session 156c7b57e7ddca6a Todos
#+DATE: 2026-08-30
#+TODO: ITEM DOING BLOCKED | DONE

* Evidence

** DONE Trace bootstrap and drift execution paths
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Read lib/beam_lisp.ex, lib/beam_lisp/aot.ex, lib/beam_lisp/compiler.ex, and lib/mix/tasks/compile.beam_lisp.ex. Establish process/VM boundaries, exact ordering before AOT.stale?/2, and whether an already-interned compiler/compile var remains usable after source recompilation. Acceptance: cite exact paths/lines and eliminate cold-VM ambiguity.

Observed: custom AOT compiler runs after :elixir in the same Mix VM (mix.exs; compile.beam_lisp.ex run/1), calls BeamLisp.init/0, and dispatches through Env.loaded_ns?("compiler"). Separate shell `mix` is cold. AOT.ensure_loaded gates before __bl_init__; Loader then falls to source. Current init loads core/multi/sugar before compiler. Compiler seed requires reader-node. Therefore a cold hard-cutover needs the entire bootstrap namespace artifact closure, not only BeamLisp.Ns.Compiler.beam.

* Design

** DONE Derive sound compiler provenance key
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Read lib/beam_lisp/aot_cache.ex and AOT provenance emission/checks. Identify every compilation-semantic input and separate bootstrap transport/loading code from output-affecting machinery. Acceptance: recommend an implementable key definition and explain both false-positive and false-negative invalidation risks.

Verdict: decouple from Genesis, but not by blindly dropping the whole Compiler module. Physically isolate Genesis from output-affecting driver code, exclude only Genesis/installer/loading policy, and hash a versioned semantic closure: self-hosted compiler + reader-node + implicit prelude sources, exact Elixir/OTP target, and host reader/eval/emit/link/runtime modules that can alter output. Current provenance lacks dependency closure and current Mix manifest does not store compiler_key despite the background assumption; both must be corrected for sound invalidation.

* Verdict

** DONE Construct and stress-test cutover sequence
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:VERIFY_REVIEW: Every recommendation is grounded in observed source or explicitly labeled inferred; exact failure modes and acceptance checks are included.
:DEPENDS: task-2
:END:

Use observed code paths to specify tracked seed placement, copy/install timing, build-in-live-VM procedure, cold-start verification, and rollback-safe ordering. For each step name the guarded failure mode. Acceptance: sequence addresses questions (a), (b), and (c), with caveats limited to unverified runtime behavior.

Sequence finalized: redesign key/provenance and bootstrap installer while Genesis exists; generate a complete five-namespace multi-BEAM bootstrap closure through the already-interned BL compiler; prove stage N→N+1 in a cold VM with Genesis forbidden; commit verified bundle; only then remove Genesis and its swallowed fallback. Installer runs before BeamLisp.init/AOT gates and repairs partial builds. Fresh empty-build validation is mandatory.
