#+TITLE: Session 155ffe86756bed37 Todos
#+DATE: 2026-08-21
#+TODO: ITEM DOING BLOCKED | DONE

* review

** DONE Map changed Datomic APIs and harness
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Inspect `git diff` plus `priv/datom/**/*.bl` and existing `test/bl/datom/*_test.bl`; identify executable API shapes and a no-repo-edit strategy for `mix beam_lisp.run --path priv <file.bl>`. Acceptance: exact callable forms for q/pull/entity/datoms/changes/transact and exact diff anchors are known.

Milestone range is `c52d6d3...6360fd8`; public forms mapped from `datom.conn`, `datom.db`, `datom.pull`, `datom.query.engine`, and `datom.time`. Ephemeral probes run without repository edits via `mix beam_lisp.run --path priv <(printf '%s\n' "$SCRIPT")`.

** DONE Attack empty pull and reference paths
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Run empty database/read/transaction cases plus self-ref, A↔B cycles, unnamespaced keyword pull, dangling ref, and reverse dangling ref against `priv/datom/`. Acceptance: each requested case has observed output/error/timing and any defect is traced to a changed line.

** DOING Attack codec scale and Unicode values
:PROPERTIES:
:TASK_ID: task-3
:STATUS: in_progress
:DEPENDS: task-1
:END:

Run 100k-character string through transaction/read/index paths with elapsed time; run multibyte UTF-8 including supplementary-plane characters and inspect NUL/terminator handling in `priv/datom/codec.bl`. Acceptance: round-trip equality and timings are observed; any crash/wrong answer maps to changed code.

** DONE Attack adversarial queries and transactions
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:DEPENDS: task-1
:END:

Run 20-clause query, cartesian product, throwing predicate, nil function clause, nested `:not`, value-only tempid, same-datom assert/retract, empty transaction/map, and 1000-op transaction. Acceptance: exact observed answers/errors/timings for every case and source trace for defects.

** ITEM Validate findings against patch context
:PROPERTIES:
:TASK_ID: task-5
:STATUS: pending
:DEPENDS: task-2 task-3 task-4
:END:

Reproduce candidate failures, distinguish accepted clear errors from wrong answers/infinite loops, ensure each report overlaps `git diff`, and submit verdict. Acceptance: all nine attack families accounted for and only evidence-backed introduced defects reported.
