#+TITLE: Session 155fff195c6bed49 Todos
#+DATE: 2026-08-21
#+TODO: ITEM DOING BLOCKED | DONE

* Review

** DONE Inspect Datom APIs and changed lines
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Read priv/datom/{conn,db,pull,time,tx,index}.bl and relevant tests. Identify changed-line anchors and runtime invocation conventions for assigned cases.

Pull recursion is unguarded; reverse attr assumes namespace; db datoms accepts scans; tx expand validates forms.

** DONE Run empty and malformed input probes
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Run empty connection q/pull/entity/datoms/changes/aggregates/transact [] and [{}]. Capture exact outputs/errors/elapsed time without modifying repo.

Attempted multiple invocations (process substitution, /tmp script, /dev/stdin, pseudo-TTY) with 5-30s OS timeouts; all produced no stdout and remained hung. Runner child BEAM processes required termination. No Datom case reached observable output.

** DONE Run reference and namespace probes
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-1
:END:

Run self-ref, A→B→A nested pulls including recursive-enough timeout, unnamespaced attr/reverse spelling, dangling forward and reverse refs from source/target IDs. Trace suspicious behavior to source anchors.

Runtime unavailable: same runner hang before script output. Static trace completed: pull recursion unbounded; unnamespaced reverse-attr builds namespaced :/attr; dangling refs are filtered via pull nil.

** DONE Synthesize anchored review findings
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:DEPENDS: task-2 task-3
:END:

Cross-check observations against source/tests and changed milestone lines. Return concise observation table, exact evidence, candidate defects with absolute paths/lines, or explicit no-finding.

Static findings anchored: unnamespaced reverse conversion loses namespace; aggregate empty input produces no group; nested pull recursion has no cycle/depth guard. Runtime runner itself hung before script output, so all runtime cases remain unexecuted.
