#+TITLE: Session 156c280c429848e8 Todos
#+DATE: 2026-08-30
#+TODO: ITEM DOING BLOCKED | DONE

* records

** DONE Inspect Cluster A tests and implementations
:PROPERTIES:
:TASK_ID: task-a
:STATUS: completed
:END:

Read test/beam_lisp/wave24_records_test.exs exact assertions; inspect lib/beam_lisp/compiler.ex compile_defrecord, compile_deftype, record reader/printer, dot access; inspect priv/compiler.bl record-related functions. Acceptance: capture verbatim snippets and gaps with line anchors.

Genesis and .bl record paths are both present; likely divergence is list/vector construction in inline grouping or runtime compile support, requiring exact source comparison.

* destructure

** DONE Inspect Cluster B tests and implementations
:PROPERTIES:
:TASK_ID: task-b
:STATUS: completed
:DEPENDS: task-a
:END:

Read wave19_restpattern_test.exs and wave18_destructure_test.exs exact assertions; inspect genesis destructure_vector, destructure_steps, rest/as logic; inspect priv/compiler.bl destructure-vector, destructure-steps, destructure-symbol, amp?, compile-loop, binding-pairs. Acceptance: capture verbatim snippets and divergence locations.

Genesis validates :as position/name, normalizes exhausted rest to nil, rejects map rest, and loop_bindings destructures patterns. .bl vector handler lacks validation; split-variadic ignores extra/missing rest forms; compile-loop assumes every pattern is a plain symbol.

* spec

** DONE Synthesize precise porting specification
:PROPERTIES:
:TASK_ID: task-c
:STATUS: completed
:DEPENDS: task-b
:END:

Reconcile both clusters into requested schema: exact assertions, genesis snippets, current .bl snippets, concrete beam-lisp port sketches respecting list/vector and intrinsic gotchas. Acceptance: every named test quoted and every requested implementation path covered.

Spec identifies reader.bl as the decisive Cluster A blocker, plus .bl :as validation, variadic validation, composite rest map rejection, and loop destructuring gaps.
