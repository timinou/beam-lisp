#+TITLE: Session 156188d591d9451e Todos
#+DATE: 2026-08-22
#+TODO: ITEM DOING BLOCKED | DONE

* Review

** DONE Map writer patch and write call graph
:PROPERTIES:
:TASK_ID: review-patch
:STATUS: completed
:END:

Inspect commit 925a364 diff plus full context in priv/datom/conn.bl and referenced store/schema functions. Acceptance: enumerate every store/counter mutation path and all transact! recursion paths.

Observed: transact! alone enters with-writer; install-schema! mutates schema, allocates ids, scans/backfills, and commits outside it; connect-with creates one writer per Conn and allocates fresh schema ids outside it. No current transact-in-writer call path recurses to transact!. Basis is Conn-local while counters/store are shared.

* Probes

** DONE Probe serialization and basis edge cases
:PROPERTIES:
:TASK_ID: probe-serialization
:STATUS: completed
:DEPENDS: review-patch
:END:

Create temporary /tmp beam-lisp probes only; test install-schema! vs transact!, connect-with initialization, two connections sharing each ETS/map/redb store where supported, reopen behavior, and as-of above basis. Acceptance: capture exact command and observed output for any defect.

Probe /tmp/qa_serialization2.bl via mix run --no-compile. Observed two Conn writers over one store produced 2 entities for one :db.unique/identity value on ETS/map/redb; stale Conn basis hid c2 tx; as-of above snapshot basis exposed later write. Concurrent ETS install-schema!: 80 success returns/live defs, only 74 recovered after reopen. Concurrent connect-with: 50/50 recovered in this run.

** DONE Probe writer death and timeout outcomes
:PROPERTIES:
:TASK_ID: probe-writer-failures
:STATUS: completed
:DEPENDS: review-patch
:END:

Create temporary /tmp probes that kill the Agent writer then transact, and force a write beyond with-writer's 30s timeout. Acceptance: determine recovery, caller result, final data visibility, and partial/apply-after-timeout behavior from observed output.

Reproduced dead writer: after Process/exit :kill, next transact returned {:exit, {:noproc,...}} in 9ms; Conn retained same dead PID and did not recover. Timeout probe attempted with 31s delayed store and suspended writer, but module-load/runtime exceeded 60/90s ceilings before observable post-timeout output; no timeout finding without reproduction.

** DONE Probe exceptions and reentrant deadlock paths
:PROPERTIES:
:TASK_ID: probe-errors-deadlock
:STATUS: completed
:DEPENDS: review-patch
:END:

Probe schema-invalid ExInfo round-trip including :problems, a genuine non-ExInfo transaction crash, and any statically reachable transact! recursion from writer execution. Acceptance: exact output for error identity/data and timeout/deadlock evidence if reachable.

Focused /tmp/qa_exdata.bl reproduced intact ExInfo round-trip: message transaction rejected by schema and {:problems [{:error :wrong-type, :value "bad", :expected :db.type/long, :attr :qa/n}]}; subsequent tx succeeded and writer stayed alive. No static call path from transact-in-writer back to transact!, so no reentrant deadlock probe target. Non-ExInfo custom-store probe exceeded runtime ceiling without output; no finding.

* Report

** DONE Validate evidence and submit review verdict
:PROPERTIES:
:TASK_ID: report-review
:STATUS: completed
:DEPENDS: probe-serialization probe-writer-failures probe-errors-deadlock
:END:

Anchor each reproduced introduced defect to ≤10 changed lines in priv/datom/conn.bl, call report_finding once per issue, and submit one final structured verdict. Acceptance: findings include probe, observed/expected output, impact, store scope; theoretical items excluded from findings.

Reported three reproduced P1 defects: writer/basis scoped per Conn over shared stores; install-schema! bypasses writer and loses acknowledged ETS schema on reopen; dead Agent is never restarted. ExInfo preservation passed; no reentrant call path; timeout and non-ExInfo outcomes left unreported because probes did not complete within execution ceiling.
