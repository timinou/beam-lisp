#+TITLE: Session 155fe0242f6bed15 Todos
#+DATE: 2026-08-21
#+TODO: ITEM DOING BLOCKED | DONE

* review

** DONE Inspect DB visibility and datom paths
:PROPERTIES:
:TASK_ID: inspect-db
:STATUS: completed
:END:

Read priv/datom/db.bl and supporting source; map visible?, current?, retracted-later?, keep-datom?, entity, datoms and exact current line anchors. Acceptance: documented code paths and suspected invariants.

Found current? only checks later tx; tx key excludes op. tx realization current-values includes historical assertions, causing re-add bug.

** DONE Inspect transaction realization behavior
:PROPERTIES:
:TASK_ID: inspect-tx
:STATUS: completed
:DEPENDS: inspect-db
:END:

Read priv/datom/tx.bl realization and related index behavior. Acceptance: exact transaction ordering and key overwrite semantics.

current-values filters assertion bit but not later retractions, so re-add of a previously retracted value is skipped. index-components omit op, so same [e a v tx] add/retract overwrite in store.

** DONE Run targeted Datomic visibility probes
:PROPERTIES:
:TASK_ID: run-probes
:STATUS: completed
:DEPENDS: inspect-tx
:END:

Create only /tmp probes and run supported mix beam_lisp.run commands for add/retract/re-add, cardinality-one, same-tx order, since-t defaults, history datoms/entity. Acceptance: capture observed outputs and distinguish bugs introduced by commits.

mix beam_lisp.run --path priv /tmp/db_visibility_probe.bl returned expected card-one [2], defaults [1]/[2], history entity {:v 2, :db/id 1}; wrong re-add current [] and history omission under same-tx key collision.

** DONE Validate findings against commit diffs
:PROPERTIES:
:TASK_ID: finalize-review
:STATUS: completed
:DEPENDS: run-probes
:END:

Compare candidate behavior to commits cb7bac7 and cd7f9de; retain only introduced silent wrong-result issues with exact current line anchors and reproductions.

Two introduced candidates: tx re-add skips because current-values includes historical assertions; index key omits op so same-tx add/retract collapses history. No issue observed in since defaults, cardinality-one replacement, current visibility, or history entity for distinct txs.
