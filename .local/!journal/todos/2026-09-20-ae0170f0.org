#+TITLE: Session 1585934c870b57f3 Todos
#+DATE: 2026-09-20
#+TODO: ITEM DOING BLOCKED | DONE

* zero-ex

** DONE lazy_memo.ex to defnative vm/cell.bl + Rust rustler::init! + caller moves
:PROPERTIES:
:TASK_ID: z4
:STATUS: completed
:DEFERRED_TO: FUP-113-zero-ex-sweep-remainder-nif-trace-lazy-m
:END:

VERDICT: infeasible by construction — lazy_memo.ex is native runtime BELOW bl (core.bl lazy-seq -> LazySeq -> LazyMemo; the reader/compiler produce LazySeq). Moving to .bl inverts the bootstrap. Correctly stays native — it IS the native-free-boot-closure boundary.

** DONE aot_cache.ex to boot-tier keys + build-tier store + caller moves
:PROPERTIES:
:TASK_ID: z5
:STATUS: completed
:DEFERRED_TO: FUP-113-zero-ex-sweep-remainder-nif-trace-lazy-m
:END:

VERDICT: infeasible — aot_cache.ex keys (compiler_key/key_for_ns) ARE the drift gate bootstrap.ex runs to decide if bl loads, before bl exists; store/fetch run inside the loader's compile path. Boot-closure substrate; can't be bl.
