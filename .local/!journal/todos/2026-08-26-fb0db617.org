#+TITLE: Session 1566ecd2704e6c5c Todos
#+DATE: 2026-08-26
#+TODO: ITEM DOING BLOCKED | DONE

* review

** DONE Inspect dialect request casting semantics
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Read /home/user/code/ora/relay/src/relay/dialect.bl relevant cast-anthropic definitions and malformed system-array handling; record exact lines and source-level raise behavior. Acceptance: evidence-backed findings only.

system-text filters malformed blocks and returns nil for invalid/empty input; no raise observed. cast maps stop_sequences to stop and omits absent max_tokens.

** DONE Trace provider request wire transformations
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Read request.bl, provider/anthropic.bl, provider/openai-compat.bl, req_llm anthropic.ex and options.ex; trace missing max_tokens and stop_sequences ["A","B"] through both paths to exact downstream wire shape.

Missing max_tokens is omitted by dialect/request opts; Anthropic req_llm build_request_body inserts 1024, while OpenAI ChatAPI omits token limit. stop list stays flat: Anthropic translates stop -> stop_sequences ["A","B"], OpenAI emits stop ["A","B"].

** DONE Cross-check patch and report clean focus
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Inspect commit ad7118f diff for patch anchors, corroborate relevant API/library semantics via repository and authoritative sources, then produce only material bugs with exact changed lines, wire outcome, impact, confidence, minimal fix; mark clean cases.

One material contract bug: dialect omission of max_tokens reaches Anthropic provider, which silently emits 1024 instead of rejecting missing required client field. Stop list and malformed system-array paths are clean.
