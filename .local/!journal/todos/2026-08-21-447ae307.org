#+TITLE: Session 15604dcf43594502 Todos
#+DATE: 2026-08-21
#+TODO: ITEM DOING BLOCKED | DONE

* review

** DONE Map changed datom API surface
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Run git diff/status for /home/user/code/undefine/beam-lisp, then enumerate public definitions in priv/datom/**/*.bl. Acceptance: identify all patch-touched files/functions and valid diff line anchors.

Observed origin/main...HEAD: all 14 priv/datom modules are newly added; every source line is patch-anchorable. Enumerated 94 defn forms (public/private).

** DONE Trace duplicate and dead functions
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Use find symbol/reference searches across priv/datom/**/*.bl and test/bl/datom/**/*.bl for range-count, scan-range, validate-attr-def, keep-datom?, variable extractors, scan/datoms, and any other public defs. Acceptance: every redundancy/dead-code claim has observed caller evidence.

Observed: keep-datom? has one test-only caller; scan-range has no caller; attr-datoms/install-schema! have no repo callers; range-count is test-only; validate-attr-def is used by install; all variable helpers have distinct consumers except plan/clause-variables is an exact public alias of parse/all-variables. Raw index/scan is used in production only by db visibility internals and tx current-state logic.

** DONE Audit design docs and contracts
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Read full context around headers and public functions in priv/datom/codec.bl, index.bl, tx.bl, db.bl, plus query and pull modules; inspect tests for malformed transact!, q, and pull behavior. Acceptance: stale claims/API defects are tied to implementation and patch lines without executing builds.

Read full codec/index/tx/db/query/pull/schema/store contracts and malformed-input paths. Confirmed structured transact!/q errors; pull lacks integration validation. Identified stale index-key layout, schema-as-data claim, unused range claim, and concrete lookup-ref/nested pull failures. No mix process run per review constraint.

** DONE Validate and submit review findings
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:DEPENDS: task-3
:END:

Cross-check each candidate against user exclusions and review criteria, report each proven patch-introduced issue via report_finding, then submit one final correctness verdict. Acceptance: no speculative/style-only findings; all line ranges overlap git diff and are ≤10 lines.

Validated nine patch-anchored findings against exclusions: one silent query wrong-answer bug, pull integration/validation defects, indexed schema/range gaps, one dead quadratic helper, and stale key/schema/store contracts. All evidence came from origin/main...HEAD diff, full source, tests, and repository-wide call tracing; no prohibited build/test run.
