#+TITLE: Session 1586c4136bc4422b Todos
#+DATE: 2026-09-20
#+TODO: ITEM DOING BLOCKED | DONE

* impl

** DOING Add watch-debounce owner to reload.bl
:PROPERTIES:
:TASK_ID: task-1
:STATUS: in_progress
:END:

priv/std/reload.bl: (a) extend ns requires with [proc.server :as ps] [proc.tick]; (b) add a `(ps/defserver watch-debounce ...)` whose `(tick QUIET :ms {:once true} [st] ...)` fires `:flush_pending` at the watcher and whose `[:reload/activity owner]` handler calls `(proc.tick/tick-reset quiet {:name 'watch-debounce})`; (c) add `(defn watch-debounce-start [quiet-ms] (start watch-debounce {:quiet quiet-ms} {}))`. Insert before the `;; ── drain:` section. Acceptance: `./bin/bl -p priv/std eval '(reload/watch-debounce-start 40)'` returns a pid (or the BL test proves it).

** ITEM Delegate watcher debounce to BL tick
:PROPERTIES:
:TASK_ID: task-2
:STATUS: pending
:DEPENDS: task-1
:END:

lib/beam_lisp/reload_watcher.ex: drop Process.send_after/flush_timer. init starts the BL quiet-window owner (Loader.ensure_loaded("reload") + RT.invoke(Env.fetch!("reload","watch-debounce-start"),[quiet_ms])) and monitors it. defer/2 = pending + send(owner, {:reload/activity, self()}); owner replies :flush_pending (existing handler). handle_info({:DOWN, ref, ...}) flushes pending and clears the owner. terminate stops the owner. Update the moduledoc + defer comment. Acceptance: existing ExUnit watcher test + bl reload suite stay green.

** ITEM Add debounce tests under test/bl/reload
:PROPERTIES:
:TASK_ID: task-3
:STATUS: pending
:DEPENDS: task-2
:END:

New test/bl/reload/debounce_test.bl: (1) a burst of N synthetic file_events spread across the quiet window produces exactly ONE :applied journal entry for the ns and the LAST on-disk content is live; (2) killing the quiet-window owner does not strand a later save. Uses bl.util/kw + to-list to start BeamLisp.ReloadWatcher with :quiet_ms, :sys.get_state for the fs pid, erlang/list_to_tuple for the FileSystem message shape. Acceptance: `./bin/bl test test/bl/reload/debounce_test.bl` green.

* verify

** ITEM Run reload suites and correct the P4 doc
:PROPERTIES:
:TASK_ID: task-4
:STATUS: pending
:DEPENDS: task-2 task-3
:END:

Run: ./bin/bl test test/bl/reload (whole dir) and ./bin/bl test test/beam_lisp/reload_watcher_test.exs. Then update docs/scheduling/the-clock-and-the-wheel.md lines ~103, ~995 and ~1243 (P4 leg + the 'lives in Elixir' finding) so the doc states the landed cutover. Acceptance: all green; doc no longer claims a mechanism the code does not use.
