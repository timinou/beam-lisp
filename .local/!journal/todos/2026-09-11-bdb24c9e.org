#+TITLE: Session 157bcd2b86edf751 Todos
#+DATE: 2026-09-11
#+TODO: ITEM DOING BLOCKED | DONE

* portability

** DONE Fix native compiler for darwin (.dylib→.so)
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:END:

lib/mix/tasks/compile.beam_lisp_native.ex: resolve cargo artifact name per OS; install rustler-consistent name (.so unix/.dll win).

built_path probes .so/.dylib/.dll; install name keeps .so on unix. Verified by removing lazy_memo.so and forcing a rebuild.

** DONE Make wry_webview Linux-only
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:END:

native/wry_webview/Cargo.toml: target-gate gtk/gtk-layer-shell/wry/tao; lib.rs: crate-level cfg(target_os=linux). Proves macOS mix compile no longer fails.

cargo check --target {x86_64,aarch64}-apple-darwin passes for wry_webview with gtk excluded; Linux still builds it.

** DONE Fix drop trailer target arch
:PROPERTIES:
:TASK_ID: task-5
:STATUS: completed
:END:

tooling/drop/src/pack.rs cmd_pack: encode target arch, not host_arch().

drop pack --target linux/aarch64 → inspect shows arch=1 (was host arch=0 before).

* release

** DONE Tag-derived version in mix.exs
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:END:

version: System.get_env("BL_VERSION") || "0.1.0" so v2026.0 tag -> beam-lisp 2026.0.

BL_VERSION=2026.0.0 build → bl version prints beam-lisp 2026.0.0. Note: Elixir Version requires X.Y.Z, so v2026.0 normalizes to 2026.0.0.

** DONE Add GitHub Actions release workflow
:PROPERTIES:
:TASK_ID: task-6
:STATUS: completed
:END:

.github/workflows/release.yml: matrix linux/x86_64 (ubuntu-24.04), linux/aarch64 (ubuntu-24.04-arm), macos/x86_64 (macos-15-intel), macos/arm64 (macos-15); setup-beam otp 29/elixir 1.20; deps.get, z3.fetch, bl.build, smoke, upload; release job attaches to tag.

release.yml: 4-leg matrix, verified YAML, reviewer pass (0 defects), smoke script validated verbatim against a real drop from a checkout-free cwd.

* proof

** DONE Re-verify: rebuild drop after fixes, smoke native tier
:PROPERTIES:
:TASK_ID: task-7
:STATUS: completed
:DEPENDS: task-2 task-3 task-4 task-5
:END:

Rebuild Linux drop with the portability edits; assert version/eval/datom/z3/explorer still work. cargo check --target x86_64-apple-darwin for the crates if rustup target add works.

Rebuilt with all fixes + BL_VERSION=2026.0.0; smoke 6/6 green (version/language/fjall/explorer/lazy_memo/z3); trailer os=0 arch=0.

* Review

** DONE Review the CI workflow and portability edits
:PROPERTIES:
:TASK_ID: task-8
:STATUS: completed
:END:
