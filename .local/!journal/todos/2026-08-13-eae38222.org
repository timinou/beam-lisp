#+TITLE: Session 155612548c70f8cc Todos
#+DATE: 2026-08-13
#+TODO: ITEM DOING BLOCKED | DONE

* syntax

** DONE Map syntax representations and parser layers
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Read /home/user/code/ora/verse/src/syntax/ARCHITECTURE.md, syntax/mod.rs, syntax/cst/mod.rs, syntax/cst/ast.rs, and targeted lexer/parser symbols. Acceptance: record exact type names, node enums, spans/comments/trivia, serde traits, and file:line citations.

CST is rowan SyntaxNode with SyntaxKind; typed wrappers are AST views. Public parser path is legacy parser::parse -> StFile via cst_to_stfile; architecture document's FormMatch-only pipeline claims are stale relative to observed source.

* pipeline

** DONE Trace loader compiler IR outputs
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Inspect /home/user/code/ora/verse/src/loader.rs, compiler.rs, and every file under src/ir/. Acceptance: produce ordered source-to-output stages with exact Rust types/module paths, lossiness, serde status, and file:line citations.

Loader uses parser::ast::StFile; compiler compile_pipeline consumes StFile.matches and emits pipeline types then CompiledSpacetime. ir::code types are structured codegen IR but not serde-serializable.

* edge-cases

** DONE Locate unparser and error behavior
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-1
:END:

Search /home/user/code/ora/verse/src for pretty-printer/unparser/formatter APIs and parser poison/error representations. Acceptance: answer bidirectional text-generation question and document concrete error nodes/types with citations.

serializer::serialize(StFile)->String exists but is explicitly simplified and omits comments/trivia, meta defs, HTML, raw blocks, and most matches; not lossless unparser. CST parser has ERROR nodes and ParseResult.errors; lexer preserves invalid text; legacy parser returns ParseErrors.

* synthesis

** DONE Synthesize cited pipeline findings
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:DEPENDS: task-2 task-3
:END:

Reconcile findings into a stage table and direct answers to all four critical questions, clearly separating observed facts from inference. Acceptance: complete structured handoff with no uncited claims.

Stage map and critical answers assembled; key finding is dual parser layers: rowan CST/typed wrappers, then parser::ast::StFile conversion, then FormMatch pipeline. serializer is non-lossless.
