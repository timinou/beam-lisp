#+TITLE: Session 15702e7218820fcf Todos
#+DATE: 2026-09-02
#+TODO: ITEM DOING BLOCKED | DONE

* SMT conversion

** DONE Convert preserves?, establishes?, sufficient?, fireable? to data forms
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Lines 31-113 in core.bl. Each fn builds SMT strings via (str ...) and calls z3/check. Convert to:
- Build form lists (e.g., (list 'assert (list '= ...)))
- Pass forms directly to z3/check (no smt/script wrapping)
- Return {:preserved bool :model ...} same structure
Acceptance: functions accept form lists instead of string assembly

** DOING Convert candidates, abduce, repair-transition to data forms
:PROPERTIES:
:TASK_ID: task-2
:STATUS: in_progress
:DEPENDS: task-1
:END:

Lines 77-164. These enumerate predicates and check sufficiency. Convert string renders (render-term) to form lists. Helper forms for (op a b) construction as data, not strings.
Acceptance: candidates returns form lists; abduce uses them

** ITEM Convert guard-implies?, prove-box to data forms
:PROPERTIES:
:TASK_ID: task-3
:STATUS: pending
:DEPENDS: task-1
:END:

Lines 65, 47-62. prove-box orchestrates init + transitions. guard-implies? checks implication. Both build SMT via (str ...).
Acceptance: use form lists; z3/check receives forms directly

** ITEM Convert simulates?, migration-preserves?, footprint-in-caps? to data forms
:PROPERTIES:
:TASK_ID: task-4
:STATUS: pending
:DEPENDS: task-1
:END:

Lines 189-213. Refinement + sandbox checks. Replace (str decls ...) patterns with form list construction.
Acceptance: forms passed to z3/check, no string assembly

** ITEM Convert verify-process, repair-process inner SMT assembly (lines 600+)
:PROPERTIES:
:TASK_ID: task-5
:STATUS: pending
:DEPENDS: task-1 task-2
:END:

These fns build complex SMT via string concat (inv-define-fun, decl-of patterns). Convert all (str ...) to form lists. decl-of already returns a form; generalize for all constructs.
Acceptance: inv-define-fun, decls, all assertions are forms

** ITEM Convert capacity + discovery checks: verify-capacity, synthesize-capacity, etc.
:PROPERTIES:
:TASK_ID: task-6
:STATUS: pending
:DEPENDS: task-5
:END:

Lines 1000+. Same pattern: (str decls ...) assembly. Convert to form lists. Length-var declarations/asserts as forms.
Acceptance: all decls/asserts are forms

** ITEM Convert content + liveness checks: verify-content, verify-liveness
:PROPERTIES:
:TASK_ID: task-7
:STATUS: pending
:DEPENDS: task-6
:END:

Lines 1200+. Array theory + datatype checks. Convert (str ...) patterns to form lists. preserve distinct-def as data.
Acceptance: forms passed to z3, no (str)

** ITEM Convert all multi-line SMT builders (inv-define-fun, decl-of, keep-translatable-multi, etc.)
:PROPERTIES:
:TASK_ID: task-8
:STATUS: pending
:DEPENDS: task-5
:END:

Helper fns that assemble SMT forms. All must return form lists (not strings). Update callers to expect lists.
Acceptance: helpers return forms; callers concat them

* Verification

** ITEM Parse-check core.bl with ../beam-lisp/bl
:PROPERTIES:
:TASK_ID: task-9
:STATUS: pending
:DEPENDS: task-7
:END:

After all conversions, run beam-lisp parser to verify syntax. Fix any typos introduced.

** ITEM Manual smoke test: z3 receives form lists correctly
:PROPERTIES:
:TASK_ID: task-10
:STATUS: pending
:DEPENDS: task-8
:END:

Spot-check a few converted fns to ensure form list structure matches expected SMT. Verify z3/check accepts them.
