#+TITLE: Session 1575564feec64dde Todos
#+DATE: 2026-09-06
#+TODO: ITEM DOING BLOCKED | DONE

* Implementation

** DONE Migrate record modules to canonical descriptors
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

File: lib/beam_lisp/record.ex. Replace quoted Module.create route with BeamLisp.Emit descriptor_for/compile_descriptor/load_binary!, using canonical ANF literals, remote calls, and fn clauses. Preserve struct ABI, metadata registry, field order/defaults, unknown-field semantics, redefinition, deftype identity. Acceptance: no Module.create/quote route in Record; compile happens before registry publication.

Record route now emits canonical __struct__/0,1 or deftype marker descriptors, compiles before purge/load and registry publication.

** DONE Migrate native host to canonical descriptors
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

File: lib/beam_lisp/native.ex. Replace quoted Module.create host with canonical Emit descriptor APIs and ANF clauses. Ensure load_nif runs inside generated host, preserve stubs/signatures, on_load and AOT byte recording, malformed signature rejection, packaging. Acceptance: no Module.create/quote route; compile before publication; missing NIF refuses cleanly.

Native host now uses canonical descriptor, on_load attribute, direct generated :erlang.load_nif call, stubs, compile-before-ETS publication, and existing AOT declaration replay.

* Tests

** DONE Add record and native cutover proofs
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Files: test/beam_lisp/wave24_records_test.exs and test/beam_lisp/native_test.exs. Add behavior and source-route assertions proving records/deftypes/native stubs work without Module.create/quote; missing-NIF fixture refuses without Rust compilation. Acceptance: focused source assertions and runtime behavior coverage present. Do not run tests/builds.

Added struct ABI/redefinition/deftype identity proof, native missing-NIF stub proof, and source assertions excluding Module.create/quote routes. Per instruction, no builds or tests run; git diff --check passed and LSP diagnostics were empty.
