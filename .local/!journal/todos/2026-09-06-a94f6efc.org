#+TITLE: Session 1575311456e3b19e Todos
#+DATE: 2026-09-06
#+TODO: ITEM DOING BLOCKED | DONE

* Implementation

** DONE Implement bounded cache generation cleanup
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

File: lib/beam_lisp/aot_cache.ex only. Add conservative GC API using cache root, validate compiler-key directory names, reject symlinks, preserve active key, enforce configurable generation/age/deletion bounds, and throttle store-triggered sweeps. Preserve all existing interface-key edits. Acceptance: targeted GC tests compile and pass.

Added public bounded cleanup, validated real SHA-256 generation directories, active-key protection, config/default normalization, and persistent-term store throttling.

* Tests

** DONE Add hermetic cache cleanup coverage
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

File: test/beam_lisp/aot_cache_gc_test.exs only. Use unique temporary cache roots; cover active-key protection, retention and deletion cap, age cleanup, invalid names, symlink safety, and throttled store sweeping without touching the real cache. Acceptance: mix test test/beam_lisp/aot_cache_gc_test.exs passes.

Created four hermetic tests covering active retention, generation/deletion bounds, age expiry, invalid-name and symlink safety, plus store sweep throttling.

* Verification

** DONE Run targeted cleanup verification commands
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

No file changes expected. Run only focused tests for test/beam_lisp/aot_cache_gc_test.exs plus formatter check on the two scoped files; do not run global build/tests. Acceptance: commands exit 0 and exact commands/results reported.

Passed: mix test --no-start --no-compile test/beam_lisp/aot_cache_gc_test.exs (4 tests); mix format --check-formatted on both scoped files; git diff --check. Standard targeted mix test could not reach ExUnit because pre-existing interface-key work triggered broken full AOT compilation.
