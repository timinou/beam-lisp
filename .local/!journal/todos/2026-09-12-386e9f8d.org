#+TITLE: Session 157b5513e8d5bdf2 Todos
#+DATE: 2026-09-12
#+TODO: ITEM DOING BLOCKED | DONE

* cache

** DONE Red tests: three-rung store location + tree-root walk
:PROPERTIES:
:TASK_ID: c1-tests
:STATUS: completed
:END:

test/bl/datom/blanalysis_cache_test.bl: project rung is <tree>/.local/bl/cache; a nested corpus dir walks UP to the tree root (resolve_root); a read-only project falls back to the ambient dir keyed by tree_id; a corpus with no tree root uses the corpus dir itself; .gitignore carries .local/bl/. test/bl/bl_search_test.bl: end-to-end, bl search -p <tmp tree> from a different cwd writes the store under the tmp tree, not the cwd.

** DONE BeamLisp.Cache: root, project_dir, ambient_dir; Model reuses it
:PROPERTIES:
:TASK_ID: c2-elixir
:STATUS: completed
:DEPENDS: c1-tests
:END:

lib/beam_lisp/cache.ex — root/0 = :filename.basedir(:user_cache, ~c"beam_lisp"); tree_root/1 = Daemon.Paths.resolve_root (falls back to the path itself on :no_root); project_dir/1 = <tree>/.local/bl/cache; ambient_dir/1 = <root>/cache/<tree_id>. lib/beam_lisp/model.ex::root reuses BeamLisp.Cache.root + "models" (BEAM_LISP_MODEL_DIR still wins).

** DONE Route the store dir: rung chain in codebase, root from the CLI
:PROPERTIES:
:TASK_ID: c3-resolver
:STATUS: completed
:DEPENDS: c2-elixir
:END:

codebase/blanalysis-dir gains a [root] arity: $BLANALYSIS_DIR → (BeamLisp.Cache/project_dir root) if creatable → (BeamLisp.Cache/ambient_dir root). cache-path takes the root. analyze-cached opts gains :root (and threads it to cache-path); code.semantic/index! passes it; bl.search and bl.ask resolve the root from -p/PATH and pass it down.

** DONE .gitignore, doctor probe, CLI docs quote
:PROPERTIES:
:TASK_ID: c4-artifacts
:STATUS: completed
:END:

.gitignore: replace .blanalysis/ with .local/bl/ (comment kept accurate). priv/std/bl/doctor.bl.md: probe .local/bl/cache/ with an honest detail line. docs/bl/00-the-cli.md: paste the real bl doctor output.

** DOING Red tests + cap and prune over store entries
:PROPERTIES:
:TASK_ID: c5-evict
:STATUS: in_progress
:END:

New test/bl/bl_cache_test.bl: entries lists ns.sha.fjall plus .fjall.blobs siblings with sizes; prune deletes oldest-first until under the cap, keeps the newest, reports freed bytes; a dry run deletes nothing; cap via BL_CACHE_MAX_MB. Implementation: priv/lib or priv/std/bl/cache.bl private fns.

** ITEM bl cache status | prune, registered in all three lists
:PROPERTIES:
:TASK_ID: c6-verb
:STATUS: pending
:DEPENDS: c5-evict
:END:

priv/std/bl/cache.bl: status (both tiers for the current tree, counts + bytes) and prune (--max-mb N, --dry-run). Register the command in commands, command-help and usage-groups in priv/std/bl/cli.bl, asserted like bl.search's registration. Automatic enforcement after an index run that wrote stores, one line when it frees anything.

** ITEM Docs: the store's home, the tiers, the verb, refreshed costs
:PROPERTIES:
:TASK_ID: c7-docs
:STATUS: pending
:DEPENDS: c6-verb
:END:

docs/code-semantic-search.md: rewrite the location section (project rung .local/bl/cache, ambient rung XDG keyed by tree, BLANALYSIS_DIR override), document bl cache status/prune, and refresh the cost table with measured cold/warm numbers from this tree.

** ITEM Suites green, measured end to end, committed
:PROPERTIES:
:TASK_ID: c8-verify
:STATUS: pending
:DEPENDS: c7-docs c4-artifacts
:END:

mix bl test test/bl/datom/blanalysis_cache_test.bl -p examples; mix bl test test/bl/bl_cache_test.bl; mix bl test test/bl/bl_search_test.bl; mix bl test test/bl/code/semantic_test.bl -p examples; mix test test/beam_lisp/examples_test.exs; e2e: rm -rf .local && bl search -p priv/lib/code, then bl cache status; bl doctor; git status --short. Commit in two commits (location; verb+docs).
