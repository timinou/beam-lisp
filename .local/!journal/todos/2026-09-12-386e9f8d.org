#+TITLE: Session 157b5513e8d5bdf2 Todos
#+DATE: 2026-09-12
#+TODO: ITEM DOING BLOCKED | DONE

* store

** DONE Red tests: BL_CACHE_DIR overrides every tier, and expands
:PROPERTIES:
:TASK_ID: a1-tests
:STATUS: completed
:END:

test/bl/datom/blanalysis_cache_test.bl: BL_CACHE_DIR wins over a project tree AND over BLANALYSIS_DIR (set both, assert the new one); a relative value resolves against the cwd; the value is used verbatim when absolute. Also a bl_cache_test assertion that store-dirs follows it (status/prune point at it).

** DONE Implement BL_CACHE_DIR, document it, teach doctor to report it
:PROPERTIES:
:TASK_ID: a2-rung
:STATUS: completed
:DEPENDS: a1-tests
:END:

codebase/blanalysis-dir: first rung reads BL_CACHE_DIR, then BLANALYSIS_DIR, both Path/expand'd (identity when absolute); docstring rewritten with the precedence and the CI use. priv/std/bl/doctor.bl.md: the store probe follows the override and names it. Docs: docs/code-semantic-search.md (tiers), docs/bl/00-the-cli.md (bl cache section), plus wherever BLANALYSIS_DIR is already documented (grep).

OBSERVED: BL_CACHE_DIR wins over BLANALYSIS_DIR (red first: 'expected abs, actual /tmp/blanalysis_should_lose'), relative values resolve against the cwd, the store is written there, bl doctor prints 'present (<dir> — BL_CACHE_DIR)', and bl cache status reports the override. blanalysis_cache_test 11/40/0, bl_cache_test 11/31/0.

** DONE Fix FUP-039 part 2: bl ask resolves its store from its PATHs
:PROPERTIES:
:TASK_ID: a3-ask
:STATUS: completed
:END:

priv/std/bl/ask.bl.md threads a root (the first resolved PATH, as bl search does) into facts-cache-path / cached-facts / remember-facts! / the asked-set path and its mkdir, so `bl ask symbols /elsewhere/priv` writes with that corpus instead of under the cwd's project. Red test first in test/bl/ask_test.bl or wherever ask's cache is covered — find it; if nothing covers it, a test that the cache path lands under the given root.

OBSERVED red: bl ask arity-mismatches over a corpus in /tmp took the worktree's askset stores 0 -> 2 (and symbols wrote nothing anywhere, filed as FUP-040). After the fix, test/bl/bl_ask_test.bl passes 1/3/0: exit 0, the store lands with the sources, the shell's project gains nothing.

** DONE Verify: suites, the override end to end in CI shape, close FUP-039
:PROPERTIES:
:TASK_ID: a4-verify
:STATUS: completed
:DEPENDS: a2-rung a3-ask
:END:

Observed: BL_CACHE_DIR=/tmp/bl_ci mix bl search -p priv/lib/code puts the store there and bl cache status reports it, with nothing written to .local/bl; bl ask over a corpus outside the cwd writes with that corpus. Suites: bl_cache, blsearch, blanalysis_cache, semantic; bl lint clean on touched files. Then close FUP-039 with the evidence, and commit.

OBSERVED: with BL_CACHE_DIR set, ask+search over a /tmp corpus leave askset/fjall/facts in that dir; `bl cache status` -> '2 store(s) 64 MB <that dir>'; the corpus tree and the worktree stores untouched; lint shows 0 smells on lines I wrote (8 pre-existing, elsewhere in the files). Suites: bl_ask 1/3/0, bl_cache 11/31/0, bl_search 6/12/0, blanalysis_cache 11/40/0, semantic 19/58/0. FUP-039 closed with the evidence. NOT CHECKED: the examples suite was not re-run this turn (the user set mix test aside), and no drop payload was exercised.
