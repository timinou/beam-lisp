#+TITLE: Session 1556d5a6bcf0f8f8 Todos
#+DATE: 2026-08-14
#+TODO: ITEM DOING BLOCKED | DONE

* review

** DONE Map emitter patch and reference serializer
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Inspected feature range fc29a0a..HEAD, full priv/contract.bl, Elixir __before_compile__/contract_entries, and existing tests. Acceptance met: exact shapes/order/coercion code and changed lines mapped.

contract.bl is introduced by 43c99b8; reply projection added by e0cac62. Working tree changes are unrelated. Elixir metadata values are always to_string; events fields are declared but beam parser currently records only handler body.

** DONE Audit edge-case byte equivalence independently
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Compare quote/backslash/newline/unicode escaping, event fields, key ordering, number/boolean metadata, empty contract, dotted/unicode modules, and Jason map ordering. Acceptance: concrete byte divergence or proof constrained by DSL surface, anchored to changed lines.

** DONE Judge reply projection fingerprint collision
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-1
:END:

Trace contract-json, fingerprint, and contract-json-with-replies in /home/user/code/undefine/beam-lisp/priv/contract.bl plus consumers/tests. Acceptance: determine whether materially different reply declarations share fingerprints and whether that violates stated seam guarantees; identify patch-anchored impact.

** DONE Synthesize actionable patch-anchored review verdict
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:DEPENDS: task-2 task-3
:END:

Validated runtime-proven candidates against changed lines and reported five actionable issues. Acceptance met: all findings patch-anchored to ≤10 lines with concrete inputs/bytes and impact.

Five findings: event fields omitted (P1); JSON controls unescaped, slash strings truncated, duplicates collapsed, and >32 field maps ordered differently from Jason (P2). Two reply variants do share one base fingerprint by design; current Verse consumer validates replies separately and does not use fingerprint, so no finding.
