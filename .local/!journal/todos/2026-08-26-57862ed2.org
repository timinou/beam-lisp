#+TITLE: Session 1566e7903f4e6c48 Todos
#+DATE: 2026-08-26
#+TODO: ITEM DOING BLOCKED | DONE

* review

** DONE Inspect observability diffs and target context
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

In /home/user/code/ora/relay, inspect git shows fc8fdac and 0bfa6ab for src/relay/ratelimit.bl, src/relay/gate.bl, and tests; read full current files. Acceptance: exact changed lines and intended data flow identified.

fc8fdac introduces ratelimit module and gate integration; 0bfa6ab adds direct and gate tests. Current targets match patches.

** DONE Verify atom window and reset semantics
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Trace src/relay/ratelimit.bl bump/allow?/current-bucket/reset! and beam-lisp symbol resolution. Acceptance: prove swap! read-modify-write atomicity, N/N+1 behavior, bucket rollover, unlimited cases, and whether reset! recurses.

swap! is Agent.get_and_update, so cur/read/roll/increment and per-call count return are serialized; Nth allowed and N+1 denied. reset! initially resolves to core reset!/2 because defn body compiles before its own var installs; green tests invoke it. Found boundary rollback race because now-bucket is sampled before serialized swap.

** DONE Trace gate ordering and limit precedence
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-1
:END:

Trace src/relay/gate.bl budget then rate-limit and config/key sources. Acceptance: confirm failed-budget accounting choice, nil/default/per-key/zero behavior, and identify any concrete regression with patch line.

Gate deliberately short-circuits budget failures before rate accounting. Missing per-key attr flows to dynamic default; absent/invalid default -> nil unlimited; explicit 0 persists and overrides default, then allow? treats it unlimited. No gate precedence bug found.

** DONE Cross-check tests and submit verdict
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:DEPENDS: task-2 task-3
:END:

Read test/relay/observability-test.bl rate-limit coverage; reconcile all evidence, report each material patch bug, and submit final correctness. Acceptance: every requested focus has an evidence-backed conclusion and findings are diff-anchored.

Tests cover sequential N/N+1, unlimited nil/0, key isolation, and gate 429 but not boundary concurrency. Independent read-only review confirmed one stale-bucket rollback race; all requested reset/gate/default checks are clean.
