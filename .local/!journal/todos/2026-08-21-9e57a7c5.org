#+TITLE: Session 155fe5df632bed1b Todos
#+DATE: 2026-08-21
#+TODO: ITEM DOING BLOCKED | DONE

* Review

** DONE Inspect store mutation and held db visibility
:PROPERTIES:
:TASK_ID: review-store-writes
:STATUS: completed
:END:

Read priv/datom store put/delete/commit/update paths and compare commit cb7bac7..cd7f9de. Determine whether later transactions overwrite pre-existing datoms visible to held db values. Acceptance: exact source lines and safe/unsafe conclusion.

Later transactions use tx in binary keys, so held db rows are not overwritten. Same-transaction duplicate [e,a,v,tx] keys collide because op is omitted; runtime probes show final ETS value depends on operation order.

** DONE Inspect counter keys and index scan bounds
:PROPERTIES:
:TASK_ID: review-counters-scans
:STATUS: completed
:DEPENDS: review-store-writes
:END:

Trace atom counter key construction and all index scan bounds, including -range nil nil callers. Determine collision or scan contamination with binary index keys. Acceptance: exact source lines and runtime evidence if useful.

Counter keys are atom keywords while index keys are binaries tagged 1..4; no ETS key collisions. Bounded index scans cannot include counters; raw store -range nil nil can include them by contract but no index caller uses unbounded raw range for datoms.

** DONE Inspect commit exceptions and basis publication order
:PROPERTIES:
:TASK_ID: review-basis-order
:STATUS: completed
:DEPENDS: review-counters-scans
:END:

Trace write-datoms, commit exceptions, and conn reset! ordering under single writer. Determine whether db basis can become newer than committed datoms. Acceptance: exact source lines plus runtime proof where useful.

write-datoms precedes reset!; exception probe on malformed op leaves basis unchanged and no datoms. During the small write/reset interval basis is older, never newer. Under single writer no basis-ahead bug; ETS torn commit remains excluded.
