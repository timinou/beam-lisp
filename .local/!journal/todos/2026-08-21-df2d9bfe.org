#+TITLE: Session 155fe1746cabed19 Todos
#+DATE: 2026-08-21
#+TODO: ITEM DOING BLOCKED | DONE

* review

** DONE Inspect target diff and transaction pipeline
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Read git diff for cb7bac7 and cd7f9de affecting priv/datom/tx.bl and priv/datom/conn.bl; map changed lines and transaction phases without modifying files.

Current target code maps expansion → validation → tx counter → tempid resolution/entity counter → realization → commit → basis reset. Workspace diff unavailable through find; exact current anchors captured.

** DONE Trace schema store and tests
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Inspect schema/index/store dependencies and relevant tests for identity uniqueness, tempids, counters, and exception behavior; record exact observable contracts.

Schema declares identity/value but tx.validate checks only types. AVET retains history; db current filtering excludes retracted assertions, while upsert-target scans raw AVET. eav_test covers basic identity upsert/tempids but not requested edge cases.

** DONE Evaluate edge cases and report findings
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

For each requested edge case, derive concrete transaction/state outcomes from changed code; classify bugs versus safe behavior and produce exact line anchors and verdict.

Findings: stale historical AVET causes retracted identity reuse; no unique/value enforcement for explicit ids or duplicate identities; tempid mapping is store-only and fails transaction-local unification/conflict detection. Same existing target is safe when all identities agree. Counter gaps after realize failure are harmless; millionth entity numeric overlap is harmless; ETS torn commit excluded.
