#+TITLE: Session 157c006552a1131e Todos
#+DATE: 2026-09-11
#+TODO: ITEM DOING BLOCKED | DONE

* impl

** DONE Author priv/std/bl/watch.bl.md verb
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

NEW literate verb bl.watch: (defn render [r]) one renderer (status glyph + outcome + :path + namespaces, reasons indented); with-path helper; (defn run [args st]) local path (usage 2, register-paths, resolve, trap_exit, ReloadWatcher start_link w/ :apply tagging :path + :on_result println render, park, watch-error 1). Prose: two hosts, :file_system, reload semantics, result shape.
Accept: mix bl test test/bl/system/watch_test.bl green.

** DONE Register bl.watch in cli.bl, drop inline copy
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

priv/std/bl/cli.bl: commands map "watch" → (lazy "bl.watch"); delete defn watch-error and defn cmd-watch; keep usage/command-help text identical. Verify no other refs.
Accept: grep shows no cmd-watch/watch-error; mix bl help unchanged.

** DONE Registry: monitor subscribers, tag path
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:END:

lib/beam_lisp/daemon/watch_registry.ex: trap exits in init; track pid->{ref,count}; on DOWN remove ALL that pid's subs (stop dir watcher when empty); demonitor on unwatch; apply_fun tags result with :path (Map.put on map, else normalized error map); moduledoc documents absolute-dir contract + real monitor promise.
Accept: mix test test/beam_lisp/daemon/watch_registry_test.exs green.

** DONE Server: route watch requests off the Executor
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:END:

lib/beam_lisp/daemon/server.ex: default_execute clause for %{argv: ["watch"|_]}; resolve dir against req.cwd (dir or file→dirname, else stderr+exit 2; no arg → usage stderr+exit 2); render via Loader.ensure_loaded("bl.watch")+Env.fetch!+RT.invoke; notify sends Protocol.stdout with a per-request :atomics seq; WatchRegistry.watch(dir,{conn,id},notify); error → stderr+exit 1; ok → stdout watching line, then park (monitor conn, heartbeat every 20s, NO :exit). Comment why no terminal :exit.
Accept: compiles; manual e2e later.

** DONE Launcher: watch rides the daemon
:PROPERTIES:
:TASK_ID: task-5
:STATUS: completed
:END:

tooling/drop/src/launcher.rs ~line 276: remove "watch" from owns_process matches!; keep repl/monitor/serve/mcp/bare/lsp serve; update comment to say watch is daemon-served.
Accept: grep shows watch absent from matches!; cargo not required.

* verify

** DONE Write registry + verb tests
:PROPERTIES:
:TASK_ID: task-6
:STATUS: completed
:DEPENDS: task-1 task-3
:END:

NEW test/beam_lisp/daemon/watch_registry_test.exs (dedup+fan-out via send(reg,{:reload_result,dir,…}), unwatch keeps watcher, DOWN removes all pid subs + stops last; skip cleanly if FileSystem unavailable). NEW test/bl/system/watch_test.bl (render non-empty/mentions path/no %{ ; hold shows reason; run no-args → 2).
Accept: mix test test/beam_lisp/daemon/watch_registry_test.exs && mix bl test test/bl/system/watch_test.bl.

** DONE Compile, run tests, manual daemon e2e
:PROPERTIES:
:TASK_ID: task-7
:STATUS: completed
:DEPENDS: task-2 task-4 task-5 task-6
:END:

mix compile; run both test suites; then stop/start daemon via mix bl, drive the daemon watch path with tmp/bl-drop-linux-x86_64 watch <tmpdir>, edit a .bl file, capture /tmp/watch.out; also exercise the local mix bl watch path; stop daemon. Record exact frames + renderer format + unverified.
Accept: transcripts captured.
