#+TITLE: Session 157555d47163b1cd Todos
#+DATE: 2026-09-06
#+TODO: ITEM DOING BLOCKED | DONE

* Review

** DONE Inspect native lazy memo lifecycle
:PROPERTIES:
:TASK_ID: InspectNative
:STATUS: completed
:END:

Read native/lazy_memo/src/lib.rs and Rustler 0.38 init/resource unload behavior. Determine whether persistent_term load state and permanent reclaimer create provable bugs. Acceptance: source-backed verdicts and line anchors.

Rustler init macro explicitly emits unload: None. RECLAIMER OnceLock spawns recv loop with no termination; persistent marker is module-atom global.

** DONE Inspect Elixir lazy memo stubs
:PROPERTIES:
:TASK_ID: InspectElixir
:STATUS: completed
:DEPENDS: InspectNative
:END:

Read lib/beam_lisp/lazy_memo.ex and correlate module replacement/load behavior with native NIF stubs. Acceptance: exact source-backed lifecycle implications.

OTP docs state load_nif is per module instance; reload means already loaded for that instance. Persistent marker is keyed only by module atom and survives code purge.

** DONE Validate both lifecycle candidates
:PROPERTIES:
:TASK_ID: ValidateCandidates
:STATUS: completed
:DEPENDS: InspectElixir
:END:

Synthesize strict findings for both candidates, accounting for resource destructors, reclaim-only worker startup, same-library upgrades, and Rust static/OnceLock lifetime. Acceptance: validated/rejected, exact reason, best added line, minimal fix.

Both candidates validated. OTP load_nif is per module instance; Rustler 0.38 init emits unload: None. Resource lifetimes delay unload only while resources exist, not for arbitrary Rust threads.
