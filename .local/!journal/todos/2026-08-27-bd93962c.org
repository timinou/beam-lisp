#+TITLE: Session 15660dc6528e6c24 Todos
#+DATE: 2026-08-27
#+TODO: ITEM DOING BLOCKED | DONE

* Tasks

** DONE Add Explorer dep + verify DataFrame/Series API
:PROPERTIES:
:TASK_ID: explorer-dep
:STATUS: completed
:END:

explorer 0.12.0 added to mix.exs (optional runtime). Compile needs table/table_rex/decimal built first. API confirmed: DataFrame.new([{name, data}...]) preserves column order (map form sorts alphabetically - avoid). Series.from_list infers dtype. to_columns for conformance.

** DONE Build datom.frame: q-df + pull->df + dtype mapping
:PROPERTIES:
:TASK_ID: frame-mod
:STATUS: completed
:DEPENDS: explorer-dep
:END:

datom.frame/q-df + pull->df working end-to-end. q-df: plain query + aggregate both correct, :find column order preserved, sorted rows for determinism. pull->df: one row/entity, db/id + attr cols, absent attr -> nil (Explorer null), rectangular. Explorer gate via Code.ensure_loaded?. Key gotcha: Explorer.DataFrame.new needs Erlang LISTS (of {name,col} tuples + list columns), not bl vectors (Table.Reader protocol).

** DONE Re-export q-df/pull->df from datom.bl
:PROPERTIES:
:TASK_ID: reexport
:STATUS: completed
:DEPENDS: frame-mod
:END:

datom/q-df + datom/pull->df delegate to datom.frame, verified via facade.

** DONE Conformance tests: q-df == q + hand-built cols
:PROPERTIES:
:TASK_ID: frame-tests
:STATUS: completed
:VERIFY_CMD: cd /home/user/code/undefine/beam-lisp && mix beam_lisp.test test/bl/datom/frame_test.bl
:DEPENDS: frame-mod
:END:

test/bl/datom/frame_test.bl: 7 tests/19 assertions, 0 failures. Conformance (q-df==q hand-built cols), :find order preserved, aggregate grouping+naming, empty-frame shape, dtypes, pull->df nulls, available? gate. Full datom suite 355/782 green, no regressions.

** DONE examples/datom/df/ working full examples
:PROPERTIES:
:TASK_ID: examples
:STATUS: completed
:DEPENDS: reexport
:END:

examples/datom/df/: 01-query-to-frame (q vs q-df, order, aggregates), 02-analytics (full Polars pipeline: group/summarise/sort/mutate/reduce on spend ledger), 03-pull-to-frame (ragged entities -> rectangular table w/ nulls, mask filter) + README. All three run clean.

** DONE Wire relay.analytics df-for onto real q-df
:PROPERTIES:
:TASK_ID: wire-relay
:STATUS: completed
:DEPENDS: reexport
:END:

relay.analytics/spend-df returns a real Explorer DataFrame via datom/q-df (native Session-A backend live in relay). df-for keeps {:label :value} rows for the fixed dashboard (charts need rows not DataFrames; forcing Polars marshalling adds cost). Explorer compiles in relay; vendored datom_redb NIF rebuilt for PLAN-042. Test: spend-df + Polars mask+sum=75c proves live. 120/369 green. Committed da6ccd1.

** DONE Close PLAN-039: mark DONE, supersede ETF sections
:PROPERTIES:
:TASK_ID: close-039
:STATUS: completed
:DEPENDS: frame-tests examples wire-relay
:END:

PLAN-039 STATE -> DONE with completion banner: DataFrame surface delivered, ETF-NIF design superseded by PLAN-042. Committed 12eb202. Final: beam-lisp datom 355/782, relay 120/369, both 0 failures.
