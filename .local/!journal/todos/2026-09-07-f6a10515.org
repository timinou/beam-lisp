#+TITLE: Session 15751ba43523b193 Todos
#+DATE: 2026-09-07
#+TODO: ITEM DOING BLOCKED | DONE

* pulse

** DONE Fold incremental+trace into tooling.pulse; add trace to frame
:PROPERTIES:
:TASK_ID: pulse-integrate
:STATUS: completed
:END:

tooling.pulse now requires tooling.trace+incremental, holds a process-wide trace, re-exports traced/component/note-patch!/clear-trace!, and snapshot carries {:trace {:recomputed :patch-ops}}. Page gets a 'why did the ui update?' panel (flashes on change); chip gets a trace line. 12 tests/31 assertions.

** DONE Prove integrated pulse: trace stream in snapshot
:PROPERTIES:
:TASK_ID: pulse-tests
:STATUS: completed
:DEPENDS: pulse-integrate
:END:

snapshot-carries-render-trace + traced-is-incremental proven.

* demo

** DONE Interactive SPA demo: clicks -> ws -> traced render -> push
:PROPERTIES:
:TASK_ID: demo-interactive
:STATUS: completed
:DEPENDS: pulse-integrate
:END:

examples/data/explorer.bl: interactive SPA over websocket. Clicks -> ws -> handle-event mutates cell -> re-render via pulse/traced subtrees -> diff -> push HTML back. Three traced panels (counter/fib/theme) each depend on one cell.

** DONE Drive it in-browser: click, see reaction + trace, screenshots
:PROPERTIES:
:TASK_ID: demo-prove
:STATUS: completed
:DEPENDS: demo-interactive
:END:

Driven live in browser: clicks register (0->4), fib computes via cache (6765->10946), theme toggles (dark->light pink). Chip trace narrates correctly per interaction: 'counter -> 1 op' for click, 'fib -> 2 op' for fib. Screenshot captured. Caught+fixed REAL bug: derived over a Config record never recomputed (record identity stable); added data.config/ref accessor returning the inner atom so config is a first-class reactive dep.

* finish

** DONE Docs + full verify + commit
:PROPERTIES:
:TASK_ID: finish
:STATUS: completed
:DEPENDS: pulse-tests demo-prove
:END:

Committed 9f32a36. All 6 data+tooling suites green (config 8/19, pulse 12/31), core cell+watches 35 unaffected, explorer loads. data.config/ref added + tested.
