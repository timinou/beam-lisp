#+TITLE: Session 155cd9f452af0334 Todos
#+DATE: 2026-08-18
#+TODO: ITEM DOING BLOCKED | DONE

* Review

** DONE Inspect W4 loop patch and current implementation
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Read commits a827f5f and 38201e9 for lib/beam_lisp/spell/loop.ex, plus the current file. Acceptance: identify all changed lines in fence_rung/fence_eval/safe_eval/replay_journal and their exact current line anchors.

Observed W4 loop changes at current lines 136-154 and 714-787; commit 38201e9 adds tests only. Current working tree has unrelated uncommitted files, none in loop.ex.

** DONE Trace eval namespace and registry mutation semantics
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Inspect Compiler.eval_string, BeamLisp.Link namespace/module interning, vars registry/ETS ownership, and defn/def failure paths. Acceptance: prove what state brutal_kill can leave and whether a later same-name defn repairs it, citing exact source paths/symbols.

Compiler permits unrestricted BEAM remote calls; def evaluates initializer before Env.intern. Link.defvar mutates body module, namespace module, ns_defs, link ETS, then var ETS non-transactionally. A brutal kill can interrupt any stage; later same-name defn rebuilds these stages, but rejected state/side effects persist until then and arbitrary initializer effects are not repaired.

** DONE Analyze monitor mailbox timeout and replay behavior
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Trace spawn_monitor receive ordering, kill/send races, GenServer serialization, replay during init, and start_link timeout behavior using current source plus dependency/runtime docs where needed. Acceptance: classify each requested concern as safe, deliberate, or a patch-introduced actionable bug with evidence.

Safe: same-sender result precedes DOWN; success demonitor flush prevents stray DOWN; DOWN removes monitor; queued runs remain serialized and unmatched during selective receive. No implicit start_link timeout: OTP gen timeout default is infinity. Bugs: Process.exit(:brutal_kill) is trappable (only :kill is untrappable), and timeout returns/demonitors without awaiting DOWN; late result may be logged by default handle_info but does not normally persist indefinitely.

** DONE Validate findings and submit review verdict
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:DEPENDS: task-3
:END:

Cross-check candidate findings against W4 diff and tests, ensure each overlaps changed lines and satisfies provable/actionable/introduced criteria, report each via report_finding, then submit exactly one verdict. Acceptance: no speculative findings; final verdict includes confidence.

Reported two P1 defects: unrestricted BEAM interop defeats process fence and can stop VM/loop or spawn escaped work; timeout uses trappable :brutal_kill and returns without awaiting DOWN. Did not report deliberate 5s policy, GenServer serialization, normal monitor ordering, or replay start timeout because source/docs show those are safe or intentional.
