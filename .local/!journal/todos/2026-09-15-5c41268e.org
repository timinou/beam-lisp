#+TITLE: Session 157f0c246a28a2b5 Todos
#+DATE: 2026-09-15
#+TODO: ITEM DOING BLOCKED | DONE

* cutover

** DONE Prove compile locks are .bl-only (claim, no Mix lock)
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:REF: org://PLAN-107-retire-mix-the-dependency-map-the-three
:END:

claim.bl's rule; wired at cli.bl:422-455 (bl build), build.bl:436-462 (per crate), selfbuild.bl:151-181 (stage). Suite: mix test build_session_test.exs -> 4 passed, 0.8s (was 2/4, 60.6s). grep: no Mix.Sync / compile.lock on any path.

VERIFIED by running, not by reading: the 4/4 suite and the CLI's named refusal of a live holder.

** DONE Wire `bl build --native` (the dead flag)
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:DEPENDS: task-3
:END:

build/run:534 dispatches on (:native opts); cli.bl passes :native from env.bl's :native ["native/*"]. End-to-end: ./bin/bl build --native --out /tmp/bl-native-out -> exit 0, six crates built.

VERIFIED end-to-end this turn, which the earlier read-only check did not do.

** DONE `bl deps compile` + search-path rule
:PROPERTIES:
:TASK_ID: task-5
:STATUS: completed
:DEPENDS: task-4
:REF: org://FUP-050-a-mix-free-dependency-path-fetch-and-com
:END:

New priv/build/deps-compile.bl + bl deps compile. Measured: 47 fetched, 45 compiled, 2 passes, second run all 45 :cached. Search-path rule: util/register-paths + bin/bl, both filtering on the SAME runtime tag (verified identical) and the completion marker.

VERIFIED: foreign-runtime dir planted -> 0 on the path; our runtime -> 1; tests 3 passed.

* ship

** DONE Tag v2026.2, push, verify binaries generated
:PROPERTIES:
:TASK_ID: task-6
:STATUS: completed
:DEPENDS: task-5
:END:

Annotated tag v2026.2 (re-cut once after the smoke-test fix; the failed run published nothing). Release run 34958818361: 4/4 targets + publish, all green.

VERIFIED from the published artifact: sha256 OK, version -> beam-lisp 2026.2.0, (+ 40 2) -> 42, embedding bundled.
