#+TITLE: Session 155fc725faebecfa Todos
#+DATE: 2026-08-21
#+TODO: ITEM DOING BLOCKED | DONE

* review

** DONE Inspect target patch and protocol contract
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Run git diff for ce4eb26/current workspace, read priv/datom/store.bl and PLAN-033 wave requirements. Acceptance: enumerate each protocol promise and map wave 2-6 operations to available methods.

Mapped PLAN-033: -commit handles known four-index writes; no primitive makes tx-counter read/decision/write or unique upsert atomic. Ascending eager range can express time filters, reverse, joins, and counts but may impose later performance costs.

** DONE Audit ETS implementation edge cases
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Read priv/datom/store-ets.bl plus host ETS/compare APIs it invokes. Acceptance: prove range boundary/sentinel behavior, commit ordering, close/name semantics, and mutable handle behavior from concrete code paths.

Inclusive stop operator is correct. Native ETS order disagrees with beam-lisp compare for mixed term ranks; sentinel is guarded and ETS officially reserves it. Commit partitions ops and loses delete→put last-write semantics. open/0 interns a fresh permanent atom though the table is unnamed. close/ownership behavior matches ETS docs; mutable handle is documented.

** DONE Measure conformance test contract coverage
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-1
:END:

Read test/bl/datom/store_test.bl and match every documented Store promise/edge to a test. Acceptance: list uncovered promises and identify only omissions whose impact is introduced by this patch.

Covered: get absent/present, put replacement, delete idempotence, inclusive/unbounded/ordered ranges, batch success, opaque value round-trip. Uncovered or contradicted: mutator return/threading semantics; true commit all-or-none under failure/concurrency (known ETS gap); key-order agreement across raw term ranks/encoded fixtures; repeated-key batch semantics are unspecified and untested.

** DONE Validate findings against repository context
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:DEPENDS: task-2 task-3
:END:

Trace concrete upper-layer callers/plans and patch lines, reject known/pre-existing/speculative issues, then submit findings. Acceptance: every retained issue has a diff-overlapping line, trigger, impact, wave, and actionable fix.

Retained five unique issues: missing conditional transaction primitive; ETS bound-order mismatch; repeated-key commit reordering; conformance suite violates return threading; open/0 leaks atoms. Rejected reverse/count/lazy scan as performance-only, sentinel as ETS-reserved/encoded-key-invalid, close/mutable handle as documented, and mixed-batch atomicity as already known.
