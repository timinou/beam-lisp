#+TITLE: Session 155cdaacda6f033c Todos
#+DATE: 2026-08-18
#+TODO: ITEM DOING BLOCKED | DONE

* Review

** DONE Review monitor timeout result ordering
:PROPERTIES:
:TASK_ID: MonitorOrdering
:STATUS: completed
:END:

Inspect loop.ex fence_rung/fence_eval/safe_eval/replay_journal around lines 136-154 and 733-793. Establish exact diff-overlapping anchors for DOWN/result ordering, demonitor flush, timeout kill/send races, and mailbox/monitor leak claims. Acceptance: evidence-backed verdict or finding only.

Result-first path demonitor flushes DOWN; DOWN path is already consumed. Timeout kill+demonitor can race a late result send, leaving stale fence_result mailbox messages.

** DONE Review initializer interop and fence escape
:PROPERTIES:
:TASK_ID: InitializerInterop
:STATUS: completed
:DEPENDS: MonitorOrdering
:END:

Inspect W4 loop code plus compiler/link/env internals for unrestricted def initializer, source-triggered loop death, and spawned work outliving fence. Acceptance: only provable patch-introduced actionable findings anchored to W4 loop lines.

Compiler.slash_target permits arbitrary Erlang remote calls; evaluator can call erlang/whereis+exit against named loop or erlang/spawn unlinked descendants. fence_eval only kills evaluator pid.

** DONE Review replay state and startup deadlines
:PROPERTIES:
:TASK_ID: ReplayPartialState
:STATUS: completed
:DEPENDS: InitializerInterop
:END:

Inspect Link.defvar behavior under brutal_kill, same-name redefinition repair, vars replay during init, cumulative deadlines/start_link timeout. Acceptance: concise safe/deliberate concerns plus exact actionable findings anchored in loop.ex.

Sequential vars replay is acceptance-ordered within vars and deliberate. However each entry may consume 5s, so N timeout entries can exceed default GenServer startup timeout. Failed eval side effects are not transactional; same-name redefinition only repairs one key.
