#+TITLE: Session 156c7aac9d1dca66 Todos
#+DATE: 2026-08-30
#+TODO: ITEM DOING BLOCKED | DONE

* bootstrap

** DONE Inspect compiler delegation and environment boot hooks
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Read lib/beam_lisp/compiler.ex and lib/beam_lisp/env.ex symbol outlines/bodies; quote compile/2, use_bl_compiler?/0, compile_elixir/2, enable_bl_backend/0, loaded_ns?/1, interning, and __bl_init__ behavior with exact observed line ranges.

Observed BeamLisp.init seeds core/multi/sugar first, then enable_bl_backend; enable_bl_backend loads compiler via Loader.ensure_loaded and forces genesis during replay. loaded_ns? checks Agent/env loaded set, while AOT __bl_init__ interns vars then mark_loaded.

* aot

** DONE Trace AOT task ordering and cache invalidation
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:END:

Read lib/beam_lisp/aot.ex, lib/mix/tasks/compile.beam_lisp.ex, and lib/beam_lisp/aot_cache.ex; establish seed loading, compile ordering, manifest/hash/force behavior, emitted init/provenance hooks, and compiler_key inputs with exact line evidence.

Task calls BeamLisp.init before source reduction; init seeds core/multi/sugar then attempts compiler load. compiler_key hashes BeamLisp.Compiler beam plus AOT/Emit/Link/etc beams and every priv/**/*.bl, not compiler.ex text directly; deleting genesis changes BeamLisp.Compiler beam and therefore key. AOT seed must be on code path; code_path_module? is :code.which != :non_existing.

* breakage

** DONE Enumerate genesis compiler callers and oracle path
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:END:

Search lib/, test/, priv/, and research/ for compile_elixir and Compiler.compile_elixir; inspect priv/self/oracle.bl and research/sh13_cutover/fixpoint.bl to map exact oracle dependency and breakage.

Direct external call is only priv/self/oracle.bl:49, `(BeamLisp.Compiler/compile_elixir form env)`. compiler.ex has compile_elixir's recursive internal calls throughout genesis body. Research gates/test/bl/self_hosting_test.bl call o/oracle-compile indirectly; fixpoint.bl lines 2,18,22 compares oracle against self compiler. No other direct compile_elixir references observed in lib/test/research; task planning docs mention it only.

* verdict

** DONE Synthesize evidence-backed cutover verdict
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:END:

Integrate observed ordering, interning, cache invalidation, and caller results into a concrete safe-as-is verdict and prerequisite change sequence; do not modify files.

Verdict: not safe as-is. Seed must be physically on code path before BeamLisp.init/AOT, survive mix clean, and carry provenance compiler_key computed from post-cutover BeamLisp.Compiler. Otherwise stale gate routes compiler seed to source, and with genesis deleted source fallback cannot compile compiler.bl. Oracle and all differential gates need retirement/replacement before deleting compile_elixir.
