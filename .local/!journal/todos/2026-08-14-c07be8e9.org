#+TITLE: Session 1556e49a8ef0f91c Todos
#+DATE: 2026-08-14
#+TODO: ITEM DOING BLOCKED | DONE

* investigation

** DONE Read provider implementation and tests
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Inspect /home/user/code/undefine/beam-lisp/priv/provider.bl and test/bl/provider_test.bl via find outlines and targeted bodies; record changed lines, parser/control-flow contracts, and test coverage. Acceptance: exact relevant symbols and line anchors identified.

provider.bl is entirely new at c28017f; stream always sends done for any HTTP 200 after parse-sse-deltas, while parser is line-based targeted scanning. Tests cover only parser happy paths and empty parser output, not stream lifecycle.

* analysis

** DONE Trace SSE and lifecycle edge cases
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Analyze exact CRLF/event separator/error/malformed/empty/DONE/redirect/target-pid scenarios against actual httpc full-body behavior; use repository references and authoritative docs only as needed. Acceptance: each candidate classified as silent wrong success or explicit failure, with exact body/status/trigger and emitted result.

CRLF is normalized by trim and non-200/redirect statuses are explicitly failed unless httpc follows redirect to a final 200. High-confidence silent-success cases: any 200 body without DONE, 200 empty/error event, malformed JSON containing a content marker, and dead target pid because send is non-failing and stream returns :sent.

* review

** DONE Validate findings against tests and scope
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Check corresponding tests and commit diff scope; retain only high-confidence patch-anchored actionable bugs. Acceptance: explain why tests miss each retained issue, or conclude empty issue list.

Retained four high-confidence issues. Tests invoke parse-sse-deltas only, never stream/stream-async, and only use valid normal SSE; parser empty test does not assert stream must fail. CRLF is handled by trim; non-200 statuses explicitly fail, so neither is reported.
