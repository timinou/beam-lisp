#+TITLE: Session 157629415ea81aed Todos
#+DATE: 2026-09-07
#+TODO: ITEM DOING BLOCKED | DONE

* Prototype

** DONE Inspect typed and ANF compiler APIs
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Read /home/user/code/undefine/beam-lisp--engines-anf/priv/std/typed.bl and existing research prototypes to identify callable APIs, reader forms, ANF binding conventions, and output patterns. Acceptance: concrete implementation approach grounded in source.

Observed typed/walk-call returns any for unseeded calls; check-source only merges core/host/env signatures. Existing fp-anf prototype establishes compile-node/read_string conventions and RT.invoke global-call shape.

** DONE Create interprocedural ANF type prototype
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Create only /home/user/code/undefine/beam-lisp--engines-anf/research/anf-engines/typed_interproc.bl. Print marker-prefixed surface typed/check-source results and ANF prototype results for 3-4 programs; infer callee returns and catch through-call errors. Acceptance: runnable syntax and no edits to priv/.

Created standalone prototype with four-case corpus, surface baseline, two-pass ANF return map, numeric call checking, and :ann retention in warnings.

* Verification

** DONE Run prototype and capture measurements
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:VERIFY_REVIEW: Required systemd-run execution was observed successful with four TINTERPROC corpus records and final :ok; git diff --check also succeeded.
:DEPENDS: task-2
:END:

Run typed_interproc.bl using the required serialized systemd-run command, 4G/1G caps, 250s timeout, BEAM_LISP_Z3 path, and warning-noise filter. Fix prototype as needed. Acceptance: process succeeds and marker output proves surface misses versus ANF catches over corpus.

Required scoped command exited successfully (:ok). Measured: both interproc-invalid cases surface=[], ANF=one type-error; valid call both clean; direct invalid control caught by both. git diff --check passed. Other untracked files pre-existed/out of scope.
