#+TITLE: Session 1570128c7a820fc7 Todos
#+DATE: 2026-09-02
#+TODO: ITEM DOING BLOCKED | DONE

* Implementation

** DONE Add SMT form emission to smt.bl
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Target functions in priv/lib/system/core.bl that build SMT as strings:

1. inv-define-fun (line ~634) → return form not string
2. decl-of (line ~634) → return form
3. preserves-joint? (line ~639) → convert smt var to form list, pass to z3/check
4. establishes-joint? (line ~656) → same
5. verify-process (line ~669+) → adapt internal smt building
6. repair-process (line ~828+) → same
7. joint-fireable? (line ~895+) → same
8. guard-implies-multi? (line ~906+) → same
9. holds-at-init? (line ~978+) → same
10. inductive-under? (line ~1000+) → same
11. discover-invariant (line ~1067+) → same
12. verify-capacity (line ~1200+) → same
13. len-bound-inductive? (line ~1268+) → same
14. synthesize-capacity (line ~1300+) → same
15. verify-content (line ~1360+) → same + distinct-def
16. nondeterministic-pairs (line ~1480+) → same

Acceptance: All parse via ../beam-lisp/bl CLI; no `(str "(` patterns remain for SMT; z3/check receives lists directly.

* Tasks

** DONE Update z3/check to accept forms and strings
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:END:

** DOING Convert core.bl SMT string patterns to forms
:PROPERTIES:
:TASK_ID: task-3
:STATUS: in_progress
:END:

22 patterns need conversion from (str "(assert...) to form builders.
Critical path: verify-process and repair-process.
Approach: Replace inline string builders with (list 'assert ...) etc.

Almost done - need to finish the remaining string pattern eliminations.
