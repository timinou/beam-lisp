#+TITLE: Session 155fc766496bed00 Todos
#+DATE: 2026-08-21
#+TODO: ITEM DOING BLOCKED | DONE

* scope

** DONE Inspect target commit and sorted collection definitions
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Read git diff for ce4eb26 affecting lib/beam_lisp/rt.ex, priv/core.bl, lib/beam_lisp/sorted.ex, and sorted tests; inspect surrounding symbols and record exact changed lines. Acceptance: complete list of sorted-related changes and relevant dispatch helpers.

Sorted dispatches are present in RT; core dissoc/map ops and LazySeq.cell lack sorted handling. Exact commit diff unavailable through read-only navigation, so anchors use current target source lines.

* analysis

** DONE Trace sorted operations through runtime behavior
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Follow sorted collection handling for empty?, into, keys, vals, dissoc, update, select-keys, get-in, assoc-in, merge, equality, hash, nth, peek, pop, reduce, and count variants using source navigation only. Acceptance: each path classified with concrete expression and changed-line anchor where defective.

Defects: reduce/into source fail because LazySeq.cell lacks SortedMap/SortedSet; dissoc Map.delete edits struct instead of tree; eqv falls to Kernel.== and rejects cross-representation equality; merge seeds {} and loses first sorted-map type; generic peek/pop expose seq results although Clojure supports list/vector/queue only. empty/count/seq/get/keys/vals/update/select-keys/get-in/assoc-in/nth behavior is either correct or intentionally unsupported. No sorted hash implementation exists.

* review

** DONE Cross-check tests and report source defects
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Inspect relevant sorted tests and compare expected Clojure semantics, excluding known issues and pre-existing behavior. Acceptance: report only patch-introduced, concrete, silent wrong behavior; otherwise state no finding.

Report five patch-introduced defects: reduce/into crash on sorted source; dissoc no-op on sorted map; equality rejects sorted/plain maps and sorted/hashed sets; merge loses sorted-map result type; peek/pop incorrectly treat sorted collections as stacks/sequences. No hash path exists to assess.
