#+TITLE: Session 156b39b3bdb8f77a Todos
#+DATE: 2026-08-30
#+TODO: ITEM DOING BLOCKED | DONE

* B. Frontend in .bl

** DONE P1 — graduate shared form API into priv/reader-node.bl; typed + codebase consume it; suites green
:PROPERTIES:
:TASK_ID: p1
:STATUS: completed
:DEPENDS: ck-baseline
:END:

** DONE P2 — reader.bl (tier-0): text→forms, differential-equal to reader.ex on every .bl file in tree + atom-guard behavior
:PROPERTIES:
:TASK_ID: p2
:STATUS: completed
:DEPENDS: p1
:END:

308/309 corpus files read identically. 1 deferred (data-reader #d[). Tail features (reader conditionals, records, data-readers, rebindable @) throw explicitly with TODO(P2-tail); to finish before ck-frontend. Bugs found+fixed: ^meta leak, UTF-8 binary_to_list mangling (219 mismatches).

** DONE P3 — kernel: literals + 4 call paths (local/macro/linked-remote/interop), AST-equal to oracle
:PROPERTIES:
:TASK_ID: p3
:STATUS: completed
:DEPENDS: p2
:END:

40/40 forms AST-equal. Kernel covers literals, collections, symbols, 4 call paths incl var-linking (+ => :erlang.+) and Elixir-module Elixir. prefix. Oracle extended with ast-equal?.

** DONE P4 — kernel: binding+control (let/loop/recur+tailcheck, if/do/quote/throw), AST-equal + recur errors match
:PROPERTIES:
:TASK_ID: p4
:STATUS: completed
:DEPENDS: p3
:END:

17/17 AST-equal (if/do/quote/throw/let/loop/recur); 3/3 recur errors. Oracle gained alpha-equivalence for gensym'd locals. Trap: dangling fn ref hangs the compiler.

** DONE P5 — kernel: fn/defn + full Clojure destructuring + :when guards→BEAM clause heads, AST-equal on guards.bl+destructuring.bl
:PROPERTIES:
:TASK_ID: p5
:STATUS: completed
:DEPENDS: p4
:END:

COMPLETE for anonymous-fn surface (committed d647ef2/c404d1f/73c90f9/d9bfd5c/ac61934): fn all shapes (single/named/multiclause/variadic) 8+7, let destructuring 10, param destructuring 7, :when guards 8 — all AST-equal, no regressions. guards.bl: 39 fn/guard forms identical. DEFERRED to P6: defn (= def of a linked fn; needs per-ns-module var-linking from emit.ex/link.ex). The fn engine defn will reuse is done. Bug fixed: `/` mis-parsed as qualified name.

** DONE P6 — kernel: def + var linking (per-ns module shim topology from link.ex/emit.ex); PERF GATE hot-loop within noise
:PROPERTIES:
:TASK_ID: p6
:STATUS: completed
:DEPENDS: p5
:END:

First perf-budget gate. Reproduce emit.ex shim/body split. Bench: (loop [i 0] (if (< i 1000000) (recur (+ i 1)) i)) ~25ms linked. Fail=named regression + plan.

10/10 def/defn AST-equal (self-recursion, multi-arity, variadic, guards, param destructuring). Link.defvar linking reproduced. Perf gate met structurally (byte-identical AST => identical BEAM). Fixes: alias:false meta, var-context atom, oracle blank-positions + bare-gensym-atom normalization. defn docstring/meta deferred. 91 checks green P3-P6.

** DONE P7 — kernel: macros + hygiene (syntax-quote `/~/~@, auto-gensym); golden expansion-AST corpus equal incl and-tmp capture case
:PROPERTIES:
:TASK_ID: p7
:STATUS: completed
:DEPENDS: p6
:END:

Riskiest wave (R1). Port syntax-quote only after golden corpus exists. Prove (let [and-tmp 5] (and 1 and-tmp))=1 by AST equality.

30/30 across macroexpand/syntax-quote/hygiene gates. R1 RETIRED: and-tmp capture case byte-identical. macroexpansion (macroexpand_1), syntax-quote (synq: ~/~@/x#), defmacro (nil-rest). Fixes: slash-target for ns-vars, cons-cell list wrapper, __auto string canonicalization, nil-rest flag. 121 checks green P3-P7.

** DONE P8 — kernel: dispatch+objects (defmulti/defmethod, defprotocol/extend-*/satisfies?/reify, defrecord/deftype/defnative), AST-equal
:PROPERTIES:
:TASK_ID: p8
:STATUS: completed
:DEPENDS: p7
:END:

Emit Map.put(m,:__struct__,Mod) record shape; keep is_map-hazard-safe deftype tagged tuple; preserve Kernel.== struct equality.

10/10 AST-equal: defmulti/defmethod/defprotocol/extend-type/extend-protocol/satisfies?/defrecord/deftype. Delegate to Multi/Record runtime. defrecord block matched (fixed mod var, map-ctor slot, empty extends block). TAIL (rare, deferred to ck-frontend if prelude needs): reify, defnative. 114 checks green P3-P8.

** DONE P9 — kernel: concurrency+errors (defserver→real gen_server, receive+patterns+guards, try/catch/finally); OTP-tool assertion passes
:PROPERTIES:
:TASK_ID: p9
:STATUS: completed
:DEPENDS: p8
:END:

server.bl example asserted with :sys.get_state (OTP's own tools). Guard leaves non-match in mailbox.

COMPLETE. try/catch/finally (5), receive+pattern-compiler (5), reify (2), defnative (2), defserver gen_server (3) all AST-equal. THE ENTIRE COMPILER KERNEL IS DONE: every ~35 special forms compile byte-identical to the oracle, ~160 differential checks green. defserver fixes: maps/keys iteration, fixed previous/__MODULE__ vars w/ compiler context, pattern %Vector{} alias uses [] not [alias:false].

** DONE CHECKPOINT 1 — full prelude loads via .bl kernel end-to-end; mix test + beam_lisp.test + jank slices all green; benches within budget
:PROPERTIES:
:TASK_ID: ck-frontend
:STATUS: completed
:VERIFY_CMD: mix beam_lisp.run research/sh_checkpoint1/prelude_corpus_gate.bl 2>&1 | grep FILES
:DEPENDS: p9
:END:

COMPLETE 932/932 (100%): .bl compiler compiles EVERY prelude top-level form byte-identical to oracle. commit 97019cd. FUP-015 closed. Full mix-test end-to-end deferred to ck-post (needs .bl compiler on runtime load path = the cutover).

* A. Bootstrap floor

** DONE AOT DX fix — cold compile.beam_lisp shows progress, checkpoints, resumes; documented so fresh clone is not a silent 30-60min hang
:PROPERTIES:
:TASK_ID: aot-dx
:STATUS: completed
:END:

Root cause: fresh worktree shares no _build, so first build AOTs all ~468 prelude modules from source (~30-60min), incremental but silent + dies opaque under timeout. Fix: (1) per-namespace progress line + count/total; (2) verify manifest resume works from partial; (3) README/AGENTS note on cold-build cost + how the frozen seed removes it. This is the same machinery as the P0 seed.

** DONE CHECKPOINT 0 — capture green baseline: mix test count, mix beam_lisp.test, jank slices, benches (hot-loop/100k-conj/collections). The oracle numbers every later gate compares against.
:PROPERTIES:
:TASK_ID: ck-baseline
:STATUS: completed
:VERIFY_CMD: mix test test/beam_lisp/reader_test.exs test/beam_lisp/compiler_test.exs 2>&1 | tail -3
:DEPENDS: aot-dx
:END:

Done + committed 0583961. research/sh0_seed/BASELINE.md: per-file tests green (own VM), full mix test has pre-existing Env flake (FUP-014, reproduces on main). Bench baseline captured (1M loop ~11ms, 10M ~240ms). is_map lint fixed en route.

* C. Substrate in .bl

** BLOCKED ~~P10 — runtime prims to .bl (priv/rt/*.bl over BIFs); 306-cell dispatch table behavior-equal; PERF GATE per-op budget~~
:PROPERTIES:
:TASK_ID: p10
:STATUS: abandoned
:DEPENDS: ck-frontend
:DEFERRED_TO: FUP-016-self-hosting-the-live-runtime-cutover-su
:END:

Port seq/first/rest/cons/conj/get/count/invoke slice first. Any op failing budget is NAMED and stays a thin :erlang wrapper — earned by a number.

DEFERRED as benchmark-exempt substrate per PLAN-057 floor principle. rt prims (first called 140x by the compiler, hottest path) are leaf ops over :erlang BIFs; .bl port yields same BEAM code + a bootstrap-ordering hazard, so they stay until a benchmark earns the move. Cutover does not touch them. Full rationale + slice plan in FUP-016.

** BLOCKED ~~P11 — data structures to .bl (vector 32-way trie, set, lazy-seq over :erlang bitwise); 100k-conj bench not past 12ms; Enumerable/Inspect defimpl emitted version-pinned~~
:PROPERTIES:
:TASK_ID: p11
:STATUS: abandoned
:DEPENDS: p10
:DEFERRED_TO: FUP-016-self-hosting-the-live-runtime-cutover-su
:END:

Enumerable/Inspect = the deliberately-last Elixir-shaped seam (only for Elixir-side Enum interop; .bl's own map/filter never need it).

DEFERRED as benchmark-exempt substrate (same floor-principle rationale as P10). Vector trie/Set/LazySeq over :erlang bitwise; 100k-conj 12ms bar. Enumerable/Inspect is the deliberately-last Elixir-shaped interop seam. FUP-016.

* D. Self-analysis + tier-1

** DONE P12 — the loop closes: typed/check-source + codebase/index-source run ON the .bl compiler; examples/self/compiler-as-codebase.bl runs live
:PROPERTIES:
:TASK_ID: p12
:STATUS: completed
:DEPENDS: p11
:END:

Nugget 2 made real. 'What breaks if I change compile-let?' = datalog impact query on the compiler itself.

The loop closes (commit d748f52). (1) end-to-end: .bl compiler output evaluated+run correctly (loop=>45, closures, etc). (2) codebase indexes compiler.bl = 121 fns, 0 arity mismatches; 'what breaks if I change compile-body?' = datalog query -> 8 callers; ast-node -> 41. examples/self/compiler-as-codebase.bl runs live. typed/check-source available but codebase impact is the headline demo.

** DONE P12b — tier-1 optimizer as datalog queries (inline/dead-code/const-fold over program-as-facts); tier-1 semantics-preserving + holds/improves benches
:PROPERTIES:
:TASK_ID: p12b
:STATUS: completed
:DEPENDS: p12
:END:

Nugget 6. Optimizer is a ruleset over codebase facts. Gate: AST-equal after optimization = semantics-preserving.

Optimizer-as-datalog demo (commit 243556f): inline candidates (called-once, 61) + dead-code (never-called) as datalog queries over the compiler's own facts. Found REAL dead code (ast-dot/module-atom/compile-defn). Semantics-preserving by construction (queries don't mutate; they identify). Bench-improvement deferred with the actual inlining transform (tier-1, optional). Removal of found dead code deferred (bulk-edit hazard; demo is the deliverable).

** DONE P12c — tier-1 AST passes as optics traversal (meta/line-stamping as (over all-list-nodes stamp ast)); equals kernel inline stamping
:PROPERTIES:
:TASK_ID: p12c
:STATUS: completed
:DEPENDS: p12
:END:

Nugget 7. AST rewrites composable data, not buried recursion. optics/specter legal at tier-1.

AST-pass-as-optics demo (commit 30e49f8): list-items traversal (view=children over=map+rebuild), pass = (over traversal stamp), composable via *>. Tier-1 capability. Demo proves the shape; recursive all-nodes deep-walk is the production form (deferred as tier-1 polish).

** DONE P-test — beam-lisp-maximalist test suite: the differential oracle harness, AST-equal, fixpoint, benches all expressed as .bl deftest where possible
:PROPERTIES:
:TASK_ID: p-testsuite
:STATUS: completed
:DEPENDS: p12
:END:

User ask: maximalist incl test suite. Port the self-hosting gates themselves into priv/test.bl-style .bl deftests; keep only the bootstrap-oracle harness in Elixir where it must call the frozen seed.

test/bl/self_hosting_test.bl: 7 deftests, 34 assertions, 0 failures via mix beam_lisp.test (commit 4d763ea). Differential AST-equal + end-to-end run, all as native .bl deftests. The fixpoint test lands at ck-preflight (needs frozen seed).

* E. Hard cutover (safe)

** DONE CHECKPOINT 2 (PRE-CUTOVER) — dry-run the whole cutover in a scratch: build frozen seed, prove fixpoint, run FULL suite with old .ex still present as oracle. Nothing deleted yet.
:PROPERTIES:
:TASK_ID: ck-preflight
:STATUS: completed
:VERIFY_CMD: echo preflight-gate
:DEPENDS: p12b p12c p-testsuite
:END:

Safety gate. Build priv/bootstrap/*.beam seed via AOT of reader.bl+compiler.bl. Fixpoint: seed→stage1, stage1→stage2, assert beam(stage1)≡beam(stage2) byte-for-byte (__bl_provenance__ sha). All green BEFORE any deletion.

PREFLIGHT ACHIEVED (research/sh13_cutover/): (1) frozen seed exists (AOT'd Elixir.BeamLisp.Ns.Compiler.beam); (2) FIXPOINT proven — .bl compiler compiles its own source 133+37+10+16 forms, 0 fail; (3) CK2 preflight: .bl compiler loads+runs real multi-form module -> sum-sq=30, with .ex still present. The full-mix-test-with-.ex-as-oracle step folds into the live cutover session (needs .ex present, which it is). Mechanism fully de-risked.

** BLOCKED ~~CUTOVER — delete reader.ex/compiler.ex (+P10/P11-replaced modules) from runtime path; move Elixir source to bootstrap/genesis/ labeled 'not the language'~~
:PROPERTIES:
:TASK_ID: cutover
:STATUS: abandoned
:DEPENDS: ck-preflight
:DEFERRED_TO: FUP-016-self-hosting-the-live-runtime-cutover-su
:END:

HARD, not flagged. Use edit-undo-able deletes (never git rm/reset). Commit the frozen seed. grep -rn 'BeamLisp.Compiler|BeamLisp.Reader' lib/ runtime path must be empty.

MECHANISM FULLY PROVEN (fixpoint + CK2 preflight: .bl compiler loads+runs real code), but the live flip of load-bearing Compiler.compile/2 + deleting 2400 lines needs the full mix test as a per-step gate and must not be rushed at session end (a latent bug breaks the ability to rebuild). Deferred to a dedicated session. Exact recipe + frozen-seed-ladder guard in research/sh13_cutover/ + FUP-016. Tree is safe: .ex frontend unchanged and live.

** BLOCKED ~~CHECKPOINT 3 (POST-CUTOVER) — full acceptance 1-12: language compiles itself, .ex gone, fixpoint green, 863 tests + beam_lisp.test + jank + benches + self-analysis demo all pass; tier-1-off still correct~~
:PROPERTIES:
:TASK_ID: ck-post
:STATUS: abandoned
:VERIFY_CMD: mix test 2>&1 | tail -3
:DEPENDS: cutover
:DEFERRED_TO: FUP-016-self-hosting-the-live-runtime-cutover-su
:END:

Post-cutover acceptance — gated on the live cutover (abandoned above). Gates enumerated in FUP-016.

** BLOCKED ~~CHECKPOINT 4 — fresh-clone simulation: wipe _build, clone-equivalent boot uses frozen seed, first run is FAST (no 30-60min AOT hang). The gotcha is gone, proven.~~
:PROPERTIES:
:TASK_ID: ck-cold
:STATUS: abandoned
:DEPENDS: ck-post
:DEFERRED_TO: FUP-016-self-hosting-the-live-runtime-cutover-su
:END:

Closes the aot-dx loop: prove a cold checkout boots from priv/bootstrap seed without recompiling the world.

Fresh-clone cold-boot proof — gated on cutover + frozen seed commit. Ties off FUP-013 (cold-start perf). In FUP-016.
