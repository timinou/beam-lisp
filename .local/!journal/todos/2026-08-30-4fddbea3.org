#+TITLE: Session 156b39b3bdb8f77a Todos
#+DATE: 2026-08-30
#+TODO: ITEM DOING BLOCKED | DONE

* B. Frontend in .bl

** ITEM P1 — graduate shared form API into priv/reader-node.bl; typed + codebase consume it; suites green
:PROPERTIES:
:TASK_ID: p1
:STATUS: pending
:DEPENDS: ck-baseline
:END:

** ITEM P2 — reader.bl (tier-0): text→forms, differential-equal to reader.ex on every .bl file in tree + atom-guard behavior
:PROPERTIES:
:TASK_ID: p2
:STATUS: pending
:DEPENDS: p1
:END:

** ITEM P3 — kernel: literals + 4 call paths (local/macro/linked-remote/interop), AST-equal to oracle
:PROPERTIES:
:TASK_ID: p3
:STATUS: pending
:DEPENDS: p2
:END:

** ITEM P4 — kernel: binding+control (let/loop/recur+tailcheck, if/do/quote/throw), AST-equal + recur errors match
:PROPERTIES:
:TASK_ID: p4
:STATUS: pending
:DEPENDS: p3
:END:

** ITEM P5 — kernel: fn/defn + full Clojure destructuring + :when guards→BEAM clause heads, AST-equal on guards.bl+destructuring.bl
:PROPERTIES:
:TASK_ID: p5
:STATUS: pending
:DEPENDS: p4
:END:

** ITEM P6 — kernel: def + var linking (per-ns module shim topology from link.ex/emit.ex); PERF GATE hot-loop within noise
:PROPERTIES:
:TASK_ID: p6
:STATUS: pending
:DEPENDS: p5
:END:

First perf-budget gate. Reproduce emit.ex shim/body split. Bench: (loop [i 0] (if (< i 1000000) (recur (+ i 1)) i)) ~25ms linked. Fail=named regression + plan.

** ITEM P7 — kernel: macros + hygiene (syntax-quote `/~/~@, auto-gensym); golden expansion-AST corpus equal incl and-tmp capture case
:PROPERTIES:
:TASK_ID: p7
:STATUS: pending
:DEPENDS: p6
:END:

Riskiest wave (R1). Port syntax-quote only after golden corpus exists. Prove (let [and-tmp 5] (and 1 and-tmp))=1 by AST equality.

** ITEM P8 — kernel: dispatch+objects (defmulti/defmethod, defprotocol/extend-*/satisfies?/reify, defrecord/deftype/defnative), AST-equal
:PROPERTIES:
:TASK_ID: p8
:STATUS: pending
:DEPENDS: p7
:END:

Emit Map.put(m,:__struct__,Mod) record shape; keep is_map-hazard-safe deftype tagged tuple; preserve Kernel.== struct equality.

** ITEM P9 — kernel: concurrency+errors (defserver→real gen_server, receive+patterns+guards, try/catch/finally); OTP-tool assertion passes
:PROPERTIES:
:TASK_ID: p9
:STATUS: pending
:DEPENDS: p8
:END:

server.bl example asserted with :sys.get_state (OTP's own tools). Guard leaves non-match in mailbox.

** ITEM CHECKPOINT 1 — full prelude loads via .bl kernel end-to-end; mix test + beam_lisp.test + jank slices all green; benches within budget
:PROPERTIES:
:TASK_ID: ck-frontend
:STATUS: pending
:VERIFY_CMD: mix test 2>&1 | tail -3
:DEPENDS: p9
:END:

* A. Bootstrap floor

** DONE AOT DX fix — cold compile.beam_lisp shows progress, checkpoints, resumes; documented so fresh clone is not a silent 30-60min hang
:PROPERTIES:
:TASK_ID: aot-dx
:STATUS: completed
:END:

Root cause: fresh worktree shares no _build, so first build AOTs all ~468 prelude modules from source (~30-60min), incremental but silent + dies opaque under timeout. Fix: (1) per-namespace progress line + count/total; (2) verify manifest resume works from partial; (3) README/AGENTS note on cold-build cost + how the frozen seed removes it. This is the same machinery as the P0 seed.

** DOING CHECKPOINT 0 — capture green baseline: mix test count, mix beam_lisp.test, jank slices, benches (hot-loop/100k-conj/collections). The oracle numbers every later gate compares against.
:PROPERTIES:
:TASK_ID: ck-baseline
:STATUS: in_progress
:VERIFY_CMD: mix test 2>&1 | tail -3
:DEPENDS: aot-dx
:END:

* C. Substrate in .bl

** ITEM P10 — runtime prims to .bl (priv/rt/*.bl over BIFs); 306-cell dispatch table behavior-equal; PERF GATE per-op budget
:PROPERTIES:
:TASK_ID: p10
:STATUS: pending
:DEPENDS: ck-frontend
:END:

Port seq/first/rest/cons/conj/get/count/invoke slice first. Any op failing budget is NAMED and stays a thin :erlang wrapper — earned by a number.

** ITEM P11 — data structures to .bl (vector 32-way trie, set, lazy-seq over :erlang bitwise); 100k-conj bench not past 12ms; Enumerable/Inspect defimpl emitted version-pinned
:PROPERTIES:
:TASK_ID: p11
:STATUS: pending
:DEPENDS: p10
:END:

Enumerable/Inspect = the deliberately-last Elixir-shaped seam (only for Elixir-side Enum interop; .bl's own map/filter never need it).

* D. Self-analysis + tier-1

** ITEM P12 — the loop closes: typed/check-source + codebase/index-source run ON the .bl compiler; examples/self/compiler-as-codebase.bl runs live
:PROPERTIES:
:TASK_ID: p12
:STATUS: pending
:DEPENDS: p11
:END:

Nugget 2 made real. 'What breaks if I change compile-let?' = datalog impact query on the compiler itself.

** ITEM P12b — tier-1 optimizer as datalog queries (inline/dead-code/const-fold over program-as-facts); tier-1 semantics-preserving + holds/improves benches
:PROPERTIES:
:TASK_ID: p12b
:STATUS: pending
:DEPENDS: p12
:END:

Nugget 6. Optimizer is a ruleset over codebase facts. Gate: AST-equal after optimization = semantics-preserving.

** ITEM P12c — tier-1 AST passes as optics traversal (meta/line-stamping as (over all-list-nodes stamp ast)); equals kernel inline stamping
:PROPERTIES:
:TASK_ID: p12c
:STATUS: pending
:DEPENDS: p12
:END:

Nugget 7. AST rewrites composable data, not buried recursion. optics/specter legal at tier-1.

** ITEM P-test — beam-lisp-maximalist test suite: the differential oracle harness, AST-equal, fixpoint, benches all expressed as .bl deftest where possible
:PROPERTIES:
:TASK_ID: p-testsuite
:STATUS: pending
:DEPENDS: p12
:END:

User ask: maximalist incl test suite. Port the self-hosting gates themselves into priv/test.bl-style .bl deftests; keep only the bootstrap-oracle harness in Elixir where it must call the frozen seed.

* E. Hard cutover (safe)

** ITEM CHECKPOINT 2 (PRE-CUTOVER) — dry-run the whole cutover in a scratch: build frozen seed, prove fixpoint, run FULL suite with old .ex still present as oracle. Nothing deleted yet.
:PROPERTIES:
:TASK_ID: ck-preflight
:STATUS: pending
:VERIFY_CMD: echo preflight-gate
:DEPENDS: p12b p12c p-testsuite
:END:

Safety gate. Build priv/bootstrap/*.beam seed via AOT of reader.bl+compiler.bl. Fixpoint: seed→stage1, stage1→stage2, assert beam(stage1)≡beam(stage2) byte-for-byte (__bl_provenance__ sha). All green BEFORE any deletion.

** ITEM CUTOVER — delete reader.ex/compiler.ex (+P10/P11-replaced modules) from runtime path; move Elixir source to bootstrap/genesis/ labeled 'not the language'
:PROPERTIES:
:TASK_ID: cutover
:STATUS: pending
:DEPENDS: ck-preflight
:END:

HARD, not flagged. Use edit-undo-able deletes (never git rm/reset). Commit the frozen seed. grep -rn 'BeamLisp.Compiler|BeamLisp.Reader' lib/ runtime path must be empty.

** ITEM CHECKPOINT 3 (POST-CUTOVER) — full acceptance 1-12: language compiles itself, .ex gone, fixpoint green, 863 tests + beam_lisp.test + jank + benches + self-analysis demo all pass; tier-1-off still correct
:PROPERTIES:
:TASK_ID: ck-post
:STATUS: pending
:VERIFY_CMD: mix test 2>&1 | tail -3
:DEPENDS: cutover
:END:

** ITEM CHECKPOINT 4 — fresh-clone simulation: wipe _build, clone-equivalent boot uses frozen seed, first run is FAST (no 30-60min AOT hang). The gotcha is gone, proven.
:PROPERTIES:
:TASK_ID: ck-cold
:STATUS: pending
:DEPENDS: ck-post
:END:

Closes the aot-dx loop: prove a cold checkout boots from priv/bootstrap seed without recompiling the world.

* F. Finish

** ITEM DOCS — README 'clever turn' rewritten (now self-hosted), docs/self-hosting.md from zero context, every .bl compiler fn docstring'd for a newcomer
:PROPERTIES:
:TASK_ID: docs
:STATUS: pending
:DEPENDS: ck-post
:END:
