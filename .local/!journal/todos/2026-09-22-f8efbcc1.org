#+TITLE: Session 158a227ed563bbff Todos
#+DATE: 2026-09-22
#+TODO: ITEM DOING BLOCKED | DONE

* host

** DONE host.mjs: machine guard, critical leaves, boot
:PROPERTIES:
:TASK_ID: t1
:STATUS: completed
:END:

host.mjs ONLY. Add rt fields (criticalDepth/errors/maxFrames) in newRuntime. runTurn: high-water frames, defer preempt while criticalDepth>0, unwind criticalDepth to cd0 on every error path. RT_HOST: park guard (throw while critical), critical-enter, critical-leave, record-error, max-frames. boot: await host-task macrotask on :yield; reject (throw + exitCode 1) on done with errors>0 and on root error. Acceptance: node --check host.mjs passes.

** DONE host.mjs: safe-int arithmetic + erlang routing
:PROPERTIES:
:TASK_ID: t2
:STATUS: completed
:DEPENDS: t1
:END:

host.mjs ONLY. arith add/sub/mul: require numbers; integer path validates Number.isSafeInteger on both inputs and result, else throw overflow; float path = documented IEEE-754 subset (deadline clock is float). div/rem guard non-numbers + safe output on integer path. Route ERLANG + - * rem through arith. Acceptance: node --check passes; no other arith op removed.

* runtime

** DONE runtime.bl: FIFO runnable vector + scheduler
:PROPERTIES:
:TASK_ID: t3
:STATUS: completed
:END:

runtime.bl ONLY. :runnable is a vector (host/vec-from-list empty); conj appends; rt-run-head pops head via vec-el/vec-remove (new rt-pop-runnable!). rt-wake-expired runs EVERY scheduler cycle (also when runnable non-empty). Blocked-with-no-deadline -> host/record-error then :done (no silent success). Acceptance: read-back shows vec ops + wake before branch.

** DONE runtime.bl: receive snapshot/timeout semantics
:PROPERTIES:
:TASK_ID: t4
:STATUS: completed
:END:

runtime.bl ONLY. rt-deadline-of: nil | :infinity -> nil; nonnegative int -> now+ms; else raise. rt-deadline-passed?. rt-scan-from scans a mailbox SNAPSHOT (BL local) from cursor i, returns nil|[:match idx sel]. rt-receive/rt-receive-wait: scan pass; if no match and deadline passed -> [:timeout after-fn] with NO park (after 0); else atomic rt-proc-wait!, recheck mailbox (arrival during pass), park, resume, rescan. Deadline computed once (never reset). Acceptance: after-0 path has no park; timeout path rescans before timeout.

** DONE runtime.bl: critical sections on all runtime RMW
:PROPERTIES:
:TASK_ID: t5
:STATUS: completed
:END:

runtime.bl ONLY. Wrap rt-spawn, rt-send, rt-requeue!, rt-set-proc-field, rt-drop-mailbox-msg, rt-run-head pop (rt-pop-runnable! via rt-vec-index), rt-proc-wait!, rt-proc-resume! in host/critical-enter ... host/critical-leave. Nested critical allowed (rt-send -> rt-requeue!). No host/park inside critical. rt-run-head restores self to nil after rt-run-pid. rt-run-pid records actor error via host/record-error. Acceptance: every enter has a matching leave on the success path.

* verify

** DONE Verify host.mjs syntax + read-back runtime
:PROPERTIES:
:TASK_ID: t6
:STATUS: completed
:DEPENDS: t2 t5
:END:

Run node --check on host.mjs (syntax only; no compiler, no test run, no build artifact). Read back runtime.bl changed symbols to confirm balance of critical enter/leave, vector runnable, no park-in-critical. NO build, NO test suite (main active). Acceptance: node --check exit 0; read-back clean.

node --check host.mjs OK; paren balance runtime.bl OK; no undefined locals; build mirror check allowlist==hostNames() (67==67); machine harness 26/26; key harness 17/17; receive protocol model 8/8.
