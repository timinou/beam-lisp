#+TITLE: Session 1563ddc638f51506 Todos
#+DATE: 2026-08-24
#+TODO: ITEM DOING BLOCKED | DONE

* Review

** DONE Map read-model patch and schema contracts
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Inspect git diff plus full projects/reel/src/reel/read.bl and schema.bl; enumerate every query function, claimed semantics, changed lines, attribute cardinalities, and concrete result shapes.

Reel is an independent clean git repo with two commits; read.bl and schema.bl were added in f54d39b. Enumerated all query/read functions and cardinalities. Candidate mismatches: task lifecycle ignored by unrealised-components; direct film blockers ignored by showable; changed-since joins only new slug datoms; non-total list sorts; open-tasks doc claims newest but sorts title; lead-board role ambiguity.

** DONE Verify temporal and host-filter semantics
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Read priv/datom.bl, priv/datom/time.bl, and relevant datalog/query implementation; prove since boundary, changes record/order, not-join semantics, host-bound set behavior, duplicate generation, and snapshot reuse against projects/reel/src/reel/read.bl.

Verified: since excludes tx=t and current-mode visibility hides old slug/retraction datoms; changed-since therefore sees creations whose slug was newly asserted, not ordinary task/component updates. changes is oldest-first and cardinality-one replacement records old retraction/new assertion; filtering :added is correct. overview and nested helpers reuse the supplied immutable db. load-by-lead host filtering preserves centralized open? semantics, though its aggregate docstring is stale.

** DONE Validate findings against callers and tests
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:VERIFY_REVIEW: Every reported issue is introduced by this patch, patch-anchored, and includes a concrete triggering data shape and demonstrated wrong result.
:DEPENDS: task-2
:END:

Inspect projects/reel callers, fixtures/corpus, and tests without running builds; for each candidate bug identify exact triggering datoms, user-visible wrong answer, and a ≤10-line diff anchor; reject intentional or pre-existing behavior.

Validated seven patch-anchored findings. Supported paths prove changed-since state updates and dropped-task invisibility; intent record-film comments prove direct blockers must affect showability; film-blockers' nil-state default proves showability should fail closed. Same-title tasks are explicitly supported, lead schema explicitly allows duplicate roles, and film number is non-unique. No tests/builds rerun per user instruction.
