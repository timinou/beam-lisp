#+TITLE: Session 1570dfc869020fe0 Todos
#+DATE: 2026-09-03
#+TODO: ITEM DOING BLOCKED | DONE

* evidence

** DONE Trace current CLI startup and load boundary
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Inspected mix.exs and priv/std/bl/cli.bl. Acceptance met: escript directly enters BeamLisp.Ns.Bl.Cli.main/1; main calls BeamLisp.AOT.boot before parse-argv, and BEAM resolves remote module calls lazily. Earliest viable probe is first operation in main before AOT.boot, implemented with a tiny Elixir client API to avoid initializing .bl registry.

Observed main/1 ordering and direct escript config; archive-only client latency still needs a runtime spike because current binary always boots AOT.

** DONE Map build locking watcher and command flow
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:END:

Inspect priv/boot/build.bl, Loader/AOT locking, ReloadWatcher, cmd-run/eval/test/doc/watch implementations. Acceptance: document current command boundaries, I/O assumptions, CWD/path handling, global lock behavior, and reusable watcher APIs with exact paths/lines.

** DONE Verify transport lifecycle and security constraints
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:END:

Use official OTP documentation and local runtime probes where read-only to compare AF_UNIX, loopback TCP, and distribution startup/security/staleness. Acceptance: evidence-backed transport verdict addressing peer credentials/permissions, cookie exposure, stale endpoint behavior, and framing.

* design

** DONE Synthesize protocol lifecycle and implementation gates
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:VERIFY_REVIEW: Every requested decision is concrete, singular, and backed by observed repo or official OTP evidence; assumptions/caveats explicit.
:DEPENDS: task-1 task-2 task-3
:END:

Combined repo evidence, local timing probes, official OTP/Linux documentation, and adversarial design review. Acceptance met: singular AF_UNIX architecture; native client decision; exact components/files; ETF protocol; lifecycle/fallback/restart; serialized build/watch execution; CWD/path/argv/I/O handling; staged tests; ranked risks.

Key correction vs delegated transport scout: local measurement shows net_kernel adds no material startup cost (0.547s distributed vs 0.543s bare with +S 1:1); distribution is rejected for security/surface, not a claimed 13s startup. System.halt cannot be trapped; owned call sites must be removed, while user System.halt remains daemon-fatal and must not trigger replay.
