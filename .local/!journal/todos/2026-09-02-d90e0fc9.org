#+TITLE: Session 1570141104c20fcc Todos
#+DATE: 2026-09-02
#+TODO: ITEM DOING BLOCKED | DONE

* Tasks

** DOING Eliminate string assembly in inv-define-fun and decl-of
:PROPERTIES:
:TASK_ID: task-1
:STATUS: in_progress
:END:

priv/lib/system/core.bl

1. inv-define-fun (line ~629): Build list forms instead of strings:
   - input: prelude (list), svars, sorts, inv-smt (form)
   - output: list of forms: [prelude (define-fun Inv ((v Sort)...) Bool inv-smt)]
   - update call sites to use returned list form

2. decl-of (line ~634): Return form list not string:
   - (decl-of nm sort) → [(list 'declare-const (symbol nm) (symbol sort))]
   - All callers wrap with concat/mapcat

Acceptance: Both functions return data forms, not strings. No (str "(

** ITEM Convert inv-define-fun and decl-of call sites in preserves-joint?
:PROPERTIES:
:TASK_ID: task-2
:STATUS: pending
:DEPENDS: task-1
:END:

priv/lib/system/core.bl lines ~636-656

preserves-joint? currently builds SMT string with str/join. Convert to:
- Collect forms: prelude, decls (from decl-of), prime-decls, inv-def, guard, prime-eqs, neg-Inv
- Build single list: (concat [prelude decls ... (assert ...)])
- Pass form list to z3/check via (smt/script forms) or pass raw forms
- z3/check currently expects string; will need wrapper

Acceptance: z3/check receives (smt/script cmd-forms) or command list directly

** ITEM Create local helper to wrap form lists for z3 port
:PROPERTIES:
:TASK_ID: task-3
:STATUS: pending
:DEPENDS: task-1
:END:

priv/lib/system/core.bl, add near top

(defn- smt-script [cmd-forms] (smt/script cmd-forms))

Or: z3/check migration may accept lists directly (TBD by external interface).
For now wrap all command lists with (smt/script) before z3/check.

Acceptance: All z3/check calls receive SMT string from (smt/script) not manual (str ...)

** ITEM Convert establishes-joint? to use form lists
:PROPERTIES:
:TASK_ID: task-4
:STATUS: pending
:DEPENDS: task-1
:END:

priv/lib/system/core.bl lines ~658-664

Replace string assembly with form builders. Build command list, wrap with smt/script.
Pre-conditions: inv-define-fun returns forms.

Acceptance: No (str "( in establishes-joint?

** ITEM Convert all preserve/establish/verify calls in core.bl to use form lists
:PROPERTIES:
:TASK_ID: task-5
:STATUS: pending
:DEPENDS: task-3
:END:

priv/lib/system/core.bl

Affected functions (scan for z3/check + str patterns):
- joint-fireable? (lines ~818+)
- guard-implies-multi? (lines ~835+)
- holds-at-init? (lines ~908+)
- inductive-under? (lines ~915+)
- len-bound-inductive? (lines ~1081+)

Pattern: Replace (str ...) assembly with (list ...) / form builders, collect into lists, wrap with (smt/script).

Acceptance: No (str "( remains in any z3/check call path

** ITEM Convert verify-content's distinct-def to form (if present in smt.bl)
:PROPERTIES:
:TASK_ID: task-6
:STATUS: pending
:DEPENDS: task-3
:END:

priv/lib/system/core.bl lines ~1207-1210

The `distinct-def` is a (def ^:private distinct-def (str ...)) with SMT boilerplate.
Convert to a form list or helper function returning forms.
Update verify-content to build forms and use smt/script.

Acceptance: distinct-def uses data forms, no (str "(

** ITEM Check smt.bl for duplicate emit/script and require statements
:PROPERTIES:
:TASK_ID: task-7
:STATUS: pending
:END:

priv/lib/system/smt.bl

1. Line ~510: (str "(" pattern in emit-list — is this the only printer?
   Confirm no other emit implementation.

2. Check if smt.bl itself requires [smt] — should not (it IS smt).

3. Verify only one script/emit pair.

Acceptance: No duplicate emit/script. smt.bl does not require itself.

** ITEM Verify parser and run smoke test
:PROPERTIES:
:TASK_ID: task-8
:STATUS: pending
:DEPENDS: task-6 task-7
:END:

After all edits:

1. Parse with: ../beam-lisp/bl priv/lib/system/core.bl
2. Parse with: ../beam-lisp/bl priv/lib/system/smt.bl
3. Check no parse errors.
4. Do NOT run global test suite (out of scope).
5. Verify no (str "( patterns remain in target files.

Acceptance: Both files parse without error via beam-lisp CLI.
