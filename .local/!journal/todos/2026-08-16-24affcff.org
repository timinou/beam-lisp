#+TITLE: Session 155a14cd1274a977 Todos
#+DATE: 2026-08-16
#+TODO: ITEM DOING BLOCKED | DONE

* Review

** DONE Inspect cassette diffs and module context
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Read git show 0546b49 and 3911f1e, plus full spell/src/spell/cassette.bl, scripts/record_cassette.exs, and test/cassettes/*.edn. Acceptance: map every changed parser/writer line and cassette field layout.

W2 adds the entire 270-line cassette module and recorder; W3 adds one derived cassette. Layout is metadata, :stream, :status, :request, then :body.

** DONE Prove hostile cassette parsing behavior
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Analyze escape/unescape and up-to-unescaped-quote on literal backslash+n beside newline, trailing backslash, backslash-backslash-quote, NUL/control bytes, and long bodies; analyze field confusion and malformed integer behavior. Acceptance: exact counterexample or proof for each claim, anchored to changed lines.

Modeled exact replacement pipeline: backslash+n+LF fails (`\\n\\n` decodes to backslash+LF+LF); trailing backslash, two backslashes+quote, and NUL/BS/FF/0x01 round-trip. Quote scanner handles writer-produced even/odd backslash runs. Invalid/missing stream coerces to false; nonnumeric status raises; unterminated body returns nil success.

** DONE Audit cassette writers and produce verdict
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Search repository for spell.cassette/write!, File.write/File.write!, and cassette output paths; inspect call paths and report only introduced, actionable defects. Acceptance: submit_result verdict, with report_finding calls for all proven issues.

Only current cassette write caller is scripts/record_cassette.exs; File/write! occurs only inside cassette.write!. Public write! remains callable by a deliberate test, but there is no cache-miss/network recording path outside the script. Four patch-anchored defects reported.
