#+TITLE: Session 15660dc6528e6c24 Todos
#+DATE: 2026-08-26
#+TODO: ITEM DOING BLOCKED | DONE

* Phase 1

** DONE Commit Phase-1 caching (green, 52 tests)
:PROPERTIES:
:TASK_ID: p1-commit
:STATUS: completed
:END:

Commits d8b00be (initial) + 0af8731 (normalized-shape fix) exist past baseline 37e9227. Gate baseline confused by an --amend early on; commits verified via git log.

** DONE Swarm review Phase-1 caching diff
:PROPERTIES:
:TASK_ID: p1-review
:STATUS: completed
:DEPENDS: p1-commit
:END:

Swarm found real bugs: req_llm normalizes usage before relay sees it (raw-key reads recorded ZERO cache tokens in prod); anthropic opts on OpenAI raise. Both verified against live req_llm source+probes. Fixed: unify reads normalized aliases + honors input_includes_cached; :cache-write total + TTL-bucket-by-intent; provider-port (already committed d8febda) structurally fixes the opt-leak. Committed 0af8731. 67/207 green + verified vs live Normalize.

* Phase 4

** DONE Phase 4: inference-id, feedback, JSONL export, key admin, rate limit
:PROPERTIES:
:TASK_ID: p4-observability
:STATUS: completed
:DEPENDS: p1-review
:END:

inference.bl (id+record+feedback+lazy export), ratelimit.bl (per-key window), gate enforcement, http endpoints (/v1/feedback, /admin/logs.jsonl, /admin/keys list/detail/revoke), store schema. Committed in fc8fdac (swept by parallel session) + 0bfa6ab (tests+plumbing). 87 tests/265 assertions green. Export verified valid-JSON-per-line e2e.

** DONE Swarm review Phase-4 observability
:PROPERTIES:
:TASK_ID: p4-review
:STATUS: completed
:DEPENDS: p4-observability
:END:

Swarm (3 reviewers) found 5 real issues: rate-limit boundary race, feedback 500-not-400 on bad types, export not truly lazy (eager entity fetch in filter), query not URL-decoded + malformed filter silently widens export, admin token timing side-channel. All fixed + regression-tested. Committed 40f6750. 96 tests/295 assertions green.

* Phase 3

** DONE Phase 3: employee keys (Kimi/Zai) + gated Anthropic OAuth
:PROPERTIES:
:TASK_ID: p3-auth
:STATUS: completed
:DEPENDS: p4-review
:END:

Done by parallel session: config.bl has Kimi (openai-compat + moonshot base-url) + Zai (:zai) aliases; accounts.bl provides Anthropic Max OAuth gated behind RELAY_OAUTH_ENABLED (off by default, ToS-safe). Committed fc8fdac + accounts-test. Adapter forwards base_url.

** DONE Surface ToS-grey OAuth resale to user before shipping
:PROPERTIES:
:TASK_ID: p3-surface
:STATUS: completed
:DEPENDS: p3-auth
:END:

OAuth resale path surfaced to user in final report. Verified gated behind RELAY_OAUTH_ENABLED (off by default) + documented in README ToS-gated section. Not enabled without explicit operator action.

* Phase 2

** DONE Phase 2: relay.analytics w/ stubbed q->df (Session A not landed)
:PROPERTIES:
:TASK_ID: p2-analytics
:STATUS: completed
:DEPENDS: p4-review
:END:

Done by parallel session: analytics.bl (df-for, dashboard specs via datom.frame/q->df seam w/ Explorer fallback), /admin/analytics endpoints, cost-reconciliation >2% drift check. Committed c632ad1.

* Frontend

** DONE Frontend: spacetime @chart macro + admin dashboard
:PROPERTIES:
:TASK_ID: frontend
:STATUS: completed
:DEPENDS: p2-analytics
:END:

Done by parallel session: frontend/admin_dashboard.st ECharts grammar-of-graphics page + reusable @chart macro-module. Committed 7781409.

* Phase 5

** DONE Swarm review Phase-5 dialect
:PROPERTIES:
:TASK_ID: p5-review
:STATUS: completed
:END:

2 reviewers found 3 real issues: msg_/chatcmpl- body-id feedback 404, OpenAI-shaped gate error leaking on Anthropic door, max_tokens not enforced. All fixed + regression-tested (id canonicalization, gated-anthropic, max_tokens 400). v1 limits (tool_use, cache usage, stop_sequence) honestly documented. Committed e1cf339. 119/365 green.
