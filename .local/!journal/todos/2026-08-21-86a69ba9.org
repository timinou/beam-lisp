#+TITLE: Session 155ffe85b2abed33 Todos
#+DATE: 2026-08-21
#+TODO: ITEM DOING BLOCKED | DONE

* review

** DONE Map patch and Datom layer APIs
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Inspect `git diff`, all modified files under `/home/user/code/undefine/beam-lisp/priv/datom/`, and test/run entry points. Acceptance: identify changed lines, callable DB/query/pull/time APIs, and a non-editing method to execute review constructions.

Milestone patch is 1d62717..HEAD; wave-5 delta is 2b95e6e..HEAD. APIs mapped in conn/db/tx/time/pull/query. Verified non-editing stdin execution: `printf ... | mix beam_lisp.run --path priv /dev/stdin` returned 3.

** DONE Run aggregate and nil constructions
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Execute constructions 1 and 7 using `mix beam_lisp.run --path priv <file.bl>` or equivalent without modifying repository files. Record setup, expected Datomic semantics, actual values for as-of/history aggregates and missing-attribute sum/avg/count.

** DONE Run pull and lookup constructions
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-1
:END:

Execute constructions 2, 3, and 8. Record nested as-of pull behavior when target is future-only, lookup-ref identity ownership at each transaction, and wildcard/reverse-ref/cardinality-many pull behavior against Datomic semantics.

** DONE Run temporal transaction constructions
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:DEPENDS: task-1
:END:

Execute constructions 4, 5, and 6. Record cardinality-many values/entity shape over transaction points, upsert behavior from an old DB value, and same-transaction cardinality-one retract/assert visibility.

** DOING Trace discrepancies and submit verdict
:PROPERTIES:
:TASK_ID: task-5
:STATUS: in_progress
:DEPENDS: task-2 task-3 task-4
:END:

For every observed mismatch, trace exact affected path through `priv/datom/`, confirm it was introduced by the reviewed patch and anchor to ≤10 changed lines. Report qualifying findings, then submit one structured correctness verdict. Acceptance: all eight constructions accounted for; no known/accepted or prior-wave defect re-reported.

Constructions 1-8 all executed by delegated reviewers. Candidate nil-count was rejected because assignment explicitly specifies count counts rows; dangling nested pull was rejected because official Datomic docs say nonmatching nested subpulls are removed. Additional aggregate basis reproduction proved no-`:with` count/sum fail to coalesce duplicate values.
