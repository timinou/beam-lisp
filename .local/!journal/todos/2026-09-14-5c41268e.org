#+TITLE: Session 157f0c246a28a2b5 Todos
#+DATE: 2026-09-14
#+TODO: ITEM DOING BLOCKED | DONE

* board

** DONE Renumber the 13 org IDs the merge duplicated
:PROPERTIES:
:TASK_ID: renumber
:STATUS: completed
:END:

Branch-side items collide with main's: BUG-037..041, FUP-053,055..060, PLAN-110. Rename files, fix CUSTOM_ID, update cross-references.

13 branch-side items renamed BUG-042..046 / FUP-061..067 / PLAN-111; CUSTOM_IDs and 13 cross-refs updated. Duplicates now = the 16 pre-existing ones.

* build

** DONE Regenerate the stale bootstrap seed
:PROPERTIES:
:TASK_ID: seed
:STATUS: completed
:VERIFY_CMD: mix test test/beam_lisp/aot_reproducible_test.exs
:END:

compiler_key d5c463d7 != seed 3ac9ac2e; branch changed priv/boot/compiler.bl. mix compile then mix run priv/bootstrap/gen_manifest.exs; verify key + aot_reproducible_test.

mix test test/beam_lisp/aot_reproducible_test.exs → 2 passed (run verbatim). Seed key d5c463d7… == AOTCache.compiler_key.

* tests

** DONE Isolate the OS env per test file in ward
:PROPERTIES:
:TASK_ID: envleak
:STATUS: completed
:VERIFY_CMD: mix bl test test/bl/code test/bl/bl_cache_test.bl test/bl/bl_ask_test.bl test/bl/datom/blanalysis_cache_test.bl
:END:

test/bl/code/* + datom/blanalysis_cache_test set BLANALYSIS_DIR at file scope, never restore -> later files misresolve the store tier (bl_cache_test/bl_ask_test red when batched). Fix in priv/std/reload/ward.bl: snapshot/restore :os env per file.

mix bl test test/bl/code test/bl/bl_cache_test.bl test/bl/bl_ask_test.bl test/bl/datom/blanalysis_cache_test.bl → 6 files, 0 failed (run verbatim; was bl.cache 43/1 + bl.ask 5/2). NB code.index-manifest.test also measures content-addressed memos, so it can go red while the other session recompiles the tree under it — it was green 5 of 6 runs, including 2 back-to-back immediately after the one red (both runs waited on the build lock).

* docs

** DONE Fix the stale in-tree test entry points
:PROPERTIES:
:TASK_ID: devloop
:STATUS: completed
:END:

.local/bltest calls the deleted mix beam_lisp.ward; docs/async-testing.md documents it; test/bl/callable_test.bl Run: line names deleted mix beam_lisp.test. Repoint to `mix bl test`. Note the drop's own stdlib vs the tree.

.local/bltest delegates to mix bl test (flags + BL_ASYNC passthrough); callable_test Run line; docs/build/core-iteration.md warns the drop runs its own stdlib.

* verify

** DONE Re-run the gates on the fixed tree
:PROPERTIES:
:TASK_ID: verify
:STATUS: completed
:VERIFY_CMD: mix bl test test/bl/code
:DEPENDS: renumber seed envleak devloop
:END:

mix bl test over the new suites batched; mix test (Elixir) untimed; org validate has no duplicate IDs.

mix bl test test/bl/code → 3 files green, twice back-to-back (verbatim). Wider battery: + bl.search 12, bl.cache 44, bl.ask 7, datom.blanalysis-cache 42, system.ask 39, print 30, callable 209, reload.ward 42 = 11 files, 0 failed. mix test 1576/1578 (was 1574/1578); the 2 reds are known flaky examples (FUP-054 mcp-demo timeout; BUG-044 02-live native-not-loaded, alternating with z3-conversation). Full 195-file `mix bl test test/bl -p examples` did not complete here (cancelled/timeout) — merge surface is what was gated.
