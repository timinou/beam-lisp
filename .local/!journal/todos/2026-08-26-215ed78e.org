#+TITLE: Session 1566e78f7c4e6c46 Todos
#+DATE: 2026-08-26
#+TODO: ITEM DOING BLOCKED | DONE

* Review

** DONE Inspect observability inference and feedback patch
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

In /home/user/code/ora/relay, inspect git shows fc8fdac and 0bfa6ab for src/relay/inference.bl and src/relay/http.bl, then read full current files and test/relay/observability-test.bl. Acceptance: identify changed-line anchors and all feedback/export control paths.

Observed fc8fdac introduces inference + feedback/export controllers/routes; 0bfa6ab adds tests. Read all 171 lines of inference.bl and all 200 lines of observability-test.bl; current feedback controller is lines 301-315 and route lines 438-439.

** DONE Trace ownership upsert and export execution
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Inspect datom schema/transaction implementation, relay.http jsonl-response, and lib/relay/bridge.ex stream consumption. Acceptance: prove ownership mutation ordering, serialized identity upsert behavior, and eager versus lazy allocations for a 100k-row export without running tests/builds.

Proved Conn Agent serializes all feedback transactions and identity claims resolve onto the existing entity. Bridge is incremental, but export-lines eagerly realizes q results, filters (including one entity read per row), globally sorts/reverses, and retains O(N) row data before returning a Stream; entity maps are transient, not all retained.

** DONE Validate findings against patch and tests
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Cross-check every candidate against observability tests, full call sites, and diff lines. Acceptance: retain only introduced, concrete, actionable material bugs; report each through report_finding, or conclude clean.

Reported three patch-anchored issues: eager O(N) export preprocessing, rejection of the prefixed non-stream completion body id, and malformed feedback scalars becoming 500. Clean: absent/unowned responses share the same 404 path; wrong keys never call feedback; Agent serialization + unique identity prevent duplicate inference entities; row nil/ref scalars JSON-encode safely. Error outcomes are not recorded, but treated as an intentional scope gap rather than a correctness finding.
