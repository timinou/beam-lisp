#+TITLE: Session 15820d072bc00a95 Todos
#+DATE: 2026-09-16
#+TODO: ITEM DOING BLOCKED | DONE

* build

** DONE Write reload.ward-diag contamination ledger
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:VERIFY_CMD: cd /home/user/code/undefine/beam-lisp && bin/bl eval '(do (BeamLisp.Loader/ensure_loaded "reload.ward-diag") (println (BeamLisp.Env/fetch! "reload.ward-diag" "registered")))'
:END:

Create priv/std/reload/ward_diag.bl, ns reload.ward-diag.
- registry: register!/registered (name -> entries fn returning [{:key :owner}])
- built-in surfaces: lsp/analyzed, lsp/growth, lsp/diagnostics, read via BeamLisp.Env/fetch on lsp's private cells (no edit to lsp.bl; other agents own it)
- owner-of: key is a vector whose 1st element is :global or an erlang tuple {:env, ref}; else nil = unscoped
- observer: ward on-event callback; at :file-start snapshot mailbox/os-env/surface keys, at :file-done diff and record findings
- mailbox: erlang/process_info(self,:messages), element 2; [:ward-result ...] ignored; tuples=:daemon (only when self is the daemon executor), else :foreign
- os-env: vars present after a file that were not present before
- memo: new entries during a file (:write, info); a surface with any unscoped key warns (:unscoped, names surface+key+owner)
- report renders warnings only; deep=true adds per-surface counters + per-key detail
Acceptance: (require) loads; (observer) returns a 1-arity fn; report returns nil on a clean ledger.

bin/bl eval loads reload.ward-diag and exports registered ✓

** DONE Wire bl test --diagnose to the ledger
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:VERIFY_CMD: cd /home/user/code/undefine/beam-lisp && bin/bl test --diagnose test/bl/reload/ward_isolation_test.bl
:DEPENDS: task-1
:END:

priv/std/bl/cli.bl: accept --diagnose (st :diagnose), add to st map + test help line + flags list.
priv/std/bl/test.bl.md: in run-isolated, when :diagnose, load reload.ward-diag, install its observer as ward's on-event, print its report after ward's report; in --json mode put findings under :contamination instead of printing; usage-error when combined with --shared/--async (the ledger needs per-file events).
NOTE: 6-HarnessCleanup is editing ward.bl/test.bl.md concurrently — wait until its edits settle (ward.bl parses; jsonl stops growing) before touching test.bl.md.
Acceptance: bl test --diagnose <file> works on a clean file with no extra output; --diagnose --shared is a usage error.

bin/bl test --diagnose test/bl/reload/ward_isolation_test.bl → 10 passed, no ledger noise ✓

* verify

** DONE Test the ledger clean vs contaminated
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:VERIFY_CMD: cd /home/user/code/undefine/beam-lisp && bin/bl test test/bl/reload/ward_diag_test.bl
:DEPENDS: task-2
:END:

Create test/bl/reload/ward_diag_test.bl, ns reload.ward-diag-test, requiring [reload.ward-diag :as wd] [reload.ward :as ward] [env].
Tests:
1. a contaminated run is named: reset ledger, run ward/run over two inline sources where the first reads (wd/runner) and sends it [:ward-diag-probe 1]; assert findings contain a :mailbox warning naming tag :ward-diag-probe.
2. a clean run is silent: same with sources that send nothing; assert (wd/warnings) is empty and (wd/report) is nil.
3. an unscoped memo surface is named: register a surface whose entry owner is nil; drive the observer; assert the warning names the surface and the key.
4. a scoped surface is not a warning (lsp-style keys).
Acceptance: bin/bl test test/bl/reload/ward_diag_test.bl green; and every test fails before the ledger exists (i.e. it exercises real paths).

bin/bl test test/bl/reload/ward_diag_test.bl → 14 passed ✓ (mutation-checked)

* docs

** DONE Walkthrough doc + docs render check
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:VERIFY_CMD: cd /home/user/code/undefine/beam-lisp && bin/bl doc run docs/dev/ward-contamination.bl.md
:DEPENDS: task-3
:END:

Create docs/dev/ward-contamination.bl.md (literate .bl.md like docs/exact-decimals.bl.md): 'my test passed alone and failed in the suite' → bl test --diagnose output naming the leak → fix patterns (memo key discipline / conveying spawn / narrow receive). Reference BUG-041. Cells must execute under the doc runner.
Acceptance: show the rendered/executed output (bl doc run --check docs/dev/ward-contamination.bl.md, or the nearest .bl.md test) in the result.

bin/bl doc run docs/dev/ward-contamination.bl.md → 5 cells, 0 cell errors ✓; bl run renders the walkthrough

** DONE Org note on BUG-041
:PROPERTIES:
:TASK_ID: task-5
:STATUS: completed
:VERIFY_CMD: cd /home/user/code/undefine/beam-lisp && grep -c "OBSERVABILITY SURFACE SHIPPED" "!tasks/bugs/BUG-041-bl-check-lsp-diagnostics-counts-leak-bet.org"
:DEPENDS: task-3
:END:

org note on BUG-041-bl-check-lsp-diagnostics-counts-leak-bet describing the observability surface: bl test --diagnose, the ledger's three surfaces (mailbox/os-env/memo), what it names, and how to reproduce (contaminated fixture).

org note appended (the `org` CLI is absent; verified by grep on the item file) ✓
