#+TITLE: Session 157652026cab534c Todos
#+DATE: 2026-09-07
#+TODO: ITEM DOING BLOCKED | DONE

* startup

** DONE Map daemon and warm runner
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Inspected lib/beam_lisp/daemon.ex, daemon/server.ex, daemon/executor.ex, priv/std/bl/cli.bl, and bl.build task; identified one daemon per tree, AOT boot, Unix socket, serialized executor, watcher registry.

Daemon facade starts Server foreground; Server boots substrate and starts Executor + WatchRegistry; Executor runs requests FIFO in isolated envs.

** DONE Trace app startup and serving
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Mapped Mix application supervision, mix beam_lisp.run app.start, no defapp convention, explicit web/serve routers, Bandit/WebSockAdapter path, live.app frame and examples.

HTTP/WebSocket serving is explicit in user .bl side effects; live.app is convenience, hearth demonstrates hand-written router and live socket.

* development

** DONE Trace dev gating and hotpatch
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Confirmed Mix.env :dev + lib/dev compile gating for DevServer, BEAM_LISP_DEV_PORT override, reload policy via Mix module presence or BEAM_LISP_RELOAD, filesystem watcher scope, daemon FIFO ordering, and BL_HOTPATCH test hook.

Reload watcher is explicit, not supervision-tree resident; daemon WatchRegistry owns client subscriptions and serializes commits through Executor. DevHotpatch swaps compiled boot modules directly.

* report

** DONE Synthesize cited architecture report
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:DEPENDS: task-3
:END:

Final report covers all requested lifecycle and hook points with observed citations and distinguishes BeamLisp.Application DevServer from user app web/serve servers.

No files modified; repository search found no application.bl or defapp convention. Dashboard must be added at user router/live.app :http seam or provided wrapper, not BeamLisp.Application's Tidewave plug.
