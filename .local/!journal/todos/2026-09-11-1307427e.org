#+TITLE: Session 157bef911fe112ff Todos
#+DATE: 2026-09-11
#+TODO: ITEM DOING BLOCKED | DONE

* Review

** DONE Map target patch and changed symbols
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Run git status/diff for priv/std/bl/{cli.bl,util.bl.md,repl,lint,fix,check,ask,doctor,serve,monitor,examples,mcp}.bl.md, priv/lib/lsp/{rpc,server}.bl.md, lib/mix/tasks/bl.ex; identify changed hunks and files requiring full-context review. Acceptance: complete changed-file/hunk map without modifying files.

Observed target patch: tracked changes in lib/mix/tasks/bl.ex and priv/std/bl/cli.bl; 13 target .bl.md files are newly untracked, so their entire contents are patch additions. docs/AGENTS.md requires present-tense, zero-context prose.

** DONE Review daemon safety and exit contracts
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Inspect cli.bl, util.bl.md, repl/serve/monitor/mcp, rpc/server, and bl.ex changes for System/halt outside bl.cli/main, uncaught bad-input failures, and 0/1/2 exit-code violations. Acceptance: each candidate has a patch-overlapping line and a provable trigger/impact, or record no finding.

Reviewed daemon execution and exit paths. Retained new --port classification and long-lived daemon routing candidates; rejected pre-existing cmd-help and cmd-watch defects despite patch-overlapping context.

** DONE Review JSON and utility consolidation
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-1
:END:

Inspect util.bl.md plus lint/fix/check/ask/doctor/examples and all --json branches; trace bl.util/plain conversion and identify non-plain BeamLisp data, duplicated helpers, dead or parallel implementations. Acceptance: only patch-introduced, actionable defects with exact diff lines survive.

Traced JSON lowering and verb payloads. Retained examples stdout pollution, examples raw glob cwd, malformed baseline false-green, ask connection lifecycle, and doctor z3 port leak. No nested BeamLisp collection escapes u/plain in reviewed emitters.

** DONE Review prose and output consistency
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:DEPENDS: task-1
:END:

Read docs/AGENTS.md then inspect .bl.md prose/headers and changed output strings for present-tense violations, relative-vs-absolute paths, plurals, and 'bl <verb>:' prefix consistency. Acceptance: report only concrete defects material enough for review, anchored to changed lines.

Found explicit tense-rule violations in check/doctor/ask; doc command prints newly resolved absolute paths while other commands use cwd-relative display paths; several verbs use '(s)' instead of lint's plural helper.

** DONE Validate findings against full context
:PROPERTIES:
:TASK_ID: task-5
:STATUS: completed
:DEPENDS: task-2 task-3 task-4
:END:

Cross-check candidate findings against definitions/callers and diff provenance; eliminate pre-existing, intentional, speculative, or non-blocking issues. Acceptance: report_finding called once per retained bug and final verdict submitted exactly once.

Validated 17 retained findings against patch provenance and direct callees. Rejected cmd-help/cmd-watch because the faulty behavior predates this patch. Verification was static only as required: git diff/status plus full source and caller review; no files modified and no builds/tests run.
