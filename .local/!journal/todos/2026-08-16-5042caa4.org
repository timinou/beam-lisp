#+TITLE: Session 155a067feb34a961 Todos
#+DATE: 2026-08-16
#+TODO: ITEM DOING BLOCKED | DONE

* Evidence

** DONE Inspect allocation instrument and patch context
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Read commits 55210e5 and dc0ef48, then inspect test/support/spell_case.ex and test/beam_lisp/spell/allocation_test.exs. Acceptance: identify exact measured invariants, warm-up behavior, atom threshold, and diff-anchored lines.

Observed W1 adds two implicit warm-up executions inside allocations/2, narrows module counting to loaded atoms prefixed Elixir.BeamLisp.Eval.M, and allows 25 globally interned atoms. allocation_test also does one explicit warm-up before each allocations/1 call, so measured paths execute three full batches before observation.

** DONE Trace every runtime module compilation path
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Inspect lib/beam_lisp/compiler.ex and search repository for Module.create, Code.compile*, :code.load_binary, and Code.eval_string. Acceptance: prove whether eval_modules/0 covers all event-path dynamic module creation and record exact alternate module names/call paths.

Compiler.eval_form/2 names every per-form evaluator Elixir.BeamLisp.Eval.M<n>, matching eval_modules/0 exactly. Other Module.create paths build BeamLisp.Ns.Fn.M*, stable namespace, record, or defserver modules only while compiling definitions/namespace loads; current Server mount/event/info use Env.fetch! + apply and cannot reach them. Code.compile_* occurs in AOT and serve scripts; :code.load_binary only in a test; no Code.eval_string occurrences.

* Analysis

** DONE Evaluate warmup payload and atom blind spots
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Trace allocation_test.exs inputs through contract event code and analyze two warm-ups, payload diversity, per-process/once-only branches, and @atom_noise 25 against observed repository evidence. Acceptance: each possible blind spot is either concretely reachable or rejected; no speculative finding.

allocations/2 invokes the identical closure twice before measuring. Every test closure restarts fixed ranges (m1..m50, tok1..tok200, same proposal), in the same ExUnit process. Therefore an eval_string leak gated once per process or memoized once per distinct payload is fully consumed before eval_before; production remains unbounded across LiveView processes/novel payloads. Separately, mount measures 20 calls but accepts @atom_noise=25, so one permanently interned atom per visitor passes.

** DONE Assess provider environment cross-suite interference
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:DEPENDS: task-2
:END:

Trace PROVIDER reads and ExUnit async settings across spell tests. Acceptance: determine whether System.put_env setup can overlap another async suite, name exact affected suite/test behavior if reachable, and anchor any issue to patch lines.

* Verdict

** DONE Synthesize evidence and submit review verdict
:PROPERTIES:
:TASK_ID: task-5
:STATUS: completed
:VERIFY_REVIEW: Every reported issue is patch-anchored, demonstrated by a reachable input/invariant violation, and excludes planned W2-W8 work.
:DEPENDS: task-3 task-4
:END:

Cross-check findings against patch-introduced/actionable/provable criteria, report each valid issue with a ≤10-line diff anchor, and submit correctness verdict. Acceptance: structured final result exactly once.

Reported two patch-anchored proof defects: identical full-workload warmups hide once-per-process/distinct-payload evaluator leaks; mount's 20-call sample fits wholly below 25-atom tolerance. Confirmed eval module prefix is correct, no current alternate event-path compiler exists, and PROVIDER=fake cannot overlap other suites because AllocationTest is async:false and restores env.
