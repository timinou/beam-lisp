#+TITLE: Session 1560a7a5bb594506 Todos
#+DATE: 2026-08-21
#+TODO: ITEM DOING BLOCKED | DONE

* review

** DONE Inspect patch and isolate storage changes
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Run read-only git diff in /home/user/code/undefine/beam-lisp; identify modified storage-port, implementation, call-site, and test files. Acceptance: exact changed files and diff hunks catalogued.

Worktree has no source diff; current HEAD 29fb870 modifies db/index/query/cost test but not the port. Audit therefore treats the current Datom subsystem as the proposed deliverable and uses current source lines as anchors.

** DONE Trace protocol contracts through every caller
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Read priv/datom/store.bl, priv/datom/store-ets.bl, and all references under priv/datom/**; enumerate direct ETS/record-shape leaks and caller reliance on ordering, half-open bounds, commit sequencing, update atomicity, and empty-range shape. Acceptance: each claim anchored to exact source path/line and compared against port docs.

No direct ETS/:table/EtsStore leak above L1. conn.bl's store-ets import/open is only the default factory; public/inner connect-with accepts any Store. Actual contracts: -range is documented/tested inclusive+ordered; index scans preserve order and pull/entity expose it; empty nil is tolerated by callers and current tests; -commit order is tested/report-documented but omitted from protocol doc and current production ops are puts only; -update atomicity is relied on for ID counters. Found key-domain contradiction and ignored returned-store values.

** DONE Audit backend conformance proof and coverage
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Inspect storage tests and any second implementation in the patch/repository without modifying files or running Mix; determine whether backend substitution and all six protocol guarantees are tested. Acceptance: exhaustive conformance-property list plus observed proof gaps.

store_test has 24 cases for get/absence, overwrite/delete, ascending inclusive ranges with nil/missing/inverted bounds, batch/order, opaque values, store isolation, update nil/sequential increments, and partial return checks. Missing: actual binary-key/prefix contract, literal empty result shape, returned-store threading for delete/update/commit, concurrent -update atomicity, commit all-or-none visibility/failure atomicity, and a parameterized second semantic implementation. CountingStore is only a delegating decorator and returns its inner store from mutators; cost tests exercise injection but not independent semantics.

** DONE Validate findings against patch introduction
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:DEPENDS: task-3
:END:

For every candidate defect, confirm it overlaps a changed line, has a concrete affected path, and is not accepted/pre-existing; use repository evidence and existing test/probe artifacts only. Acceptance: only actionable patch-anchored findings remain.

All retained defects were introduced by Datom feature commits: ce4eb26 defines value-returning/inclusive contract, cb7bac7 adds keyword counters and ignored update/commit returns, 8cfc81a adds connect-with with fresh metadata, e40ee55 adds half-open report claim. Excluded accepted ETS atomicity and single-writer limitations themselves; retained only missing production-backend conformance coverage.

** DONE Report defects and submit review verdict
:PROPERTIES:
:TASK_ID: task-5
:STATUS: completed
:DEPENDS: task-4
:END:

Call report_finding once per qualifying issue with absolute path and ≤10-line changed range, then submit_result exactly once. Acceptance: structured verdict reflects all and only reported blocking correctness defects.

Seven patch-anchored findings reported: returned-store loss, existing-store metadata reset, raw counter keys, range-contract documentation conflict, absent atomicity proof, unstated commit ordering, and no independent backend run. Ready for final structured verdict.
