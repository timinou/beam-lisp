#+TITLE: Session 155ffe8716ebed39 Todos
#+DATE: 2026-08-21
#+TODO: ITEM DOING BLOCKED | DONE

* Review

** DONE Map changed Datom read-write paths
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Run git diff in /home/user/code/undefine/beam-lisp; read modified priv/datom/*.bl files plus tests needed to construct supported transaction/query/pull/time APIs. Acceptance: identify exact benchmark entrypoints and diff-anchored lines.

Milestone diff is origin/main...HEAD; direct worktree diff only contains journal metadata. Benchmark APIs and cost paths identified; delegated source traces locate tx/current-values + realize and query/eval-pattern + run-clauses.

* Benchmark

** DOING Measure seven paths at three sizes
:PROPERTIES:
:TASK_ID: task-2
:STATUS: in_progress
:DEPENDS: task-1
:END:

Create only /tmp/perf.bl (no repository edits), execute with mix beam_lisp.run --path priv /tmp/perf.bl, and record monotonic-time measurements for N=100,500,1000: bulk transact, entity, column query, two-clause join, nested pull, aggregate count, historical as-of query. Acceptance: complete repeatable table with outputs validated for expected result counts.

* Analysis

** ITEM Trace scaling and range-call costs
:PROPERTIES:
:TASK_ID: task-3
:STATUS: pending
:DEPENDS: task-2
:END:

Compute 500/100 and 1000/500 runtime ratios; trace each superlinear result to exact functions in priv/datom transaction/query/pull/time/store code. Determine datom.store/-range calls for two-clause join by instrumentation if feasible, otherwise code-grounded inference with exact N formula. Acceptance: ranked actionable findings with measured impact and diff-overlapping source lines.

* Report

** ITEM Report patch-anchored performance findings
:PROPERTIES:
:TASK_ID: task-4
:STATUS: pending
:DEPENDS: task-3
:END:

Call report_finding once per provable introduced performance defect, anchored to absolute paths and changed lines; submit verdict exactly once. Acceptance: output includes measurement table/ranking in evidence, separates inference from observation, and omits accepted/pre-existing issues.
