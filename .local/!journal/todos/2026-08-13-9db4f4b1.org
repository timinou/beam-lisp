#+TITLE: Session 155612551770f8ce Todos
#+DATE: 2026-08-13
#+TODO: ITEM DOING BLOCKED | DONE

* investigation

** DONE Inspect editable schema and validation layers
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Read /home/user/code/ora/verse/src/editable/schema.rs, mod.rs, validate.rs via outlines and targeted bodies; identify all public types, serde derives, validation semantics, and grep callers across /home/user/code/ora/verse. Acceptance: evidence paths and line ranges for each conclusion; no file changes.

Schema is editable rich-text projection metadata, not general .st AST. No serde derives on Rust structs; schemas_to_json manually emits JSON. Consumers are compiler bundle injection, server endpoint, sync validation, and editor runtime.

** DONE Trace serializer output and completeness
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Inspect /home/user/code/ora/verse/src/serializer.rs and its callers/types; enumerate input/output formats, emitted constructs, TODOs, unsupported/lossy paths, and tests with exact evidence paths and line ranges. Read-only.

serialize(&StFile)->String emits imports, presets, and simplified scopes. Scope macro captures become literal (...), so not lossless; docs explicitly limit focus. Server save_handler consumes it.

** DONE Map introspection export artifacts and limits
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Inspect /home/user/code/ora/verse/src/introspect/*.rs and src/export/*.rs plus module callers; determine machine-readable contracts/registries, bundle/artifact formats, source preservation, and explicit lossy/partial behavior. Acceptance: per-module evidence paths and line ranges, read-only.

Introspection exposes compiled bundle Structure IR and JSON projection for MCP/dev inspector, with spans/bindings/template params; it is not a full source AST. Export discovers/compiles/emits static JS/CSS/HTML and artifact manifests; build_artifact explicitly excludes .st/.org source.
