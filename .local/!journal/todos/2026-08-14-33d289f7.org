#+TITLE: Session 1556e8a84470f922 Todos
#+DATE: 2026-08-14
#+TODO: ITEM DOING BLOCKED | DONE

* investigation

** DONE Read repository rules and target emitter anchors
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Read /home/user/code/undefine/beam-lisp/AGENTS.md if present and inspect priv/contract.bl symbols bind-form, bind-arg, on-driver-body-form, edn-bind-scope plus commit diffs for 0e4f9cb and c28017f. Acceptance: exact changed line anchors and source snippets recorded; no files modified.

No AGENTS.md found at repository root. Target anchors are priv/contract.bl lines 572-649; current tree diff is intentionally not used.

** DONE Trace Verse parser printer and real on AST
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Inspect /home/user/code/ora/verse reader, macros/forms, printer, fixtures, and any existing artifacts for @on AST shape and st forms. Acceptance: distinguish observed source representation from runtime inference for no-arg st, map arg, on click, and multiple selectors.

Observed read.rs selector wrapper `(sel "selector" ...)`, keyword capture encoding, on.st driver/body grammar, and EDN codec Named/Array encoding. Existing fixtures cover @on &.click bodies and selector scopes.

* analysis

** DONE Compare semantics and report concrete mismatches
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Cross-check changed beam-lisp code against Verse interpretation and commit anchors. Acceptance: report only provable introduced actionable mismatches, or explicitly no mismatch, with exact author input, emitted representation, required representation, and evidence paths/lines.

Concrete findings: edn-bind-scope drops selector; on-driver-body-form encodes a nonexistent call/target shape, adds empty subject, and uses paramlist for call args; bind-arg flattens map args. `(st/foo)` has no mismatch for a zero-required-capture form, but cannot support required captures.
