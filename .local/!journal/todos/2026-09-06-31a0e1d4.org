#+TITLE: Session 157535ed8363b1a4 Todos
#+DATE: 2026-09-06
#+TODO: ITEM DOING BLOCKED | DONE

* investigate

** DONE Inspect boot planner and Mix staging
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Read priv/boot/build.bl, lib/mix/tasks/compile.beam_lisp.ex, relevant existing tests read-only. Identify minimal scheduler partition, closure validation, failure/cache gate, and stale staged-build refresh affordance. Acceptance: exact symbols and test seams identified.

Plan exposes :order/:waves/:deps. Minimal seam: public build/partition-plan keyed by expanded priv/boot path; run boot order as singleton waves, validate every boot dependency remains boot. Loader staging can exclude only build before ensure_loaded, forcing normal stale/source path while retaining prior compiler stage.

* implementation

** DONE Implement boot barrier and refresh
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Modify only priv/boot/build.bl and lib/mix/tasks/compile.beam_lisp.ex. Partition boot-source plan tier, validate dependency closure conservatively, execute serially in dependency order before parallel ordinary waves, stop ordinary work on boot failure without current cache output. Refresh stale staged build namespace through normal source evaluation only. Acceptance: source inspection and focused tests demonstrate contracts.

Implemented path-based boot partition, closure rejection, serial boot execution, ordinary failure gate, failed-output invalidation, and staged build source refresh via loader.

* verification

** DONE Add focused boot barrier regressions
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:VERIFY_REVIEW: Formatting and direct pure Mix decision passed; focused scheduler suite exists but runtime setup is blocked by stale reader beam.
:DEPENDS: task-2
:END:

Create only test/beam_lisp/boot_build_barrier_test.exs. Add pure scheduler partition, failure gate/cache-current prevention, and stale boot build refresh-decision tests where feasible without triggering global compilation. Acceptance: run this focused test file and non-global syntax/format checks.

Added partition/order, closure rejection, boot failure gate, and staged refresh decision regressions. Formatting/diff checks pass; direct refresh decision passes. Focused no-compile test was attempted but invalidated during setup by pre-existing undefined reader/delimiters in stale compiled runtime; no global Beam Lisp compilation was run.
