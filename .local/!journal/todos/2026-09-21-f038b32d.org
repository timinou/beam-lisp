#+TITLE: Session 158830f00a2b40b3 Todos
#+DATE: 2026-09-21
#+TODO: ITEM DOING BLOCKED | DONE

* Tasks

** DONE Trace compiler key inputs and cache consumers
:PROPERTIES:
:TASK_ID: key-contract
:STATUS: completed
:END:

Read lib/beam_lisp/aot_cache.ex symbols compute_compiler_key, codegen_part, toolchain_source_contents, cache_root and key consumers. No edits. Acceptance: enumerate actual hashed inputs, provenance/cache path behavior, and any version-sensitive artifact dependency.

Verified compiler_key inputs, cache path and provenance stamping. No intrinsic app-version-dependent codegen found. Compiler macros execute arbitrary functions at expansion time, so version-reading macros are a bounded counterexample to universal source-only determinism.

** DONE Verify launcher generation checks and floor migration
:PROPERTIES:
:TASK_ID: boot-contract
:STATUS: completed
:DEPENDS: key-contract
:END:

Read bin/bl generation/rebuild check, priv/bootstrap/seed manifest references, env.bl version/build configuration, and relevant release/seed callers. No edits. Acceptance: establish A/B consequences and migration requirements from file evidence.

Verified bin/bl compares destination boot namespace stamps before child rebuild; Bootstrap.key_matches? compares compiler and build keys; key formula change rotates both. Existing seed generator validates embedded stamps before publication, so new floor requires coherent rebuild, not manifest relabel. Release stamp precedes build --force.

** DONE Deliver source-grounded recommendation and cache caveats
:PROPERTIES:
:TASK_ID: verdict
:STATUS: completed
:DEPENDS: boot-contract
:END:

Cross-check competing hypotheses against lib/beam_lisp/aot_cache.ex and bin/bl. No edits. Acceptance: definitive A/B/other recommendation, concrete wrong-hit scenario or bounded absence, and key-change blast radius with exact source references.

Verdict: A plus one-time coherent floor/artifact migration; retain strict launcher check. No intrinsic version-dependent compiler ABI found. Literal universal safety has a concrete caveat: user macro can return bl.util/version at expansion time; same source closure then emits a different literal. Explain explicit-input/noncacheable treatment rather than product version as global toolchain identity. Recommend version-invariance, real-source invalidation, fresh/warm build, cache-hit and packaged-drop validation; no builds run under read-only scope.
