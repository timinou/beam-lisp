#+TITLE: Session 155fde6abf2bed0f Todos
#+DATE: 2026-08-21
#+TODO: ITEM DOING BLOCKED | DONE

* Review

** DONE Inspect transaction patch and surrounding contracts
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Read git diff for commits cb7bac7 and cd7f9de, then full priv/datom/tx.bl, priv/datom/conn.bl, and directly called schema/index/db/store APIs. Acceptance: map every changed transaction path and exact diff line anchors.

Patch adds tx/conn wholesale; dependency trace confirms raw indexes retain historical assertions/retractions, db visibility filters them only through datom.db/datoms, and validation accepts known attributes without valueType.

** DONE Trace six Datomic transaction invariants
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Analyze cardinality-one duplicate assertions, mismatched retract, undeclared refs/tempids, identity/value uniqueness, id-space collision, and failed-write tx allocation against priv/datom/tx.bl and conn.bl. Acceptance: each question has evidence-backed outcome and reproducer/affected path where possible.

Outcomes: same-tx retractions use the correct tx; nonmatching retract does not hide a different value; tx/entity counter gaps are recoverable absent the documented partial-commit gap. Confirmed risks: transaction-local duplicate card-one assertions, historical assertions treated as current, unresolved tempid values on untyped attrs, stale historical upsert hits, absent uniqueness enforcement, overlapping numeric id spaces.

** DONE Cross-check findings against tests and semantics
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Inspect transaction/datastore tests and independently review suspected issues for introducedness, impact, and patch anchoring. Acceptance: remove false positives and assign priorities to actionable bugs only.

Independent review and official Datomic docs corroborate six actionable defects. Excluded nonissues: generated supersession datoms share tx correctly; different-value retract leaves current value visible; two tempids with one live identity map to the same entity; consumed ID gaps are recoverable; numeric e/tx overlap has no current context-confusion impact; documented ETS partial commit excluded.

** DONE Submit anchored review verdict and findings
:PROPERTIES:
:TASK_ID: task-4
:STATUS: completed
:DEPENDS: task-3
:END:

Call report_finding once per confirmed issue with absolute path and <=10 changed lines, then submit_result exactly once. Acceptance: verdict reflects only correctness blockers; known excluded issues are not reported.

Recorded five P1 silent-integrity findings and one P2 history/redundancy finding, all anchored to changed tx.bl lines. Ready to submit incorrect verdict.
