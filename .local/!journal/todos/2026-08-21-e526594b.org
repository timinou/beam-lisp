#+TITLE: Session 156026a82459b05a Todos
#+DATE: 2026-08-21
#+TODO: ITEM DOING BLOCKED | DONE

* Review

** DONE Inspect datom layers and existing APIs
:PROPERTIES:
:TASK_ID: task-1
:STATUS: completed
:END:

Read priv/datom/db.bl, priv/datom/time.bl, priv/datom/query/engine.bl, priv/datom/query/agg.bl and relevant tests. Map setup syntax for four assigned constructions. No edits.

Identified db filter semantics, history retractions, entity refusal, aggregate folds and query path.

** DONE Execute isolated semantic probes
:PROPERTIES:
:TASK_ID: task-2
:STATUS: completed
:DEPENDS: task-1
:END:

Create only /tmp probe .bl files and run mix beam_lisp.run --path priv /tmp/probe.bl. Execute count views, cardinality-many, cardinality-one replacement, aggregate nil/unbound, and empty inputs where exposed. Do not run full mix test.

Probe outputs: current count 2, as-of count 2, history age count 4 with retraction; many entity x→x,y→y; one replacement as-of tx shows both Bob 2 and Ada 3; unbound aggregate ran; empty aggregate returned #{}; 4-term history pattern raised.

** DONE Report verified cross-layer mismatches
:PROPERTIES:
:TASK_ID: task-3
:STATUS: completed
:DEPENDS: task-2
:END:

Anchor only actionable defects introduced in origin/main...HEAD, with exact file lines and observed runtime evidence. Distinguish passing constructions. Submit structured result.

Two verified bugs: history query exposes retractions in 3-term patterns; aggregate variable validation misses where-boundness. Cardinality-many/one and empty aggregate probes passed/observed.
