#+TITLE: Session 1560265ae2d9b052 Todos
#+DATE: 2026-08-21
#+TODO: ITEM DOING BLOCKED | DONE

* review

** DOING Map patch and public read APIs
:PROPERTIES:
:TASK_ID: task-1
:STATUS: in_progress
:END:

Inspect `git diff`, modified `priv/datom/*.bl` and test call patterns. Identify exact constructors and public function signatures needed for probes. Acceptance: attack harness can invoke q, pull, entity, datoms, changes, aggregates, and transact without guessing.

** ITEM Validate defects and submit verdict
:PROPERTIES:
:TASK_ID: task-5
:STATUS: pending
:DEPENDS: task-2 task-3 task-4
:END:

Trace each suspicious runtime outcome into modified diff lines, confirm introduced-by-patch status, call `report_finding` once per qualifying issue, then `submit_result` exactly once. Acceptance: every finding is patch-anchored, evidence-backed, actionable, and not on the accepted/prior-fixed list.

* runtime

** ITEM Execute empty and transaction abuse attacks
:PROPERTIES:
:TASK_ID: task-2
:STATUS: pending
:DEPENDS: task-1
:END:

Create no files; invoke `mix beam_lisp.run --path priv` on temporary probe files only. Exercise empty db/read paths, empty tx, `[{}]`, value-only tempid, assert+retract same datom, 1000 ops. Acceptance: capture exact outputs/errors/timings and isolate reproducible wrong answers or confusing crashes.

** ITEM Execute pull and value edge attacks
:PROPERTIES:
:TASK_ID: task-3
:STATUS: pending
:DEPENDS: task-1
:END:

Run temporary probes for self refs, A↔B cycles, 100k strings, UTF-8 including non-BMP, unqualified keyword attrs, missing refs, and reverse refs. Enforce process timeouts around recursive pulls. Acceptance: exact outcomes, timing, and any hangs are observed.

** ITEM Execute adversarial query interaction attacks
:PROPERTIES:
:TASK_ID: task-4
:STATUS: pending
:DEPENDS: task-1
:END:

Run temporary probes for 20 clauses, cartesian products, throwing predicates, nil function clauses, nested `:not`, and aggregate behavior on empty/time views. Acceptance: compare actual results/errors to API semantics and identify only reproducible defects.
